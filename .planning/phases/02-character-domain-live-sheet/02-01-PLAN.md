---
phase: 02-character-domain-live-sheet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/schema.ts
  - src/main/db/charactersRepo.ts
  - src/main/db/contentLoader.ts
  - src/main/db/contentTypes.ts
  - src/main/characters/calculations.ts
  - src/main/characters/calculations.test.ts
  - src/main/db/charactersRepo.test.ts
  - src/main/db/contentLoader.test.ts
  - src/main/trpc/routers/characters.test.ts
  - src/main/trpc/schemas.ts
  - resources/migrations/0001_characters.sql
  - resources/migrations/meta/_journal.json
autonomous: true
requirements: [CHAR-01, CHAR-07]
must_haves:
  truths:
    - "Drizzle schema includes characters, character_resources, character_items tables and coverImagePath column on campaigns"
    - "Migration 0001 generated by drizzle-kit applies cleanly on a fresh DB"
    - "Pure calculation functions (calcHP, calcAC, calcAbilityModifier, calcInitiativeBonus, buildSpellSlots) return correct D&D 5e values"
    - "charactersRepo.createWithResources atomically inserts 3 rows in a synchronous transaction"
    - "JSON columns (spellSlots, conditions, savingThrowProficiencies, skillProficiencies, skillExpertise, languages, toolProficiencies, armorProficiencies, weaponProficiencies) are JSON.parse()'d in the repo layer before returning"
    - "contentLoader.loadContent() reads JSON files from resources/ using app.isPackaged path resolution"
    - "charactersRepo.updateXp delta-mutates the characters.xp column (clamped to a non-negative integer)"
  artifacts:
    - path: "src/main/db/schema.ts"
      provides: "characters, characterResources, characterItems tables and coverImagePath column on campaigns"
      contains: "export const characters"
    - path: "src/main/db/charactersRepo.ts"
      provides: "createWithResources, getWithResources, getByCampaignId, updateAbilityScore, updateXp, updateHp, updateTempHp, updateCurrency, updateSpellSlot, toggleCondition, updateDeathSaves, toggleInspiration, toggleItemAttuned, updatePortraitPath"
    - path: "src/main/characters/calculations.ts"
      provides: "Pure D&D 5e calculation functions"
    - path: "src/main/db/contentLoader.ts"
      provides: "loadContent() module-level cache"
    - path: "resources/migrations/0001_characters.sql"
      provides: "DDL for characters domain tables + coverImagePath column"
  key_links:
    - from: "src/main/db/charactersRepo.ts"
      to: "src/main/db/schema.ts"
      via: "import { characters, characterResources, characterItems }"
      pattern: "import.*from.*schema"
    - from: "src/main/characters/calculations.test.ts"
      to: "src/main/characters/calculations.ts"
      via: "import { calcHP, calcAC, ... }"
      pattern: "import.*from.*calculations"
---

<objective>
Build the domain core for the character system: Drizzle schema additions, generated migration, pure calculation utilities, character repo with JSON-aware parsing, content loader with packaged-path resolution, and Wave 0 test stubs for all 5 test files identified in 02-VALIDATION.md.

Purpose: Establish a tested foundation for the character domain that all downstream plans (tRPC routers, wizard UI, sheet UI, image service) build on. Tests are RED until Wave 2 wires the tRPC layer.

Output: Schema compiles, migration 0001 generated, pure functions tested green, test scaffolds in place (some red), repo layer ready for tRPC wiring in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-character-domain-live-sheet/02-CONTEXT.md
@.planning/phases/02-character-domain-live-sheet/02-RESEARCH.md
@.planning/phases/02-character-domain-live-sheet/02-PATTERNS.md
@.planning/phases/02-character-domain-live-sheet/02-VALIDATION.md
@src/main/db/schema.ts
@src/main/db/campaignsRepo.ts
@src/main/db/migrate.ts
@src/main/db/index.ts
@src/main/trpc/schemas.ts
@src/main/trpc/routers/secrets.test.ts
@resources/migrations/0000_absent_thunderball.sql
@resources/migrations/meta/_journal.json

<interfaces>
<!-- From src/main/db/schema.ts (existing) -->
export const campaigns = sqliteTable('campaigns', { id, name, createdAt })
export type Campaign = typeof campaigns.$inferSelect
export type NewCampaign = typeof campaigns.$inferInsert

<!-- From src/main/db/campaignsRepo.ts (analog) -->
export const campaignsRepo = { list(), create({name}), get(id) }
// Pattern: synchronous, getDb() from './index', randomUUID(), null-guard after .get()

<!-- From src/main/trpc/schemas.ts (existing) -->
export const campaignIdSchema = z.string().uuid()
export const campaignNameSchema = z.string().trim().min(1).max(80)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Drizzle schema and generate migration 0001</name>
  <files>src/main/db/schema.ts, resources/migrations/0001_characters.sql, resources/migrations/meta/_journal.json</files>
  <read_first>
    - src/main/db/schema.ts (existing campaigns table — extend it, do not replace)
    - resources/migrations/0000_absent_thunderball.sql (DDL syntax reference)
    - resources/migrations/meta/_journal.json (journal format reference)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 1 (full schema definition)
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §schema.ts (pattern rules)
  </read_first>
  <behavior>
    - schema.ts exports characters, characterResources, characterItems sqliteTables
    - campaigns table gains coverImagePath text column (nullable, no .notNull())
    - characters table has unique constraint on campaign_id (D-03): (table) => ({ uniqueCampaign: unique().on(table.campaignId) })
    - Foreign keys: characters.campaignId references campaigns.id ON DELETE CASCADE; characterResources.characterId and characterItems.characterId reference characters.id ON DELETE CASCADE
    - JSON array columns use text('col').notNull().default('[]')
    - Boolean columns use integer('col', { mode: 'boolean' }).notNull().default(false)
    - Timestamps use integer('col', { mode: 'timestamp_ms' }).notNull().default(sql`(unixepoch() * 1000)`)
    - Item weight uses real('weight').notNull().default(0)
    - Exports type Character, NewCharacter, CharacterResources, NewCharacterResources, CharacterItem, NewCharacterItem via $inferSelect / $inferInsert
  </behavior>
  <action>
    Extend src/main/db/schema.ts per RESEARCH.md §Pattern 1. Add `coverImagePath: text('cover_image_path')` to campaigns. Define characters table with these columns (all from RESEARCH.md): id, campaignId, name, race, subrace, class, subclass, background, level (default 1), xp (default 0), backstory, strength, dexterity, constitution, intelligence, wisdom, charisma, savingThrowProficiencies, skillProficiencies, skillExpertise, ac, initiativeBonus, speed, proficiencyBonus (default 2), languages, toolProficiencies, armorProficiencies, weaponProficiencies, racialTraitsText, classFeatureText, backgroundFeatureText, equipmentPackage, portraitPath, createdAt, updatedAt. Define characterResources: id, characterId, hpCurrent, hpMax, hpTemp (default 0), spellSlots (default '{}'), cp/sp/ep/gp/pp (default 0), conditions (default '[]'), deathSaveSuccesses (default 0), deathSaveFailures (default 0), hasInspiration (boolean default false), updatedAt. Define characterItems: id, characterId, name, quantity (default 1), weight (real default 0), isAttuned (boolean default false), isMagic (boolean default false), description, sortOrder (default 0), createdAt. Add unique constraint on characters.campaignId per D-03.

    Extend imports at line 1 from `import { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core'` to include `real, unique`.

    After saving schema.ts, run `npm run db:generate` to produce resources/migrations/0001_<random>.sql and update meta/_journal.json. **[BLOCKING]** This must complete before any test files or tRPC routers reference the new tables.

    Export types Character, NewCharacter, CharacterResources, NewCharacterResources, CharacterItem, NewCharacterItem.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/main/db/schema.ts contains `export const characters = sqliteTable('characters',`
    - src/main/db/schema.ts contains `export const characterResources = sqliteTable('character_resources',`
    - src/main/db/schema.ts contains `export const characterItems = sqliteTable('character_items',`
    - src/main/db/schema.ts contains `coverImagePath: text('cover_image_path')` inside campaigns
    - src/main/db/schema.ts contains `unique().on(table.campaignId)`
    - src/main/db/schema.ts contains `import { sqliteTable, text, integer, real, unique } from 'drizzle-orm/sqlite-core'`
    - resources/migrations/0001_*.sql exists and contains `CREATE TABLE \`characters\``
    - resources/migrations/0001_*.sql contains `CREATE TABLE \`character_resources\``
    - resources/migrations/0001_*.sql contains `CREATE TABLE \`character_items\``
    - resources/migrations/0001_*.sql contains `ALTER TABLE \`campaigns\` ADD \`cover_image_path\` text` (or equivalent)
    - resources/migrations/0001_*.sql contains `UNIQUE(\`campaign_id\`)` for characters table
    - resources/migrations/meta/_journal.json includes an entry with idx 1 for migration 0001
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Schema extended, migration 0001 generated by drizzle-kit, types exported, typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure calculations, content types, content loader, and shared Zod schemas (with tests)</name>
  <files>src/main/characters/calculations.ts, src/main/characters/calculations.test.ts, src/main/db/contentTypes.ts, src/main/db/contentLoader.ts, src/main/db/contentLoader.test.ts, src/main/trpc/schemas.ts</files>
  <read_first>
    - src/main/db/migrate.ts (path resolution two-branch pattern)
    - src/main/trpc/schemas.ts (existing Zod schemas — extend, don't replace)
    - src/main/trpc/routers/secrets.test.ts (vitest + electron mock pattern; tRPC v10 caller pattern)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 3, §Pattern 7, §Content JSON Schema
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §calculations.ts, §contentLoader.ts
  </read_first>
  <behavior>
    - calcAbilityModifier(score): Math.floor((score - 10) / 2). calcAbilityModifier(10) === 0, calcAbilityModifier(16) === 3, calcAbilityModifier(8) === -1, calcAbilityModifier(20) === 5
    - calcHP(hitDie, conScore): hitDie + calcAbilityModifier(conScore). calcHP(10, 16) === 13
    - calcAC(dexScore, armorBase?): armorBase !== undefined ? armorBase + calcAbilityModifier(dexScore) : 10 + calcAbilityModifier(dexScore). calcAC(14) === 12, calcAC(14, 13) === 15
    - calcInitiativeBonus(dexScore): calcAbilityModifier(dexScore)
    - buildSpellSlots('cleric', 1, spellSlotsByClass) returns { '1': { used: 0, max: 2 } } when spellSlotsByClass.cleric[1] === { '1': 2 }. Returns {} when class is not in table.
    - contentTypes.ts exports interfaces Race, DndClass, Subclass, EquipmentPackageOption, Background, SpellSlotsByClass, SpellSlotMap (per RESEARCH.md §Content JSON Schema)
    - contentLoader.loadContent() uses app.isPackaged ? process.resourcesPath : path.join(__dirname, '../../resources') and reads races.json, classes.json, backgrounds.json, equipment.json, spells-by-class.json synchronously with fs.readFileSync utf-8, parses each. Module-level singleton cache.
    - contentLoader logs errors via electron-log with [contentLoader] prefix and rethrows
    - schemas.ts gains characterNameSchema, backstorySchema, abilityScoreSchema, hpDeltaSchema, currencyDeltaSchema, xpDeltaSchema, conditionNameSchema (z.enum of 14 conditions)
  </behavior>
  <action>
    Create src/main/characters/calculations.ts exporting calcAbilityModifier, calcHP, calcAC, calcInitiativeBonus, buildSpellSlots as pure functions per RESEARCH.md §Pattern 7. No electron, no DB imports.

    Create src/main/characters/calculations.test.ts with vitest test cases per the Behavior block. No electron mock needed (pure functions). Use `import { describe, it, expect } from 'vitest'`. All tests must pass.

    Create src/main/db/contentTypes.ts with TypeScript interfaces matching RESEARCH.md §Content JSON Schema verbatim: Race, DndClass, Subclass, EquipmentPackageOption, Background, SpellSlotsByClass, SpellSlotMap (= Record<string, { used: number; max: number }>).

    Create src/main/db/contentLoader.ts with `loadContent(): ContentCache` per RESEARCH.md §Pattern 3 and PATTERNS.md §contentLoader.ts. Use `let _content: ContentCache | null = null` module-level cache. Path resolution via `app.isPackaged ? process.resourcesPath : path.join(__dirname, '../../resources')`. Wrap fs.readFileSync calls in try/catch, log.error('[contentLoader] failed to load', err), rethrow.

    Create src/main/db/contentLoader.test.ts with vitest. Mock electron app per secrets.test.ts pattern. Create a tmpdir, write minimal valid JSON files (races.json: [{id:'human',name:'Human',size:'Medium',speed:30,abilityScoreIncreases:[],traits:[],languages:[],source:'test'}], etc.), set app.getPath('userData') and override resourcesPath via point __dirname to tmpdir. Assert loadContent() returns object with races/classes/backgrounds/equipment/spellSlotsByClass keys. Cleanup tmpdir in afterEach. Tests may be RED until resources JSON exists in Wave 3 — that is acceptable per Wave 0 design.

    Extend src/main/trpc/schemas.ts: add `export const characterNameSchema = z.string().trim().min(1).max(100)`, `export const backstorySchema = z.string().max(2000)`, `export const abilityScoreSchema = z.number().int().min(1).max(30)`, `export const hpDeltaSchema = z.number().int().min(-9999).max(9999)`, `export const currencyDeltaSchema = z.number().int().min(-999999).max(999999)`, `export const xpDeltaSchema = z.number().int().min(-999999).max(999999)`, `export const conditionNameSchema = z.enum(['blinded','charmed','deafened','exhaustion','frightened','grappled','incapacitated','invisible','paralyzed','petrified','poisoned','prone','restrained','stunned'])`.
  </action>
  <verify>
    <automated>npm test -- src/main/characters/calculations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/characters/calculations.ts exports calcAbilityModifier, calcHP, calcAC, calcInitiativeBonus, buildSpellSlots
    - `npm test -- src/main/characters/calculations.test.ts` exits 0 with all tests passing
    - src/main/db/contentTypes.ts exports interface Race, interface DndClass, interface Subclass, interface EquipmentPackageOption, interface Background, type SpellSlotsByClass, type SpellSlotMap
    - src/main/db/contentLoader.ts exports `loadContent(): ContentCache`
    - src/main/db/contentLoader.ts contains `app.isPackaged ? process.resourcesPath`
    - src/main/db/contentLoader.test.ts exists with at least one `it(` block
    - src/main/trpc/schemas.ts contains `export const characterNameSchema`, `abilityScoreSchema`, `hpDeltaSchema`, `conditionNameSchema`, `currencyDeltaSchema`, `xpDeltaSchema`, `backstorySchema`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Pure calculations green-tested, content loader written, content types exported, shared Zod schemas extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: charactersRepo with synchronous transaction + JSON parse layer (with test stub)</name>
  <files>src/main/db/charactersRepo.ts, src/main/db/charactersRepo.test.ts, src/main/trpc/routers/characters.test.ts</files>
  <read_first>
    - src/main/db/campaignsRepo.ts (CRUD pattern, error pattern, getDb usage)
    - src/main/db/schema.ts (after Task 1 — Character types must exist)
    - src/main/db/contentTypes.ts (SpellSlotMap type)
    - src/main/trpc/routers/secrets.test.ts (electron mock + tmpdir pattern)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 2 (transaction), §Pitfall 1 (synchronous), §Pitfall 5 (delta-based), §Pitfall 8 (JSON parse)
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §charactersRepo.ts (JSON parse rule)
  </read_first>
  <behavior>
    - charactersRepo.createWithResources(input) wraps inserts to characters + characterResources + characterItems in a single SYNCHRONOUS db.transaction((tx) => {...}) — never `async (tx) =>` per Pitfall 1
    - getWithResources(characterId) returns CharacterWithResources or undefined; JSON.parse()'s all JSON text columns before returning (per Pitfall 8)
    - getByCampaignId(campaignId) returns CharacterWithResources or undefined (used by D-04 auto-launch check)
    - updateAbilityScore(characterId, ability, value) — value validated as 1-30 in tRPC layer
    - updateXp(characterId, delta) — delta-based mutation on the characters.xp column (not character_resources); clamps via `MAX(0, xp + ?)` to prevent negative XP; tRPC layer enforces delta bounds
    - updateHp(characterId, delta) — delta-based, clamps via `MAX(0, MIN(hp_max, hp_current + ?))` per Pitfall 5
    - updateTempHp(characterId, delta) — clamps at 0 minimum
    - updateCurrency(characterId, denomination, delta) — denomination in {cp,sp,ep,gp,pp}, clamps at 0
    - updateSpellSlot(characterId, slotLevel, delta) — mutates the spellSlots JSON object: parse, mutate used count clamped to [0, max], stringify, write back
    - toggleCondition(characterId, condition) — parse conditions JSON array, add or remove the condition string, stringify, write
    - updateDeathSaves(characterId, successes, failures) — sets absolute values 0-3 each (toggle behavior happens in renderer)
    - toggleInspiration(characterId) — flips hasInspiration boolean
    - toggleItemAttuned(itemId) — flips isAttuned on character_items row
    - updatePortraitPath(characterId, relativePath) — sets characters.portraitPath
  </behavior>
  <action>
    Create src/main/db/charactersRepo.ts. Follow campaignsRepo.ts patterns exactly: synchronous functions, `const db = getDb()` inside each method, `randomUUID()` for new IDs, null-guard after `.get()`. Use `db.transaction((tx) => {...})` synchronously per Pitfall 1. Imports: `import { eq, asc, sql } from 'drizzle-orm'`, `import { randomUUID } from 'node:crypto'`, `import { getDb } from './index'`, `import { characters, characterResources, characterItems } from './schema'`, `import log from 'electron-log'`, plus types from schema and contentTypes.

    Implement all methods listed in Behavior. For delta-based HP/currency/spell-slot/XP mutations, use raw SQL via `sql` template: e.g. `db.update(characterResources).set({ hpCurrent: sql\`MAX(0, MIN(hp_max, hp_current + ${delta}))\` }).where(eq(characterResources.characterId, characterId)).run()`. For updateXp: `db.update(characters).set({ xp: sql\`MAX(0, xp + ${delta})\` }).where(eq(characters.id, characterId)).run()`. For JSON column mutations (conditions, spellSlots), read → JSON.parse → mutate in-memory → JSON.stringify → write back atomically inside a transaction.

    In getWithResources, JSON.parse these columns on the character row: savingThrowProficiencies, skillProficiencies, skillExpertise, languages, toolProficiencies, armorProficiencies, weaponProficiencies (all as string[]). On the resources row: conditions (string[]), spellSlots (SpellSlotMap). Return shape: { ...char, ...parsedFields, resources: { ...res, conditions, spellSlots }, items }.

    Note: updateCoverImagePath is owned by campaignsRepo (added in Plan 04) — it operates on the campaigns table and is cohesive with campaign-level concerns. Do not add it here.

    Create src/main/db/charactersRepo.test.ts with vitest. Mock electron app per secrets.test.ts pattern. Use real in-memory better-sqlite3 (`new Database(':memory:')` via getDb override) and apply migrations from resources/migrations/ at beforeEach. Test cases (stubs OK, will go green when migration + schema in place):
      - createWithResources inserts all 3 tables atomically in a single transaction
      - getWithResources returns parsed objects (typeof result.resources.spellSlots === 'object', not 'string')
      - getWithResources returns parsed conditions array
      - updateHp delta-based: hpCurrent=10, delta=+5 → 15; hpCurrent=10, hpMax=12, delta=+5 → 12 (clamp)
      - updateHp delta=-100 → clamps to 0
      - updateXp delta=+300 starting from 0 → 300; delta=-500 starting from 300 → clamps to 0
      - toggleCondition adds when not present, removes when present
      - getByCampaignId returns the character row by FK

    Create src/main/trpc/routers/characters.test.ts as a Wave 0 STUB. Mark all tests with `it.todo(...)` or `it.skip(...)` for now — they go green in Plan 02 when the router is built. Stub these cases per 02-VALIDATION.md tasks 2-xx-05, 2-xx-06, 2-xx-08, 2-xx-11:
      - characters.create writes a character to DB and returns CharacterWithResources
      - characters.create rejects malformed input (Zod throws on empty name, non-integer ability score)
      - characters.updateHp delta-based mutation clamps at 0 and hpMax
      - characters.updateHp rejects out-of-range delta via Zod
      - characters.updateXp delta mutates the xp column (clamps at 0, persists across getByCampaignId)
      - characters.updateXp rejects out-of-range delta via Zod
      - characters.toggleItemAttuned flips isAttuned on the items row
      - characters.toggleCondition persists across query refetch
  </action>
  <verify>
    <automated>npm test -- src/main/db/charactersRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/db/charactersRepo.ts exports `export const charactersRepo = {` with methods: createWithResources, getWithResources, getByCampaignId, updateAbilityScore, updateXp, updateHp, updateTempHp, updateCurrency, updateSpellSlot, toggleCondition, updateDeathSaves, toggleInspiration, toggleItemAttuned, updatePortraitPath
    - src/main/db/charactersRepo.ts contains `db.transaction((tx) =>` (synchronous, NOT `async (tx) =>`)
    - src/main/db/charactersRepo.ts contains `JSON.parse(` for each of: spellSlots, conditions, savingThrowProficiencies, skillProficiencies (use grep to confirm)
    - src/main/db/charactersRepo.ts contains `sql\`MAX(0, MIN(hp_max, hp_current` (delta-clamp pattern)
    - src/main/db/charactersRepo.ts contains `sql\`MAX(0, xp +` (XP delta-clamp pattern)
    - src/main/db/charactersRepo.test.ts exists with `describe('charactersRepo'` and at least 6 `it(` cases (including an updateXp case)
    - src/main/trpc/routers/characters.test.ts exists with at least 8 `it.todo(` or `it.skip(` cases referencing characters.create, characters.updateHp, characters.updateXp, characters.toggleCondition, characters.toggleItemAttuned
    - `npm run typecheck` exits 0
    - `npm test -- src/main/db/charactersRepo.test.ts` runs without throwing import/type errors (tests may pass or partially pass — passing is preferred, but file MUST be syntactically valid and runnable)
  </acceptance_criteria>
  <done>charactersRepo implemented with sync transactions, JSON parse layer, delta-clamp mutations (including XP). Repo tests green or close to green; tRPC router test scaffold ready for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (tRPC IPC) | All character mutations cross this boundary — Plan 02 tRPC layer enforces Zod; Plan 01 schemas.ts defines the contracts |
| filesystem ↔ main (content JSON read) | contentLoader reads from a fixed path under resources/ — no user-supplied paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | characters.create Zod input | mitigate | characterNameSchema (min 1, max 100), backstorySchema (max 2000), abilityScoreSchema (int 1-30) defined in schemas.ts; enforced at Plan 02 tRPC layer |
| T-02-02 | Tampering | hp/currency/xp delta values | mitigate | hpDeltaSchema (int -9999..9999), currencyDeltaSchema (int -999999..999999), xpDeltaSchema (int -999999..999999); delta-based mutations prevent last-write-wins races; SQL MAX(0, ...) clamps prevent negative XP |
| T-02-03 | Tampering | conditions JSON column | mitigate | conditionNameSchema (z.enum of 14 fixed strings); never JSON.parse user input directly into DB column |
| T-02-04 | Tampering | FK violation (characterId not in characters) | mitigate | PRAGMA foreign_keys = ON (Phase 1); Drizzle .references() generates FK constraint in 0001 migration |
| T-02-SC | Tampering | Wave 1 has no package installs | accept | jimp/wasm-webp install deferred to Plan 02; legitimacy already audited in 02-RESEARCH.md §Package Legitimacy Audit |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm test -- src/main/characters/calculations.test.ts` passes 100%
- `npm test -- src/main/db/charactersRepo.test.ts` runs without import errors
- `npm test` (full suite) does not regress Phase 1 tests
- resources/migrations/0001_*.sql exists and was generated by drizzle-kit (presence of meta/_journal.json idx 1 entry)
</verification>

<success_criteria>
Wave 1 foundation in place: schema extended, migration generated, calculations tested, repo with JSON-aware layer ready (including updateXp for CHAR-07), test scaffolds for Wave 2 created. No regressions in Phase 1 test suite.
</success_criteria>

<output>
Create `.planning/phases/02-character-domain-live-sheet/02-01-SUMMARY.md` when done.
</output>
