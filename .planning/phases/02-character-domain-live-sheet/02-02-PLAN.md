---
phase: 02-character-domain-live-sheet
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/main/trpc/routers/characters.ts
  - src/main/trpc/routers/content.ts
  - src/main/trpc/router.ts
  - src/main/trpc/routers/characters.test.ts
  - electron-builder.yml
  - package.json
autonomous: true
requirements: [CHAR-01, CHAR-07, CHAR-09]
must_haves:
  truths:
    - "characters tRPC router exposes create, getByCampaignId, getPortraitDataUrl, importPortrait, updateAbilityScore, updateXp, updateHp, updateTempHp, updateCurrency, updateSpellSlot, toggleCondition, updateDeathSaves, toggleInspiration, toggleItemAttuned procedures"
    - "content tRPC router exposes races.list, classes.list, backgrounds.list, equipment.listForClass, spellSlots.forClass procedures"
    - "Both routers registered in src/main/trpc/router.ts and accessible via trpc.characters.* and trpc.content.* in renderer"
    - "All inputs Zod-validated using shared schemas from src/main/trpc/schemas.ts"
    - "characters.create auto-calculates HP/AC/initiative/spellSlots via calculations.ts before db.transaction"
    - "characters.updateXp delta mutation persists across getByCampaignId and rejects out-of-range delta via xpDeltaSchema"
    - "electron-builder.yml asarUnpack and extraResources include resources/*.json"
    - "jimp and @jimp/wasm-webp installed as direct dependencies"
  artifacts:
    - path: "src/main/trpc/routers/characters.ts"
      provides: "characters tRPC router with create, query, all mutation procedures (including updateXp)"
    - path: "src/main/trpc/routers/content.ts"
      provides: "content tRPC nested router for races/classes/backgrounds/equipment/spellSlots"
    - path: "src/main/trpc/router.ts"
      provides: "AppRouter type including characters and content keys"
    - path: "electron-builder.yml"
      provides: "resources/*.json in asarUnpack and extraResources"
  key_links:
    - from: "src/main/trpc/routers/characters.ts"
      to: "src/main/db/charactersRepo.ts"
      via: "import { charactersRepo }"
      pattern: "from.*charactersRepo"
    - from: "src/main/trpc/routers/characters.ts"
      to: "src/main/characters/calculations.ts"
      via: "import { calcHP, calcAC, buildSpellSlots }"
      pattern: "from.*calculations"
    - from: "src/main/trpc/router.ts"
      to: "src/main/trpc/routers/characters.ts"
      via: "import { charactersRouter }"
      pattern: "import.*charactersRouter"
---

<objective>
Wire the character domain through tRPC: characters router (CRUD + live-play mutations + image queries) and content router (JSON content delivery). Register both in the main router. Install jimp dependencies and configure electron-builder to ship resources/*.json with the package.

Purpose: This wave makes the Plan 01 foundation reachable from the renderer. Tests 2-xx-05, 2-xx-06, 2-xx-08, 2-xx-11 from 02-VALIDATION.md go green here.

Output: tRPC routers complete and tested. Build config supports content JSON files. Dependencies installed. Renderer can call trpc.characters.* and trpc.content.* — but content data is missing until Plan 03 authors the JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-character-domain-live-sheet/02-CONTEXT.md
@.planning/phases/02-character-domain-live-sheet/02-RESEARCH.md
@.planning/phases/02-character-domain-live-sheet/02-PATTERNS.md
@.planning/phases/02-character-domain-live-sheet/02-VALIDATION.md
@.planning/phases/02-character-domain-live-sheet/02-01-SUMMARY.md
@src/main/trpc/routers/campaigns.ts
@src/main/trpc/routers/prefs.ts
@src/main/trpc/router.ts
@src/main/trpc/_base.ts
@src/main/trpc/schemas.ts
@src/main/trpc/routers/secrets.test.ts
@electron-builder.yml
@package.json

<interfaces>
<!-- From Plan 01 -->
charactersRepo: createWithResources, getWithResources, getByCampaignId, updateAbilityScore, updateXp, updateHp, updateTempHp, updateCurrency, updateSpellSlot, toggleCondition, updateDeathSaves, toggleInspiration, toggleItemAttuned, updatePortraitPath
calculations: calcHP(hitDie, conScore), calcAC(dexScore, armorBase?), calcInitiativeBonus(dexScore), calcAbilityModifier(score), buildSpellSlots(className, level, spellSlotsByClass)
contentLoader: loadContent() returns { races, classes, backgrounds, equipment, spellSlotsByClass }
schemas: campaignIdSchema, characterNameSchema, backstorySchema, abilityScoreSchema, hpDeltaSchema, currencyDeltaSchema, xpDeltaSchema, conditionNameSchema

<!-- tRPC base -->
src/main/trpc/_base.ts exports `t` (initialized tRPC instance with superjson transformer)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Legitimacy gate — jimp and @jimp/wasm-webp install</name>
  <what-built>About to install npm packages flagged in 02-RESEARCH.md §Package Legitimacy Audit as [OK] but never installed before in this project.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/jimp — confirm publisher is "jimp-dev" or maintainer is recognized; repo links to github.com/jimp-dev/jimp; downloads > 1M/week.
    2. Open https://www.npmjs.com/package/@jimp/wasm-webp — confirm same monorepo (github.com/jimp-dev/jimp); version 1.6.x current.
    3. Neither package has a postinstall script (verified during research — re-confirm by inspecting "scripts" on the npmjs page).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (block install).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install jimp deps and update electron-builder.yml</name>
  <files>package.json, package-lock.json, electron-builder.yml</files>
  <read_first>
    - electron-builder.yml (current asarUnpack and extraResources entries)
    - package.json (current dependencies block)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 3 (electron-builder.yml additions block)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pitfall 2 (ASAR + extraResources path resolution)
  </read_first>
  <action>
    Run `npm install jimp@^1.6.1 @jimp/wasm-webp@^1.6.1`. Both go in `dependencies` (NOT devDependencies — runtime needs them).

    Edit electron-builder.yml. In the `asarUnpack:` block, add the line `- "resources/*.json"` after the existing `- "resources/migrations/**"`. In the `extraResources:` block, add a second entry mirroring RESEARCH.md §Pattern 3:
      ```
      - from: resources
        to: .
        filter:
          - "*.json"
      ```
    Keep the existing `from: resources/migrations` entry unchanged.

    Do not modify any other electron-builder.yml keys.
  </action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); if(!pkg.dependencies.jimp) throw new Error('jimp missing'); if(!pkg.dependencies['@jimp/wasm-webp']) throw new Error('@jimp/wasm-webp missing'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies.jimp` matches `^1.6.x`
    - package.json `dependencies["@jimp/wasm-webp"]` matches `^1.6.x`
    - electron-builder.yml `asarUnpack:` block contains `"resources/*.json"`
    - electron-builder.yml `extraResources:` block contains two entries (one for migrations, one for resources root with `*.json` filter)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Dependencies installed. Build config copies JSON content into packaged build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: characters tRPC router (CRUD + live-play mutations + image queries) with green tests</name>
  <files>src/main/trpc/routers/characters.ts, src/main/trpc/routers/characters.test.ts</files>
  <read_first>
    - src/main/trpc/routers/campaigns.ts (procedure pattern)
    - src/main/trpc/routers/secrets.test.ts (tRPC v10 caller pattern — router.createCaller({}) NOT createCallerFactory per Pitfall 9)
    - src/main/trpc/schemas.ts (after Plan 01 — has all required Zod schemas including xpDeltaSchema)
    - src/main/db/charactersRepo.ts (from Plan 01)
    - src/main/characters/calculations.ts (from Plan 01)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 2 (auto-calc inside transaction), §Pitfall 5, §Pitfall 7, §Pitfall 9
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §characters.ts
  </read_first>
  <behavior>
    - characters.create: input { campaignId, name, race, subrace?, class, subclass?, background, level=1, abilityScores (6 scores), savingThrowProficiencies (string[]), skillProficiencies (string[]), languages (string[]), backstory? (string), equipmentPackageId, startingItems ([]), startingGold (int), traitsText, classFeatureText, backgroundFeatureText, hitDie (int), armorBaseAc? (int), speed (int) }. Inside handler: call calcHP(hitDie, abilityScores.constitution), calcAC(abilityScores.dexterity, armorBaseAc), calcInitiativeBonus(abilityScores.dexterity), look up spellSlots via loadContent().spellSlotsByClass and buildSpellSlots(class, 1, ...). Then call charactersRepo.createWithResources with the assembled input. Catch SQLite UNIQUE constraint errors and throw TRPCError({ code: 'CONFLICT', message: 'A character already exists for this campaign.' }) per Pitfall 7.
    - characters.getByCampaignId: input { campaignId }. Returns charactersRepo.getByCampaignId(campaignId) — may be null/undefined.
    - characters.getPortraitDataUrl: input { characterId }. Reads characters.portraitPath, prefixes app.getPath('userData'), reads file via fs/promises.readFile, returns `data:image/{ext};base64,{base64}` string or null if no portrait.
    - characters.importPortrait: input { characterId }. Calls imageService.importImage(campaignId, 'portrait'). On non-null result, calls charactersRepo.updatePortraitPath(characterId, relativePath). Returns the relative path or null.
    - characters.updateAbilityScore: input { characterId, ability (z.enum of 6 ability strings), value (abilityScoreSchema) }. Calls charactersRepo.updateAbilityScore.
    - characters.updateXp: input { characterId, delta (xpDeltaSchema — int -999999..999999) }. Calls charactersRepo.updateXp. Per D-15/CHAR-07 the level-up flow is Phase 5; this procedure only mutates the xp counter.
    - characters.updateHp: input { characterId, delta (hpDeltaSchema) }. Calls charactersRepo.updateHp.
    - characters.updateTempHp: input { characterId, delta }. Same shape.
    - characters.updateCurrency: input { characterId, denomination (z.enum(['cp','sp','ep','gp','pp'])), delta (currencyDeltaSchema) }.
    - characters.updateSpellSlot: input { characterId, slotLevel (z.string()), delta (z.number().int().min(-9).max(9)) }.
    - characters.toggleCondition: input { characterId, condition (conditionNameSchema) }.
    - characters.updateDeathSaves: input { characterId, successes (z.number().int().min(0).max(3)), failures (z.number().int().min(0).max(3)) }.
    - characters.toggleInspiration: input { characterId }.
    - characters.toggleItemAttuned: input { itemId }. (Item identified by itemId, not characterId — itemId is unique.)
  </behavior>
  <action>
    Create src/main/trpc/routers/characters.ts following campaigns.ts structure. Import `t` from `'../_base'`, `TRPCError` from `'@trpc/server'`, `charactersRepo` from `'../../db/charactersRepo'`, calculation functions from `'../../characters/calculations'`, `loadContent` from `'../../db/contentLoader'`, all needed Zod schemas (including xpDeltaSchema) from `'../schemas'`, and `importImage` from `'../../imageService'` (will exist in Plan 04 — until then, stub the importPortrait procedure with `throw new TRPCError({ code: 'NOT_IMPLEMENTED' })` so router compiles; replace in Plan 04).

    For characters.create: assemble the inserts. Call calcHP/calcAC/calcInitiativeBonus/buildSpellSlots before db.transaction. Apply racial ASI bonuses to ability scores BEFORE passing to charactersRepo (per D-15 — "racial ASI bonuses added to base ability scores"). Wrap repo call in try/catch; on SqliteError with code SQLITE_CONSTRAINT_UNIQUE rethrow TRPCError CONFLICT per Pitfall 7.

    For characters.updateXp: simple wiring — `t.procedure.input(z.object({ characterId: z.string().uuid(), delta: xpDeltaSchema })).mutation(({ input }) => { charactersRepo.updateXp(input.characterId, input.delta) })` — returns void (consistent with updateHp and other mutation procedures).

    For characters.getPortraitDataUrl: read file via `fs/promises.readFile`, infer extension from path, return `data:image/{ext};base64,{buffer.toString('base64')}`. Wrap in try/catch returning null on ENOENT.

    Replace the stub tests in src/main/trpc/routers/characters.test.ts (Plan 01 created the file with it.todo). Convert to real tests using router.createCaller({}) per Pitfall 9. Mock electron app per secrets.test.ts. Use in-memory better-sqlite3 with migrations applied at beforeEach. Test cases (per 02-VALIDATION.md):
      - characters.create writes a character to DB (call caller.characters.create with valid input, then call caller.characters.getByCampaignId and assert non-null with expected name)
      - characters.create rejects malformed input: empty name → Zod throws; ability score 0 → throws; ability score 31 → throws
      - characters.create throws CONFLICT TRPCError when called twice for same campaignId
      - characters.updateHp delta=+5 starting from 10 → 15
      - characters.updateHp delta=+100 starting from 10/12 max → clamps to 12
      - characters.updateHp delta=-100 → clamps to 0
      - characters.updateHp rejects delta=10000 (out of hpDeltaSchema range)
      - characters.updateXp delta=+300 starting from xp=0 → xp persists as 300 via getByCampaignId
      - characters.updateXp delta=-500 starting from xp=300 → clamps to 0
      - characters.updateXp rejects delta=10_000_000 (out of xpDeltaSchema range)
      - characters.toggleCondition('poisoned') adds it; calling again removes it; persisted via getByCampaignId
      - characters.toggleItemAttuned flips isAttuned and persists
      - characters.toggleInspiration flips hasInspiration

    Remove all it.todo() / it.skip() from this file.
  </action>
  <verify>
    <automated>npm test -- src/main/trpc/routers/characters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/trpc/routers/characters.ts contains `export const charactersRouter = t.router({`
    - src/main/trpc/routers/characters.ts contains procedure keys: create, getByCampaignId, getPortraitDataUrl, importPortrait, updateAbilityScore, updateXp, updateHp, updateTempHp, updateCurrency, updateSpellSlot, toggleCondition, updateDeathSaves, toggleInspiration, toggleItemAttuned
    - src/main/trpc/routers/characters.ts contains `code: 'CONFLICT'` (unique constraint handler)
    - src/main/trpc/routers/characters.ts imports calcHP, calcAC, calcInitiativeBonus, buildSpellSlots
    - src/main/trpc/routers/characters.ts imports xpDeltaSchema from '../schemas'
    - src/main/trpc/routers/characters.test.ts contains no `it.todo(` and no `it.skip(` occurrences
    - src/main/trpc/routers/characters.test.ts uses `router.createCaller({})` (NOT createCallerFactory)
    - src/main/trpc/routers/characters.test.ts contains at least 3 `it(` blocks referencing `updateXp`
    - `npm test -- src/main/trpc/routers/characters.test.ts` exits 0 with all tests passing
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>characters router complete with 14 procedures (including updateXp); Wave 0 tRPC tests are green (CHAR-01, CHAR-07, CHAR-09).</done>
</task>

<task type="auto">
  <name>Task 3: content tRPC router + register both routers in main router</name>
  <files>src/main/trpc/routers/content.ts, src/main/trpc/router.ts</files>
  <read_first>
    - src/main/trpc/routers/prefs.ts (nested router pattern)
    - src/main/trpc/router.ts (current registration block)
    - src/main/db/contentLoader.ts (from Plan 01)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 3 (contentRouter)
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §content.ts
  </read_first>
  <behavior>
    - contentRouter is a nested router: races.list (query, no input), classes.list (query, no input), backgrounds.list (query, no input), equipment.listForClass (query, input { className: z.string() }), spellSlots.forClass (query, input { className: z.string() })
    - Each query handler calls loadContent() (which is module-level cached after first call) and returns the appropriate slice
    - router.ts adds `characters: charactersRouter` and `content: contentRouter` keys
  </behavior>
  <action>
    Create src/main/trpc/routers/content.ts per RESEARCH.md §Pattern 3. Imports: `import { z } from 'zod'`, `import { t } from '../_base'`, `import { loadContent } from '../../db/contentLoader'`. Define `contentRouter = t.router({ races: t.router({ list: t.procedure.query(() => loadContent().races) }), classes: ..., backgrounds: ..., equipment: t.router({ listForClass: t.procedure.input(z.object({ className: z.string() })).query(({ input }) => loadContent().equipment[input.className] ?? []) }), spellSlots: t.router({ forClass: ... }) })`.

    Edit src/main/trpc/router.ts: add `import { charactersRouter } from './routers/characters'` and `import { contentRouter } from './routers/content'` at the top with existing imports. Add `characters: charactersRouter,` and `content: contentRouter,` to the `t.router({})` body, placed after `campaigns:` for alphabetical/logical ordering.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/main/trpc/routers/content.ts contains `export const contentRouter = t.router({`
    - src/main/trpc/routers/content.ts contains nested keys: `races: t.router({`, `classes: t.router({`, `backgrounds: t.router({`, `equipment: t.router({`, `spellSlots: t.router({`
    - src/main/trpc/router.ts contains `import { charactersRouter } from './routers/characters'`
    - src/main/trpc/router.ts contains `import { contentRouter } from './routers/content'`
    - src/main/trpc/router.ts router object contains `characters: charactersRouter` and `content: contentRouter`
    - `npm run typecheck` exits 0
    - `npm test` (full suite) exits 0 (no regressions)
  </acceptance_criteria>
  <done>Both routers registered. AppRouter type now exposes trpc.characters.* and trpc.content.* in renderer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (tRPC) | Every characters.* procedure crosses here — all inputs Zod-validated using schemas from Plan 01 |
| main → SQLite | All writes through charactersRepo synchronous methods; PRAGMA foreign_keys = ON enforces FK constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | characters.create payload | mitigate | Zod input schema enforces all field shapes; SQLite UNIQUE constraint catches duplicate-character race; TRPCError CONFLICT surfaces to UI |
| T-02-06 | Tampering | delta-based mutations (HP / currency / XP) | mitigate | hpDeltaSchema/currencyDeltaSchema/xpDeltaSchema bound integer range; charactersRepo SQL clamp prevents negative HP/XP/overflow |
| T-02-07 | Information disclosure | getPortraitDataUrl arbitrary file read | mitigate | Procedure takes characterId only — derives path from DB column (controlled by importImage which uses OS dialog); never accepts a raw path from renderer per Pitfall 4 |
| T-02-SC | Tampering | jimp + @jimp/wasm-webp install | mitigate | Task 0 blocking-human checkpoint verifies legitimacy on npmjs.com (per 02-RESEARCH.md §Package Legitimacy Audit [OK] disposition) |
</threat_model>

<verification>
- `npm test` (full suite) passes
- `npm run typecheck` passes
- Both new routers reachable via type inspection of AppRouter
</verification>

<success_criteria>
Wave 2 complete: tRPC layer wired, jimp deps installed, electron-builder ships JSON content. Tests 2-xx-05/06/08/11 green. Renderer can now call trpc.characters.* and trpc.content.* — though content queries return undefined until Plan 03 ships JSON data, and importPortrait is stubbed until Plan 04.
</success_criteria>

<output>
Create `.planning/phases/02-character-domain-live-sheet/02-02-SUMMARY.md` when done.
</output>
