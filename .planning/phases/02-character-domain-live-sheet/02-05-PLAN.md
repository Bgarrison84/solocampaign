---
phase: 02-character-domain-live-sheet
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - src/renderer/src/components/ui/select.tsx
  - src/renderer/src/components/CharacterSheetTab.tsx
  - src/renderer/src/components/CreateCharacterWizard.tsx
  - src/renderer/src/components/wizard/WizardProgress.tsx
  - src/renderer/src/components/wizard/StepRace.tsx
  - src/renderer/src/components/wizard/StepClass.tsx
  - src/renderer/src/components/wizard/StepAbilityScores.tsx
  - src/renderer/src/components/wizard/StepBackground.tsx
  - src/renderer/src/components/wizard/StepEquipment.tsx
  - src/renderer/src/components/wizard/StepReview.tsx
  - src/renderer/src/components/wizard/wizardTypes.ts
  - src/renderer/src/stores/characterStore.ts
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [CHAR-01]
must_haves:
  truths:
    - "When a campaign with no character is opened, the wizard auto-launches per D-04 and cannot be dismissed by Escape or backdrop click"
    - "All 6 wizard steps render per UI-SPEC §3 with two-column layout (200px selector + 460px stat block), 720px dialog"
    - "WizardProgress shows 6 numbered dots; completed-up-to dots are clickable for backward navigation; future dots are visually disabled"
    - "Step 1 collects character name + backstory above the species selector"
    - "Step 2 shows subclass picker only for Cleric/Sorcerer/Warlock (choosesSubclassAtLevel1)"
    - "Step 3 dropdown UX (Q2 answer) — each ability has shadcn Select showing remaining unassigned standard array values + currently-assigned value; manual Override link toggles to numeric Input"
    - "Step 6 Review shows computed HP/AC/Initiative/spell slots using the same calculation logic as main"
    - "Confirm fires characters.create useMutation; on success queryClient.invalidateQueries(['characters','getByCampaignId',campaignId])"
    - "Wizard cannot be dismissed; Cancel button shows confirmation dialog and on confirm navigates to campaign list"
    - "CampaignViewScreen line 137 placeholder replaced with <CharacterSheetTab campaignId={id} />"
  artifacts:
    - path: "src/renderer/src/components/CharacterSheetTab.tsx"
      provides: "Orchestrator: queries characters.getByCampaignId; renders Wizard or sheet"
    - path: "src/renderer/src/components/CreateCharacterWizard.tsx"
      provides: "6-step modal wizard"
    - path: "src/renderer/src/components/ui/select.tsx"
      provides: "shadcn Select component"
  key_links:
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "src/renderer/src/components/CharacterSheetTab.tsx"
      via: "<CharacterSheetTab campaignId={id} />"
      pattern: "<CharacterSheetTab"
    - from: "src/renderer/src/components/CharacterSheetTab.tsx"
      to: "trpc.characters.getByCampaignId"
      via: "useQuery"
      pattern: "characters\\.getByCampaignId"
    - from: "src/renderer/src/components/CreateCharacterWizard.tsx"
      to: "trpc.characters.create"
      via: "useMutation"
      pattern: "characters\\.create"
---

<objective>
Build the 6-step character creation wizard per UI-SPEC §3 plus the CharacterSheetTab orchestrator that auto-launches it when characterData === null. This is the vertical slice that takes the player from "no character" to "character persisted in DB" — the Plan 06 sheet UI displays the result.

Purpose: Closes the wizard portion of CHAR-01. After this plan, the user can complete the wizard and see "character created" — though the sheet itself is still a placeholder until Plan 06 ships.

Output: Auto-launching wizard with all 6 steps, full UI-SPEC §3 compliance, character persisted via tRPC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-character-domain-live-sheet/02-CONTEXT.md
@.planning/phases/02-character-domain-live-sheet/02-RESEARCH.md
@.planning/phases/02-character-domain-live-sheet/02-PATTERNS.md
@.planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md
@src/renderer/src/components/CreateCampaignModal.tsx
@src/renderer/src/components/ui/dialog.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/input.tsx
@src/renderer/src/components/ui/label.tsx
@src/renderer/src/screens/CampaignViewScreen.tsx
@src/renderer/src/lib/trpc.ts
@src/main/characters/calculations.ts
@src/main/db/contentTypes.ts

<interfaces>
<!-- tRPC procedures available (from Plan 02 + 04) -->
trpc.characters.create({...full input...}) → CharacterWithResources
trpc.characters.getByCampaignId({campaignId}) → CharacterWithResources | null
trpc.content.races.list() → Race[]
trpc.content.classes.list() → DndClass[]
trpc.content.backgrounds.list() → Background[]
trpc.content.equipment.listForClass({className}) → EquipmentPackageOption[]
trpc.content.spellSlots.forClass({className}) → SpellSlotMap | null

<!-- Types from src/main/db/contentTypes.ts -->
import type { Race, DndClass, Subclass, EquipmentPackageOption, Background } from '../../../main/db/contentTypes'

<!-- Pure calculations (renderer can import directly — they have no electron deps) -->
import { calcHP, calcAC, calcInitiativeBonus, calcAbilityModifier, buildSpellSlots } from '../../../main/characters/calculations'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Select; create wizard types and orchestrator</name>
  <files>src/renderer/src/components/ui/select.tsx, src/renderer/src/components/wizard/wizardTypes.ts, src/renderer/src/components/CharacterSheetTab.tsx, src/renderer/src/stores/characterStore.ts, src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/components/ui/button.tsx (shadcn install convention)
    - src/renderer/src/screens/CampaignViewScreen.tsx lines 137-144 (placeholder to replace)
    - src/renderer/src/stores/panelSizeStore.ts (zustand pattern)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §1.1 (Select required), §4.1 (CharacterSheetTab layout)
    - .planning/phases/02-character-domain-live-sheet/02-PATTERNS.md §CharacterSheetTab.tsx, §characterStore.ts
  </read_first>
  <action>
    Run `npx shadcn@latest add select` to install shadcn Select component into src/renderer/src/components/ui/select.tsx. Verify the file exports Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectTrigger, SelectValue (per shadcn convention). If the CLI errors due to project config, fall back to copying the official shadcn select.tsx source from ui.shadcn.com/docs/components/select.

    Create src/renderer/src/components/wizard/wizardTypes.ts. Export the WizardState interface from RESEARCH.md §Pattern 6 with these fields: step (0-5), selectedRace (Race|null), selectedSubrace (string|null), selectedClass (DndClass|null), selectedSubclass (string|null), abilityScores ({strength,dexterity,constitution,intelligence,wisdom,charisma} all number|null), selectedSkillProficiencies (string[]), selectedSaveProficiencies (string[]), selectedBackground (Background|null), selectedLanguage (string|null), selectedEquipmentPackage (EquipmentPackageOption|null), characterName (string), backstory (string), personalityTrait/ideal/bond/flaw (each string). Export STEP_NAMES array per UI-SPEC §3.1 and TOTAL_STEPS=6. Export `initialWizardState` constant.

    Create src/renderer/src/stores/characterStore.ts per PATTERNS.md §characterStore.ts. Zustand store with wizardDraft (WizardState | null), setWizardDraft, updateWizardStep. Note: live-resource mutations go through TanStack Query optimistic updates, NOT this store.

    Create src/renderer/src/components/CharacterSheetTab.tsx. Props: { campaignId: string }. Uses useQuery({ queryKey: ['characters', 'getByCampaignId', campaignId], queryFn: () => trpc.characters.getByCampaignId.query({ campaignId }), enabled: !!campaignId }). Render branches per UI-SPEC §6.5 / D-04:
      - isLoading → `<p>Building your character…</p>` per UI-SPEC §6.5
      - data === null → `<CreateCharacterWizard campaignId={campaignId} />` (no dismiss handler — D-04)
      - data → placeholder for now: `<div>Character sheet TODO (Plan 06)</div>` displaying character.name. Plan 06 replaces this with the full 10-section sheet.

    Edit src/renderer/src/screens/CampaignViewScreen.tsx line 137-144. Replace the inner placeholder div (the `<div className="flex flex-col items-center ...">` containing "Character Sheet" h2 and the placeholder p) with `<CharacterSheetTab campaignId={id} />`. Keep the `<TabsContent value="character-sheet" className="flex-1 overflow-auto p-6">` wrapper but remove the inner p-6 if it conflicts with CharacterSheetTab's own padding — adjust per UI-SPEC §4.1 (CharacterSheetTab provides its own `p-4` and overflow). Recommend changing the TabsContent className from `flex-1 overflow-auto p-6` to `flex-1 overflow-hidden p-0` to defer scroll+padding to the child.

    Add import for CharacterSheetTab in CampaignViewScreen.tsx.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/ui/select.tsx exists and exports Select, SelectContent, SelectItem, SelectTrigger, SelectValue
    - src/renderer/src/components/wizard/wizardTypes.ts exports interface WizardState, const TOTAL_STEPS = 6, STEP_NAMES array of 6 strings starting with 'Race'
    - src/renderer/src/stores/characterStore.ts exports useCharacterStore zustand store with wizardDraft field
    - src/renderer/src/components/CharacterSheetTab.tsx contains `useQuery` with queryKey including 'getByCampaignId'
    - src/renderer/src/components/CharacterSheetTab.tsx contains `<CreateCharacterWizard campaignId={campaignId}` (auto-launch per D-04)
    - src/renderer/src/screens/CampaignViewScreen.tsx contains `<CharacterSheetTab campaignId={id}`
    - src/renderer/src/screens/CampaignViewScreen.tsx no longer contains the literal string "Your character sheet will appear here after character creation (Phase 2)"
    - `npm run typecheck` exits 0
    - `npm test` (full suite) exits 0 (catches Phase 1 regressions — W7 fix)
  </acceptance_criteria>
  <done>Select added; orchestrator + types + store + integration into CampaignViewScreen complete. Wizard component itself is built next.</done>
</task>

<task type="auto">
  <name>Task 2: Build CreateCharacterWizard + WizardProgress + StepRace + StepClass</name>
  <files>src/renderer/src/components/CreateCharacterWizard.tsx, src/renderer/src/components/wizard/WizardProgress.tsx, src/renderer/src/components/wizard/StepRace.tsx, src/renderer/src/components/wizard/StepClass.tsx</files>
  <read_first>
    - src/renderer/src/components/CreateCampaignModal.tsx (Dialog + useMutation patterns)
    - src/renderer/src/components/wizard/wizardTypes.ts (Task 1 — WizardState)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §2.1 (WizardProgress spec), §3.1 (wizard container), §3.2 (Step 1 Race), §3.3 (Step 2 Class), §3.8 (footer), §6.1 (copy), §6.2 (validation messages)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-04 (auto-launch, non-dismissible), D-08, D-09 (subclass for Cleric/Sorcerer/Warlock), D-13 (name in Step 1)
  </read_first>
  <behavior>
    - CreateCharacterWizard: Dialog `open={true} onOpenChange={(isOpen) => { /* no-op per D-04 */ }}` (cannot be dismissed). DialogContent className="sm:max-w-[720px] w-full h-auto max-h-[90vh] overflow-hidden flex flex-col". Holds wizardState via useState (initialWizardState). Renders WizardProgress at top, active StepN component in middle (flex-1 overflow-hidden), footer with Cancel/Back/Next/Confirm at bottom. Tracks `completedUpTo` (highest step index that has passed validation). Cancel button opens AlertDialog ("Cancel character creation?") which on confirm navigates to `/` (campaign list) via useNavigate.
    - Confirm (Step 6) fires useMutation calling trpc.characters.create.mutate with assembled input. On success: invalidates queryKey ['characters','getByCampaignId',campaignId]. On error: surfaces TRPCError CONFLICT or generic error per UI-SPEC §6.4.
    - Pre-fetch all content via useQuery for races/classes/backgrounds/equipment+spellSlots-for-selected-class on mount; pass as props to step components (per PATTERNS.md "prefer passing content data as props to avoid redundant queries")
    - WizardProgress: row of 6 circles 32×32px rounded-full per UI-SPEC §2.1. States: future (gray outline disabled), completed (gold fill, clickable backward), active (gold ring). Clicking completed dot calls onStepClick(index). Future dots: cursor-not-allowed opacity-50.
    - StepRace: name + backstory inputs above two-column area; left column 200px scrollable race list (group by parentRace as headers); right column 460px stat block per UI-SPEC §3.2. isValid = characterName.trim().length >= 1 && selectedRace !== null.
    - StepClass: left column list of classes from content.classes.list; right column stat block including hit die, primary ability, saves, proficiencies, skill choices, equipment package preview, level-1 features full text per UI-SPEC §3.3. When selectedClass.choosesSubclassAtLevel1 === true, render shadcn Select below class list with subclass options from selectedClass.subclasses; label per UI-SPEC §3.3 ('Divine Domain' / 'Sorcerous Origin' / 'Otherworldly Patron'). isValid = selectedClass !== null && (!choosesSubclassAtLevel1 || selectedSubclass !== null).
  </behavior>
  <action>
    Create src/renderer/src/components/wizard/WizardProgress.tsx per UI-SPEC §2.1. Props: { totalSteps, currentStep, completedUpTo, stepLabels, onStepClick }. Render row of circles using `flex flex-row items-center justify-center gap-2` with a `border-t border-border` line behind. Each circle is a `<button>` with type-button. Conditional Tailwind classes per the 3 states. Below each circle, label in `text-xs text-muted-foreground` (or `text-accent-gold font-medium` when active). Use accent-gold from Phase 1 tokens — class `bg-accent-gold`, `border-accent-gold`, `text-accent-gold` referencing the existing theme variables.

    Create src/renderer/src/components/CreateCharacterWizard.tsx. Imports: React, useState, useMutation, useQuery, useQueryClient from @tanstack/react-query, useNavigate from react-router-dom, trpc, Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter, Button, AlertDialog (if not present add via shadcn), WizardProgress, StepRace, StepClass, etc. Hold wizardState + completedUpTo + step in useState. Pre-fetch content queries on mount (raceQuery, classQuery, backgroundQuery; equipmentQuery+spellSlotsQuery activate after class selected via `enabled: !!wizardState.selectedClass`). Compose footer: ghost Cancel button (left), outline Back button (hidden when step===0), default Next button (steps 0-4) OR default Create Character button (step 5) with isPending spinner state. Wire isValid per step (compute via helper fns or per-step exports).

    Create StepRace.tsx and StepClass.tsx per the Behavior block. Each receives current wizardState and an `onChange(partial: Partial<WizardState>)` callback. Each exports a named React function component and optionally a `isStepNValid(state)` helper.

    All copy strings come from UI-SPEC §6.1 exactly. Validation messages from §6.2.

    Confirm mutation (placed in wizard, fires only on Step 6 Confirm — implemented in Task 3 but wired here as the useMutation hook):
      ```
      const createMutation = useMutation({
        mutationFn: (input) => trpc.characters.create.mutate(input),
        onSuccess: () => queryClient.invalidateQueries({ queryKey: ['characters','getByCampaignId',campaignId] }),
        onError: (err) => setError(err.message)
      })
      ```
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/CreateCharacterWizard.tsx contains `<Dialog open={true}` (or equivalent — wizard not dismissible per D-04)
    - src/renderer/src/components/CreateCharacterWizard.tsx contains `sm:max-w-[720px]`
    - src/renderer/src/components/CreateCharacterWizard.tsx contains `useMutation` calling `trpc.characters.create`
    - src/renderer/src/components/CreateCharacterWizard.tsx contains `invalidateQueries` with `'getByCampaignId'`
    - src/renderer/src/components/wizard/WizardProgress.tsx renders 6 buttons (totalSteps prop)
    - src/renderer/src/components/wizard/StepRace.tsx contains Character Name and Backstory fields above the race list
    - src/renderer/src/components/wizard/StepClass.tsx contains conditional subclass Select that appears only when selectedClass.choosesSubclassAtLevel1
    - All four files contain only copy strings sourced from UI-SPEC §6.1 (grep-spot-check: 'Choose a Species', 'Choose a Class', 'Divine Domain', 'Sorcerous Origin', 'Otherworldly Patron')
    - `npm run typecheck` exits 0
    - `npm test` (full suite) exits 0 (W7 fix — catches regressions in Phase 1 or earlier Phase 2 routers)
  </acceptance_criteria>
  <done>Wizard shell + WizardProgress + StepRace + StepClass complete.</done>
</task>

<task type="auto">
  <name>Task 3: Build StepAbilityScores + StepBackground + StepEquipment + StepReview; wire confirm</name>
  <files>src/renderer/src/components/wizard/StepAbilityScores.tsx, src/renderer/src/components/wizard/StepBackground.tsx, src/renderer/src/components/wizard/StepEquipment.tsx, src/renderer/src/components/wizard/StepReview.tsx</files>
  <read_first>
    - src/renderer/src/components/wizard/wizardTypes.ts (Task 1)
    - src/renderer/src/components/CreateCharacterWizard.tsx (Task 2 — confirm mutation hook)
    - src/main/characters/calculations.ts (calcHP, calcAC, calcInitiativeBonus, calcAbilityModifier, buildSpellSlots — renderer imports for Step 6 preview)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §3.4 (Step 3 dropdown + override + skills), §3.5 (Step 4 background + language picker + role-play fields), §3.6 (Step 5 equipment packages), §3.7 (Step 6 Review computed preview), §6.1 (copy), §6.2 (validation messages)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-10 (standard array 15/14/13/12/10/8 with manual override), D-11, D-12, D-15 (auto-calc inputs), D-16 (saving throw / skill choices semantics — see clarification in action block)
  </read_first>
  <behavior>
    - StepAbilityScores: left column shows standard array values 15/14/13/12/10/8 as pills with assignment status. Right column: 6 ability rows, each with shadcn Select dropdown showing remaining unassigned values + currently assigned (so user can re-pick). Below row: "Override" link toggling to numeric Input (1-30). Computed modifier shown live. Saving throw checkboxes pre-checked + read-only for class's two fixed saves (see D-16 clarification in action). Skill proficiency picker showing class's eligible skills with checkboxes and count badge. isValid = all 6 scores assigned AND skill count matches class's skillChoiceCount.
    - StepBackground: left list, right stat block. Below stat block, conditional Language picker (shadcn Select) when freeLanguageChoices > 0. Below the two columns, full-width inputs for personality trait / ideal / bond / flaw (optional). isValid = selectedBackground !== null && (freeLanguageChoices === 0 || selectedLanguage !== null).
    - StepEquipment: left column has 2-3 selectable cards (the class's startingEquipmentPackages from equipment.json). Right column shows full item list of selected package. isValid = selectedEquipmentPackage !== null.
    - StepReview: single full-width column. Show summary with calculated HP/AC/Initiative using calcHP/calcAC/calcInitiativeBonus (call them in the renderer — pure functions). Display spell slots via buildSpellSlots(class, 1, spellSlotsQuery.data). All fields read-only. Confirm button at footer level (the wizard footer handles this — Step 6 doesn't render a Confirm button itself, the wizard footer's Next becomes Create Character).
    - Confirm action (handled in CreateCharacterWizard, fired when on step 5 and user clicks Create Character): Assemble the characters.create input. Apply racial ASI bonuses to base ability scores before sending. Compute hitDie from selectedClass.hitDie, derive armorBaseAc from selectedEquipmentPackage items (if package contains armor, use its AC base — for Phase 2, can default to undefined and rely on `10 + DEX`; Plan 06 sheet will use the stored AC value). Build savingThrowProficiencies from selectedClass.savingThrowProficiencies (auto-applied per D-15). Build skillProficiencies from selectedSkillProficiencies + selectedBackground.skillProficiencies. Languages = race languages + selectedBackground.languages + (selectedLanguage ? [selectedLanguage] : []). Armor/weapon/tool proficiencies from class + background union. Traits text: race.traits as block + class.level1Features as block + background.feature as text. startingItems = selectedEquipmentPackage.items + selectedBackground.startingEquipment. startingGold = selectedEquipmentPackage.startingGold ?? 0 + selectedBackground.startingGold. Backstory concatenates the backstory text + the 4 role-play fields if any.
  </behavior>
  <action>
    Create the four step files following the patterns from Task 2's StepRace/StepClass. Use shadcn Select for ability score dropdown and language picker. For skill proficiency checkboxes use native `<input type="checkbox">` styled with Tailwind, or shadcn Checkbox if available — if not available, add via `npx shadcn@latest add checkbox` (only if needed).

    Per D-16 (updated 2026-05-25): saving throw proficiencies are class-fixed, auto-applied, shown read-only. Render the two class saves pre-checked and disabled (read-only) in the Ability Scores step so the player can see them. DO NOT build a saving-throw picker UI. Skill proficiency picking IS player-driven (class grants a count + a pool of eligible skills) and uses the checkbox + count badge per the Behavior block.

    Implement the assembly logic in CreateCharacterWizard.tsx (extend Task 2's component): a `handleConfirm()` function that builds the create input per the Behavior block and calls createMutation.mutate(input). Wire to the wizard footer's Create Character button (visible only on step === 5). The full assembly logic is non-trivial (racial ASI application, proficiency unions, language unions, item assembly, gold summation) — this is the load-bearing functional behavior that the Plan 02 characters.create end-to-end test covers (verify block runs that test).

    All copy from UI-SPEC §6.1. All validation messages from §6.2. Manual score override shows tooltip "Enter a number between 1 and 30." on invalid input.

    Add cancel confirmation. Use shadcn AlertDialog if available — otherwise use a small inline confirmation modal (separate Dialog with `Cancel character creation?` body and Yes/No buttons per UI-SPEC §6.1). On Yes, useNavigate to `/`.

    Step 6 Review must call the pure calculation functions (imported from src/main/characters/calculations.ts — they have no electron dependencies and can run in renderer) to preview HP/AC/Initiative. Use the selectedClass.hitDie for calcHP. Use buildSpellSlots with the spellSlotsQuery data for the class.
  </action>
  <verify>
    <automated>npm run typecheck && npm test -- src/main/trpc/routers/characters.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/wizard/StepAbilityScores.tsx renders 6 ability rows; uses Select component; has Override link
    - src/renderer/src/components/wizard/StepAbilityScores.tsx contains skill proficiency checkboxes with count badge `{n} / {required} selected`
    - src/renderer/src/components/wizard/StepAbilityScores.tsx renders saving throw checkboxes as pre-checked + disabled (read-only) per D-16 clarification
    - src/renderer/src/components/wizard/StepBackground.tsx renders language Select conditionally on selectedBackground?.freeLanguageChoices
    - src/renderer/src/components/wizard/StepBackground.tsx renders 4 optional inputs for personality trait / ideal / bond / flaw
    - src/renderer/src/components/wizard/StepEquipment.tsx renders 2-3 selectable cards
    - src/renderer/src/components/wizard/StepReview.tsx imports `calcHP` `calcAC` `calcInitiativeBonus` `buildSpellSlots` from '../../../../main/characters/calculations'
    - src/renderer/src/components/CreateCharacterWizard.tsx handleConfirm assembles create input including: name, race, class, abilityScores, savingThrowProficiencies, skillProficiencies, languages, equipmentPackageId, startingItems, hitDie
    - `npm run typecheck` exits 0
    - `npm test -- src/main/trpc/routers/characters.test.ts` exits 0 — functional confirmation that characters.create accepts the assembled payload shape (B2 fix: typecheck alone does not prove the wizard's payload shape will round-trip; this test runs the router end-to-end with the same input contract)
    - `npm test` (full suite) exits 0 (no regressions across Phase 1 + Phase 2 routers)
  </acceptance_criteria>
  <done>All 6 wizard steps complete. Confirm action assembles full create input and persists character via tRPC; functional round-trip verified by characters.test.ts. UI-SPEC §3 compliance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (characters.create) | Wizard payload Zod-validated by Plan 02 router; UI also enforces validation via isValid gates per UI-SPEC §6.2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | Wizard bypass via deep link | mitigate | CharacterSheetTab queries characters.getByCampaignId; if null, wizard auto-launches non-dismissible per D-04; no other code path creates a character |
| T-02-14 | Tampering | Client-side validation bypass | accept | UI validation is UX only — Zod schemas on tRPC inputs enforce server-side bounds (Plan 02) |
</threat_model>

<verification>
- `npm run typecheck` passes
- `npm test` full suite passes (no regression)
- `npm test -- src/main/trpc/routers/characters.test.ts` passes (round-trip confirms wizard payload contract)
- Manual: open app → navigate to campaign with no character → wizard auto-launches → complete all 6 steps → confirm → character persisted (verify via SQLite)
</verification>

<success_criteria>
Wizard delivers vertical slice: user can complete character creation end-to-end, and the resulting CharacterWithResources row exists in the DB. Plan 06 displays it.
</success_criteria>

<output>
Create `.planning/phases/02-character-domain-live-sheet/02-05-SUMMARY.md` when done.
</output>
