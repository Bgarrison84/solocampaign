---
phase: 02-character-domain-live-sheet
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - src/renderer/src/components/CharacterSheetTab.tsx
  - src/renderer/src/components/sheet/SheetHeader.tsx
  - src/renderer/src/components/sheet/AbilityScoresSection.tsx
  - src/renderer/src/components/sheet/SavingThrowsSection.tsx
  - src/renderer/src/components/sheet/SkillsSection.tsx
  - src/renderer/src/components/sheet/CombatStatsSection.tsx
  - src/renderer/src/components/sheet/ResourcesSection.tsx
  - src/renderer/src/components/sheet/CurrencySection.tsx
  - src/renderer/src/components/sheet/EquipmentSection.tsx
  - src/renderer/src/components/sheet/ProficienciesSection.tsx
  - src/renderer/src/components/sheet/TraitsSection.tsx
  - src/renderer/src/components/sheet/Stepper.tsx
  - src/renderer/src/components/sheet/ProficiencyDot.tsx
  - src/renderer/src/components/sheet/ConditionBadge.tsx
  - src/renderer/src/components/sheet/SpellSlotPips.tsx
  - src/renderer/src/components/sheet/PortraitSlot.tsx
  - src/renderer/src/components/sheet/sheetHelpers.ts
autonomous: true
requirements: [CHAR-07, CHAR-09, CHAR-06]
must_haves:
  truths:
    - "All 10 sheet sections render per UI-SPEC §4.2–§4.11 inside CharacterSheetTab when character data is loaded"
    - "HP / temp HP / XP / spell slot / currency / death save / condition / inspiration / attunement mutations fire IMMEDIATELY with optimistic updates and rollback on error (zero-debounce per D-20)"
    - "Ability score edits persist on blur per D-20"
    - "Portrait click triggers characters.importPortrait; PortraitSlot displays base64 dataUrl from characters.getPortraitDataUrl"
    - "Attunement count badge shows {n} / 3 Attuned reactively"
    - "Spell slots section shows only the slot levels the class has (or 'Not a spellcaster' message)"
    - "All copy strings match UI-SPEC §6.3 exactly"
  artifacts:
    - path: "src/renderer/src/components/sheet/Stepper.tsx"
      provides: "Reusable +/- stepper control with sm/md/lg sizes"
    - path: "src/renderer/src/components/sheet/ResourcesSection.tsx"
      provides: "HP/temp HP/inspiration/death saves/spell slots/conditions with zero-debounce mutations"
    - path: "src/renderer/src/components/CharacterSheetTab.tsx"
      provides: "Orchestrator rendering all 10 sections"
  key_links:
    - from: "src/renderer/src/components/sheet/ResourcesSection.tsx"
      to: "trpc.characters.updateHp / toggleCondition / updateDeathSaves / toggleInspiration / updateSpellSlot"
      via: "useMutation with onMutate optimistic + onError rollback + onSettled invalidate"
      pattern: "onMutate.*cancelQueries.*setQueryData"
    - from: "src/renderer/src/components/sheet/SheetHeader.tsx"
      to: "trpc.characters.importPortrait"
      via: "useMutation"
      pattern: "characters\\.importPortrait"
---

<objective>
Build the full 10-section character sheet UI per UI-SPEC §4 with all live-play interactivity (HP/spell-slot/condition/death-save/currency/attunement steppers using zero-debounce optimistic mutations per D-20 and RESEARCH.md §Pattern 5). Replace the Plan 05 placeholder in CharacterSheetTab.

Purpose: Closes CHAR-07 (live-play resources) and CHAR-09 (attunement tracking). Wires CHAR-06 portrait import UI. After this plan, the user has a fully interactive character sheet that persists every interaction immediately.

Output: 10 sheet section components + 5 reusable sheet primitives + interactive Sheet in CharacterSheetTab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-character-domain-live-sheet/02-CONTEXT.md
@.planning/phases/02-character-domain-live-sheet/02-RESEARCH.md
@.planning/phases/02-character-domain-live-sheet/02-PATTERNS.md
@.planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md
@src/renderer/src/components/CharacterSheetTab.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/input.tsx
@src/renderer/src/lib/trpc.ts
@src/main/characters/calculations.ts

<interfaces>
<!-- CharacterWithResources shape (from charactersRepo) -->
type CharacterWithResources = Character & {
  savingThrowProficiencies: string[]
  skillProficiencies: string[]
  skillExpertise: string[]
  languages: string[]
  toolProficiencies: string[]
  armorProficiencies: string[]
  weaponProficiencies: string[]
  resources: CharacterResources & { conditions: string[]; spellSlots: SpellSlotMap }
  items: CharacterItem[]
}

<!-- tRPC mutations to wire -->
trpc.characters.updateAbilityScore({ characterId, ability, value })
trpc.characters.updateHp({ characterId, delta })
trpc.characters.updateTempHp({ characterId, delta })
trpc.characters.updateCurrency({ characterId, denomination, delta })
trpc.characters.updateSpellSlot({ characterId, slotLevel, delta })
trpc.characters.toggleCondition({ characterId, condition })
trpc.characters.updateDeathSaves({ characterId, successes, failures })
trpc.characters.toggleInspiration({ characterId })
trpc.characters.toggleItemAttuned({ itemId })
trpc.characters.updateXp({ characterId, delta })
trpc.characters.importPortrait({ characterId, campaignId })
trpc.characters.getPortraitDataUrl({ characterId })

<!-- Optimistic update pattern -->
QUERY_KEY = ['characters', 'getByCampaignId', campaignId]
useMutation onMutate: cancelQueries → snapshot prev → setQueryData with optimistic value → return {prev}
useMutation onError: setQueryData(QUERY_KEY, context.prev)
useMutation onSettled: invalidateQueries(QUERY_KEY)

<!-- 18 D&D 5e skills (governing ability) -->
acrobatics:dex, animal handling:wis, arcana:int, athletics:str, deception:cha, history:int, insight:wis, intimidation:cha, investigation:int, medicine:wis, nature:int, perception:wis, performance:cha, persuasion:cha, religion:int, sleight of hand:dex, stealth:dex, survival:wis
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sheet primitives (Stepper, ProficiencyDot, ConditionBadge, SpellSlotPips, PortraitSlot) + helpers</name>
  <files>src/renderer/src/components/sheet/Stepper.tsx, src/renderer/src/components/sheet/ProficiencyDot.tsx, src/renderer/src/components/sheet/ConditionBadge.tsx, src/renderer/src/components/sheet/SpellSlotPips.tsx, src/renderer/src/components/sheet/PortraitSlot.tsx, src/renderer/src/components/sheet/sheetHelpers.ts</files>
  <read_first>
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §2.2 (Stepper), §2.3 (ProficiencyDot), §2.4 (ConditionBadge), §2.5 (SpellSlotPips), §2.6 (PortraitSlot)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 5 (zero-debounce optimistic mutation example)
    - src/renderer/src/components/ui/button.tsx (Button variant convention)
    - src/main/characters/calculations.ts (calcAbilityModifier for sheetHelpers)
  </read_first>
  <behavior>
    - Stepper: props { value, min=0, max=Infinity, onChange (delta), step=1, disabled, size: 'sm'|'md'|'lg', label }. Renders `[ − ] [ value ] [ + ]` with sizes per UI-SPEC §2.2 (sm=20px, md=28px, lg=36px). Minus disabled when value===min; plus disabled when value===max. onChange receives +step or -step delta. Wrapping div has role="group" with aria-label={label}.
    - ProficiencyDot: props { state: 'none'|'proficient'|'expertise', size=10 }. SVG or div with class per state per UI-SPEC §2.3. Tooltip on hover via title attribute.
    - ConditionBadge: props { condition, active, onToggle }. Pill per UI-SPEC §2.4 with warning conditions (amber) vs severe conditions (red) coloring. The warning list is: blinded, charmed, deafened, frightened, grappled, invisible, prone, restrained. The severe list is: exhaustion, incapacitated, paralyzed, petrified, poisoned, stunned. Click/Enter/Space toggles.
    - SpellSlotPips: props { slotLevel, used, max, onUse, onRecover }. Renders row of `max` pip circles; rightmost `used` rendered as expended (empty); leftmost (max - used) as available (gold). Click available → onUse; click expended → onRecover. Label ordinal "1st"/"2nd"/etc.
    - PortraitSlot: props { dataUrl, characterName, onImportClick, isLoading }. 80×80 rounded-lg overflow-hidden with border-2; if dataUrl present, img with alt=characterName; else lucide-react User icon centered. Hover overlay with Camera icon. role="button" tabIndex={0}.
    - sheetHelpers: ordinal(n) ('1st','2nd',...,'9th'); formatModifier(mod: number) ('+2', '-1', '+0'); skillsList constant with [{key,name,ability}] for 18 skills; xpThresholds (levels 1-20).
  </behavior>
  <action>
    Create all 6 files per UI-SPEC §2.2-§2.6. Use shadcn Button as base for Stepper buttons (`variant="outline"`). All Tailwind classes from UI-SPEC. Use `lucide-react` icons (User for PortraitSlot fallback, Camera for hover overlay, Plus/Minus for Stepper if iconified).

    sheetHelpers.ts exports:
      - `export function ordinal(n: number): string` ('1st', '2nd', ...)
      - `export function formatModifier(mod: number): string` (+2 / -1 / +0)
      - `export const SKILLS: { key: string; name: string; ability: 'strength'|'dexterity'|... }[]` for 18 D&D 5e skills with governing ability per the interface block above
      - `export const XP_THRESHOLDS: number[]` for levels 1-20 (D&D 5e XP table: [0,300,900,2700,6500,14000,23000,34000,48000,64000,85000,100000,120000,140000,165000,195000,225000,265000,305000,355000])
      - `export const WARNING_CONDITIONS` and `export const SEVERE_CONDITIONS` string arrays per ConditionBadge coloring rule
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/sheet/Stepper.tsx exports default or named `Stepper`; props include value, min, max, onChange (delta-style), size
    - src/renderer/src/components/sheet/ConditionBadge.tsx differentiates active styling for warning vs severe conditions (grep `amber-` and `red-`)
    - src/renderer/src/components/sheet/SpellSlotPips.tsx renders `max` pips with `used` rightmost styled as expended
    - src/renderer/src/components/sheet/PortraitSlot.tsx contains both an `<img>` branch and a fallback icon branch
    - src/renderer/src/components/sheet/sheetHelpers.ts exports SKILLS array with 18 entries, XP_THRESHOLDS with 20 entries, WARNING_CONDITIONS, SEVERE_CONDITIONS
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>All sheet primitives in place; CharacterSheetTab sections in Task 2 can compose them.</done>
</task>

<task type="auto">
  <name>Task 2: Build all 10 sheet section components (read-only + display sections)</name>
  <files>src/renderer/src/components/sheet/SheetHeader.tsx, src/renderer/src/components/sheet/AbilityScoresSection.tsx, src/renderer/src/components/sheet/SavingThrowsSection.tsx, src/renderer/src/components/sheet/SkillsSection.tsx, src/renderer/src/components/sheet/CombatStatsSection.tsx, src/renderer/src/components/sheet/CurrencySection.tsx, src/renderer/src/components/sheet/EquipmentSection.tsx, src/renderer/src/components/sheet/ProficienciesSection.tsx, src/renderer/src/components/sheet/TraitsSection.tsx</files>
  <read_first>
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §4.2 (SheetHeader), §4.3 (AbilityScores), §4.4 (SavingThrows), §4.5 (Skills), §4.6 (Combat), §4.8 (Currency), §4.9 (Equipment), §4.10 (Proficiencies), §4.11 (Traits)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §6.3 (copy contract — all strings)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-18 (10 sections order), D-19 (editable/read-only matrix), D-20 (persist on blur for ability scores)
    - src/main/characters/calculations.ts (calcAbilityModifier)
    - src/renderer/src/components/sheet/sheetHelpers.ts (SKILLS, XP_THRESHOLDS, ordinal, formatModifier)
  </read_first>
  <behavior>
    - SheetHeader: portrait (PortraitSlot) + name (text-2xl font-bold) + "{race} {class} · Level {level}" + "XP: {xp} / {XP_THRESHOLDS[level]}" with a Stepper (size sm) beside the XP display. Stepper onChange fires updateXp mutation with delta; zero-debounce optimistic per D-20 (same onMutate/onError/onSettled pattern as HP). XP optimistic update clamps at 0 (Math.max(0, prev.xp + delta)). Portrait click fires importPortrait useMutation; on success invalidates portrait query. Portrait dataUrl comes from useQuery(characters.getPortraitDataUrl). isLoading on mutation passes to PortraitSlot.
    - AbilityScoresSection: 6-cell grid 3×2. Each cell shows ability abbreviation + editable score Input + computed modifier via formatModifier(calcAbilityModifier(score)). Input persists on blur via updateAbilityScore mutation (per D-20 ability scores persist on blur, not on every keystroke). Input min=1 max=30; invalid → red ring + tooltip per UI-SPEC §4.3.
    - SavingThrowsSection: 6 rows, ProficiencyDot per row, modifier = abilityMod + (isProf ? proficiencyBonus : 0). Read-only display.
    - SkillsSection: 18 rows iterating SKILLS, each with ProficiencyDot (state derived from skillProficiencies/skillExpertise arrays), governing ability label, modifier. Footer: Passive Perception = 10 + perception modifier.
    - CombatStatsSection: 3 stat blocks (AC, Initiative, Speed) per UI-SPEC §4.6 — all read-only, text-4xl font-bold numerals.
    - CurrencySection: 5-column grid (CP/SP/EP/GP/PP). Each cell has Stepper (size md, min=0, onChange delta) wired to updateCurrency mutation with denomination key. Zero-debounce optimistic.
    - EquipmentSection: header includes "{n} / 3 Attuned" badge computed from items.filter(i => i.isAttuned).length. Table with Name/Qty/Weight/Attune columns. Attune column shows attune button only when item.isMagic. Click triggers toggleItemAttuned mutation, optimistic.
    - ProficienciesSection: 4 rows (Armor / Weapons / Tools / Languages) showing comma-joined arrays. "None" if empty per UI-SPEC §6.3. Read-only.
    - TraitsSection: collapsible header. Renders racialTraitsText + classFeatureText + backgroundFeatureText with group headers per UI-SPEC §4.11. Collapse state via local useState (default collapsed when text content estimated > 300px height — simplify by defaulting to collapsed unconditionally or expanded based on content length heuristic). Read-only.
  </behavior>
  <action>
    Create all 9 section files. Each receives `character: CharacterWithResources` as prop, plus mutation hooks/wiring as needed (or get characterId from props and create mutations inside). All copy from UI-SPEC §6.3 verbatim. All Tailwind classes from UI-SPEC §4.

    For AbilityScoresSection's editable Input persisting on blur: use local useState for the displayed value, sync to character prop value via useEffect, on blur compare to original and fire updateAbilityScore mutation with the new value. Validate via Zod on the client: range 1-30, integer; show red ring if invalid (don't fire mutation).

    For CurrencySection / EquipmentSection attunement / Inspiration toggle (when present in ResourcesSection — Task 3): use the optimistic mutation pattern from RESEARCH.md §Pattern 5 — extract a `useOptimisticCharacterMutation` hook in a shared file if duplication is high (optional, judgment call), otherwise inline per section.

    For TraitsSection collapse: simple useState(false) for collapsed, click on `[▼ Collapse]` / `[▲ Expand]` button toggles. Animate with `transition-all duration-200` if simple to do via tailwind (max-height or display toggle).

    NONE of these sections render their own h2 section header except where UI-SPEC specifies it — SheetHeader has no `<h2>`; other sections do. Use `<h2 className="text-xl font-semibold">` per UI-SPEC §1.3 typography role.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 9 section files exist and export named function components
    - SheetHeader.tsx imports PortraitSlot, uses useQuery for `getPortraitDataUrl`, uses useMutation for `importPortrait` and `updateXp`
    - SheetHeader.tsx contains XP Stepper that fires `characters.updateXp` with delta (grep: `characters\.updateXp` in SheetHeader.tsx)
    - AbilityScoresSection.tsx has editable Input that calls updateAbilityScore on blur
    - SkillsSection.tsx iterates over SKILLS from sheetHelpers (18 entries)
    - CurrencySection.tsx renders 5 columns (CP/SP/EP/GP/PP) each with a Stepper
    - EquipmentSection.tsx attunement count text contains `/ 3 Attuned`
    - ProficienciesSection.tsx renders Armor / Weapons / Tools / Languages rows
    - TraitsSection.tsx has collapse useState
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>9 of 10 sections complete. ResourcesSection (largest, all zero-debounce live-play) is Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Build ResourcesSection (HP/temp HP/inspiration/death saves/spell slots/conditions) with zero-debounce optimistic mutations; wire CharacterSheetTab to render all 10 sections</name>
  <files>src/renderer/src/components/sheet/ResourcesSection.tsx, src/renderer/src/components/CharacterSheetTab.tsx</files>
  <read_first>
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §4.7 (Resources sub-sections), §6.3 (copy)
    - .planning/phases/02-character-domain-live-sheet/02-RESEARCH.md §Pattern 5 (optimistic mutation example — copy structure exactly)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-20 (zero-debounce), D-21 (14 conditions), D-22 (3+3 death saves always visible), D-23 (spell slots per slot level)
    - src/renderer/src/components/sheet/Stepper.tsx (Task 1)
    - src/renderer/src/components/sheet/ConditionBadge.tsx (Task 1)
    - src/renderer/src/components/sheet/SpellSlotPips.tsx (Task 1)
  </read_first>
  <behavior>
    - HP sub-section: Hit Points label; Current HP Stepper size lg with min=0, max=hpMax, fires updateHp mutation with delta. Display "/ {hpMax}" to right of stepper. When hpCurrent===0, current value styled text-destructive font-bold. Temp HP Stepper size sm below labeled "Temp HP", min=0.
    - Inspiration: single toggle Button outline with star icon; active style bg-accent-gold/20 border-accent-gold; click fires toggleInspiration mutation, optimistic.
    - Death Saves: 3 success checkboxes (gold when checked) + 3 failure checkboxes (red when checked). Each click fires updateDeathSaves mutation with the new absolute count (compute next absolute by adding +1 if unchecked, or computing the highest still-checked count if unchecking — simplify: clicking the Nth success checkbox sets successes to N if not already checked, or to N-1 if checked; same for failures).
    - Spell Slots: iterate slotLevels from character.resources.spellSlots (parsed object). For each level "1".."9" present: render label (ordinal) + SpellSlotPips with used/max + Stepper size sm beside it (or use pips only — UI-SPEC §4.7 says "Each slot level shows used/max with a stepper or pip row"; choose SpellSlotPips with onUse/onRecover firing updateSpellSlot with delta +1/-1 on the `used` count). If character is not a spellcaster (Object.keys(spellSlots).length === 0), render the italic muted message from UI-SPEC §6.3 §6.5: "Not a spellcaster — spell slots are tracked here when available."
    - Conditions: flex-wrap row of 14 ConditionBadge with toggleCondition mutation; each toggle is optimistic (cancelQueries → setQueryData with conditions array toggled → invalidate on settled).
  </behavior>
  <action>
    Create src/renderer/src/components/sheet/ResourcesSection.tsx implementing all 5 sub-sections per UI-SPEC §4.7.

    For each mutation, follow RESEARCH.md §Pattern 5 EXACTLY: define QUERY_KEY = ['characters','getByCampaignId',campaignId]. useMutation with onMutate (cancelQueries → snapshot getQueryData → setQueryData with optimistic update → return {prev}), onError (setQueryData(prev)), onSettled (invalidateQueries).

    For HP: optimistic update clamps via Math.max(0, Math.min(prev.resources.hpMax, prev.resources.hpCurrent + delta)).

    For conditions toggle: optimistic update reads current conditions array, toggles the named condition, returns updated character.

    For spell slots: optimistic update reads spellSlots[slotLevel].used, applies delta, clamps to [0, max], writes back.

    For inspiration: optimistic flip of resources.hasInspiration boolean.

    For death saves: clicking checkbox N fires updateDeathSaves with absolute values — if clicking an already-checked checkbox, the new count is N-1 (uncheck this and higher); if clicking unchecked, count is N (check this and lower). Same logic for failures separately.

    Now extend src/renderer/src/components/CharacterSheetTab.tsx (created in Plan 05 Task 1 as a placeholder for the loaded state). Replace the `<div>Character sheet TODO (Plan 06)</div>` placeholder with the full vertical layout per UI-SPEC §4.1:
      ```
      <div className="flex flex-col gap-4 p-4 overflow-y-auto h-full">
        <SheetHeader character={data} />
        <AbilityScoresSection character={data} />
        <SavingThrowsSection character={data} />
        <SkillsSection character={data} />
        <CombatStatsSection character={data} />
        <ResourcesSection character={data} />
        <CurrencySection character={data} />
        <EquipmentSection character={data} />
        <ProficienciesSection character={data} />
        <TraitsSection character={data} />
      </div>
      ```
    Import all 10 section components.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/sheet/ResourcesSection.tsx contains 5 sub-section headings or labels: "Hit Points", "Inspiration", "Death Saves", "Spell Slots", "Conditions"
    - src/renderer/src/components/sheet/ResourcesSection.tsx contains the literal optimistic pattern: `cancelQueries`, `setQueryData`, `invalidateQueries` (grep all three present)
    - src/renderer/src/components/sheet/ResourcesSection.tsx wires mutations to `updateHp`, `updateTempHp`, `toggleInspiration`, `updateDeathSaves`, `updateSpellSlot`, `toggleCondition`
    - src/renderer/src/components/sheet/ResourcesSection.tsx renders 14 ConditionBadge (use grep `<ConditionBadge` with multiline or rely on `.map(condition => <ConditionBadge`)
    - src/renderer/src/components/sheet/ResourcesSection.tsx contains the literal string "Not a spellcaster — spell slots are tracked here when available."
    - src/renderer/src/components/CharacterSheetTab.tsx imports all 10 sheet section components and renders them in the order: SheetHeader, AbilityScoresSection, SavingThrowsSection, SkillsSection, CombatStatsSection, ResourcesSection, CurrencySection, EquipmentSection, ProficienciesSection, TraitsSection
    - src/renderer/src/components/CharacterSheetTab.tsx no longer contains `Character sheet TODO`
    - `npm run typecheck` exits 0
    - `npm test` (full suite) exits 0
  </acceptance_criteria>
  <done>Complete 10-section character sheet with zero-debounce live-play mutations. CHAR-07 and CHAR-09 mechanically complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (every section's mutations) | All inputs Zod-validated server-side by Plan 02 router; UI value bounds align with schemas (HP delta ±9999, ability 1-30, currency ±999999, slot delta ±9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | Optimistic update race condition during rapid presses | mitigate | RESEARCH.md §Pattern 5: cancelQueries before setQueryData; delta-based mutations serialize at DB layer; optimistic clamp matches server clamp |
| T-02-16 | Information disclosure | Portrait dataUrl payload size | accept | Images resized to ≤1024px in Plan 04 imageService; base64 payload bounded; assumption A4 in 02-RESEARCH.md |
</threat_model>

<verification>
- `npm run typecheck` and `npm test` both pass
- Manual: After Plan 05 wizard completion, sheet renders all 10 sections; pressing HP + / − increments persists; closing and reopening app shows persisted state
</verification>

<success_criteria>
Character sheet fully interactive. Every D-20 live-play interaction persists immediately with optimistic UI. CHAR-06 (portrait UI) + CHAR-07 (resources) + CHAR-09 (attunement) all delivered.
</success_criteria>

<output>
Create `.planning/phases/02-character-domain-live-sheet/02-06-SUMMARY.md` when done.
</output>
