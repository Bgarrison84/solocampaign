---
phase: 02-character-domain-live-sheet
plan: 07
type: execute
wave: 6
depends_on: ["02-06"]
files_modified:
  - src/renderer/src/components/CampaignCard.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [WORLD-02]
must_haves:
  truths:
    - "CampaignCard renders cover image as base64 dataUrl from campaigns.getCoverDataUrl when available; falls back to gradient placeholder with 'Add cover image' tooltip per UI-SPEC §5.2"
    - "Clicking cover image area (on CampaignCard hover or via explicit button) triggers campaigns.importCoverImage useMutation; on success invalidates the cover query"
    - "Hover state on a card with an existing cover shows 'Change cover image' overlay per UI-SPEC §5.2"
    - "CampaignViewScreen header includes a 'Change Cover Image' button that triggers the same import mutation for the current campaign"
    - "Click on the cover image button does NOT propagate to the parent button that navigates into the campaign (event.stopPropagation)"
  artifacts:
    - path: "src/renderer/src/components/CampaignCard.tsx"
      provides: "Cover image display + import trigger; preserves existing navigate-on-click behavior"
    - path: "src/renderer/src/screens/CampaignViewScreen.tsx"
      provides: "[Change Cover Image] button in the campaign view header"
  key_links:
    - from: "src/renderer/src/components/CampaignCard.tsx"
      to: "trpc.campaigns.importCoverImage / trpc.campaigns.getCoverDataUrl"
      via: "useMutation + useQuery"
      pattern: "campaigns\\.(importCoverImage|getCoverDataUrl)"
---

<objective>
Wire the campaign cover image UI in CampaignCard and the campaign view header. Plan 04 built the tRPC + image service; this plan delivers the user-facing trigger and display surfaces.

Purpose: Closes WORLD-02 (campaign cover image import) end-to-end. After this plan, the user can import a cover image from either the campaign list (via CampaignCard) or from inside a campaign (via the view header button).

Output: CampaignCard displays cover image; campaign view header has [Change Cover Image] button; both trigger the same image import mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-character-domain-live-sheet/02-CONTEXT.md
@.planning/phases/02-character-domain-live-sheet/02-RESEARCH.md
@.planning/phases/02-character-domain-live-sheet/02-PATTERNS.md
@.planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md
@src/renderer/src/components/CampaignCard.tsx
@src/renderer/src/screens/CampaignViewScreen.tsx
@src/renderer/src/lib/trpc.ts

<interfaces>
<!-- From Plan 04 -->
trpc.campaigns.importCoverImage({ campaignId }) → string | null
trpc.campaigns.getCoverDataUrl({ campaignId }) → string | null

<!-- Existing -->
Campaign type now includes coverImagePath: string | null (from Plan 01 schema extension)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire cover image display + import trigger into CampaignCard</name>
  <files>src/renderer/src/components/CampaignCard.tsx</files>
  <read_first>
    - src/renderer/src/components/CampaignCard.tsx (current cover image placeholder at lines 29-36)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §5.2 (Cover Image Import Flow), §6.4 (image strings)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-27 (cover from CampaignCard or campaign view header)
  </read_first>
  <behavior>
    - useQuery for campaigns.getCoverDataUrl({ campaignId: campaign.id }) — keyed ['campaigns','getCoverDataUrl',campaign.id]
    - When dataUrl present, render `<img src={dataUrl}>` in the existing 40-height cover slot
    - When dataUrl null, render the fallback: gradient placeholder per UI-SPEC §5.2 (bg-gradient-to-br from-surface to-muted) with Camera icon centered and 'Add cover image' tooltip on hover
    - Add a click handler (or overlay button) on the cover area that fires campaigns.importCoverImage useMutation; on success invalidates the dataUrl query AND the campaigns.list query
    - Cover area click must NOT propagate to the parent navigate button (stopPropagation). The outer button still navigates into the campaign on body click; the cover image area is a sub-region with its own click handler.
    - Loading state during import: spinner overlay on the cover area
    - Error: surface a toast OR an alert per UI-SPEC §6.4 ("Could not import image. Please try a different file.")
  </behavior>
  <action>
    Refactor src/renderer/src/components/CampaignCard.tsx. Change the outer `<button>` to a `<div role="button" tabIndex={0} onClick={handleClick} onKeyDown={(e) => e.key === 'Enter' && handleClick()}>` (or wrap the body separately) so an inner clickable cover area doesn't trigger button-in-button errors. Alternative: keep `<button>` but make the cover area a sibling/sub-button with stopPropagation — buttons CANNOT nest, so the div-with-role approach is the right pattern.

    Add imports: `useMutation, useQuery, useQueryClient` from '@tanstack/react-query', `trpc` from '../lib/trpc', `Camera` icon from 'lucide-react'.

    Add useQuery for `getCoverDataUrl`. Add useMutation for `importCoverImage` with onSuccess invalidating both `['campaigns','getCoverDataUrl',campaign.id]` and `['campaigns','list']`. Add useState for `coverError: string | null`.

    Replace the cover slot block (current lines 29-36):
      - If `coverQuery.data` truthy: `<img src={coverQuery.data} alt="" className="w-full h-full object-cover" />` with a hover overlay showing "Change cover image" per UI-SPEC §5.2 visible on group-hover
      - If null: gradient `bg-gradient-to-br from-surface to-muted` with centered Camera icon and "Add cover image" tooltip per UI-SPEC §5.2
      - Loading spinner overlay (Loader2 from lucide-react with animate-spin) when mutation.isPending
      - Click handler on the cover area: `e.stopPropagation(); importMutation.mutate({ campaignId: campaign.id })` — wrap in a `<button type="button" onClick={...}>` with absolute positioning over the cover area, or use a div with role=button. Use the group/group-hover Tailwind pattern.
      - Error message rendered below cover (or in a toast — keep it simple with inline text-destructive)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/CampaignCard.tsx imports `useMutation` and `useQuery` from '@tanstack/react-query'
    - src/renderer/src/components/CampaignCard.tsx contains `trpc.campaigns.getCoverDataUrl.query` (or equivalent useQuery wrapping it)
    - src/renderer/src/components/CampaignCard.tsx contains `trpc.campaigns.importCoverImage.mutate` (or equivalent useMutation)
    - src/renderer/src/components/CampaignCard.tsx contains `e.stopPropagation()` on the cover click handler
    - src/renderer/src/components/CampaignCard.tsx contains both `'Add cover image'` and `'Change cover image'` literal strings (per UI-SPEC §6.4)
    - src/renderer/src/components/CampaignCard.tsx contains `'Could not import image. Please try a different file.'` (error string per UI-SPEC §6.4)
    - src/renderer/src/components/CampaignCard.tsx no longer references `/placeholder-cover.svg` (the placeholder is replaced by the gradient fallback)
    - The outer wrapper element is no longer a nested `<button>` containing the new cover `<button>` (use `<div role="button">` for outer OR sibling layout)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>CampaignCard displays cover image and supports import via click. Tooltip and error copy match UI-SPEC §6.4 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Add [Change Cover Image] button to campaign view header</name>
  <files>src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (current file — Plan 05 already modified the character-sheet TabsContent)
    - .planning/phases/02-character-domain-live-sheet/02-CONTEXT.md D-27 (cover image imported from CampaignCard OR campaign view header)
    - .planning/phases/02-character-domain-live-sheet/02-UI-SPEC.md §5.2 (same flow as CampaignCard)
  </read_first>
  <behavior>
    - Above the PanelGroup (or in the existing tabs header area), add a small unobtrusive [Change Cover Image] button per D-27
    - Button uses Button variant outline size sm with Camera icon from lucide-react and label "Change Cover Image"
    - Click fires the same campaigns.importCoverImage useMutation; on success invalidates ['campaigns','getCoverDataUrl', id] and ['campaigns','list']
    - Display matches the existing title bar / header styling — placement is at the developer's discretion within the existing top-of-view real estate (best fit: as a trailing action in the tabs row, OR inline next to a future campaign-name header)
  </behavior>
  <action>
    Edit src/renderer/src/screens/CampaignViewScreen.tsx. Identify the best placement for the [Change Cover Image] button. Two reasonable options:
      1. At the top of the right panel TabsList (trailing action, right-aligned via `ml-auto`)
      2. As a separate header bar above the PanelGroup

    Choose option 1 unless it disrupts the tabs layout. Implementation: inside the `<TabsList>`, append a `<div className="ml-auto pr-2"><Button variant="outline" size="sm" onClick={handleChangeCover}>...</Button></div>` sibling at the end.

    Add useMutation: `const coverMutation = useMutation({ mutationFn: () => trpc.campaigns.importCoverImage.mutate({ campaignId: id }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['campaigns','getCoverDataUrl',id] }); queryClient.invalidateQueries({ queryKey: ['campaigns','list'] }); } })`.

    Add `useQueryClient` to imports. Add `Button` from '../components/ui/button' and `Camera` from 'lucide-react'.

    Disable the button when coverMutation.isPending; show "Importing..." text.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/screens/CampaignViewScreen.tsx contains `trpc.campaigns.importCoverImage`
    - src/renderer/src/screens/CampaignViewScreen.tsx contains `"Change Cover Image"` literal string
    - src/renderer/src/screens/CampaignViewScreen.tsx imports `Camera` from 'lucide-react' AND `Button` from '../components/ui/button' (if not already present)
    - src/renderer/src/screens/CampaignViewScreen.tsx invalidates `['campaigns','getCoverDataUrl', id]` on cover import success
    - `npm run typecheck` exits 0
    - `npm test` (full suite) exits 0 — no regressions
  </acceptance_criteria>
  <done>Cover image can be imported from either CampaignCard (list view) or the campaign view header. WORLD-02 fully delivered end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A new — all cross-boundary calls go through the existing Plan 04 importImage flow which uses OS dialog (T-PATH mitigated).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | Cover import path traversal | mitigate | Inherited from Plan 04 — importImage uses OS dialog; renderer never supplies path; this plan only triggers the mutation |
</threat_model>

<verification>
- `npm test` full suite passes
- `npm run typecheck` passes
- Manual: From campaign list, click cover area of any card → file picker → image appears on card; reopen list → image persists. From inside a campaign → click [Change Cover Image] in tabs header → file picker → new image; return to list → updated.
</verification>

<success_criteria>
WORLD-02 cover image flow complete end-to-end. Both entry points (CampaignCard click + view header button) work. Phase 2 complete.
</success_criteria>

<output>
Create `.planning/phases/02-character-domain-live-sheet/02-07-SUMMARY.md` when done.
</output>
