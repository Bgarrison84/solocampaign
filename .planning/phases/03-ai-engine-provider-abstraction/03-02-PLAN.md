---
phase: 03-ai-engine-provider-abstraction
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - src/main/ai/llmProvider.ts
  - src/main/ai/retryHandler.ts
  - src/main/ai/referenceDocLoader.ts
  - src/main/ai/contextBuilder.ts
  - src/main/ai/llmProvider.test.ts
  - src/main/ai/retryHandler.test.ts
  - src/main/ai/referenceDocLoader.test.ts
  - src/main/ai/contextBuilder.test.ts
autonomous: true
requirements: [FOUND-03, SESS-06, SESS-07, SESS-08]
must_haves:
  truths:
    - "streamChat wraps the Vercel AI SDK and delivers tokens via callbacks for both openai-compatible and gemini providers"
    - "A failed provider call retries 3 times with 1s/2s/4s backoff before invoking the final error callback"
    - "The system prompt contains the strictness directive, DM personality, and the formatted character summary"
    - "Selected reference documents are read from disk and appended to the system prompt as labelled sections"
  artifacts:
    - path: "src/main/ai/llmProvider.ts"
      provides: "streamChat() + buildModel() factory + LLMProviderConfig interface + 15s timeout + 16ms token batching"
      exports: ["streamChat", "LLMProviderConfig", "StreamCallbacks"]
    - path: "src/main/ai/retryHandler.ts"
      provides: "withRetry() — 3 attempts, exponential backoff"
      exports: ["withRetry"]
    - path: "src/main/ai/contextBuilder.ts"
      provides: "buildContext() — system prompt assembly + last 20 messages"
      exports: ["buildContext"]
    - path: "src/main/ai/referenceDocLoader.ts"
      provides: "listReferenceDocs() + readReferenceDocs() + title cleaning"
      exports: ["listReferenceDocs", "readReferenceDocs"]
  key_links:
    - from: "src/main/ai/contextBuilder.ts"
      to: "src/main/db/charactersRepo.ts"
      via: "getByCampaignId for character summary (D-21)"
      pattern: "getByCampaignId"
    - from: "src/main/ai/contextBuilder.ts"
      to: "src/main/db/messagesRepo.ts"
      via: "getLastN(campaignId, 20)"
      pattern: "getLastN"
    - from: "src/main/ai/llmProvider.ts"
      to: "ai (Vercel SDK)"
      via: "streamText + createOpenAICompatible + createGoogleGenerativeAI"
      pattern: "streamText"
---

<objective>
Build the main-process AI engine module under `src/main/ai/`: the provider-agnostic `streamChat` wrapper over the Vercel AI SDK (D-22), the 3-retry exponential-backoff handler (D-18/SESS-08), the reference document loader with title cleaning (D-05), and the ContextBuilder v1 that assembles the system prompt + character summary + last-20-message window (D-20/D-21, SESS-06/SESS-07).

Purpose: This is the AI backend that the Wave 2 IPC handler orchestrates. Wrapping the SDK behind `streamChat` from day one lets Phase 4 slot in three-layer memory without touching call sites.
Output: 4 implementation files + their Wave 0 test stubs turned green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md
@.planning/phases/03-ai-engine-provider-abstraction/03-VALIDATION.md

<interfaces>
<!-- AI-SPEC §3 — the streamChat signature and buildModel factory to implement: -->
<!-- streamChat(config, messages: CoreMessage[], systemPrompt: string, callbacks): Promise<void> -->
<!-- LLMProviderConfig: { type: 'openai-compatible'|'gemini', endpointUrl?, modelName, apiKey? } -->
<!-- StreamCallbacks: { onToken(token), onFinish(), onError(error) } -->
<!-- buildModel: gemini → createGoogleGenerativeAI({apiKey})(modelName); openai-compatible → createOpenAICompatible({name:'custom', baseURL: endpointUrl, apiKey: apiKey ?? 'none'})(modelName) -->

<!-- CoreMessage from 'ai': { role: 'user'|'assistant'|'system', content: string } -->

<!-- messagesRepo (from plan 03-01): insert({campaignId, role, content}), getLastN(campaignId, n) -> Message[] chronological -->
<!-- charactersRepo.getByCampaignId(campaignId) -> CharacterWithResources | undefined (see src/main/db/charactersRepo.ts CharacterWithResources shape: name, race, subrace, class, subclass, level, ability scores, ac, initiativeBonus, speed, proficiencyBonus, resources{hpCurrent,hpMax,spellSlots:SpellSlotMap,conditions:string[],hasInspiration}) -->

<!-- D-21 character summary format and D-20 strictness directives are spelled out verbatim in 03-AI-SPEC.md §4b "Prompt Engineering Discipline" -->

<!-- contentLoader.ts getResourcesPath() pattern: app.isPackaged ? process.resourcesPath : path.join(__dirname,'../../resources') -->
<!-- Reference docs live at "Reference Documents/Converted/<name>/<name>.md" in dev; bundled to resources/reference-docs/ in packaged build (electron-builder wiring is plan 03-06). referenceDocLoader must resolve BOTH locations. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LLMProvider streamChat wrapper + retryHandler</name>
  <read_first>
    - src/main/ai/llmProvider.test.ts (Wave 0 stub from 03-01 — the assertions to satisfy; vi.mock('ai') async-generator pattern)
    - src/main/ai/retryHandler.test.ts (Wave 0 stub — expected attempt count + backoff timing assertions)
    - .planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md §3 (Entry Point Pattern, IPC Streaming Pattern, Common Pitfalls 1/3/5)
    - .planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md §4 + §4b (model config: temperature 0.8, no maxTokens unless local loops; 15s timeout guard; 16ms IPC batching)
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-18, D-22
    - CLAUDE.md § "AI Provider Abstraction" (Vercel AI SDK imports)
  </read_first>
  <behavior>
    - streamChat with a mocked streamText yielding ['Hello, ','adventurer.'] invokes onToken twice then onFinish once, never onError
    - streamChat where streamText rejects invokes onError with an Error, never onFinish
    - buildModel('gemini') calls createGoogleGenerativeAI; buildModel('openai-compatible') with no apiKey passes apiKey 'none' to createOpenAICompatible (Pitfall 1)
    - If no token arrives within 15s, onError fires with message containing "did not respond in time" and the stream is abandoned
    - withRetry(fn) where fn rejects 3 times then would-succeed: calls fn exactly 3 times (per D-18: 3 retries), waits ~1s, ~2s, ~4s between attempts (assert with fake timers), then surfaces the final error
    - withRetry where fn succeeds on attempt 1: calls fn once, returns its value
  </behavior>
  <action>
    Create `src/main/ai/llmProvider.ts` implementing `streamChat(config, messages, systemPrompt, callbacks)` and the `buildModel(config)` factory exactly per AI-SPEC §3. Import `streamText` and `CoreMessage` from `ai`, `createOpenAICompatible` from `@ai-sdk/openai-compatible`, `createGoogleGenerativeAI` from `@ai-sdk/google`. Set temperature 0.8 on the streamText call. Iterate `result.textStream` with `for await` (Pitfall 3 — never await full text first). Implement the 15s first-token timeout (AI-SPEC §4b Cost/Latency): start a timer after calling streamText; if no token before 15000ms, call `callbacks.onError(new Error('Provider did not respond in time'))` and stop iterating; clear the timer on first token. Implement 16ms token batching (Pitfall 5 / guardrail IPC buffer): coalesce tokens arriving within a 16ms window and flush via onToken to avoid IPC saturation — keep this internal to streamChat so callers receive batched strings. Export `LLMProviderConfig` and `StreamCallbacks` interfaces.
    Create `src/main/ai/retryHandler.ts` exporting `withRetry<T>(fn: () => Promise<T>, opts?): Promise<T>` implementing 3 attempts with exponential backoff 1000ms → 2000ms → 4000ms per D-18. On each failure, wait the backoff then retry; after the 3rd failure, reject with the final error. Make the backoff delays injectable (a `delayMs` or `sleep` option) so tests can use fake timers without real waits.
    Turn the two Wave 0 stub tests green (remove the skip / MISSING markers).
  </action>
  <verify>
    <automated>npm test -- src/main/ai/llmProvider.test.ts src/main/ai/retryHandler.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/ai/llmProvider.ts` contains `export async function streamChat(` and `createOpenAICompatible(` and `createGoogleGenerativeAI(`
    - `src/main/ai/llmProvider.ts` contains the literal `apiKey ?? 'none'` (Pitfall 1) and a 15000 timeout value
    - `src/main/ai/retryHandler.ts` contains `export` `withRetry` and backoff values 1000/2000/4000
    - `npm test -- src/main/ai/llmProvider.test.ts` exits 0 (token delivery + error path + timeout)
    - `npm test -- src/main/ai/retryHandler.test.ts` exits 0 (3 attempts, backoff timing via fake timers)
  </acceptance_criteria>
  <done>streamChat and withRetry implemented per AI-SPEC; both Wave 0 stubs pass with token-delivery, error-path, timeout, and backoff assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: referenceDocLoader + ContextBuilder v1</name>
  <read_first>
    - src/main/ai/referenceDocLoader.test.ts (Wave 0 stub — title cleaning + path construction assertions)
    - src/main/ai/contextBuilder.test.ts (Wave 0 stub — character summary format, strictness directive, personality injection)
    - src/main/db/contentLoader.ts (getResourcesPath() dev-vs-packaged resolution pattern to mirror)
    - src/main/db/charactersRepo.ts (CharacterWithResources shape consumed for the summary; getByCampaignId)
    - src/main/db/messagesRepo.ts (getLastN — from plan 03-01)
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-05, D-20, D-21
    - .planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md §4b "Prompt Engineering Discipline" (verbatim system prompt template + strictness directives) and §4 "Context Window Strategy" (last 20 messages, 200KB large-file threshold)
    - .planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md § "Reference document list" (title cleaning rules: strip OceanofPDF prefix, leading underscore, author suffix after last " - ", decode URL entities, title-case)
  </read_first>
  <behavior>
    - listReferenceDocs() returns one entry per "<name>/<name>.md" folder under Converted/ with { relativePath, title (cleaned), sizeBytes, isLarge: sizeBytes > 200_000 }
    - Title cleaning: "_OceanofPDF.com_The_Ultimate_RPG_Game_Masters_Guide_-_James_DAmato" → "The Ultimate RPG Game Masters Guide"; "D&D Players Handbook 2024 - Wizards of the Coast" → "D&D Players Handbook 2024"; "solo-adventurerx27s-guide" → "Solo Adventurer's Guide"
    - readReferenceDocs(relativePaths[]) returns [{ title, content }] reading each .md file; skips paths that resolve outside the reference-docs root (path traversal guard)
    - buildContext({campaignId, config}) returns { systemPrompt, messages } where systemPrompt = preamble + strictness directive (matching config.strictness) + personality (or generic default if empty) + character summary (D-21 exact format) + each selected reference doc as "=== {title} ===\n{content}"
    - buildContext omits the Spell Slots line when the character has no spell slots; renders "Active Conditions: None" when conditions empty
    - messages = last 20 messages from messagesRepo.getLastN mapped to CoreMessage[] ({role, content})
  </behavior>
  <action>
    Create `src/main/ai/referenceDocLoader.ts`. Resolve the reference-docs root with the same packaged-vs-dev branch as contentLoader: packaged → `path.join(process.resourcesPath, 'reference-docs')`, dev → the project-root `Reference Documents/Converted` directory (resolve from `__dirname` up to the repo root). Implement `listReferenceDocs()` enumerating each subfolder's `<name>.md` (each Converted doc is a folder containing one primary markdown file named after the folder). Implement `readReferenceDocs(relativePaths)` that joins each relative path under the root, normalizes, and rejects any resolved path that escapes the root (`resolved.startsWith(root)` guard — security T-03-02-01). Implement the title-cleaning function per the UI-SPEC rules. Compute `sizeBytes` via fs.statSync and `isLarge` against the 200_000 threshold.
    Create `src/main/ai/contextBuilder.ts` exporting `buildContext(args)` that assembles the system prompt in the exact D-20/D-21 order using the verbatim preamble and strictness directives from AI-SPEC §4b. Map the three strictness enum values to their directives. Use the generic "Classic adventure DM" fallback when dmPersonality is empty. Pull the character via `charactersRepo.getByCampaignId(campaignId)` and format the summary block (compute ability modifiers as floor((score-10)/2); render spell slots from the SpellSlotMap, omitting the line if empty). Append each selected reference doc via `readReferenceDocs`. Pull `messagesRepo.getLastN(campaignId, 20)` and map to `CoreMessage[]`. Return `{ systemPrompt, messages }`. Do NOT make the LLM call here — that is the IPC handler's job (plan 03-03).
    Turn both Wave 0 stub tests green.
  </action>
  <verify>
    <automated>npm test -- src/main/ai/referenceDocLoader.test.ts src/main/ai/contextBuilder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/ai/referenceDocLoader.ts` contains `export` `listReferenceDocs` and `readReferenceDocs` and a path-escape guard (string `startsWith` against the resolved root)
    - `src/main/ai/referenceDocLoader.ts` contains the threshold `200_000` or `200000`
    - `src/main/ai/contextBuilder.ts` contains `export` `buildContext`, `getByCampaignId`, and `getLastN`
    - `src/main/ai/contextBuilder.ts` contains all three strictness directive strings (strict/balanced/narrative) matching AI-SPEC §4b
    - `npm test -- src/main/ai/referenceDocLoader.test.ts` exits 0 (title cleaning + traversal guard)
    - `npm test -- src/main/ai/contextBuilder.test.ts` exits 0 (summary format, strictness directive, personality injection)
  </acceptance_criteria>
  <done>referenceDocLoader and contextBuilder implemented per D-05/D-20/D-21; both Wave 0 stubs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| selected doc path → filesystem read | reference doc relative paths (chosen in renderer, persisted in DB) are read from disk in main process |
| campaign config → LLM provider | endpoint URL + model + (decrypted) key flow into the SDK; key never logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering / Information Disclosure | referenceDocLoader.readReferenceDocs | mitigate | Resolved path must startWith the reference-docs root; reject traversal (../) — prevents reading arbitrary files via crafted reference_docs JSON |
| T-03-02-02 | Information Disclosure | llmProvider streamChat logging | mitigate | apiKey is never passed to log.* ; only systemPrompt.length and token counts are logged (AI-SPEC §7) |
| T-03-02-03 | Denial of Service | streamChat hang | mitigate | 15s first-token timeout cancels the stream and emits onError (D-18, AI-SPEC §4b) |
</threat_model>

<verification>
- `npm test -- src/main/ai/` exits 0 (all four ai module test files green)
- `npm run typecheck` exits 0
- streamChat handles both provider types, token delivery, error path, and 15s timeout
- withRetry performs exactly 3 attempts with 1s/2s/4s backoff
</verification>

<success_criteria>
- Provider-agnostic streamChat wraps Vercel AI SDK (FOUND-03, D-22)
- 3-retry exponential backoff implemented (SESS-08)
- ContextBuilder assembles strictness directive (SESS-07) + DM personality (SESS-06) + character summary
- Reference docs enumerated, cleaned, and injected with path-traversal protection
</success_criteria>

<output>
Create `.planning/phases/03-ai-engine-provider-abstraction/03-02-SUMMARY.md` when done.
</output>
