---
phase: 03-ai-engine-provider-abstraction
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - src/renderer/src/components/StoryScrollPanel.tsx
  - src/renderer/src/components/ChatInputArea.tsx
  - src/renderer/src/components/ChatErrorBlock.tsx
  - src/renderer/src/hooks/useAiStream.ts
  - src/renderer/src/types/aiStream.d.ts
  - src/renderer/src/styles/globals.css
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: false
requirements: [FOUND-03, SESS-05]
must_haves:
  truths:
    - "The left panel renders a continuous story scroll of AI narration and player entries instead of the placeholder"
    - "The player can type a message and send it with Ctrl+Enter or the Send button"
    - "Streamed tokens append in real time with a blinking cursor that clears on finish"
    - "After 3 failed retries an inline error block appears with Retry (and Switch to fallback when configured)"
  artifacts:
    - path: "src/renderer/src/hooks/useAiStream.ts"
      provides: "useAiStream — manages isStreaming/streamingContent/error + listener registration & cleanup"
      exports: ["useAiStream"]
    - path: "src/renderer/src/components/StoryScrollPanel.tsx"
      provides: "continuous story scroll with react-markdown for completed messages + raw streaming append"
      exports: ["StoryScrollPanel"]
    - path: "src/renderer/src/components/ChatInputArea.tsx"
      provides: "auto-growing textarea + Send + Ctrl+Enter"
      exports: ["ChatInputArea"]
    - path: "src/renderer/src/components/ChatErrorBlock.tsx"
      provides: "inline failure block (Switch to fallback / Retry / configure link)"
      exports: ["ChatErrorBlock"]
  key_links:
    - from: "src/renderer/src/hooks/useAiStream.ts"
      to: "window.aiStream"
      via: "sendMessage + onToken/onFinish/onError + removeAllListeners in useEffect cleanup"
      pattern: "window\\.aiStream"
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "StoryScrollPanel"
      via: "left panel replaces the AI-narration placeholder"
      pattern: "StoryScrollPanel"
---

<objective>
Replace the left-panel placeholder in `CampaignViewScreen` with the live story-scroll chat experience: `StoryScrollPanel` (continuous narrative, react-markdown for completed messages, raw-text streaming append, auto-scroll, empty state, blinking cursor), `ChatInputArea` (auto-growing textarea, Send button, Ctrl+Enter via react-hotkeys-hook), the `useAiStream` hook (manages streaming state + IPC listener lifecycle), and `ChatErrorBlock` (inline fallback/retry UI per D-18).

Purpose: This is the player-facing surface of FOUND-03 — sending a message and watching tokens stream is the phase's headline success criterion.
Output: 4 renderer components/hooks + CSS blink keyframe + window.aiStream type augmentation + CampaignViewScreen left-panel wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md
@.planning/phases/03-ai-engine-provider-abstraction/03-VALIDATION.md

<interfaces>
<!-- Preload surface (plan 03-03): window.aiStream = { sendMessage(payload)->Promise<{started}>, onToken(cb), onFinish(cb), onError(cb:(err)=>void), removeAllListeners() } -->
<!-- payload shape: { campaignId, content, useFallback?: boolean } -->

<!-- tRPC (plan 03-03): trpc.ai.getMessages.query({campaignId}) -> Message[] (chronological); trpc.ai.listReferenceDocs.query() ; trpc.prefs.isEncryptionAvailable.query() -->
<!-- trpc client: src/renderer/src/lib/trpc.ts (import { trpc }) — same pattern as CampaignViewScreen campaignQuery -->

<!-- CampaignViewScreen.tsx left panel currently: <Panel defaultSize=... minSize={30}><div ...><p>AI narration appears here.</p></div></Panel> — replace inner content with a flex-col: <StoryScrollPanel/> (flex-1) + <ChatInputArea/> (shrink-0) -->

<!-- react-hotkeys-hook v4: useHotkeys('ctrl+enter', handler, { enableOnFormTags: ['TEXTAREA'] }) -->
<!-- react-markdown + remark-gfm: <ReactMarkdown remarkPlugins={[remarkGfm]}>{content}</ReactMarkdown> — used ONLY for completed messages (constraint #8); streaming content appended as raw text -->

<!-- globals.css uses Tailwind v4 @theme inline; --primary gold token present. Add @keyframes blink. -->
<!-- No tooltip.tsx existed before plan 03-01; it is installed in 03-01. -->

<!-- All UI-SPEC §S1/S5 strings, classes, spacing, colors are authoritative — copy them verbatim. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useAiStream hook + window.aiStream type augmentation + blink keyframe</name>
  <read_first>
    - src/preload/index.ts (the aiStream surface shape — plan 03-03 — to type and consume)
    - src/renderer/src/lib/trpc.ts (trpc client import + query pattern)
    - src/renderer/src/screens/CampaignViewScreen.tsx (useQuery/useQueryClient + TanStack patterns to mirror)
    - src/renderer/src/styles/globals.css (@theme inline block; where to add @keyframes blink and the animate-blink utility)
    - .planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md §S1a "Streaming State" + "Auto-Scroll Behavior" + §S5 (error trigger semantics)
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-13, D-18, D-19
  </read_first>
  <action>
    Create `src/renderer/src/types/aiStream.d.ts` augmenting the global `Window` interface with `aiStream: { sendMessage(payload: { campaignId: string; content: string; useFallback?: boolean }): Promise<{ started: boolean }>; onToken(cb: (token: string) => void): void; onFinish(cb: () => void): void; onError(cb: (err: { message: string }) => void): void; removeAllListeners(): void }`. Also declare `platform: NodeJS.Platform` to consolidate the existing untyped window.platform. Ensure this file is picked up by the renderer tsconfig (it lives under src/renderer/src so it is in scope).
    Create `src/renderer/src/hooks/useAiStream.ts` exporting `useAiStream(campaignId: string)`. State: `isStreaming` (boolean), `streamingContent` (string — accumulated raw tokens during the active stream), `error` ({ message } | null). Expose a `send(content: string, opts?: { useFallback?: boolean })` function that sets isStreaming true, clears error and streamingContent, calls `window.aiStream.sendMessage({campaignId, content, useFallback})`. Register `onToken` (append to streamingContent), `onFinish` (set isStreaming false, clear streamingContent, and invalidate the getMessages query so the completed assistant message renders via react-markdown), and `onError` (set error, isStreaming false). CRITICAL (constraint #7): register listeners in a `useEffect` and call `window.aiStream.removeAllListeners()` in the cleanup function to prevent duplicate handlers across re-renders/campaign switches. Return `{ isStreaming, streamingContent, error, send, clearError }`.
    Add to `src/renderer/src/styles/globals.css`: the `@keyframes blink { 0%,100%{opacity:1} 50%{opacity:0} }` and an `animate-blink` utility class per UI-SPEC §S1a so the streaming cursor `<span>` can use `animate-[blink_1s_ease-in-out_infinite]`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/types/aiStream.d.ts` augments `Window` with `aiStream` containing `sendMessage`, `onToken`, `onFinish`, `onError`, `removeAllListeners`
    - `src/renderer/src/hooks/useAiStream.ts` exports `useAiStream` and contains `window.aiStream.removeAllListeners` inside a useEffect cleanup return
    - `src/renderer/src/hooks/useAiStream.ts` invalidates the getMessages query on finish (string `invalidateQueries`)
    - `src/renderer/src/styles/globals.css` contains `@keyframes blink`
    - `npm run typecheck` exits 0 (window.aiStream is typed, no `any` access)
  </acceptance_criteria>
  <done>useAiStream manages streaming state with proper listener cleanup; window.aiStream is typed; blink keyframe added.</done>
</task>

<task type="auto">
  <name>Task 2: StoryScrollPanel + ChatInputArea + ChatErrorBlock + CampaignViewScreen wiring</name>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (left panel placeholder to replace; campaignQuery; queryClient)
    - src/renderer/src/hooks/useAiStream.ts (from Task 1 — the streaming state consumed here)
    - src/renderer/src/components/ui/textarea.tsx (installed plan 03-01 — the shadcn Textarea)
    - src/renderer/src/components/ui/button.tsx (Button variants/sizes)
    - src/renderer/src/lib/trpc.ts (trpc.ai.getMessages query)
    - .planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md §S1a (StoryScrollPanel: layout, AI narration block, player entry block, empty state, blinking cursor, auto-scroll), §S1b (ChatInputArea: textarea sizing, Send button states, Ctrl+Enter hint, disabled/no-config states), §S5 (ChatErrorBlock: both fallback-present and fallback-absent variants, styling, strings), § "Copywriting Contract" (Chat Panel + Inline Error strings verbatim), § "Accessibility" (aria-live polite scroll, aria-hidden cursor, role=alert error block)
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-11, D-12, D-13, D-14, D-16, D-18
  </read_first>
  <action>
    Create `src/renderer/src/components/StoryScrollPanel.tsx` per UI-SPEC §S1a. Props: `campaignId`, the streaming state from useAiStream (`isStreaming`, `streamingContent`, `error`), and error-action callbacks (`onRetry`, `onSwitchToFallback`, `hasFallback`, `onOpenSettings`). Fetch completed history via `trpc.ai.getMessages` (TanStack useQuery). Render an outer `overflow-y-auto` scroll area (`max-w-[680px] mx-auto px-6 py-6`, `aria-live="polite"`). For each completed message: assistant messages render through `<ReactMarkdown remarkPlugins={[remarkGfm]}>` with the prose classes from the spec; user messages render the player-entry block (`<hr class="border-t border-border my-6"/>` + italic `You:` prefix `text-primary/80` + body text). During streaming, append `streamingContent` as RAW text (NOT markdown — constraint #8) with the blinking cursor span (`aria-hidden`, `animate-[blink_1s_ease-in-out_infinite]`); remove the cursor when not streaming. Implement empty state ("Begin your adventure." / "Type a message below to start the story.") when there are zero messages and not streaming. Implement auto-scroll: track `isUserScrolledUp` via onScroll (within 100px of bottom); auto-scroll on new tokens only when not scrolled up; force scroll to bottom on finish, on initial history load, and after the player submits. Render `<ChatErrorBlock>` at the end of the list when `error` is set.
    Create `src/renderer/src/components/ChatInputArea.tsx` per UI-SPEC §S1b. Props: `onSend(content)`, `isStreaming`, `disabled` (no AI config), `onOpenSettings`. shadcn `Textarea` with `min-h-[56px] max-h-[112px] resize-none`, auto-grow via scrollHeight on change. Send `Button` (gold, `w-16 h-9`) disabled when empty or streaming; show `Loader2` spinner + "Sending…" while streaming. `Ctrl+Enter` hint below (`text-[12px] text-muted-foreground`). Bind `useHotkeys('ctrl+enter', handleSend, { enableOnFormTags: ['TEXTAREA'] })`; plain Enter inserts newline (no override). When `disabled` (no provider configured), disable inputs and show the amber "Configure an AI provider to start playing. Open settings." notice wired to `onOpenSettings`. Add the sr-only label per accessibility spec.
    Create `src/renderer/src/components/ChatErrorBlock.tsx` per UI-SPEC §S5. Props: `message`, `hasFallback`, `onRetry`, `onSwitchToFallback`, `onOpenSettings`. Render the `role="alert" aria-live="assertive"` block with `border-l-4 border-l-destructive`, AlertTriangle icon, title "The AI DM stopped responding", the body message, and buttons: "Switch to fallback" (gold, only when hasFallback) + "Retry" (outline, always) OR "Retry" + "Configure fallback in AI settings" link when no fallback. Use the exact strings from the Copywriting Contract.
    Wire `CampaignViewScreen.tsx`: replace the left-panel inner `<div>…AI narration appears here.</div>` with a `flex flex-col h-full` containing `<StoryScrollPanel className="flex-1">` and `<ChatInputArea className="shrink-0">`. Instantiate `useAiStream(id)`. Determine `disabled`/`hasFallback` from the campaign config (campaignQuery.data.providerType present → configured; campaignQuery.data.fallbackEndpointUrl present → hasFallback). `onSend` calls the hook's send and force-scrolls; `onRetry` re-sends the last user content; `onSwitchToFallback` re-sends with `useFallback: true`; `onOpenSettings` opens the AiSettingsModal (the gear modal lands in plan 03-05 — for now stub `onOpenSettings` to a no-op or a state setter that plan 03-05 will connect; do not block on it). Keep the existing right panel, action bar, and delete dialog unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/components/StoryScrollPanel.tsx` exports `StoryScrollPanel`, imports `ReactMarkdown` and `remarkGfm`, contains `aria-live="polite"` and `trpc.ai.getMessages`
    - `StoryScrollPanel` renders streamingContent as raw text (ReactMarkdown is applied to completed messages only — no ReactMarkdown wrapping the streamingContent variable)
    - `src/renderer/src/components/ChatInputArea.tsx` exports `ChatInputArea`, contains `min-h-[56px]`, `useHotkeys('ctrl+enter'`, and the string "Ctrl+Enter to send"
    - `src/renderer/src/components/ChatErrorBlock.tsx` exports `ChatErrorBlock`, contains `role="alert"`, "The AI DM stopped responding", and conditional "Switch to fallback"
    - `src/renderer/src/screens/CampaignViewScreen.tsx` contains `StoryScrollPanel` and `ChatInputArea`; no longer contains the literal "AI narration appears here."
    - `npm run typecheck` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>Left panel renders the story scroll + chat input; streaming appends raw text with blinking cursor; inline error block shows correct buttons per fallback config; Ctrl+Enter and Send both work; typecheck + tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify live streaming chat</name>
  <what-built>The live left-panel chat: continuous story scroll, auto-growing input with Ctrl+Enter and Send, real-time streaming with blinking cursor, and the inline error/fallback block — wired to the ai:send-message IPC channel.</what-built>
  <action>Human verification only — run the steps in how-to-verify below and confirm each expected behavior. No code is written in this task.</action>
  <how-to-verify>
    1. Start LM Studio (or any OpenAI-compatible server) on http://localhost:1234/v1 with a model loaded.
    2. `npm run dev`. Create/open a campaign and configure its AI provider (if the wizard from plan 03-05 is not yet merged, set config via the gear modal once available, or temporarily via a manual updateAiConfig call — note this in the SUMMARY if the wizard is not yet present).
    3. Type "Hello, who are you?" and press Ctrl+Enter. Verify: the player entry appears with a horizontal rule + italic gold "You:" prefix; tokens stream into the panel in real time with a blinking cursor at the tail; the cursor disappears on completion; the completed AI message renders markdown (bold/italic) correctly.
    4. Click Send with the button (not Ctrl+Enter) and confirm it also sends; confirm Send is disabled while streaming.
    5. Point the endpoint at a bad URL and send; after ~3 retries confirm the inline error block appears with "Retry" (and "Switch to fallback" if a fallback is configured), NOT a toast or modal.
    6. Scroll up mid-stream and confirm auto-scroll suspends; scroll back to bottom and confirm it resumes.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the streaming/scroll/error issue observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → window.aiStream → ipcMain | message content + campaignId sent; validated in main (plan 03-03) |
| ipcMain → renderer (tokens) | only tokens + generic error strings received; renderer never sees keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | useAiStream / StoryScrollPanel | mitigate | Renderer only consumes ai:token/finish/error; no key handling in renderer. ChatErrorBlock displays only the generic message string from ai:error. |
| T-03-04-02 | Denial of Service (listener leak) | useAiStream | mitigate | removeAllListeners() in useEffect cleanup prevents duplicate IPC handlers accumulating across campaign switches (constraint #7) |
| T-03-04-03 | Tampering (markdown injection) | StoryScrollPanel | accept | react-markdown sanitizes by default (no raw HTML); AI narration is rendered as markdown, player/streaming text as plain text — no dangerouslySetInnerHTML |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm test` exits 0
- Human checkpoint: streaming, Ctrl+Enter, Send, blinking cursor, inline error block, auto-scroll suspend/resume all verified live
</verification>

<success_criteria>
- Left panel shows story-scroll chat replacing the placeholder (SESS-05 surface)
- Player can send via Ctrl+Enter and Send button; tokens stream in real time (FOUND-03)
- Blinking cursor during stream, cleared on finish (D-13)
- Inline error block with correct fallback/retry buttons after 3 retries (D-18)
- IPC listeners cleaned up on unmount (constraint #7)
</success_criteria>

<output>
Create `.planning/phases/03-ai-engine-provider-abstraction/03-04-SUMMARY.md` when done.
</output>
