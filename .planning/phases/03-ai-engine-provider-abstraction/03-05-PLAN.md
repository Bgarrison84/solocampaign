---
phase: 03-ai-engine-provider-abstraction
plan: 05
type: execute
wave: 4
depends_on: [03-03, 03-04]
files_modified:
  - src/renderer/src/components/AiProviderFields.tsx
  - src/renderer/src/components/ReferenceDocSelect.tsx
  - src/renderer/src/components/CreateCampaignModal.tsx
  - src/renderer/src/components/AiSettingsModal.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: false
requirements: [SESS-05, SESS-06, SESS-07]
must_haves:
  truths:
    - "Creating a campaign walks through 3 steps: name, AI provider + reference docs, DM personality + strictness"
    - "The gear icon in the campaign header opens an AI Settings modal pre-filled with current config"
    - "Saving config persists provider settings and stores the API key via SecretStorageService (never SQLite)"
    - "On a system without secure encryption an amber warning appears above the API key field"
  artifacts:
    - path: "src/renderer/src/components/AiProviderFields.tsx"
      provides: "shared provider type picker + conditional fields + reference docs + collapsible fallback (used by wizard Step 2 and gear modal)"
      exports: ["AiProviderFields"]
    - path: "src/renderer/src/components/AiSettingsModal.tsx"
      provides: "gear-icon reconfiguration modal (Steps 2+3 content, pre-filled)"
      exports: ["AiSettingsModal"]
    - path: "src/renderer/src/components/CreateCampaignModal.tsx"
      provides: "3-step wizard with WizardProgress totalSteps=3"
      contains: "WizardProgress"
  key_links:
    - from: "src/renderer/src/components/CreateCampaignModal.tsx"
      to: "trpc.campaigns.updateAiConfig"
      via: "after create, persist AI config from steps 2+3"
      pattern: "updateAiConfig"
    - from: "src/renderer/src/components/AiSettingsModal.tsx"
      to: "trpc.campaigns.updateAiConfig"
      via: "Save Changes mutation"
      pattern: "updateAiConfig"
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "AiSettingsModal"
      via: "gear icon button opens modal"
      pattern: "AiSettingsModal"
---

<objective>
Build the configuration UI: extend `CreateCampaignModal` into the 3-step wizard (D-01) with a shared `AiProviderFields` block (provider picker + conditional OpenAI-compatible/Gemini fields + reference doc multi-select + collapsible fallback) and a Step 3 (DM personality + strictness), add the `AiSettingsModal` gear-icon reconfiguration modal (D-09), and add the gear button to the campaign action bar. Surface the D-24 headless-Linux safeStorage warning.

Purpose: This delivers the SESS-05/06/07 configuration surface and feeds the secure updateAiConfig mutation built in Wave 2. It also connects the `onOpenSettings` seam left by plan 03-04.
Output: 2 shared sub-components + extended wizard + gear modal + action-bar gear button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md

<interfaces>
<!-- tRPC (plan 03-03): -->
<!-- trpc.campaigns.create.mutate({ name }) -> Campaign (existing) -->
<!-- trpc.campaigns.updateAiConfig.mutate(aiConfigSchema) -> Campaign  (campaignId, providerType, endpointUrl?, modelName, apiKey?, dmPersonality?, strictness, referenceDocs[], fallbackEndpointUrl?, fallbackModelName?, fallbackApiKey?) -->
<!-- trpc.campaigns.get.query({ id }) -> Campaign with AI config columns (for gear modal pre-fill) -->
<!-- trpc.ai.listReferenceDocs.query() -> { relativePath, title, isLarge }[] -->
<!-- trpc.prefs.isEncryptionAvailable.query() -> boolean (false => show amber warning, D-24) -->

<!-- API keys are write-only: updateAiConfig accepts apiKey but get() NEVER returns it (D-23). On gear-modal open, key fields render EMPTY with placeholder "(leave blank to keep current key)". -->

<!-- Reusable components: -->
<!-- WizardProgress (src/renderer/src/components/wizard/WizardProgress.tsx): props totalSteps, currentStep, completedUpTo, stepLabels[], onStepClick. Use totalSteps=3, stepLabels=['Campaign','AI Provider','DM Style'] (constraint #4). -->
<!-- shadcn ui: Dialog, Button, Input, Label, Select (Phase 1/2); Checkbox, Textarea, Collapsible, Tooltip (plan 03-01). -->
<!-- CreateCampaignModal.tsx currently: single name field + create mutation. Step 1 keeps the name field. -->
<!-- CampaignViewScreen action bar: <Button variant=ghost ...>Campaigns</Button> <span flex-1/> <Button ...>Delete Campaign</Button>. Insert the gear button between the spacer and Delete (UI-SPEC §S2). -->

<!-- All UI-SPEC §S3/§S4 strings, layouts, validation, and the safeStorage warning block are authoritative — copy verbatim. -->
<!-- Provider key naming handled in main (plan 03-03): ai-key-{campaignId} / ai-fallback-{campaignId}. The renderer only sends apiKey/fallbackApiKey in the mutation. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared AiProviderFields + ReferenceDocSelect components</name>
  <read_first>
    - src/renderer/src/components/CreateCampaignModal.tsx (existing modal structure, mutation pattern)
    - src/renderer/src/components/ui/select.tsx (provider type + strictness pickers)
    - src/renderer/src/components/ui/checkbox.tsx (installed plan 03-01 — reference doc rows)
    - src/renderer/src/components/ui/collapsible.tsx (installed plan 03-01 — fallback section)
    - src/renderer/src/components/ui/input.tsx + label.tsx (connection fields)
    - src/renderer/src/lib/trpc.ts (trpc.ai.listReferenceDocs, trpc.prefs.isEncryptionAvailable)
    - .planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md §S3b (provider picker, conditional OpenAI/Gemini fields, safeStorage warning, reference doc list with title display + Select All + large-file icon, collapsible fallback) and § "Copywriting Contract" (all Step 2 strings verbatim)
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-02, D-03, D-04, D-05, D-06, D-24
  </read_first>
  <action>
    Create `src/renderer/src/components/ReferenceDocSelect.tsx`: a controlled multi-select fed by `trpc.ai.listReferenceDocs` (TanStack useQuery). Props: `selected: string[]` (relativePaths), `onChange(next: string[])`. Render the bordered `max-h-[240px] overflow-y-auto` list of checkbox rows (each row toggles on row or checkbox click; the title is the pre-cleaned `title` from the query; show the AlertTriangle large-file icon + tooltip when `isLarge`). Add the "Select All"/"Deselect All" button and the "{n} selected" count and the helper text — all strings verbatim from the Copywriting Contract. Default selection empty.
    Create `src/renderer/src/components/AiProviderFields.tsx`: the shared provider configuration block used by BOTH the wizard Step 2 and the gear modal. Props: a controlled `value` object ({ providerType, endpointUrl, modelName, apiKey, referenceDocs, fallbackEndpointUrl, fallbackModelName, fallbackApiKey }) and `onChange`. Render the provider type `Select` (OpenAI-compatible | Gemini, default OpenAI-compatible). Conditionally render the OpenAI-compatible fields (Endpoint URL required, Model name required, API key optional) or Gemini fields (API key required, Model name required) per D-02/03/04 — preserve the OTHER provider type's field values in state when switching (UI-SPEC interaction contract). Embed `<ReferenceDocSelect>`. Render the collapsible "Add fallback provider (optional)" section (shadcn Collapsible) with the same field set as primary, using the same provider type as primary (no separate picker). Query `trpc.prefs.isEncryptionAvailable`; when false, render the amber `bg-amber-950/40` warning block ("Reduced key security" + body) ABOVE the API key field(s) per D-24. Expose a `validate()` helper or return per-field validity so parents can gate the Next/Save button. For the gear-modal use case, the apiKey/fallbackApiKey inputs render empty with a "(leave blank to keep current key)" placeholder.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/components/ReferenceDocSelect.tsx` exports `ReferenceDocSelect`, uses `trpc.ai.listReferenceDocs`, contains "Select All" and `max-h-[240px]`
    - `src/renderer/src/components/AiProviderFields.tsx` exports `AiProviderFields`, contains both provider option labels "OpenAI-compatible" and "Gemini"
    - `AiProviderFields` queries `trpc.prefs.isEncryptionAvailable` and contains the string "Reduced key security" (D-24 warning)
    - `AiProviderFields` contains a Collapsible fallback section ("Add fallback provider (optional)")
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Reusable AiProviderFields (with reference doc select + fallback collapsible + safeStorage warning) and ReferenceDocSelect built and typechecking.</done>
</task>

<task type="auto">
  <name>Task 2: 3-step CreateCampaignModal + AiSettingsModal + gear button</name>
  <read_first>
    - src/renderer/src/components/CreateCampaignModal.tsx (the modal to extend; existing create mutation + query invalidation)
    - src/renderer/src/components/AiProviderFields.tsx (from Task 1 — Step 2 + gear modal content)
    - src/renderer/src/components/ReferenceDocSelect.tsx (from Task 1)
    - src/renderer/src/components/wizard/WizardProgress.tsx (totalSteps/currentStep/completedUpTo/stepLabels/onStepClick — use totalSteps=3)
    - src/renderer/src/components/ui/textarea.tsx + select.tsx (Step 3 personality + strictness)
    - src/renderer/src/screens/CampaignViewScreen.tsx (action bar to add the gear button; the onOpenSettings seam left by plan 03-04 to connect)
    - src/renderer/src/lib/trpc.ts (campaigns.create, campaigns.updateAiConfig, campaigns.get)
    - .planning/phases/03-ai-engine-provider-abstraction/03-UI-SPEC.md §S2 (gear button), §S3a/b/c (3 steps), §S4 (gear modal), § "Copywriting Contract" + § "Wizard Navigation" (cancel confirmation) + § "Accessibility"
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-01, D-09, D-10
  </read_first>
  <action>
    Extend `CreateCampaignModal.tsx` to a 3-step wizard. Add `WizardProgress` with `totalSteps={3}` and `stepLabels={['Campaign','AI Provider','DM Style']}` (constraint #4). Widen the dialog to `max-w-[560px]`. Step 1: keep the existing name field + the non-interactive cover placeholder + step counter "Step 1 of 3"; Next disabled until name ≥ 1 char. Step 2: render `<AiProviderFields>`; Next disabled until required fields for the selected provider type are filled. Step 3: DM Personality `<Textarea rows={5} max-length={2000}>` with character counter + helper, and Rules Strictness `<Select>` (Strict | Balanced | Narrative, default Balanced) with the live explanatory callout (strings verbatim). Footer: Back / Create Campaign on Step 3. On submit: call `campaigns.create.mutate({ name })`, then with the returned campaign id call `campaigns.updateAiConfig.mutate({ campaignId, ...step2, ...step3 })` (one combined config write); invalidate ['campaigns','list']; close and navigate to the campaign view. Implement the cancel confirmation dialog ("Cancel campaign creation?") per the interaction contract. Wire WizardProgress completed-dot click navigation; preserve field state across Back/Next.
    Create `src/renderer/src/components/AiSettingsModal.tsx` (D-09): props `campaignId`, `open`, `onClose`. On open, `trpc.campaigns.get.query({ id: campaignId })` pre-fills providerType/endpointUrl/modelName/referenceDocs/dmPersonality/strictness/fallback* (API key fields render EMPTY — keys are never returned, D-23). Render a single scrollable DialogContent (NOT a wizard) titled "AI Settings — {campaign name}" containing `<AiProviderFields>`, a `<Separator>` (or border rule), the DM Personality textarea, and the Rules Strictness select. Footer Cancel / Save Changes → `campaigns.updateAiConfig.mutate(...)`; on success invalidate the campaign get query and close. Empty apiKey means "keep current key" (the main process leaves the stored key untouched on undefined/empty — coordinate: send apiKey only when the user typed one).
    In `CampaignViewScreen.tsx`: add the gear `<Button variant="ghost" size="sm">` with `<SlidersHorizontal className="h-4 w-4"/>` + "AI Settings" label and Tooltip, placed between the `flex-1` spacer and the Delete button (UI-SPEC §S2). Add `const [showAiSettings, setShowAiSettings] = useState(false)` and render `<AiSettingsModal campaignId={id} open={showAiSettings} onClose={...}/>`. Connect the gear button onClick and the `onOpenSettings` prop passed to StoryScrollPanel/ChatInputArea (the seam from plan 03-04) both to `setShowAiSettings(true)`. Do not disturb the left-panel chat wiring from plan 03-04.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/src/components/CreateCampaignModal.tsx` contains `WizardProgress`, `totalSteps={3}`, `AiProviderFields`, and `updateAiConfig`
    - `CreateCampaignModal` contains the strictness options and the cancel confirmation string "Cancel campaign creation?"
    - `src/renderer/src/components/AiSettingsModal.tsx` exports `AiSettingsModal`, calls `trpc.campaigns.get.query`, contains "Save Changes" and `updateAiConfig`
    - `AiSettingsModal` does NOT pre-fill any apiKey value (key fields default to empty string)
    - `src/renderer/src/screens/CampaignViewScreen.tsx` contains `SlidersHorizontal`, "AI Settings", and `AiSettingsModal`; the StoryScrollPanel/ChatInputArea onOpenSettings is wired to open the modal
    - `npm run typecheck` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>3-step wizard creates a campaign and writes AI config; gear modal pre-fills and saves config; gear button + onOpenSettings open the modal; keys never pre-filled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify wizard and AI settings modal</name>
  <what-built>The 3-step campaign creation wizard (name → AI provider + reference docs + fallback → DM personality + strictness) and the gear-icon AI Settings modal, both writing config through the secure updateAiConfig mutation.</what-built>
  <action>Human verification only — run the steps in how-to-verify below and confirm each expected behavior. No code is written in this task.</action>
  <how-to-verify>
    1. `npm run dev`. Create a new campaign: Step 1 enter a name; Step 2 pick OpenAI-compatible, enter endpoint + model, leave key blank, select 1-2 reference documents (verify titles are cleaned, large docs show the warning icon, Select All works), expand and optionally fill the fallback section; Step 3 write a DM personality and pick a strictness (verify the explanatory callout updates). Click Create Campaign.
    2. Confirm the campaign opens and (if LM Studio is running) you can immediately send a message that streams (end-to-end with plan 03-04).
    3. Switch the provider type to Gemini in Step 2 and confirm the fields change to API key + model name with no URL field.
    4. Click the gear "AI Settings" button in the campaign header. Confirm the modal pre-fills provider/model/docs/personality/strictness from what you saved, and that the API key field is EMPTY (never echoed back).
    5. Change the model name, leave the key blank, Save Changes. Send another message and confirm the new model is used (check electron-log) and that the previously stored key still works (blank = keep).
    6. (If on headless Linux) confirm the amber "Reduced key security" warning appears above the API key field.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the wizard/gear-modal issue observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer form → updateAiConfig (tRPC) | provider config + key cross here; validated by aiConfigSchema in main |
| campaigns.get → renderer | returns config columns but NEVER the api key (D-23) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | AiSettingsModal pre-fill | mitigate | Key fields render empty; campaigns.get returns no key column; blank key = keep current (D-23). Acceptance asserts no apiKey pre-fill. |
| T-03-05-02 | Information Disclosure | safeStorage warning | mitigate | D-24 amber warning informs the user when isEncryptionAvailable() is false before they enter a key |
| T-03-05-03 | Tampering | updateAiConfig input | mitigate | aiConfigSchema (plan 03-03) validates URL/enum/length at the IPC boundary; renderer cannot bypass |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm test` exits 0
- Human checkpoint: 3-step wizard, provider switching, reference doc select, fallback, gear modal pre-fill (no key echo), save-and-reuse all verified live
</verification>

<success_criteria>
- 3-step campaign creation wizard with AI provider config (SESS-05), DM personality (SESS-06), strictness (SESS-07)
- Gear-icon modal reconfigures all Step 2+3 fields, pre-filled, keys never echoed (D-09, D-23)
- Reference doc multi-select with cleaned titles + large-file warning + Select All (D-05)
- Collapsible fallback provider (D-06); safeStorage warning on insecure platforms (D-24)
</success_criteria>

<output>
Create `.planning/phases/03-ai-engine-provider-abstraction/03-05-SUMMARY.md` when done.
</output>
