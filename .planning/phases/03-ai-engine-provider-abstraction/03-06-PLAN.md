---
phase: 03-ai-engine-provider-abstraction
plan: 06
type: execute
wave: 5
depends_on: [03-02, 03-03]
files_modified:
  - electron-builder.yml
  - src/main/ai/aiMetrics.ts
  - src/main/ai/referenceDocLoader.ts
  - src/main/index.ts
autonomous: true
requirements: [FOUND-03, SESS-08]
must_haves:
  truths:
    - "Reference documents are bundled into the packaged app and resolvable at runtime in both dev and packaged builds"
    - "The ai:send-message handler logs the five AI-SPEC metrics via electron-log without ever logging the API key"
    - "The CSP allows the configured local + cloud provider hosts and the limitation for arbitrary local endpoints is documented"
  artifacts:
    - path: "electron-builder.yml"
      provides: "extraResources entry bundling reference docs markdown into resources/reference-docs"
      contains: "reference-docs"
    - path: "src/main/ai/aiMetrics.ts"
      provides: "logAiMetric helpers for latency_to_first_token_ms, total_tokens_received, error_count, fallback_activated, system_prompt_length"
      exports: ["logAiMetric"]
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/ai/aiMetrics.ts"
      via: "metric logging at first token / finish / error / fallback"
      pattern: "logAiMetric"
    - from: "src/main/ai/referenceDocLoader.ts"
      to: "process.resourcesPath/reference-docs"
      via: "packaged path resolution mirrors electron-builder extraResources 'to'"
      pattern: "reference-docs"
---

<objective>
Finalize the packaging and observability seams: bundle the reference document markdown into the packaged app via electron-builder `extraResources`, confirm the referenceDocLoader resolves that path in packaged builds, wire the five AI-SPEC §7 metrics into the `ai:send-message` handler via a small `aiMetrics` module (including `fallback_activated`), and review/document the CSP `connect-src` for local + cloud providers (Pitfall 8).

Purpose: Without bundling, reference docs vanish in the packaged build (FOUND-03 reference injection breaks). The metrics give the developer the electron-log seam the AI-SPEC monitoring section requires. CSP must permit the provider hosts the app actually talks to.
Output: electron-builder extraResources entry + aiMetrics module + metric wiring in the handler + CSP documentation note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md
@.planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md

<interfaces>
<!-- electron-builder.yml current extraResources: 'resources/migrations'->'migrations' and 'resources'(*.json)->'.' ; asarUnpack includes resources/migrations + resources/*.json -->
<!-- Reference docs source layout: "Reference Documents/Converted/<name>/<name>.md" (each folder also has .jpeg images — bundle only the .md files to keep size down). -->
<!-- referenceDocLoader (plan 03-02) dev path resolves the project-root "Reference Documents/Converted"; packaged path must be process.resourcesPath/reference-docs. This plan makes the two consistent: bundle the .md files to resources/reference-docs/<name>/<name>.md preserving the folder/title structure listReferenceDocs expects. -->

<!-- ai:send-message handler (plan 03-03) already emits at least latency_to_first_token + error_count via electron-log; this plan replaces ad-hoc logs with the aiMetrics helper and adds the remaining metrics. -->

<!-- AI-SPEC §7 metrics: ai.stream.latency_to_first_token_ms, ai.stream.total_tokens_received, ai.stream.error_count (by type), ai.fallback.activated (bool), ai.context.system_prompt_length -->

<!-- CSP (src/main/index.ts onHeadersReceived, prod only): connect-src 'self' https://api.openai.com https://generativelanguage.googleapis.com http://localhost:* http://127.0.0.1:* -->
<!-- Pitfall 8 (RESEARCH/AI-SPEC): a user-entered arbitrary non-localhost OpenAI-compatible endpoint (e.g. OpenRouter https://openrouter.ai) is NOT in connect-src. The LLM call happens in the MAIN process (Node fetch), NOT the renderer, so CSP connect-src does NOT block it — document this so no one "fixes" CSP unnecessarily. Renderer never makes provider calls directly. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bundle reference docs (electron-builder) + confirm packaged path resolution</name>
  <read_first>
    - electron-builder.yml (extraResources + asarUnpack structure to extend)
    - src/main/db/contentLoader.ts (getResourcesPath packaged-vs-dev pattern for reference)
    - src/main/ai/referenceDocLoader.ts (plan 03-02 — the dev path resolution + the packaged branch to confirm/adjust to 'reference-docs')
    - .planning/phases/03-ai-engine-provider-abstraction/03-CONTEXT.md D-05 (Reference Documents/Converted source)
  </read_first>
  <action>
    Add an `extraResources` entry to `electron-builder.yml` that copies the reference document markdown into the packaged app under `resources/reference-docs`, preserving the per-document folder structure that `listReferenceDocs` enumerates. Use `from: "Reference Documents/Converted"`, `to: "reference-docs"`, with `filter: ["**/*.md"]` so only markdown (not the .jpeg page images) is bundled. Keep the existing migrations + json extraResources entries unchanged.
    Confirm `src/main/ai/referenceDocLoader.ts` resolves the packaged path as `path.join(process.resourcesPath, 'reference-docs')` and the dev path as the project-root `Reference Documents/Converted` directory. If plan 03-02 used a different packaged folder name, reconcile it to `reference-docs` to match the electron-builder `to`. Ensure `listReferenceDocs` enumerates `<name>/<name>.md` consistently in both modes (the bundled structure preserves folder names so titles still clean correctly).
    Do not change asar settings unless the .md files need unpacking; markdown read via fs from extraResources lives outside the asar (extraResources are copied to resources/, not into the asar), so no asarUnpack entry is required for reference-docs.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('electron-builder.yml','utf8'); if(!y.includes('reference-docs'))throw new Error('reference-docs not bundled'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `electron-builder.yml` contains an extraResources entry with `to: reference-docs` and a `**/*.md` filter
    - `electron-builder.yml` still contains the existing `to: migrations` entry (not removed)
    - `src/main/ai/referenceDocLoader.ts` contains the literal `reference-docs` and uses `process.resourcesPath` for the packaged branch
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Reference doc markdown bundled to resources/reference-docs; referenceDocLoader resolves both dev and packaged paths consistently.</done>
</task>

<task type="auto">
  <name>Task 2: aiMetrics module + wire metrics into the handler + CSP documentation</name>
  <read_first>
    - src/main/index.ts (the ai:send-message handler from plan 03-03 — where first token, finish, error, and fallback occur; the prod CSP onHeadersReceived block)
    - src/main/ai/llmProvider.ts (streamChat callbacks — onToken/onFinish/onError timing for latency + token count)
    - src/main/ai/aiSessionState.ts (isFallbackActive — for fallback_activated metric, plan 03-03)
    - .planning/phases/03-ai-engine-provider-abstraction/03-AI-SPEC.md §7 (the five metric names + alert thresholds) and § "Common Pitfalls" / §3 (CSP / main-process-fetch reasoning, Pitfall 8)
  </read_first>
  <action>
    Create `src/main/ai/aiMetrics.ts` exporting `logAiMetric(name, value, extra?)` that writes a structured electron-log line tagged `[ai-metric]` with the metric name (one of: `ai.stream.latency_to_first_token_ms`, `ai.stream.total_tokens_received`, `ai.stream.error_count`, `ai.fallback.activated`, `ai.context.system_prompt_length`) and value. SECURITY: this helper must NEVER accept or log a key or raw message content — only numeric/boolean metrics and an error-type string. Optionally provide thin named wrappers (logLatencyToFirstToken, logTokensReceived, logError, logFallbackActivated, logSystemPromptLength).
    In `src/main/index.ts` ai:send-message handler: capture a start timestamp before streamChat; on first token call `logAiMetric('ai.stream.latency_to_first_token_ms', Date.now() - start)`; count tokens and on finish call `logAiMetric('ai.stream.total_tokens_received', count)`; on error call `logAiMetric('ai.stream.error_count', 1, { type })` with a coarse type ('timeout' | 'api' | 'network'); when the active provider is the fallback (isFallbackActive or useFallback) call `logAiMetric('ai.fallback.activated', true)`; after building context call `logAiMetric('ai.context.system_prompt_length', systemPrompt.length)`. Replace any ad-hoc log lines from plan 03-03 with these helper calls. Re-assert: no log call references the decrypted key or full message content.
    CSP review (Pitfall 8): inspect the prod CSP `connect-src` in onHeadersReceived. It already allows api.openai.com, generativelanguage.googleapis.com, and http://localhost:*/127.0.0.1:*. Because every provider HTTP call is made from the MAIN process (Node, not the sandboxed renderer), `connect-src` does NOT gate arbitrary user-entered OpenAI-compatible endpoints (e.g. OpenRouter) — the renderer never contacts them directly. Add a clear code comment above the connect-src directive documenting this: arbitrary provider endpoints work via main-process fetch and intentionally are NOT added to connect-src; do not broaden connect-src to `*` (that would weaken the renderer's posture for no benefit). Leave the directive value unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/ai/aiMetrics.ts` exports `logAiMetric` and references all five metric name strings
    - `src/main/ai/aiMetrics.ts` contains NO parameter or log call that could carry a key (no 'apiKey'/'key' parameter; no content logging)
    - `src/main/index.ts` calls `logAiMetric` for latency, token count, error, fallback, and system prompt length
    - `src/main/index.ts` CSP block contains a comment explaining the main-process-fetch / connect-src limitation (string "main process" near connect-src) and connect-src is NOT widened to `*`
    - `npm run typecheck` exits 0 and `npm test` exits 0
  </acceptance_criteria>
  <done>aiMetrics module logs the five AI-SPEC metrics (no key/content leak); handler wired to emit them; CSP limitation documented without weakening the policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main process → electron-log file | metrics written to disk in userData; must contain no secrets |
| renderer → external provider hosts | renderer never contacts providers; main-process fetch does (CSP scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Information Disclosure | aiMetrics / electron-log | mitigate | logAiMetric accepts only metric name + numeric/bool/type; never a key or message body. Acceptance asserts no key parameter. |
| T-03-06-02 | Elevation of Privilege | CSP connect-src | accept | connect-src is intentionally NOT broadened to `*`; arbitrary endpoints reached via main-process fetch only, keeping the renderer's network posture tight (Pitfall 8) |
| T-03-06-03 | Tampering | bundled reference docs | accept | Reference docs are read-only design-time assets bundled via extraResources; not user-writable in the packaged app |
</threat_model>

<verification>
- `npm run typecheck` exits 0; `npm test` exits 0
- electron-builder.yml bundles reference-docs (**/*.md); referenceDocLoader resolves packaged path
- Handler emits all five metrics; CSP comment present; connect-src unchanged (not `*`)
</verification>

<success_criteria>
- Reference docs survive packaging and resolve at runtime (FOUND-03 reference injection works in the installer)
- Five AI-SPEC §7 metrics logged via electron-log with no key/content leak
- fallback_activated metric supports SESS-08 reliability analysis
- CSP main-process-fetch limitation documented; policy not weakened
</success_criteria>

<output>
Create `.planning/phases/03-ai-engine-provider-abstraction/03-06-SUMMARY.md` when done.
</output>
