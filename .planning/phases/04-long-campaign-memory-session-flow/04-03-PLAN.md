---
phase: 04-long-campaign-memory-session-flow
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/main/ai/recapGenerator.ts
  - src/main/trpc/routers/sessions.ts
  - src/main/trpc/router.ts
  - src/main/trpc/schemas.ts
  - src/preload/index.ts
  - src/main/index.ts
  - src/main/ai/recapGenerator.test.ts
  - src/main/trpc/routers/sessions.test.ts
  - src/main/db/campaignsRepo.ts
autonomous: true
requirements:
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "recapGenerator.ts exports generateSessionRecap and generateRollingSummary using generateText"
    - "sessions tRPC router has start/end/saveRecap/updatePlayerNotes/list/getActive/getLastLocation procedures"
    - "sessionsRouter registered in router.ts"
    - "window.sessionRecap IPC surface exposed in preload"
    - "ai:recap-start ipcMain.handle added to index.ts"
    - "app.before-quit handler synchronously ends active sessions (D-06)"
    - "sessions.start sets sessionActiveMap in main process"
    - "sessions.saveRecap clears sessionActiveMap and schedules background rolling summary"
  artifacts:
    - path: "src/main/ai/recapGenerator.ts"
      provides: "generateText wrappers for recap + rolling summary"
      exports: ["generateSessionRecap", "generateRollingSummary", "RECAP_SYSTEM_PROMPT", "ROLLING_SUMMARY_SYSTEM_PROMPT"]
    - path: "src/main/trpc/routers/sessions.ts"
      provides: "session tRPC procedures"
      exports: ["sessionsRouter"]
      contains: "sessionsRouter"
    - path: "src/preload/index.ts"
      provides: "window.sessionRecap contextBridge surface"
      contains: "sessionRecap"
  key_links:
    - from: "src/preload/index.ts window.sessionRecap"
      to: "src/main/index.ts ai:recap-start handler"
      via: "ipcRenderer.invoke('ai:recap-start')"
      pattern: "ai:recap-start"
    - from: "src/main/trpc/routers/sessions.ts sessions.start"
      to: "src/main/ai/aiSessionState.ts sessionActiveMap"
      via: "sessionActiveMap.set(campaignId, session.id)"
      pattern: "sessionActiveMap.set"
---

<objective>
Create recapGenerator.ts (generateText wrappers), create sessions tRPC router, register it in router.ts, extend preload with window.sessionRecap IPC surface, add ai:recap-start handler and app.before-quit session auto-end to index.ts.

Purpose: This plan wires the server-side session lifecycle (create, end, save recap, rolling summary) and the IPC surface the renderer needs for streaming recap generation. After this plan, the full session data flow exists from DB through tRPC to IPC.

Output: recapGenerator.ts, sessions.ts router, preload extension, index.ts extension. Sessions can be started/ended/saved via tRPC and recap streaming works via the new IPC channel.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\SoloCampaign\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-CONTEXT.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-RESEARCH.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-PATTERNS.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-02-SUMMARY.md

<interfaces>
From src/main/ai/llmProvider.ts (analog for recapGenerator — read for buildModel signature):
- `export function buildModel(config: LLMProviderConfig): LanguageModelV1`
- `export type LLMProviderConfig = { type: 'openai-compatible' | 'gemini'; apiKey?: string; endpointUrl?: string; modelName: string; fallbackEndpointUrl?: string; fallbackModelName?: string }`

From src/main/trpc/routers/campaigns.ts (analog for sessions router — read for mutation pattern):
- t.procedure.input(z.object({...})).mutation(({ input }) => { ... return result })

From src/main/trpc/schemas.ts (read before extending):
- `export const campaignIdSchema = z.string().uuid()`

From src/main/index.ts (read lines 165-340 — the ai:send-message handler for senderFrame validation + safeSend + streamText pattern to copy into ai:recap-start):
- senderFrame validation block (lines 175-184)
- safeSend helper pattern (lines 268-271)
- Campaign config loading pattern (lines 194-233)
- streamText with onChunk/onFinish/onError callbacks

From src/preload/index.ts (current — the window.aiStream block to mirror for window.sessionRecap):
```typescript
contextBridge.exposeInMainWorld('aiStream', {
  sendMessage: (payload) => ipcRenderer.invoke('ai:send-message', payload),
  onToken: (cb) => { ipcRenderer.on('ai:token', (_, t) => cb(t)) },
  onFinish: (cb) => { ipcRenderer.on('ai:finish', () => cb()) },
  onError: (cb) => { ipcRenderer.on('ai:error', (_, m) => cb({ message: ... })) },
  removeAllListeners: () => { ipcRenderer.removeAllListeners('ai:token'); ... },
})
```

From src/main/db/campaignsRepo.ts (read for updateRollingSummary pattern — needs new method):
- db.update(campaigns).set({ rollingSummary: value }).where(eq(campaigns.id, id)).run()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create recapGenerator.ts + fill recapGenerator.test.ts stubs</name>
  <read_first>
    src/main/ai/llmProvider.ts
    src/main/ai/recapGenerator.test.ts
  </read_first>
  <behavior>
    - generateSessionRecap calls generateText with system=RECAP_SYSTEM_PROMPT, messages=sessionMessages, temperature=0.3
    - generateSessionRecap returns the text property from generateText result
    - generateRollingSummary calls generateText with system=ROLLING_SUMMARY_SYSTEM_PROMPT, prompt=historyText, temperature=0.3, maxTokens=1000
    - Both functions log with electron-log, omitting apiKey from log output
    - Both functions propagate errors after logging (no swallowing)
  </behavior>
  <action>
    Create `src/main/ai/recapGenerator.ts`. Import: `generateText` from 'ai', `ModelMessage` type from 'ai', `buildModel` and `LLMProviderConfig` from './llmProvider', `log` from 'electron-log'.

    Export constants:
    - `RECAP_SYSTEM_PROMPT`: "You are a Dungeon Master's record-keeper. Summarize the following D&D 5e session in concise, third-person prose suitable for a DM's session notes. Include: key events in order, significant NPC interactions, decisions the player made, any combat outcomes, and where the session ended. Be factual and specific. Omit flavor prose — this is a reference record, not narrative. Aim for 3-6 paragraphs."
    - `ROLLING_SUMMARY_SYSTEM_PROMPT`: "You are a Dungeon Master's campaign archivist. The following are session summaries from earlier in a D&D 5e campaign. Synthesize them into a single cohesive campaign summary of no more than 800 words. Capture: the overall story arc, key NPCs and their relationships to the player, major plot points resolved or ongoing, and the current state of the world. This summary will be injected into future AI context windows, so be precise and information-dense. Use past tense."

    Export `async function generateSessionRecap(providerConfig: LLMProviderConfig, sessionMessages: ModelMessage[]): Promise<string>`:
    - Call buildModel(providerConfig) — uses primary provider config (not fallback, per Pitfall 4 in RESEARCH.md)
    - log.debug('[recapGenerator] generateSessionRecap', { messageCount: sessionMessages.length, providerType: providerConfig.type }) — apiKey intentionally omitted
    - Call generateText({ model, system: RECAP_SYSTEM_PROMPT, messages: sessionMessages, temperature: 0.3 })
    - Return text
    - Wrap in try/catch: catch logs error.message then re-throws

    Export `async function generateRollingSummary(providerConfig: LLMProviderConfig, olderSessions: Array<{ sessionNumber: number; aiRecap: string }>): Promise<string>`:
    - Build historyText = olderSessions.map(s => `Session ${s.sessionNumber}: ${s.aiRecap}`).join('\n\n')
    - Call buildModel(providerConfig)
    - log.debug('[recapGenerator] generateRollingSummary', { sessionCount: olderSessions.length })
    - Call generateText({ model, system: ROLLING_SUMMARY_SYSTEM_PROMPT, prompt: historyText, temperature: 0.3, maxTokens: 1000 })
    - Return text
    - Wrap in try/catch: catch logs then re-throws

    Then fill in `src/main/ai/recapGenerator.test.ts` stubs. Use vi.mock to mock the 'ai' module's generateText function. Assert that generateSessionRecap calls generateText with system=RECAP_SYSTEM_PROMPT. Assert that generateRollingSummary calls generateText with system=ROLLING_SUMMARY_SYSTEM_PROMPT and maxTokens=1000.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/recapGenerator.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>recapGenerator.test.ts passes. recapGenerator.ts exports generateSessionRecap, generateRollingSummary, RECAP_SYSTEM_PROMPT, ROLLING_SUMMARY_SYSTEM_PROMPT. npx tsc --noEmit exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sessions tRPC router, extend schemas, register in router.ts, extend preload + index.ts</name>
  <read_first>
    src/main/trpc/routers/campaigns.ts
    src/main/trpc/schemas.ts
    src/main/trpc/router.ts
    src/preload/index.ts
    src/main/index.ts
    src/main/db/campaignsRepo.ts
    src/main/trpc/routers/sessions.test.ts
  </read_first>
  <behavior>
    - sessions.start creates a session row + calls sessionActiveMap.set(campaignId, session.id) + returns { id, sessionNumber }
    - sessions.end sets ended_at + calls sessionActiveMap.clear(campaignId)
    - sessions.saveRecap persists aiRecap + playerNotes + calls sessionActiveMap.clear + schedules background rolling summary
    - sessions.updatePlayerNotes updates playerNotes only
    - sessions.list returns sessions array for campaignId ordered newest-first
    - sessions.getActive returns the active session for campaignId or null
  </behavior>
  <action>
    Part A — Extend `src/main/trpc/schemas.ts`: add after campaignIdSchema:
    - `export const sessionIdSchema = z.string().uuid()`
    - `export const sessionLocationSchema = z.string().max(200)`
    - `export const sessionGoalSchema = z.string().max(1000)`
    - `export const sessionContextNotesSchema = z.string().max(1000)`
    - `export const sessionRecapSchema = z.string().max(50000)`
    - `export const playerNotesSchema = z.string().max(10000)`

    Part B — Create `src/main/trpc/routers/sessions.ts`. Import: z from 'zod', t from '../_base', sessionsRepo from '../../db/sessionsRepo', sessionActiveMap from '../../ai/aiSessionState', campaignIdSchema + sessionIdSchema + sessionLocationSchema + sessionGoalSchema + sessionContextNotesSchema + sessionRecapSchema + playerNotesSchema from '../schemas'.

    Also import campaignsRepo from '../../db/campaignsRepo', secretStorage from '../../services/secretStorageService' (check the Phase 3 import pattern in index.ts), generateRollingSummary from '../../ai/recapGenerator', log from 'electron-log'.

    Note: campaignsRepo needs a new `updateRollingSummary(campaignId: string, value: string | null): void` method — add it to campaignsRepo.ts following the existing update pattern (db.update(campaigns).set({ rollingSummary: value }).where(eq(campaigns.id, campaignId)).run()).

    Export `sessionsRouter = t.router({...})` with these procedures:

    `start`: input z.object({ campaignId: campaignIdSchema, location: sessionLocationSchema.nullish(), goal: sessionGoalSchema.nullish(), contextNotes: sessionContextNotesSchema.nullish() }), mutation: create session row via sessionsRepo.create(), call sessionActiveMap.set(input.campaignId, session.id), return { id: session.id, sessionNumber: session.sessionNumber }.

    `end`: input z.object({ sessionId: sessionIdSchema }), mutation: call sessionsRepo.end(input.sessionId), return { ended: true }.

    `saveRecap`: input z.object({ sessionId: sessionIdSchema, campaignId: campaignIdSchema, aiRecap: sessionRecapSchema, playerNotes: playerNotesSchema.optional() }), mutation: call sessionsRepo.saveRecap(sessionId, aiRecap, playerNotes), call sessionActiveMap.clear(campaignId), schedule background rolling summary via Promise.resolve().then(async () => { ... }). In the background task: load campaign from campaignsRepo.getById(campaignId), get savedSession from sessionsRepo.getById(sessionId), compute olderSessions = sessionsRepo.getOlderThan(campaignId, (savedSession.sessionNumber - 3)), if olderSessions.length === 0 call campaignsRepo.updateRollingSummary(campaignId, null) and return. Otherwise: decrypt API key (follow index.ts pattern for key naming scheme), build providerConfig from campaign, call generateRollingSummary(providerConfig, olderSessions.map(s => ({ sessionNumber: s.sessionNumber, aiRecap: s.aiRecap ?? '' }))), truncate to 4000 chars, call campaignsRepo.updateRollingSummary(campaignId, truncated), call sessionsRepo.markSummarized(sessionId). Wrap entire background task in try/catch; on error log.error('[sessions] Rolling summary failed:', err) and do not rethrow. Return { saved: true }.

    `updatePlayerNotes`: input z.object({ sessionId: sessionIdSchema, playerNotes: playerNotesSchema }), mutation: sessionsRepo.updatePlayerNotes, return { updated: true }.

    `list`: input z.object({ campaignId: campaignIdSchema }), query: sessionsRepo.list(campaignId).

    `getActive`: input z.object({ campaignId: campaignIdSchema }), query: sessionsRepo.getActiveByCampaignId(campaignId) ?? null.

    `getLastLocation`: input z.object({ campaignId: campaignIdSchema }), query: sessionsRepo.getLastLocation(input.campaignId) — returns the location string from the most recently completed session, or null if no sessions exist. Used by SessionStartModal to pre-fill the location field on session 2+.

    Part C — Register in `src/main/trpc/router.ts`: add `import { sessionsRouter } from './routers/sessions'` and add `sessions: sessionsRouter` to the router object.

    Part D — Extend `src/preload/index.ts`: add `window.sessionRecap` contextBridge exposure after the window.aiStream block. The surface:
    - startStream: (payload: { campaignId: string; sessionId: string }) => ipcRenderer.invoke('ai:recap-start', payload)
    - onToken: (cb: (token: string) => void) => ipcRenderer.on('ai:recap-token', (_, t) => cb(t))
    - onFinish: (cb: (finalText: string) => void) => ipcRenderer.on('ai:recap-finish', (_, text) => cb(text))
    - onError: (cb: (err: { message: string }) => void) => ipcRenderer.on('ai:recap-error', (_, m) => cb(m))
    - removeAllListeners: () => { ipcRenderer.removeAllListeners('ai:recap-token'); ipcRenderer.removeAllListeners('ai:recap-finish'); ipcRenderer.removeAllListeners('ai:recap-error'); }
    Use contextBridge.exposeInMainWorld('sessionRecap', { ... }).

    Also extend the existing `window.aiStream.onFinish` callback signature in preload to accept optional meta: change `onFinish: (cb: () => void)` to `onFinish: (cb: (meta?: { isL1Overflow?: boolean }) => void)` and update the ipcRenderer.on callback to `(_, meta) => cb(meta)`.

    Part E — Extend `src/main/index.ts`:
    1. Add imports for sessionsRepo, sessionActiveMap, generateSessionRecap from the appropriate modules.
    2. Add `ipcMain.handle('ai:recap-start', async (event, payload) => { ... })` after the existing ai:send-message handler. This handler: validates senderFrame (copy verbatim from ai:send-message lines 175-184), validates payload has sessionId + campaignId via Zod, loads campaign config from campaignsRepo (always use primary provider — NOT fallback, per Pitfall 4), loads session messages via messagesRepo.getBySessionId(sessionId), calls streamText (not generateText — to enable streaming to modal) with RECAP_SYSTEM_PROMPT as system prompt. On each chunk: safeSend('ai:recap-token', chunk.textDelta). On finish: accumulate full text, safeSend('ai:recap-finish', fullText). On error: safeSend('ai:recap-error', { message: error.message }). Return { started: true }.
    3. Add `app.on('before-quit', () => { ... })` handler: synchronously find any active sessions per campaign using sessionsRepo — but since we don't know all campaignIds at quit time, use a direct DB query via getDb() to find all sessions where ended_at IS NULL and set ended_at = Date.now(). This is a raw synchronous better-sqlite3 call: `getDb().prepare("UPDATE sessions SET ended_at = ? WHERE ended_at IS NULL").run(Date.now())`. Do NOT call any async LLM functions here (Pitfall 6 in RESEARCH.md). Add comment: "D-06: isSummarized=false rows will have rolling summary generated on next session start."

    Fill in sessions.test.ts stubs with concrete tests using tRPC test caller: `sessionsRouter.createCaller({})`.
  </action>
  <verify>
    <automated>npx vitest run src/main/trpc/routers/sessions.test.ts src/main/ai/recapGenerator.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>sessions.test.ts passes. recapGenerator.test.ts passes. window.sessionRecap declared in preload. ai:recap-start handler present in index.ts. sessionsRouter registered in router.ts. app.before-quit handler present. npx tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ai:recap-start IPC handler | Renderer triggers recap generation; handler must validate senderFrame like ai:send-message |
| sessions.saveRecap tRPC | Renderer passes aiRecap text (up to 50000 chars) — validated by Zod schema |
| before-quit DB write | Synchronous raw SQL update — must complete before process exits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | ai:recap-start senderFrame | mitigate | Copy senderFrame validation verbatim from ai:send-message handler (lines 175-184 of index.ts) — same security contract |
| T-04-03-02 | Tampering | sessions.saveRecap aiRecap field | mitigate | Zod sessionRecapSchema: z.string().max(50000) — oversized content rejected at tRPC boundary |
| T-04-03-03 | Information Disclosure | API key used in ai:recap-start | mitigate | Key decryption uses same SecretStorageService pattern as ai:send-message — never transmitted to renderer |
| T-04-03-04 | Tampering | sessions.start campaignId | mitigate | campaignIdSchema: z.string().uuid() validates at tRPC boundary |
| T-04-03-05 | Denial of Service | background rolling summary blocking event loop | accept | Promise.resolve().then() scheduling; try/catch prevents crash; failure is non-fatal (next session gets empty L3) |
| T-04-03-06 | Data Loss | app-close mid-session | mitigate | before-quit handler: synchronous SQL UPDATE sets ended_at for all active sessions; isSummarized=false flag ensures rolling summary generated next open |
| T-04-03-SC | Tampering | npm/pip/cargo installs | accept | No new npm installs in this plan |
</threat_model>

<verification>
After all tasks complete:
1. `npx vitest run src/main/trpc/routers/sessions.test.ts src/main/ai/recapGenerator.test.ts` exits 0
2. `npx tsc --noEmit` exits 0
3. src/main/trpc/router.ts contains `sessions: sessionsRouter`
4. src/preload/index.ts contains `contextBridge.exposeInMainWorld('sessionRecap'`
5. src/main/index.ts contains `ipcMain.handle('ai:recap-start'`
6. src/main/index.ts contains `app.on('before-quit'` with synchronous SQL UPDATE
7. src/main/ai/recapGenerator.ts exports RECAP_SYSTEM_PROMPT and ROLLING_SUMMARY_SYSTEM_PROMPT
8. sessions.start procedure calls sessionActiveMap.set inside mutation handler
</verification>

<success_criteria>
- recapGenerator.ts: generateSessionRecap + generateRollingSummary using generateText
- sessions.ts router: 6 procedures (start/end/saveRecap/updatePlayerNotes/list/getActive) with Zod validation
- router.ts: sessions: sessionsRouter registered
- preload: window.sessionRecap with startStream/onToken/onFinish/onError/removeAllListeners
- index.ts: ai:recap-start IPC handler with senderFrame validation; before-quit synchronous DB write
- All test files pass; npx tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/04-long-campaign-memory-session-flow/04-03-SUMMARY.md` when done
</output>
