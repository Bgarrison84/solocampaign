---
phase: 04-long-campaign-memory-session-flow
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/main/index.ts
autonomous: true
requirements:
  - SESS-02
  - SESS-03

must_haves:
  truths:
    - "messagesRepo.insert called with sessionId from sessionActiveMap for both user and assistant messages"
    - "buildContext called with sessionId, sessionContext, and rollingSummary from campaign"
    - "ai:finish event payload includes isL1Overflow boolean"
    - "Pre-check for unsummarized sessions in sessions.start prevents new session if pending (D-06 detection)"
  artifacts:
    - path: "src/main/index.ts"
      provides: "Extended ai:send-message handler with session awareness"
      contains: "sessionActiveMap.get"
  key_links:
    - from: "src/main/index.ts ai:send-message"
      to: "src/main/ai/contextBuilder.ts buildContext"
      via: "sessionId + sessionContext + rollingSummary params"
      pattern: "buildContext.*sessionId"
    - from: "src/main/index.ts ai:send-message"
      to: "src/main/db/messagesRepo.ts insert"
      via: "sessionId: activeSessionId"
      pattern: "messagesRepo.insert.*sessionId"
---

<objective>
Extend the ai:send-message IPC handler in index.ts to pass sessionId, sessionContext, and rollingSummary to buildContext v2, associate messages with the active session, and include isL1Overflow in the ai:finish event payload. Also add the D-06 pre-check in sessions.start.

Purpose: This plan closes the loop between the session lifecycle (started in plan 04-03) and the actual AI message handler. After this plan, every message sent during a session will have session_id set in the DB, and the AI will receive the three-layer memory context.

Output: Modified index.ts ai:send-message handler that is fully session-aware.
</objective>

<execution_context>
@C:\Users\psyco\Claude CLI\SoloCampaign\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-CONTEXT.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-RESEARCH.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-PATTERNS.md
@C:\Users\psyco\Claude CLI\SoloCampaign\.planning\phases\04-long-campaign-memory-session-flow\04-03-SUMMARY.md

<interfaces>
From src/main/index.ts (the ai:send-message handler — read the full handler before modifying):
Current ai:send-message handler flow:
- Step 1: senderFrame validation (lines ~175-184)
- Step 2: Validate payload (campaignId, content) via Zod
- Step 3: Check fallback state
- Step 4: Load campaign config + decrypt API key
- Step 5: Call buildContext({ campaignId, config: { strictness, dmPersonality, referenceDocs } }) — v1 signature (NO sessionId)
- Step 6: streamText with onChunk (emit ai:token), onFinish (emit ai:finish), onError (emit ai:error)
- Step 4b: messagesRepo.insert({ campaignId, role: 'user', content }) — NO sessionId

Target changes:
- After Step 2, before buildContext: const activeSessionId = sessionActiveMap.get(campaignId)
- Step 4b messagesRepo.insert: add sessionId: activeSessionId
- Step 5 buildContext call: add sessionId: activeSessionId, sessionContext (loaded from sessionsRepo.getById if activeSessionId), rollingSummary: campaign.rollingSummary ?? null in config
- onFinish: messagesRepo.insert for assistant message also gets sessionId: activeSessionId
- onFinish: safeSend('ai:finish', { isL1Overflow: capturedIsL1Overflow }) — add isL1Overflow to payload

From src/main/ai/contextBuilder.ts v2 (plan 04-02 output):
```typescript
export interface BuildContextArgs {
  campaignId: string
  sessionId: string | null
  sessionContext?: { location: string | null; goal: string | null; contextNotes: string | null }
  config: { strictness; dmPersonality?; referenceDocs?; rollingSummary?: string | null }
}
export interface BuiltContext {
  systemPrompt: string
  messages: ModelMessage[]
  isL1Overflow: boolean
}
```

From src/main/ai/aiSessionState.ts (plan 04-02 output):
```typescript
export const sessionActiveMap = {
  get: (campaignId: string): string | null => ...
  set: (campaignId: string, sessionId: string) => ...
  clear: (campaignId: string) => ...
} as const
```

From src/main/db/sessionsRepo.ts (plan 04-01 output):
```typescript
sessionsRepo.getById(sessionId: string): Session | undefined
sessionsRepo.getActiveByCampaignId(campaignId: string): Session | undefined
```

From src/main/trpc/routers/sessions.ts (plan 04-03 output):
- sessions.start procedure — needs D-06 pre-check for unsummarized sessions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ai:send-message handler with session awareness + isL1Overflow in ai:finish</name>
  <read_first>
    src/main/index.ts
    src/main/ai/aiSessionState.ts
    src/main/db/sessionsRepo.ts
    src/main/db/messagesRepo.ts
    src/main/ai/contextBuilder.ts
  </read_first>
  <action>
    Read the full ai:send-message handler in index.ts (approximately lines 164-339). Make these targeted changes:

    1. Add import at the top of index.ts: `import { sessionActiveMap } from './ai/aiSessionState'` (if not already added in plan 04-03). Also import sessionsRepo if not already imported.

    2. At the start of the ai:send-message handler (after Step 2 payload validation, before loading campaign config): add `const activeSessionId = sessionActiveMap.get(campaignId)`. This must be computed from the main-process in-memory map — NOT from any renderer payload. The renderer does NOT pass sessionId in the ai:send-message payload (Pitfall 2 in RESEARCH.md — main process is authoritative).

    3. Find the `messagesRepo.insert` call for the user message (Step 4). Change it to include `sessionId: activeSessionId` — the InsertMessageInput interface now has this optional field.

    4. Find the `buildContext` call (Step 5). Change it from the v1 signature to the v2 signature:
       - Add `sessionId: activeSessionId`
       - Add `sessionContext: (() => { if (!activeSessionId) return undefined; const sess = sessionsRepo.getById(activeSessionId); return sess ? { location: sess.location ?? null, goal: sess.goal ?? null, contextNotes: sess.contextNotes ?? null } : undefined })()`
       - In the config object, add `rollingSummary: campaign.rollingSummary ?? null` (campaign is already loaded at this point)
       - Destructure the return value to capture isL1Overflow: `const { systemPrompt, messages: ctxMessages, isL1Overflow } = buildContext({...})`
       - Store: `let capturedIsL1Overflow = isL1Overflow` in scope for the onFinish callback to access.

    5. Find the `onFinish` callback (the one that calls `messagesRepo.insert` for the assistant message and `safeSend('ai:finish')`):
       - Add `sessionId: activeSessionId` to the assistant messagesRepo.insert call
       - Change `safeSend('ai:finish')` to `safeSend('ai:finish', { isL1Overflow: capturedIsL1Overflow })`

    6. Check if the existing code has `safeSend('ai:finish')` with no payload — update it to `safeSend('ai:finish', { isL1Overflow: capturedIsL1Overflow })`. If there are multiple places where ai:finish is sent, update all of them.

    After all changes, run `npx tsc --noEmit` and fix any type errors before committing.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>npx tsc --noEmit exits 0. ai:send-message handler contains sessionActiveMap.get(campaignId). messagesRepo.insert calls include sessionId. buildContext call includes sessionId and rollingSummary. safeSend('ai:finish', { isL1Overflow }) present.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-06 unsummarized session pre-check to sessions.start + verify full pipeline compiles</name>
  <read_first>
    src/main/trpc/routers/sessions.ts
    src/main/db/sessionsRepo.ts
    src/main/ai/recapGenerator.ts
  </read_first>
  <action>
    Extend the `sessions.start` mutation handler in `src/main/trpc/routers/sessions.ts` to implement the D-06 pre-check.

    Before creating the new session row, add a check: query for any sessions where campaignId matches, endedAt IS NOT NULL, and isSummarized = false. These are sessions that ended (via before-quit auto-end) but never had a rolling summary generated.

    Implementation: `const unsummarized = db.select().from(sessions).where(and(eq(sessions.campaignId, input.campaignId), isNotNull(sessions.endedAt), eq(sessions.isSummarized, false))).all()` — but since sessionsRepo methods don't expose this query directly, add a new method `getUnsummarized(campaignId: string): Session[]` to sessionsRepo.ts that returns sessions where endedAt IS NOT NULL AND isSummarized = false for the given campaignId.

    If unsummarized sessions exist, trigger rolling summary generation synchronously before creating the new session. Use the same background task logic as in saveRecap, but execute it synchronously (await the generateRollingSummary call) since the user is waiting. This is the "brief wait before allowing new session start" described in RESEARCH.md open question 1. The chat input is locked (D-03) so the user sees the "Start Session" button spinner for the ~5s duration.

    To generate the rolling summary synchronously: load campaign config + decrypt API key (same pattern as saveRecap), get olderSessions = sessionsRepo.getOlderThan(campaignId, lastSessionNumber - 2) where lastSessionNumber is the most recent ended session's number, call generateRollingSummary, update campaignsRepo.updateRollingSummary, call sessionsRepo.markSummarized for each unsummarized session. Wrap in try/catch — if it fails, log the error and proceed with session creation anyway (non-fatal: L3 just won't be updated).

    Note: this requires the sessions.start mutation to be `async` — change `.mutation(({ input }) => {` to `.mutation(async ({ input }) => {` if not already async.

    After modifying sessions.ts, run full test suite to confirm no regressions:
    `npx vitest run src/main/` to run all main-process tests.

    Run `npx tsc --noEmit` to confirm no type errors.
  </action>
  <verify>
    <automated>npx vitest run src/main/ 2>&1 | tail -20</automated>
  </verify>
  <done>npx vitest run src/main/ exits 0 (all main-process tests pass). npx tsc --noEmit exits 0. sessions.start is async. sessionsRepo has getUnsummarized method. D-06 pre-check executes before session creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ai:send-message session routing | activeSessionId comes from main-process sessionActiveMap only — renderer cannot inject sessionId |
| D-06 pre-check | Rolling summary uses same provider config path as normal generation — API key decrypted in main only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | sessionId injection from renderer | mitigate | sessionActiveMap.get(campaignId) is called in main process; renderer payload does not include sessionId field; main process is sole authority |
| T-04-04-02 | Information Disclosure | rollingSummary content in buildContext | accept | rollingSummary is campaign-owned data written by the same user; no third-party PII |
| T-04-04-03 | Tampering | isL1Overflow flag in ai:finish payload | accept | Boolean flag read by renderer for UI display only; cannot be injected from renderer to influence backend behavior |
| T-04-04-04 | Denial of Service | D-06 pre-check blocking sessions.start for 5s | accept | Only fires once per unsummarized session (after crash recovery); isSummarized is marked true afterward; bounded by generateRollingSummary call which has model-level timeout |
| T-04-04-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan |
</threat_model>

<verification>
After all tasks complete:
1. `npx tsc --noEmit` exits 0
2. `npx vitest run src/main/` passes all main-process tests
3. index.ts ai:send-message handler contains: `sessionActiveMap.get(campaignId)`, `sessionId: activeSessionId` in both messagesRepo.insert calls, `buildContext({...sessionId: activeSessionId...rollingSummary...})`, `safeSend('ai:finish', { isL1Overflow`
4. sessions.ts start procedure calls getUnsummarized + handles D-06 pre-check
5. sessionsRepo.ts has getUnsummarized method
</verification>

<success_criteria>
- All messages written during an active session have sessionId set (main-process authoritative routing)
- buildContext v2 called with full session context on every AI call
- ai:finish payload includes isL1Overflow boolean
- D-06 recovery: unsummarized sessions get rolling summary generated on next session start
- npx tsc --noEmit exits 0; all main-process tests pass
</success_criteria>

<output>
Create `.planning/phases/04-long-campaign-memory-session-flow/04-04-SUMMARY.md` when done
</output>
