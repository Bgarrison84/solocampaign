---
phase: 05-rules-engine-dice-combat
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - src/main/db/schema.ts
  - resources/migrations/0004_phase5_rules_engine.sql
  - resources/migrations/meta/_journal.json
  - resources/spells.json
  - src/main/db/combatantsRepo.ts
  - src/main/db/campaignEventsRepo.ts
  - src/main/db/characterSpellsRepo.ts
  - src/renderer/src/lib/dice.ts
  - src/main/ai/toolSchemas.test.ts
  - src/main/ai/mutationPipeline.test.ts
  - src/main/db/combatantsRepo.test.ts
  - src/main/db/characterSpellsRepo.test.ts
  - src/main/db/campaignEventsRepo.test.ts
  - src/renderer/src/lib/dice.test.ts
autonomous: true
requirements: [COMB-01, COMB-02, CHAR-08, STATE-05]
user_setup: []

must_haves:
  truths:
    - "rpg-dice-roller installed and rollExpression('4d6kh3') returns a result + breakdown"
    - "Migration 0004 applies at startup, creating combatants, campaign_events, character_spells tables and 4 new character_resources columns + permadeath_mode on campaigns"
    - "resources/spells.json exists with SRD spell metadata (name, level, castTime, range, duration, components, concentration, ritual, description) for the 8 SRD spellcasting classes' level-1 spell needs"
    - "combatantsRepo, campaignEventsRepo, characterSpellsRepo expose synchronous Drizzle CRUD methods"
    - "All 6 Wave 0 test files exist and the listed RED assertions are present"
  artifacts:
    - path: "resources/migrations/0004_phase5_rules_engine.sql"
      provides: "Migration creating 3 tables + 5 ALTER TABLE columns"
      contains: "CREATE TABLE `combatants`"
    - path: "resources/spells.json"
      provides: "SRD spell metadata content file"
      min_lines: 100
    - path: "src/main/db/combatantsRepo.ts"
      provides: "Combatant CRUD"
      exports: ["combatantsRepo"]
    - path: "src/main/db/campaignEventsRepo.ts"
      provides: "Append-only event log CRUD"
      exports: ["campaignEventsRepo"]
    - path: "src/main/db/characterSpellsRepo.ts"
      provides: "Character spell list CRUD"
      exports: ["characterSpellsRepo"]
    - path: "src/renderer/src/lib/dice.ts"
      provides: "rpg-dice-roller wrapper"
      exports: ["rollExpression", "RollResult"]
  key_links:
    - from: "src/main/db/schema.ts"
      to: "resources/migrations/0004_phase5_rules_engine.sql"
      via: "schema definitions must match migration DDL exactly"
      pattern: "combatants|campaign_events|character_spells"
    - from: "src/main/db/index.ts applyMigrations"
      to: "resources/migrations/0004_phase5_rules_engine.sql"
      via: "startup migration runner reads _journal.json then applies 0004"
      pattern: "0004_phase5_rules_engine"
---

<objective>
Wave 0 foundation for Phase 5. Install the dice library, extend the Drizzle schema with three new tables and five new columns, author migration 0004, author the SRD spell content file, build the three new synchronous repos, build the renderer dice wrapper, and create all six Wave 0 test stubs (RED) that downstream waves turn GREEN.

Purpose: Every later wave depends on these tables, repos, and the dice utility existing. Authoring the migration + schema + repos together (vertical foundation slice) ensures the DB contract is locked before any AI tooling or UI consumes it.
Output: New dependency installed, migration 0004, spells.json, 3 repos, dice.ts, 6 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-VALIDATION.md

<interfaces>
<!-- Current characterResources schema (src/main/db/schema.ts lines 134-164) the migration extends. -->
<!-- Existing columns: hpCurrent, hpMax, hpTemp, spellSlots (JSON), cp/sp/ep/gp/pp, conditions (JSON array), deathSaveSuccesses, deathSaveFailures, hasInspiration, updatedAt -->
<!-- campaigns table (schema.ts lines 4-22): id, name, coverImagePath, createdAt, providerType, endpointUrl, modelName, referenceDocs, dmPersonality, strictness, fallbackEndpointUrl, fallbackModelName, rollingSummary -->
<!-- sessions table id is text PK; combatants/campaign_events reference sessions(id) nullable and campaigns(id) ON DELETE cascade -->

<!-- HIT_DIE_BY_CLASS (RESEARCH.md): barbarian:12 fighter:10 paladin:10 ranger:10 bard:8 cleric:8 druid:8 monk:8 rogue:8 warlock:8 sorcerer:6 wizard:6 -->
<!-- Spellcasting classes (classes.json spellcaster:true): bard, cleric, druid, paladin, ranger, sorcerer, warlock, wizard -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install rpg-dice-roller + dice.ts wrapper + dice test stub</name>
  <files>package.json, electron.vite.config.ts, src/renderer/src/lib/dice.ts, src/renderer/src/lib/dice.test.ts</files>
  <read_first>
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (section "src/renderer/src/lib/dice.ts" — Pattern 7 wrapper template)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Pattern 7 + Pitfall 6 ESM bundler note)
    - electron.vite.config.ts (current renderer config — no optimizeDeps yet)
    - package.json (current dependencies block)
  </read_first>
  <action>
    Run `npm install rpg-dice-roller@5` (per RESEARCH.md Standard Stack — version 5.0.0, slopcheck [OK], approved in Package Legitimacy Audit). Create `src/renderer/src/lib/dice.ts` wrapping `rpg-dice-roller`'s `DiceRoller` class. Export `interface RollResult { expression: string; result: number; breakdown: number[] }` and `function rollExpression(expression: string): RollResult` and `function isValidExpression(expression: string): boolean`. rollExpression constructs a `new DiceRoller()`, calls `roller.roll(expression)`, returns `{ expression, result: roll.total, breakdown: <flattened individual die values> }`. The breakdown extraction flattens `roll.rolls` per the PATTERNS.md template (each entry's `.rolls` array of `{value}` or the entry's `.value`). isValidExpression wraps roll in try/catch returning false on throw. If the ESM import fails at build (Pitfall 6: `Failed to resolve module specifier`), add `optimizeDeps: { include: ['rpg-dice-roller'] }` to the `renderer` block of electron.vite.config.ts. Create `src/renderer/src/lib/dice.test.ts` with Vitest assertions: `rollExpression('d20').result` is between 1 and 20; `rollExpression('2d6+3').result` is between 5 and 15 and breakdown.length is 2; `rollExpression('4d6kh3')` returns a result and breakdown; `isValidExpression('not-a-roll')` returns false; `isValidExpression('2d6+3')` returns true.
  </action>
  <verify>
    <automated>npm test -- src/renderer/src/lib/dice.test.ts</automated>
  </verify>
  <done>rpg-dice-roller@5 in package.json dependencies; dice.test.ts passes; rollExpression and isValidExpression exported from dice.ts</done>
  <acceptance_criteria>
    - package.json `dependencies` contains `"rpg-dice-roller"` at version `^5` or `5.x`
    - `src/renderer/src/lib/dice.ts` exports `rollExpression`, `isValidExpression`, and type `RollResult`
    - `npm test -- src/renderer/src/lib/dice.test.ts` exits 0
    - dice.test.ts asserts `rollExpression('2d6+3').breakdown.length === 2`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend schema.ts + author migration 0004 + update _journal.json</name>
  <files>src/main/db/schema.ts, resources/migrations/0004_phase5_rules_engine.sql, resources/migrations/meta/_journal.json</files>
  <read_first>
    - src/main/db/schema.ts (full file — combatants/character_spells must reference campaigns.id and sessions.id; characterResources is extended)
    - resources/migrations/0003_slippery_carnage.sql (the `--> statement-breakpoint` separator format the runner splits on)
    - resources/migrations/meta/_journal.json (entries array — append idx 4)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Pattern 4 migration SQL + Pitfall 4 ALTER TABLE NOT NULL rules)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (schema.ts modification section — exact Drizzle column definitions)
  </read_first>
  <action>
    In `src/main/db/schema.ts` add three new `sqliteTable` definitions and export their `$inferSelect`/`$inferInsert` types:
    (1) `combatants` table — columns: id (text PK), campaignId (text notNull references campaigns.id onDelete cascade), sessionId (text references sessions.id, nullable), name (text notNull), hpCurrent (integer notNull), hpMax (integer notNull), ac (integer notNull default 10), initiative (integer notNull default 0), initiativeOrder (integer notNull default 0), conditions (text notNull default '[]'), isPlayer (integer boolean mode notNull default false), isActive (integer boolean mode notNull default true). Export `Combatant` and `NewCombatant`.
    (2) `campaignEvents` table — columns: id (text PK), campaignId (text notNull references campaigns.id onDelete cascade), sessionId (text references sessions.id, nullable), eventType (text notNull), payload (text notNull default '{}'), createdAt (integer timestamp_ms notNull default `sql\`(unixepoch() * 1000)\``). Export `CampaignEvent` and `NewCampaignEvent`.
    (3) `characterSpells` table — columns: id (text PK), characterId (text notNull references characters.id onDelete cascade), spellName (text notNull), spellLevel (integer notNull default 0), isPrepared (integer boolean mode notNull default true). Export `CharacterSpell` and `NewCharacterSpell`.
    Extend the existing `characterResources` table with 4 columns: `concentratingOn: text('concentrating_on')` (nullable), `hitDiceCurrent: integer('hit_dice_current')` (nullable), `hitDiceTotal: integer('hit_dice_total')` (nullable), `pactSlots: text('pact_slots').notNull().default('{}')`.
    Extend the existing `campaigns` table with `permadeathMode: integer('permadeath_mode', { mode: 'boolean' }).notNull().default(false)` (PROG-04 per RESEARCH.md Open Question 2).
    Create `resources/migrations/0004_phase5_rules_engine.sql` matching the schema EXACTLY, using `--> statement-breakpoint` between each DDL statement (verbatim — runner splits on this exact string). Use the SQL from RESEARCH.md Pattern 4 for the 3 CREATE TABLE statements. ALTER statements: `ALTER TABLE \`character_resources\` ADD \`concentrating_on\` text;`, `ALTER TABLE \`character_resources\` ADD \`hit_dice_current\` integer;`, `ALTER TABLE \`character_resources\` ADD \`hit_dice_total\` integer;`, `ALTER TABLE \`character_resources\` ADD \`pact_slots\` text DEFAULT '{}' NOT NULL;`, `ALTER TABLE \`campaigns\` ADD \`permadeath_mode\` integer DEFAULT false NOT NULL;`. Per Pitfall 4, nullable columns omit NOT NULL; pact_slots and permadeath_mode carry DEFAULT so existing rows are satisfied.
    Append entry idx 4 to `resources/migrations/meta/_journal.json` entries array: `{ "idx": 4, "version": "6", "when": <current epoch ms>, "tag": "0004_phase5_rules_engine", "breakpoints": true }`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>schema.ts has combatants, campaignEvents, characterSpells tables + 4 characterResources columns + permadeath_mode on campaigns; migration 0004 SQL file matches; _journal.json has idx 4; typecheck passes</done>
  <acceptance_criteria>
    - `src/main/db/schema.ts` exports types `Combatant`, `CampaignEvent`, `CharacterSpell`
    - `src/main/db/schema.ts` characterResources contains `concentratingOn`, `hitDiceCurrent`, `hitDiceTotal`, `pactSlots`
    - `src/main/db/schema.ts` campaigns contains `permadeathMode`
    - `resources/migrations/0004_phase5_rules_engine.sql` contains `CREATE TABLE \`combatants\``, `CREATE TABLE \`campaign_events\``, `CREATE TABLE \`character_spells\``
    - migration SQL contains `pact_slots\` text DEFAULT '{}' NOT NULL` (NOT NULL only with DEFAULT)
    - `resources/migrations/meta/_journal.json` entries array has an object with `"tag": "0004_phase5_rules_engine"`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author resources/spells.json (SRD spell metadata)</name>
  <files>resources/spells.json</files>
  <read_first>
    - resources/classes.json (structure pattern for content JSON; spellcaster flag per class)
    - resources/spells-by-class.json (slot tables — confirms which classes need spells and at what levels)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Spell Data section — gap, recommended spells.json structure, A3 assumption)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (S5b — fields the SpellListSection consumes: castTime, range, duration, components, concentration flag, ritual flag, description)
  </read_first>
  <action>
    Create `resources/spells.json` as a JSON array of SRD spell objects. Each object has the shape: `{ "name": string, "level": number (0-9), "school": string, "castTime": string (e.g. "1 action", "1 bonus action", "1 reaction"), "range": string, "duration": string, "components": string (e.g. "V, S, M"), "concentration": boolean, "ritual": boolean, "classes": string[] (subset of bard/cleric/druid/paladin/ranger/sorcerer/warlock/wizard), "description": string }`. Author a usable starter set covering common SRD cantrips and 1st-3rd level spells per class so every spellcasting class has playable level-1 spells: include at minimum these SRD spells — Cantrips: Fire Bolt, Ray of Frost, Mage Hand, Prestidigitation, Light, Sacred Flame, Guidance, Vicious Mockery, Eldritch Blast, Produce Flame, Shillelagh, Thaumaturgy, Minor Illusion. 1st level: Magic Missile, Shield, Cure Wounds, Healing Word, Bless, Detect Magic, Burning Hands, Charm Person, Mage Armor, Sleep, Faerie Fire, Hunter's Mark, Hex, Hellish Rebuke, Thunderwave, Disguise Self. 2nd level: Misty Step, Hold Person, Scorching Ray, Invisibility, Spiritual Weapon, Lesser Restoration, Aid, Darkness, Spider Climb. 3rd level: Fireball, Counterspell, Fly, Lightning Bolt, Dispel Magic, Revivify, Haste, Hypnotic Pattern. Use accurate SRD 5.1 values for castTime/range/duration/components/concentration/ritual. The `classes` array lists which classes can learn each spell per SRD. Descriptions should be 1-3 sentence SRD-faithful summaries (full descriptions, not placeholders — NO "TODO" or "description here").
  </action>
  <verify>
    <automated>node -e "const s=require('./resources/spells.json'); if(!Array.isArray(s)||s.length<40) throw new Error('expected >=40 spells'); for(const sp of s){ if(typeof sp.name!=='string'||typeof sp.level!=='number'||typeof sp.concentration!=='boolean'||!Array.isArray(sp.classes)||typeof sp.description!=='string'||sp.description.length<10) throw new Error('bad spell: '+JSON.stringify(sp.name)); } console.log('spells.json OK', s.length)"</automated>
  </verify>
  <done>resources/spells.json is a valid JSON array of >=40 spells, each with all required fields and a real description; cantrips (level 0), 1st, 2nd, and 3rd level spells present</done>
  <acceptance_criteria>
    - `resources/spells.json` parses as a JSON array with >= 40 entries
    - every entry has `name` (string), `level` (number), `castTime`, `range`, `duration`, `components`, `concentration` (boolean), `ritual` (boolean), `classes` (string array), `description` (string length >= 10)
    - at least one entry has `level: 0` (cantrip) and at least one has `concentration: true`
    - node validation command above exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Build combatantsRepo, campaignEventsRepo, characterSpellsRepo + their test stubs</name>
  <files>src/main/db/combatantsRepo.ts, src/main/db/campaignEventsRepo.ts, src/main/db/characterSpellsRepo.ts, src/main/db/combatantsRepo.test.ts, src/main/db/campaignEventsRepo.test.ts, src/main/db/characterSpellsRepo.test.ts</files>
  <read_first>
    - src/main/db/sessionsRepo.ts (exact synchronous Drizzle pattern: getDb(), .run()/.get()/.all(), no await)
    - src/main/db/messagesRepo.ts (append-only insert pattern for campaignEventsRepo)
    - src/main/db/charactersRepo.ts (JSON column parse-on-read pattern for conditions)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (the three repo sections with exact method signatures)
    - existing *.test.ts file in src/main/db/ (e.g. sessionsRepo.test.ts) for in-memory test DB setup pattern
  </read_first>
  <action>
    Create three synchronous repos following sessionsRepo.ts exactly (import getDb from './index', use .run()/.get()/.all(), NEVER await):
    `combatantsRepo` with methods: `create(input: { campaignId, sessionId?, name, hpMax, ac, initiative, initiativeOrder, isPlayer }): Combatant` (sets hpCurrent=hpMax, conditions='[]', isActive=true, generates randomUUID id, returns the inserted row); `listActive(campaignId): Combatant[]` (where campaignId matches AND isActive=true, ordered by asc initiativeOrder); `updateHp(id, hpCurrent): void`; `updateConditions(id, conditionsJson: string): void`; `remove(id): void` (delete by id); `endCombat(campaignId): void` (bulk set isActive=false where campaignId matches AND isActive=true); `clearForCampaign(campaignId): void` (alias used when a new session starts).
    `campaignEventsRepo` with methods: `insert(input: { campaignId, sessionId?: string|null, eventType: string, payload: string }): void` (randomUUID id, .run() only, no retrieval, returns void); `listByCampaign(campaignId, limit = 50): CampaignEvent[]` (where campaignId matches, ordered by desc createdAt, limited).
    `characterSpellsRepo` with methods: `seed(characterId, spells: Array<{ spellName, spellLevel, isPrepared }>): void` (delete existing rows for characterId first, then insert each with randomUUID id); `listByCharacter(characterId): CharacterSpell[]` (ordered by asc spellLevel); `removeAll(characterId): void`.
    Create the three matching test files using the existing in-memory test DB setup pattern from the sibling repo test. Tests:
    combatantsRepo.test.ts (COMB-02): create returns a combatant with hpCurrent === hpMax and isActive true; listActive returns created combatants in initiativeOrder; updateHp changes hpCurrent; endCombat sets all to inactive so listActive returns empty.
    campaignEventsRepo.test.ts (STATE-05): insert then listByCampaign returns the event with matching eventType and payload; listByCampaign respects limit and desc createdAt ordering.
    characterSpellsRepo.test.ts (CHAR-08): seed inserts the given spells; seed called twice replaces (no duplicates); listByCharacter returns spells ordered by spellLevel.
  </action>
  <verify>
    <automated>npm test -- src/main/db/combatantsRepo.test.ts src/main/db/campaignEventsRepo.test.ts src/main/db/characterSpellsRepo.test.ts</automated>
  </verify>
  <done>Three repos export synchronous CRUD methods; three test files pass; no `await` on any Drizzle call</done>
  <acceptance_criteria>
    - `src/main/db/combatantsRepo.ts` exports `combatantsRepo` with methods create, listActive, updateHp, updateConditions, remove, endCombat
    - `src/main/db/campaignEventsRepo.ts` exports `campaignEventsRepo` with methods insert, listByCampaign
    - `src/main/db/characterSpellsRepo.ts` exports `characterSpellsRepo` with methods seed, listByCharacter
    - no occurrence of `await` before a `db.` Drizzle call in any of the three repos
    - `npm test -- src/main/db/combatantsRepo.test.ts src/main/db/campaignEventsRepo.test.ts src/main/db/characterSpellsRepo.test.ts` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Create RED test stubs for toolSchemas and mutationPipeline (Wave 0 scaffolds)</name>
  <files>src/main/ai/toolSchemas.test.ts, src/main/ai/mutationPipeline.test.ts</files>
  <read_first>
    - .planning/phases/05-rules-engine-dice-combat/05-VALIDATION.md (Wave 0 Requirements — exact test files and per-requirement coverage)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Code Examples — the 12 Zod schemas these tests assert against; mutationPipeline JSON-tail parser + long/short rest behavior)
    - existing src/main/trpc/ test file for Vitest import style in this project
  </read_first>
  <action>
    Create two test files that import from modules that DO NOT YET EXIST (`./toolSchemas` and `./mutationPipeline` — created in Wave 1 plan 05-02). These are RED scaffolds per VALIDATION.md Wave 0. Use Vitest `describe`/`it`/`expect`. Import the schemas and functions by name so the tests fail to compile/run until 05-02 implements them — this is the intended RED state.
    toolSchemas.test.ts assertions (COMB-01..STATE-05, T-5-01 bounds): `updateHpSchema.safeParse({ delta: -6, source: 'Goblin' }).success` is true; `updateHpSchema.safeParse({ delta: 'lots' }).success` is false; `addCombatantSchema.safeParse({ campaignId: 'c1', name: 'Goblin', hpMax: 14 }).success` is true with ac defaulting to 10; `addCombatantSchema.safeParse({ campaignId: 'c1', name: 'X', hpMax: -5 }).success` is false (hpMax min 1); `awardXpSchema.safeParse({ campaignId: 'c1', amount: -50 }).success` is false (min 0); `deductSpellSlotSchema` defaults slotType to 'normal' and count to 1; `showDiceRollSchema.safeParse({ label:'Atk', expression:'1d20+3', result:14, breakdown:[11,3] }).success` is true; `PHASE5_TOOLS` has exactly 12 keys including updateHp, applyCondition, removeCondition, deductSpellSlot, restoreSpellSlots, awardXp, updateCurrency, addCombatant, removeCombatant, endCombat, processRest, showDiceRoll.
    mutationPipeline.test.ts assertions (PROG-02, COMB-03, T-5-01): `stripAndParseJsonTail('hello world')` returns `{ cleanText: 'hello world', mutations: null }`; `stripAndParseJsonTail` on a string ending in a fenced ```json {"mutations":[{"toolName":"updateHp","args":{"delta":-3}}]}``` block returns cleanText without the block and a mutations array of length 1; malformed JSON tail returns mutations null and cleanText unchanged; a code block NOT at end-of-string is NOT parsed as a tail (Pitfall 3); add a placeholder `it.todo` or skipped test describing `applyMutationBatch` long-rest (HP to max, all slots restored) and short-rest (hitDiceCurrent decrements) — full behavior asserted in 05-02 with a test DB.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const a=fs.readFileSync('src/main/ai/toolSchemas.test.ts','utf8'); const b=fs.readFileSync('src/main/ai/mutationPipeline.test.ts','utf8'); if(!a.includes('PHASE5_TOOLS')||!a.includes('showDiceRollSchema')) throw new Error('toolSchemas.test.ts missing assertions'); if(!b.includes('stripAndParseJsonTail')) throw new Error('mutationPipeline.test.ts missing assertions'); console.log('Wave 0 AI test stubs present')"</automated>
  </verify>
  <done>Both test files exist with the listed RED assertions; they reference toolSchemas and mutationPipeline modules that Wave 1 implements</done>
  <acceptance_criteria>
    - `src/main/ai/toolSchemas.test.ts` references `PHASE5_TOOLS`, `updateHpSchema`, `addCombatantSchema`, `awardXpSchema`, `showDiceRollSchema`
    - `src/main/ai/toolSchemas.test.ts` asserts PHASE5_TOOLS has 12 keys
    - `src/main/ai/mutationPipeline.test.ts` references `stripAndParseJsonTail` and asserts the end-of-string anchor behavior
    - node validation command above exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → SQLite | Migration 0004 DDL runs at startup; malformed SQL or NOT NULL without DEFAULT crashes the app |
| spells.json → renderer | Static bundled content; no untrusted input, but malformed JSON breaks the spell list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-MIG | Denial of Service | migration 0004 ALTER TABLE | mitigate | pact_slots + permadeath_mode carry DEFAULT; nullable columns omit NOT NULL (Pitfall 4); typecheck + startup verified by 0003 runner pattern |
| T-05-01-SC | Tampering | npm install rpg-dice-roller | accept | rpg-dice-roller@5 rated slopcheck [OK] in RESEARCH.md Package Legitimacy Audit; no postinstall scripts; registry-verified, 7+ yrs old, GitHub source confirmed |
</threat_model>

<verification>
- `npm test -- src/renderer/src/lib/dice.test.ts src/main/db/combatantsRepo.test.ts src/main/db/campaignEventsRepo.test.ts src/main/db/characterSpellsRepo.test.ts` all green
- `npm run typecheck` green (schema changes compile)
- `resources/spells.json` parses and has >= 40 fully-authored spells
- toolSchemas.test.ts and mutationPipeline.test.ts exist as RED scaffolds (will fail until 05-02)
</verification>

<success_criteria>
- rpg-dice-roller@5 installed; dice.ts wrapper proven by passing test
- Migration 0004 + schema extension applied (3 tables, 4 characterResources columns, permadeath_mode)
- spells.json authored with SRD content
- Three repos built and tested (synchronous Drizzle)
- All 6 Wave 0 test files exist
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-01-SUMMARY.md` when done
</output>
