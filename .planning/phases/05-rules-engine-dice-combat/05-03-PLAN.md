---
phase: 05-rules-engine-dice-combat
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/renderer/src/stores/combatStore.ts
  - src/main/trpc/routers/combat.ts
  - src/main/trpc/router.ts
  - src/renderer/src/components/CombatTrackerTab.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [COMB-02, COMB-04]
user_setup: []

must_haves:
  truths:
    - "Clicking 'Start Combat' in the campaign header sets combat active and auto-switches the right panel to the Combat tab (D-09, D-17)"
    - "The Combat tab shows an empty state when no combat is active and a sorted initiative list when combat is active (S2a/S2b)"
    - "Each combatant row shows initiative order, name, HP bar (color by percent), HP fraction, and condition badges (COMB-02, D-12)"
    - "Expanding a combatant row reveals an HP stepper and a condition picker that mutate via tRPC (COMB-04, D-15)"
    - "Player can add a combatant via the Add Combatant form; the row appears sorted by initiative order (D-10)"
    - "Clicking 'End Combat' marks all combatants inactive and returns the tab to the empty state (D-14)"
  artifacts:
    - path: "src/renderer/src/stores/combatStore.ts"
      provides: "Zustand combat state + activeTab controlled-tab state"
      exports: ["useCombatStore"]
    - path: "src/main/trpc/routers/combat.ts"
      provides: "combat tRPC router (listActive, startCombat, endCombat, updateHp, updateConditions, addCombatant)"
      exports: ["combatRouter"]
    - path: "src/renderer/src/components/CombatTrackerTab.tsx"
      provides: "Combat Tracker tab UI (initiative list, expandable rows, add form)"
      min_lines: 120
    - path: "src/renderer/src/screens/CampaignViewScreen.tsx"
      provides: "Controlled Tabs + Start/End Combat header buttons"
      contains: "value={activeTab}"
  key_links:
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "src/renderer/src/stores/combatStore.ts"
      via: "handleStartCombat sets isCombatActive + activeTab='combat-tracker'"
      pattern: "setActiveTab\\('combat-tracker'\\)|activeTab"
    - from: "src/renderer/src/components/CombatTrackerTab.tsx"
      to: "src/main/trpc/routers/combat.ts"
      via: "trpc.combat.listActive query + updateHp/updateConditions/addCombatant mutations"
      pattern: "trpc\\.combat\\."
    - from: "src/main/trpc/routers/combat.ts"
      to: "src/main/db/combatantsRepo.ts"
      via: "router procedures call combatantsRepo methods"
      pattern: "combatantsRepo\\."
---

<objective>
Wave 2 vertical slice: a player can start combat from the header, watch the right panel auto-switch to the Combat tab, see a live initiative list, expand any combatant to adjust HP and toggle conditions, manually add a combatant, and end combat. This delivers COMB-02 (tracker display) and the manual half of COMB-04 (player-toggled conditions). Enemy combatants populate automatically once the AI calls `addCombatant` (the tool + mutation pipeline already shipped in 05-02), so this plan reads `combatantsRepo.listActive` and renders whatever the AI or the player has added.

Purpose: Combat is the centerpiece of Phase 5. This is the first user-visible surface; everything after (dice chips, spells, rest) reads against the same combatants + activeTab plumbing introduced here.
Output: combatStore.ts, combat tRPC router (registered), CombatTrackerTab.tsx (replaces placeholder), CampaignViewScreen controlled Tabs + Start/End Combat buttons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md

<interfaces>
<!-- combatantsRepo (05-01) — synchronous Drizzle, exported methods:
       create(input: { campaignId, sessionId?, name, hpMax, ac, initiative, initiativeOrder, isPlayer }): Combatant   (sets hpCurrent=hpMax, conditions='[]', isActive=true)
       listActive(campaignId): Combatant[]   (isActive=true, asc initiativeOrder)
       updateHp(id, hpCurrent): void
       updateConditions(id, conditionsJson: string): void
       remove(id): void
       endCombat(campaignId): void   (bulk isActive=false)
   Combatant shape (schema.ts): { id, campaignId, sessionId, name, hpCurrent, hpMax, ac, initiative, initiativeOrder, conditions(JSON string), isPlayer(bool), isActive(bool) } -->
<!-- campaignEventsRepo (05-01): insert({ campaignId, sessionId?, eventType, payload }): void -->
<!-- sessions.ts router pattern (analog): import { t } from '../_base'; import { campaignIdSchema } from '../schemas'; procedures use .input(z.object({...})).query/.mutation; log.debug('[combat] ...') / log.error -->
<!-- Stepper: src/renderer/src/components/sheet/Stepper.tsx — props { value, min, max, size, label, onChange:(delta)=>void } -->
<!-- ConditionBadge: src/renderer/src/components/sheet/ConditionBadge.tsx — props { condition, active, onToggle } -->
<!-- ALL_CONDITIONS + type ConditionName exported from src/renderer/src/components/sheet/sheetHelpers.ts -->
<!-- Optimistic mutation triple (onMutate/onError/onSettled) pattern: ResourcesSection.tsx lines 22-48 -->
<!-- CampaignViewScreen currently uses <Tabs defaultValue="character-sheet"> at line ~354 (uncontrolled). Header buttons live in the action bar div ~line 223-294 (back / AI Settings / Start Session / Delete). Combat tracker placeholder is the TabsContent value="combat-tracker" at ~line 404-411. -->
<!-- shadcn components available: progress (added in 05 via npx shadcn add), badge (added), collapsible, scroll-area, button, input, label, tooltip. If progress/badge not yet installed, run: npx shadcn@latest add progress badge -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: combatStore + combat tRPC router + router registration</name>
  <files>src/renderer/src/stores/combatStore.ts, src/main/trpc/routers/combat.ts, src/main/trpc/router.ts</files>
  <read_first>
    - src/renderer/src/stores/sessionStore.ts (exact Zustand 5.x create<State>()(...) pattern to mirror)
    - src/main/trpc/routers/sessions.ts (tRPC router pattern: _base import, campaignIdSchema, .input().query/.mutation, log prefix)
    - src/main/trpc/router.ts (registration array — add combat alphabetically after campaigns/characters)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (combatStore.ts section + combat.ts router section)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Pattern 5 combatStore, Pattern 6 router registration)
  </read_first>
  <action>
    Create `src/renderer/src/stores/combatStore.ts` following sessionStore.ts exactly: `create<CombatState>()((set) => ({...}))`. State + actions: `isCombatActive: boolean` (default false), `activeCampaignId: string | null` (default null), `currentTurnOrder: number` (default 0), `activeTab: string` (default 'character-sheet' — moved here from CampaignViewScreen local state to drive D-17 auto-switch), `startCombat(campaignId)` → set isCombatActive true + activeCampaignId + activeTab='combat-tracker', `endCombat()` → set isCombatActive false + activeCampaignId null + currentTurnOrder 0 (do NOT reset activeTab — leave the player on the Combat tab per UI-SPEC S1 "tab stays on Combat"), `setCurrentTurn(order)` → set currentTurnOrder, `setActiveTab(tab: string)` → set activeTab. Export `useCombatStore`.
    Create `src/main/trpc/routers/combat.ts` following sessions.ts: import `{ t }` from '../_base', `{ campaignIdSchema }` from '../schemas', `{ combatantsRepo }` from '../../db/combatantsRepo', `{ campaignEventsRepo }` from '../../db/campaignEventsRepo', `log` from 'electron-log', `{ z }` from 'zod'. Export `combatRouter = t.router({ ... })` with procedures:
    - `listActive`: `.input(z.object({ campaignId: campaignIdSchema })).query(({input}) => combatantsRepo.listActive(input.campaignId))`
    - `startCombat`: `.input(z.object({ campaignId: campaignIdSchema })).mutation(({input}) => { campaignEventsRepo.insert({ campaignId: input.campaignId, eventType: 'combat_started', payload: '{}' }); log.debug('[combat] startCombat', {...}); return { started: true } })`
    - `endCombat`: `.input(z.object({ campaignId: campaignIdSchema })).mutation(({input}) => { combatantsRepo.endCombat(input.campaignId); campaignEventsRepo.insert({ campaignId: input.campaignId, eventType: 'combat_ended', payload: '{}' }); return { ended: true } })`
    - `updateHp`: `.input(z.object({ combatantId: z.string(), hpCurrent: z.number().int().min(0).max(9999) })).mutation(({input}) => { combatantsRepo.updateHp(input.combatantId, input.hpCurrent); return { updated: true } })`
    - `updateConditions`: `.input(z.object({ combatantId: z.string(), conditions: z.array(z.string().max(50)).max(20) })).mutation(({input}) => { combatantsRepo.updateConditions(input.combatantId, JSON.stringify(input.conditions)); return { updated: true } })`
    - `addCombatant`: `.input(z.object({ campaignId: campaignIdSchema, sessionId: z.string().optional(), name: z.string().min(1).max(100), hpMax: z.number().int().min(1).max(9999), ac: z.number().int().min(1).max(99).default(10), initiative: z.number().int().default(0), initiativeOrder: z.number().int().default(0), isPlayer: z.boolean().default(false) })).mutation(({input}) => { const c = combatantsRepo.create(input); campaignEventsRepo.insert({ campaignId: input.campaignId, sessionId: input.sessionId, eventType: 'combatant_added', payload: JSON.stringify({ name: input.name }) }); return c })`
    Register in `src/main/trpc/router.ts`: add `import { combatRouter } from './routers/combat'` and the line `combat: combatRouter,` to the `t.router({...})` object (keep alphabetical: after `characters:`).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>combatStore exports useCombatStore with isCombatActive/activeTab/startCombat/setActiveTab; combat.ts exports combatRouter with the 6 procedures; router.ts registers combat; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/stores/combatStore.ts` exports `useCombatStore` and the store has `activeTab`, `setActiveTab`, `isCombatActive`, `startCombat`, `endCombat`
    - `startCombat` sets both `isCombatActive: true` and `activeTab: 'combat-tracker'`
    - `src/main/trpc/routers/combat.ts` exports `combatRouter` with procedures listActive, startCombat, endCombat, updateHp, updateConditions, addCombatant
    - every mutation that changes combat state calls `campaignEventsRepo.insert(` (combat_started, combat_ended, combatant_added)
    - `src/main/trpc/router.ts` contains `combat: combatRouter`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: CombatTrackerTab component (empty state + initiative list + expandable rows + add form)</name>
  <files>src/renderer/src/components/CombatTrackerTab.tsx</files>
  <read_first>
    - src/renderer/src/components/CharacterSheetTab.tsx (TanStack Query + trpc import + props pattern; campaignId prop shape)
    - src/renderer/src/components/sheet/ResourcesSection.tsx (lines 17-74 — optimistic onMutate/onError/onSettled mutation triple to copy for HP + conditions)
    - src/renderer/src/components/sheet/Stepper.tsx (Stepper props: value/min/max/size/label/onChange)
    - src/renderer/src/components/sheet/ConditionBadge.tsx (ConditionBadge props: condition/active/onToggle)
    - src/renderer/src/components/sheet/sheetHelpers.ts (ALL_CONDITIONS, ConditionName, ordinal exports)
    - src/renderer/src/components/ui/collapsible.tsx (Collapsible/CollapsibleTrigger/CollapsibleContent)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S2 Combat Tracker Tab — exact className strings for empty state, collapsed/expanded/active/player rows, HP bar colors, Add Combatant form, copywriting contract)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (CombatTrackerTab.tsx section — query/optimistic mutation/Stepper/ConditionBadge reuse)
  </read_first>
  <action>
    Create `src/renderer/src/components/CombatTrackerTab.tsx` exporting `function CombatTrackerTab({ campaignId }: { campaignId: string })`. Per UI-SPEC §S2.
    Read `useCombatStore` for `isCombatActive`. Query `combatantsQuery = useQuery({ queryKey: ['combat','listActive',campaignId], queryFn: () => trpc.combat.listActive.query({ campaignId }), enabled: !!campaignId, refetchInterval: isCombatActive ? 2000 : false })` (poll during combat per PATTERNS.md). QUERY_KEY constant for invalidation.
    Two states:
    - **Empty state (S2a)** — render when `!isCombatActive` OR `combatants.length === 0`: container `<div className="flex flex-col items-center justify-center h-full gap-2 text-center px-6">`, `<Swords className="h-8 w-8 text-muted-foreground/40 mb-2" />`, heading `<p className="text-sm font-semibold text-muted-foreground">No active combat</p>`, body `<p className="text-sm text-muted-foreground/70">Click "Start Combat" in the header to begin tracking initiative and HP.</p>` (exact copy from UI-SPEC copywriting contract).
    - **Combat active list (S2b)** — `<div className="h-full flex flex-col overflow-hidden">` → `<ScrollArea className="flex-1 p-3">` → `<div className="flex flex-col gap-2">`. For each combatant (already asc initiativeOrder from repo) render a `Collapsible` row. Row className per UI-SPEC: player → `bg-card border border-border border-l-2 border-l-primary/60 rounded-lg cursor-pointer hover:bg-secondary transition-colors`; enemy → `bg-card border border-border rounded-lg cursor-pointer hover:bg-secondary transition-colors`; active-turn (combatant.initiativeOrder === currentTurnOrder) → `bg-primary/10 border border-primary/40 rounded-lg cursor-pointer transition-colors`. Collapsed inner layout `px-3 py-2 flex items-center gap-3`: initiative order span (`text-sm font-semibold text-muted-foreground w-5 text-right shrink-0`), active pulse dot (`w-2 h-2 rounded-full bg-primary animate-pulse shrink-0` when active else invisible `w-2 h-2 shrink-0` spacer), name span (`text-sm text-foreground flex-1 truncate` + `font-semibold` for player / `font-normal` enemy), condition badges (parse `JSON.parse(combatant.conditions)` → first ≤2 → `<Badge variant="outline" className="text-xs px-1 py-0 h-5">`), HP bar `<div className="w-16 shrink-0"><Progress value={hpPercent} className="h-2" /></div>` where hpPercent = round(hpCurrent/hpMax*100); apply HP-bar fill color by percent per UI-SPEC (green-700 >50, amber-500 25-50, red-600 <25) via the Progress indicator className or a wrapper; HP text `{hpCurrent} <span className="text-muted-foreground font-normal">/ {hpMax}</span>` (`text-sm font-semibold w-16 text-right shrink-0`), chevron `<ChevronDown className="h-3.5 w-3.5 ... data-[state=open]:rotate-180 shrink-0" />`.
    Expanded content (`CollapsibleContent` → `border-t border-border px-4 py-3 bg-secondary flex flex-col gap-3 rounded-b-lg`): HP stepper row using `<Stepper value={hpCurrent} min={0} max={hpMax} size="sm" label={`Adjust HP for ${name}`} onChange={(delta) => updateHpMutation.mutate({ combatantId, hpCurrent: clamp(hpCurrent+delta,0,hpMax) })} />` and `/ {hpMax}` text; AC display `AC {ac}`; condition picker `<div role="group" aria-label={`Conditions for ${name}`}>` mapping `ALL_CONDITIONS` to `<ConditionBadge condition={c} active={parsedConditions.includes(c)} onToggle={() => updateConditionsMutation.mutate({ combatantId, conditions: toggle(parsedConditions, c) })} />`.
    Add Combatant form (S2b bottom, always visible during combat): `<div className="bg-secondary border border-border rounded-lg p-3 mt-2">` with label "Add Combatant", four Inputs (Name placeholder "e.g. Goblin", Max HP type number min 1, AC type number min 1, Initiative type number) and an `<Button size="sm" variant="outline" className="h-8" onClick={handleAdd}>Add</Button>`. handleAdd validates name non-empty and hpMax≥1, then `addCombatantMutation.mutate({ campaignId, name, hpMax, ac: ac||10, initiative, initiativeOrder: initiative })` and clears the form (use initiative as initiativeOrder so manual rows sort by their roll).
    Implement `updateHpMutation`, `updateConditionsMutation`, `addCombatantMutation` as `useMutation` with the optimistic onMutate/onError/onSettled triple from ResourcesSection.tsx (cancelQueries → snapshot prev → optimistic setQueryData on the combatants array → return {prev}; onError restore; onSettled invalidate QUERY_KEY). For addCombatant, optimistic update can be skipped (just onSettled invalidate) since the new id comes from the server.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>CombatTrackerTab renders empty state when no combat; renders one row per listActive combatant with HP bar + conditions when active; expanded row has Stepper + ConditionBadge picker wired to mutations; Add Combatant form works; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/CombatTrackerTab.tsx` exports `CombatTrackerTab` and queries `trpc.combat.listActive`
    - empty-state body text is exactly `Click "Start Combat" in the header to begin tracking initiative and HP.`
    - the component maps over the combatants array and renders a Collapsible row per combatant with a `Progress` HP bar and condition `Badge` chips
    - expanded row uses `Stepper` for HP (onChange → trpc.combat.updateHp) and `ConditionBadge` for conditions (onToggle → trpc.combat.updateConditions)
    - Add Combatant form calls `trpc.combat.addCombatant` and clears after success
    - HP-bar fill uses green/amber/red by percent thresholds (>50 / 25-50 / <25)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CampaignViewScreen — controlled Tabs + Start/End Combat buttons + mount CombatTrackerTab</name>
  <files>src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (FULL — action bar header ~223-294, Tabs ~354, combat-tracker TabsContent ~404-411, existing useSessionStore + Tooltip patterns)
    - src/renderer/src/stores/combatStore.ts (the store created in Task 1 — useCombatStore activeTab/setActiveTab/isCombatActive/startCombat/endCombat)
    - src/renderer/src/components/CombatTrackerTab.tsx (the component from Task 2 — props { campaignId })
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S1 Campaign Header buttons — Start Combat / End Combat / Rest exact specs, icons, colors, tooltips; copywriting contract)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Pitfall 9 controlled Tabs)
  </read_first>
  <action>
    Per UI-SPEC §S1. Convert the right-panel Tabs from uncontrolled to controlled: read `activeTab` and `setActiveTab` from `useCombatStore` (NOT local useState — D-17 needs the store so handleStartCombat can switch it). Change `<Tabs defaultValue="character-sheet" ...>` to `<Tabs value={activeTab} onValueChange={setActiveTab} ...>`.
    Read `isCombatActive` from `useCombatStore`. Add a `combat.startCombat` / `combat.endCombat` mutation pair (useMutation calling `trpc.combat.startCombat.mutate({campaignId:id})` / `trpc.combat.endCombat.mutate({campaignId:id})`); on endCombat success, invalidate `['combat','listActive',id]`.
    `handleStartCombat = useCallback(() => { useCombatStore.getState().startCombat(id!); startCombatMutation.mutate() }, [id])` — startCombat action already sets activeTab='combat-tracker'.
    `handleEndCombat = useCallback(() => { useCombatStore.getState().endCombat(); endCombatMutation.mutate() }, [id])`.
    In the action bar (the `border-b border-border bg-card` strip), insert a Combat button BEFORE the Start/End Session button, wrapped in `TooltipProvider`/`Tooltip` (delayDuration 600) like the existing AI Settings button. When `!isCombatActive`: `<Button variant="outline" size="sm" className="gap-2" onClick={handleStartCombat}>` with `<Swords className="h-4 w-4" />` and label "Start Combat", tooltip "Begin a combat encounter". When `isCombatActive`: `<Button variant="outline" size="sm" className="gap-2 border-destructive/60 text-destructive hover:bg-destructive/10 hover:border-destructive" onClick={handleEndCombat}>` with `<Swords className="h-4 w-4" />` label "End Combat", tooltip "End the current encounter". (The Rest button is added in 05-06 — do NOT add it here; leave room in the same bar.)
    Replace the combat-tracker `TabsContent` placeholder body (lines ~404-411) with `<TabsContent value="combat-tracker" className="flex-1 overflow-hidden p-0"><CombatTrackerTab campaignId={id} /></TabsContent>`. Import `CombatTrackerTab` and `Swords` (lucide-react).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>CampaignViewScreen Tabs are controlled via combatStore activeTab; Start Combat button switches to Combat tab and starts combat; End Combat button shows when active and clears combat; CombatTrackerTab is mounted; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/screens/CampaignViewScreen.tsx` contains `value={activeTab}` and `onValueChange={setActiveTab}` on the right-panel `<Tabs>` and does NOT contain `<Tabs defaultValue="character-sheet"`
    - `activeTab`/`setActiveTab`/`isCombatActive` are read from `useCombatStore`
    - the action bar renders a "Start Combat" button (Swords icon) when not in combat and an "End Combat" button (destructive style) when in combat
    - `handleStartCombat` calls `useCombatStore.getState().startCombat(id)` (which sets activeTab='combat-tracker') and `trpc.combat.startCombat`
    - the combat-tracker `TabsContent` renders `<CombatTrackerTab campaignId={id} />` (placeholder paragraph removed)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (tRPC combat router) | Combat HP/condition/add inputs cross from the untrusted renderer into SQLite writes |
| AI tool pipeline → combatants table | addCombatant/updateHp from the AI (05-02) write the same rows this UI reads — shared mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | combat.ts tRPC inputs | mitigate | Every procedure validates input with Zod: campaignIdSchema (uuid) for campaignId; hpCurrent `.int().min(0).max(9999)`; conditions `array(string.max(50)).max(20)`; addCombatant bounds hpMax/ac/initiative as in 05-02 schemas |
| T-05-03-02 | Tampering | conditions JSON column | mitigate | Conditions stored as JSON.stringify of a validated string array; parsed on read with JSON.parse guarded (default to [] on parse failure) so malformed rows do not crash the tracker |
| T-05-03-SC | Tampering | npm/shadcn installs | mitigate | Only shadcn official `progress`/`badge` blocks if not already present (RESEARCH.md Registry Safety — first-party, no third-party registry); no new npm package installs in this plan |
</threat_model>

<verification>
- `npm run typecheck` green
- Manual (per VALIDATION.md): Start Combat switches tab + shows list; add a combatant manually → row appears sorted; expand row → adjust HP via stepper, toggle a condition; End Combat → empty state returns. AI-driven addCombatant/updateHp reliability is a phase-exit UAT item (live LLM).
</verification>

<success_criteria>
- combatStore drives both combat state and the controlled activeTab (D-17)
- combat tRPC router registered with 6 Zod-validated procedures, every state change logged to campaign_events
- CombatTrackerTab renders empty state + initiative list with HP bars, condition badges, expandable HP stepper + condition picker (COMB-02, COMB-04 manual half)
- Start/End Combat header buttons wired; combat-tracker placeholder replaced
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-03-SUMMARY.md` when done
</output>
