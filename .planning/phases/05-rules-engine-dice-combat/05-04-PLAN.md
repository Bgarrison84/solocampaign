---
phase: 05-rules-engine-dice-combat
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/renderer/src/components/DiceRollerPopover.tsx
  - src/renderer/src/components/ChatInputArea.tsx
  - src/renderer/src/components/StoryScrollPanel.tsx
autonomous: true
requirements: [COMB-01, COMB-03]

must_haves:
  truths:
    - "A dice button sits between the chat textarea and the Send button; clicking it opens a popover with 7 die buttons + an expression input (D-18)"
    - "Clicking a die button or submitting an expression prepends a roll prefix like '[d20: 14] ' to the chat textarea and closes the popover (D-19)"
    - "Invalid expressions disable the Roll button and show an inline error (UI-SPEC S3)"
    - "AI showDiceRoll calls (persisted as role 'dice_roll' messages in 05-02) render as styled amber chips inline in the story flow (COMB-03, D-22, S4)"
    - "Level-up system messages (role 'system', persisted in 05-06) render as italic amber system-event lines in the story flow (D-32, S7b)"
  artifacts:
    - path: "src/renderer/src/components/DiceRollerPopover.tsx"
      provides: "Dice roller popover (die buttons + expression input)"
      min_lines: 60
    - path: "src/renderer/src/components/ChatInputArea.tsx"
      provides: "Dice button + popover integration + prefix prepend"
      contains: "DiceRollerPopover"
    - path: "src/renderer/src/components/StoryScrollPanel.tsx"
      provides: "dice_roll chip + system event message rendering"
      contains: "dice_roll"
  key_links:
    - from: "src/renderer/src/components/DiceRollerPopover.tsx"
      to: "src/renderer/src/lib/dice.ts"
      via: "rollExpression + isValidExpression from the 05-01 wrapper"
      pattern: "rollExpression|isValidExpression"
    - from: "src/renderer/src/components/ChatInputArea.tsx"
      to: "src/renderer/src/components/DiceRollerPopover.tsx"
      via: "onRoll callback prepends prefix to textarea value"
      pattern: "onRoll"
    - from: "src/renderer/src/components/StoryScrollPanel.tsx"
      to: "messages list role field"
      via: "switch on msg.role for 'dice_roll' and 'system' branches"
      pattern: "role === 'dice_roll'|role === 'system'"
---

<objective>
Wave 2 dice slice (parallel with 05-03 — no shared files): the player gets an in-app dice roller attached to the chat input (COMB-01), and the AI's visible rolls render as chips in the story (COMB-03). This plan also adds the StoryScrollPanel rendering branches for the two new message roles introduced by the AI pipeline (`dice_roll`, persisted in 05-02) and level-up system events (`system`, persisted in 05-06) so those surfaces have somewhere to render. The dice wrapper (`src/renderer/src/lib/dice.ts`) already exists from 05-01 — this plan consumes it, it does not recreate it.

Purpose: COMB-01 (player dice) and COMB-03 (AI visible dice) are two of the four combat success criteria. Adding the message-role rendering branches now unblocks 05-06's level-up system message without a later edit to StoryScrollPanel.
Output: DiceRollerPopover.tsx, ChatInputArea dice button + prefix logic, StoryScrollPanel dice_roll + system message branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md

<interfaces>
<!-- src/renderer/src/lib/dice.ts (05-01) exports:
       interface RollResult { expression: string; result: number; breakdown: number[] }
       function rollExpression(expression: string): RollResult
       function isValidExpression(expression: string): boolean -->
<!-- ChatInputArea.tsx (current): textareaRef (HTMLTextAreaElement), handleSend reads textareaRef.current.value, handleInput auto-grows + setIsEmpty. Input row is the `flex flex-row items-end gap-2` div containing the Textarea + Send Button (~lines 118-150). The dice button goes between Textarea and Send Button. There is NO controlled value state — the textarea is uncontrolled (read via ref). To prepend a prefix, set textareaRef.current.value and call handleInput() to refresh height + isEmpty. -->
<!-- StoryScrollPanel.tsx (current): messages from useQuery(['ai','getMessages',campaignId]); the render loop is `messages.map((msg) => { if (msg.role === 'assistant') {...} return <player block> })` (~lines 166-221). Message shape: { id, role, content, createdAt }. role is a free text column ('user' | 'assistant' today; 05-02 adds 'dice_roll'; 05-06 adds 'system'). -->
<!-- shadcn Popover: src/renderer/src/components/ui/popover.tsx — Popover/PopoverTrigger/PopoverContent. Install if missing: npx shadcn@latest add popover -->
<!-- lucide-react icons: Dice6 (dice button), available in the installed set -->
<!-- 05-02 persists dice_roll messages as content = JSON.stringify({ label, expression, result, breakdown }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DiceRollerPopover component</name>
  <files>src/renderer/src/components/DiceRollerPopover.tsx</files>
  <read_first>
    - src/renderer/src/lib/dice.ts (rollExpression, isValidExpression, RollResult — the API this consumes)
    - src/renderer/src/components/ui/popover.tsx (Popover/PopoverTrigger/PopoverContent shadcn shell)
    - src/renderer/src/components/ui/input.tsx and ui/button.tsx (Input + Button primitives)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S3 Dice Roller — popover layout, 7 die buttons, expression input, roll result format, invalid-expression error, accessibility, copywriting contract)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (DiceRollerPopover.tsx section — popover shell + onRoll callback shape)
  </read_first>
  <action>
    Create `src/renderer/src/components/DiceRollerPopover.tsx` per UI-SPEC §S3. Export `function DiceRollerPopover({ open, onOpenChange, onRoll }: { open: boolean; onOpenChange: (o: boolean) => void; onRoll: (prefix: string) => void })`. The trigger Button is rendered by the CALLER (ChatInputArea) wrapped in PopoverTrigger — so this component renders `<Popover open={open} onOpenChange={onOpenChange}>` with the trigger passed via children OR (simpler) expose a `triggerProps`/render the trigger inside. Choose the PATTERNS.md shape: render `<Popover open={open} onOpenChange={onOpenChange}><PopoverTrigger asChild>{children}</PopoverTrigger><PopoverContent .../></Popover>` and accept `children` (the dice button) as a prop. Use `side="top" align="end"` and `className="w-[280px] p-3"` with `role="dialog" aria-label="Dice roller"`.
    Body:
    - Die buttons: map `['d4','d6','d8','d10','d12','d20','d100']` in a `<div className="flex flex-wrap gap-2 justify-center">`; each `<Button variant="outline" size="sm" className="h-9 w-9 p-0 text-xs font-semibold" onClick={() => handleDie(die)} aria-label={`Roll ${die}`}>{die}</Button>`.
    - Divider text "or enter expression".
    - Expression input row: `<Input>` (controlled local `expression` state) `className="flex-1 h-8 text-sm font-mono bg-secondary border-border"` placeholder "2d6+3" aria-label "Dice expression", onKeyDown Enter → handleExpression; plus `<Button size="sm" className="h-8 shrink-0" onClick={handleExpression} disabled={!isValidExpression(expression) || !expression}>Roll</Button>`.
    - Helper text `<p className="text-xs text-muted-foreground mt-1">e.g. 4d6kh3, d20+5</p>`.
    - When `expression` is non-empty AND `!isValidExpression(expression)`, show `<span className="text-xs text-destructive">Invalid expression</span>`.
    `handleDie(die)`: `const r = rollExpression(die); onRoll(`[${die}: ${r.result}] `); onOpenChange(false)`.
    `handleExpression()`: if `!isValidExpression(expression)` return; `const r = rollExpression(expression); onRoll(`[${expression}: ${r.result}] `); setExpression(''); onOpenChange(false)`.
    Do NOT log the roll here — logging happens on message send (D-21, handled by the existing ai:send-message → 05-02 dice_roll persistence path is for AI rolls; player roll logging is a phase-exit concern and the prefix carries the result into the message text).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>DiceRollerPopover renders 7 die buttons + expression input + Roll button using dice.ts; die click and valid expression call onRoll with a formatted prefix and close; invalid expression disables Roll and shows error; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/DiceRollerPopover.tsx` exports `DiceRollerPopover` and imports `rollExpression` + `isValidExpression` from `../lib/dice`
    - renders exactly the 7 die buttons d4/d6/d8/d10/d12/d20/d100
    - `onRoll` is called with a string matching the `[<die-or-expression>: <number>] ` format and the popover closes (`onOpenChange(false)`)
    - the Roll button is disabled when the expression is non-empty and `isValidExpression` returns false
    - helper text is exactly `e.g. 4d6kh3, d20+5`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ChatInputArea — dice button + popover + prefix prepend</name>
  <files>src/renderer/src/components/ChatInputArea.tsx</files>
  <read_first>
    - src/renderer/src/components/ChatInputArea.tsx (FULL — textareaRef, handleSend, handleInput, the input-row div ~118-150; uncontrolled textarea read via ref)
    - src/renderer/src/components/DiceRollerPopover.tsx (the component from Task 1 — props open/onOpenChange/onRoll/children)
    - src/renderer/src/components/ui/tooltip.tsx (Tooltip wrapper used elsewhere for the dice button tooltip)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S3 dice trigger button spec — h-9 w-9 outline between Textarea and Send; tooltip "Roll dice"; prefix replace regex; copywriting contract)
  </read_first>
  <action>
    Per UI-SPEC §S3. Add `const [dicePopoverOpen, setDicePopoverOpen] = useState(false)` to ChatInputArea. Import `DiceRollerPopover` and `Dice6` (lucide-react).
    In the input-row div (`flex flex-row items-end gap-2`), insert the dice control BETWEEN the Textarea and the Send Button. Render `<DiceRollerPopover open={dicePopoverOpen} onOpenChange={setDicePopoverOpen} onRoll={handleRoll}>` wrapping a trigger `<Button variant="outline" size="sm" className="h-9 w-9 shrink-0 p-0" aria-label="Open dice roller"><Dice6 className="h-4 w-4" /></Button>`. (Wrap the button in Tooltip "Roll dice" delayDuration 600 if the surrounding TooltipProvider is available; if not, the aria-label suffices.)
    Implement `handleRoll = useCallback((prefix: string) => { const ta = textareaRef.current; if (!ta) return; const existing = ta.value; const stripped = existing.replace(/^\[[\w\d+\-*\/]+: \d+\] /, ''); ta.value = prefix + stripped; handleInput(); ta.focus() }, [handleInput])` — prepends the new prefix, first stripping any prior roll prefix (UI-SPEC: replace old prefix before adding new), then refreshes height + isEmpty via handleInput and focuses the textarea.
    Disable the dice button when `disabled || isStreaming` (same guard as Send) so rolls aren't injected while no provider is configured or a stream is active. Do NOT change the existing handleSend/Ctrl+Enter/empty-state behavior.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>ChatInputArea renders a dice button between textarea and Send that opens DiceRollerPopover; rolling prepends/replaces the prefix in the textarea and re-focuses; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/ChatInputArea.tsx` imports and renders `DiceRollerPopover` with a `Dice6` trigger button positioned before the Send button
    - `handleRoll` sets `textareaRef.current.value` to the new prefix + previous content (stripping any existing `[...: N] ` prefix) and calls `handleInput()`
    - the dice button is disabled when `disabled || isStreaming`
    - existing Send/Ctrl+Enter logic is unchanged (handleSend still trims and clears)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: StoryScrollPanel — dice_roll chip + system event rendering</name>
  <files>src/renderer/src/components/StoryScrollPanel.tsx</files>
  <read_first>
    - src/renderer/src/components/StoryScrollPanel.tsx (FULL — the messages.map render loop ~166-221; assistant branch, player branch)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S4 showDiceRoll chip — exact className string, formatBreakdown logic; §S7b system event message className)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (StoryScrollPanel.tsx modification section — dice_roll + system branches)
  </read_first>
  <action>
    Per UI-SPEC §S4 + §S7b. In the `messages.map((msg) => {...})` loop, BEFORE the `if (msg.role === 'assistant')` branch, add two new branches:
    (1) `if (msg.role === 'dice_roll')` — parse `const data = JSON.parse(msg.content)` inside a try/catch (on parse failure return null so a malformed row does not crash the panel); render the chip per UI-SPEC §S4 exact className: outer `<div className="bg-amber-950/40 border border-amber-800/50 rounded-md px-3 py-2 mb-4 flex items-start gap-2 text-sm" role="note" aria-label={`Dice roll: ${data.label}`}>`, `<span aria-hidden="true" className="text-amber-400 shrink-0">🎲</span>`, then a flex-col with the label (`font-semibold`) + ` — ` + expression (`font-mono`) + ` = ` + result (`font-mono font-semibold text-amber-300`), and a breakdown line `<span className="text-xs font-mono text-muted-foreground">({formatBreakdown(data.breakdown)})</span>` when breakdown.length > 0.
    (2) `if (msg.role === 'system')` — render the level-up system event per UI-SPEC §S7b: `<div className="text-sm italic text-amber-400 mb-4 px-2 py-1 bg-amber-950/20 rounded" role="status">{msg.content}</div>`.
    Implement a small `formatBreakdown(breakdown: number[]): string` helper near the top of the file (or inline): UI-SPEC §S4 — render the breakdown as a readable string; a simple, correct implementation is `'[' + breakdown.join('+') + ']'` (e.g. [11,3] → "[11+3]"). The full bracket-vs-modifier grouping is a discretionary refinement; the join form satisfies the contract.
    Leave the assistant and player branches, streaming render, error block, and L1 overflow bar untouched.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>StoryScrollPanel renders dice_roll messages as amber chips with label/expression/result/breakdown and system messages as italic amber lines; malformed dice_roll JSON does not throw; assistant/player rendering unchanged; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/StoryScrollPanel.tsx` has a `msg.role === 'dice_roll'` branch that JSON.parses content in a try/catch and renders the amber chip with `role="note"`
    - the chip shows label, expression (`font-mono`), result (`font-mono font-semibold text-amber-300`), and breakdown when present
    - a `msg.role === 'system'` branch renders `text-sm italic text-amber-400 ... role="status"` with the raw content
    - a `formatBreakdown` helper exists and produces a bracketed string for a number array
    - the assistant and player message branches are unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI message content → renderer chip render | dice_roll message content is JSON produced from AI tool args (validated in 05-02) but parsed again in the renderer |
| dice expression input → renderer dice library | Player-typed expression is evaluated locally by rpg-dice-roller in the renderer only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | StoryScrollPanel dice_roll JSON.parse | mitigate | Parse inside try/catch; on failure render null (skip the row) so a malformed/oversized content string cannot crash the story panel |
| T-05-04-02 | Denial of Service | dice expression evaluation in renderer | mitigate | `isValidExpression` (try/catch around roller.roll) gates the Roll button; rpg-dice-roller bounds notation; evaluation is local, no IPC, no DB write |
| T-05-04-SC | Tampering | npm/shadcn installs | mitigate | rpg-dice-roller already installed in 05-01; only shadcn official `popover` block if missing (first-party, RESEARCH.md Registry Safety); no new npm packages |
</threat_model>

<verification>
- `npm run typecheck` green
- Manual (per VALIDATION.md): open the dice popover, click d20 → prefix appears in textarea; type `4d6kh3` → Roll prepends result; type garbage → Roll disabled + error. AI showDiceRoll chip rendering verified live (LLM) per VALIDATION.md manual-only.
</verification>

<success_criteria>
- DiceRollerPopover gives 7 die buttons + expression input, consuming the 05-01 dice wrapper (COMB-01)
- ChatInputArea dice button prepends/replaces the roll prefix and re-focuses
- StoryScrollPanel renders AI showDiceRoll chips (COMB-03) and level-up system events (D-32) — both message-role branches in place
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-04-SUMMARY.md` when done
</output>
