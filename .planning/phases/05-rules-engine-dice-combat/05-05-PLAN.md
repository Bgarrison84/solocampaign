---
phase: 05-rules-engine-dice-combat
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/main/trpc/routers/spells.ts
  - src/main/trpc/router.ts
  - src/renderer/src/components/sheet/SpellListSection.tsx
  - src/renderer/src/components/CharacterSheetTab.tsx
autonomous: true
requirements: [CHAR-08]

must_haves:
  truths:
    - "A 'Spells' collapsible section renders in the Character Sheet tab below Resources, only for spellcasters (D-23, S5)"
    - "Spells are grouped by level (Cantrips, 1st, 2nd, …) with per-level slot-remaining counts (S5b)"
    - "Each spell row expands to a card showing cast time, range, duration, components, concentration/ritual flags, and full description from spells.json (D-28, S5b)"
    - "Clicking Cast on a leveled spell deducts a slot optimistically and prepends a casting prefix to the chat input; cantrips prepend a prefix with no deduction (D-24, D-29)"
    - "Upcasting shows only the slot levels the player actually has available (D-27)"
    - "Casting a concentration spell while already concentrating opens a warning dialog before deducting (D-25, S5c)"
    - "Spell metadata is loaded from resources/spells.json via a content lookup, not duplicated in the DB (A3)"
  artifacts:
    - path: "src/main/trpc/routers/spells.ts"
      provides: "spells tRPC router (listByCharacter, seedFromJson, castSpell, undoCast, updateConcentration, listAllSpells)"
      exports: ["spellsRouter"]
    - path: "src/renderer/src/components/sheet/SpellListSection.tsx"
      provides: "Spell list UI (grouped, expandable cards, cast/upcast, concentration)"
      min_lines: 140
    - path: "src/renderer/src/components/CharacterSheetTab.tsx"
      provides: "SpellListSection mounted after ResourcesSection"
      contains: "SpellListSection"
  key_links:
    - from: "src/renderer/src/components/sheet/SpellListSection.tsx"
      to: "src/main/trpc/routers/spells.ts"
      via: "trpc.spells.listByCharacter + listAllSpells queries; castSpell/undoCast/updateConcentration mutations"
      pattern: "trpc\\.spells\\."
    - from: "src/main/trpc/routers/spells.ts"
      to: "src/main/db/characterSpellsRepo.ts + charactersRepo"
      via: "router calls characterSpellsRepo.listByCharacter/seed + charactersRepo.updateSpellSlot"
      pattern: "characterSpellsRepo\\.|charactersRepo\\."
    - from: "src/renderer/src/components/CharacterSheetTab.tsx"
      to: "src/renderer/src/components/sheet/SpellListSection.tsx"
      via: "rendered after ResourcesSection, before CurrencySection"
      pattern: "<SpellListSection"
---

<objective>
Wave 3 spell slice (CHAR-08): the spellcaster's full spell list lands in the Character Sheet tab. Spells are seeded into `character_spells` (05-01 repo) from the class content, grouped by level, expandable into cards backed by `resources/spells.json` (authored in 05-01), and castable with optimistic slot deduction, upcast level selection, and concentration tracking with a warning dialog. Casting prepends a context prefix to the chat input so the AI knows what the player did.

Purpose: CHAR-08 is the spell half of Phase 5. It depends on the character_spells table + spells.json (05-01) and the deductSpellSlot/restoreSpellSlots tools + concentratingOn summary line (05-02). It depends on 05-03 only because both register a tRPC router (shared router.ts) — run after 05-03 to avoid a same-file conflict.
Output: spells tRPC router (registered), SpellListSection.tsx (+ inline ConcentrationWarningDialog), CharacterSheetTab wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md

<interfaces>
<!-- characterSpellsRepo (05-01): seed(characterId, spells: Array<{spellName,spellLevel,isPrepared}>): void; listByCharacter(characterId): CharacterSpell[] (asc spellLevel); removeAll(characterId): void
     CharacterSpell shape: { id, characterId, spellName, spellLevel, isPrepared } -->
<!-- charactersRepo (existing): getByCampaignId(campaignId): CharacterWithResources | undefined; updateSpellSlot(characterId, slotLevel: string, delta): void (clamps used to [0,max]); class field on characters; resources.spellSlots is JSON map { "1": {used,max}, ... }; resources.concentratingOn (added 05-01, nullable) -->
<!-- CharacterWithResources type: import type { CharacterWithResources } from '../../../../main/db/charactersRepo' (as used in ResourcesSection.tsx) -->
<!-- resources/spells.json (05-01): array of { name, level, school, castTime, range, duration, components, concentration(bool), ritual(bool), classes: string[], description } -->
<!-- content router pattern: src/main/trpc/routers/content.ts loads bundled resources/*.json. Add a spells lookup there OR add listAllSpells to the new spells router that reads resources/spells.json the same way content.ts reads its JSON. -->
<!-- ResourcesSection.tsx isSpellcaster guard: `Object.keys(character.resources.spellSlots).length > 0` returns null if not a spellcaster. ordinal() from sheet/sheetHelpers.ts. -->
<!-- Optimistic spell-slot mutation: ResourcesSection.tsx spellSlotMutation (onMutate sets spellSlots[level].used+1) — copy for castSpell -->
<!-- shadcn Collapsible + Dialog already present. -->
<!-- router.ts now (after 05-03) registers `combat: combatRouter`. Add `spells: spellsRouter` alphabetically (after sessions, before window). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: spells tRPC router + register + spell seeding</name>
  <files>src/main/trpc/routers/spells.ts, src/main/trpc/router.ts</files>
  <read_first>
    - src/main/trpc/routers/sessions.ts (router structure: _base, schemas, log prefix)
    - src/main/trpc/routers/characters.ts (lines around updateSpellSlot procedure + getByCampaignId — mutation patterns, charactersRepo usage)
    - src/main/trpc/routers/content.ts (how bundled resources/*.json are read in the main process — mirror for listAllSpells reading resources/spells.json)
    - src/main/db/characterSpellsRepo.ts (seed/listByCharacter/removeAll signatures)
    - src/main/db/charactersRepo.ts (getByCampaignId, updateSpellSlot, class field; concentratingOn on resources)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Spell Data section — A4 "all spellcasters treated as known spells, seeded from a starter list"; spellcaster class list; spells.json shape)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (spells.ts router section)
  </read_first>
  <action>
    Create `src/main/trpc/routers/spells.ts` exporting `spellsRouter = t.router({...})`. Import `{ t }`, `{ campaignIdSchema }` from '../schemas', `{ characterSpellsRepo }`, `{ charactersRepo }`, `log`, `{ z }`. Read `resources/spells.json` once at module load (mirror content.ts's resource-loading approach — `app.getAppPath()`/`__dirname` resolution used by content.ts; do NOT hardcode an absolute path). Procedures:
    - `listAllSpells`: `.query(() => SPELLS_JSON)` — returns the full spells.json array (renderer uses it for metadata lookup by name). (Bounded, static content; no input.)
    - `listByCharacter`: `.input(z.object({ characterId: z.string() })).query(({input}) => characterSpellsRepo.listByCharacter(input.characterId))`
    - `seedFromJson`: `.input(z.object({ characterId: z.string(), className: z.string(), campaignId: campaignIdSchema })).mutation(({input}) => { ... })` — per A4, filter SPELLS_JSON to spells whose `classes` array includes `input.className.toLowerCase()` and `level <= <starter cap>` (cap at level the character can cast — for Phase 5 simplification seed all class spells of level 0 through the highest slot level the character has, OR seed all class spells up to level 3 if slot data unavailable). Map to `{ spellName: s.name, spellLevel: s.level, isPrepared: true }` and call `characterSpellsRepo.seed(input.characterId, mapped)`. Return `{ seeded: mapped.length }`. Idempotent (seed deletes existing first).
    - `castSpell`: `.input(z.object({ characterId: z.string(), spellName: z.string().max(100), slotLevel: z.number().int().min(0).max(9), campaignId: campaignIdSchema })).mutation(({input}) => { if (input.slotLevel > 0) charactersRepo.updateSpellSlot(input.characterId, String(input.slotLevel), 1); log.debug('[spells] castSpell', {...}); return { cast: true } })` — slotLevel 0 (cantrip) deducts nothing (D-29).
    - `undoCast`: `.input(z.object({ characterId: z.string(), slotLevel: z.number().int().min(1).max(9) })).mutation(({input}) => { charactersRepo.updateSpellSlot(input.characterId, String(input.slotLevel), -1); return { restored: true } })` — reverses a deduction for the 30s undo chip.
    - `updateConcentration`: `.input(z.object({ characterId: z.string(), spellName: z.string().max(100).nullable() })).mutation(({input}) => { ... set characterResources.concentratingOn = input.spellName (null clears) ... return { updated: true } })`. If charactersRepo lacks a setter, add a small `setConcentration(characterId, spellName: string | null)` method to charactersRepo (update characterResources.concentratingOn) and call it here.
    Register in `src/main/trpc/router.ts`: add `import { spellsRouter } from './routers/spells'` and `spells: spellsRouter,` (alphabetical: after `sessions:`, before `window:`).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>spells.ts exports spellsRouter with listAllSpells/listByCharacter/seedFromJson/castSpell/undoCast/updateConcentration; spells.json loaded via content-router-style resolution; router.ts registers spells; typecheck passes</done>
  <acceptance_criteria>
    - `src/main/trpc/routers/spells.ts` exports `spellsRouter` with procedures listAllSpells, listByCharacter, seedFromJson, castSpell, undoCast, updateConcentration
    - `castSpell` calls `charactersRepo.updateSpellSlot` only when `slotLevel > 0` (cantrips deduct nothing)
    - `updateConcentration` sets `characterResources.concentratingOn` (via a charactersRepo setter), accepting null to clear
    - spells.json is read via the same resource-path resolution as content.ts (no hardcoded absolute path)
    - `src/main/trpc/router.ts` contains `spells: spellsRouter`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SpellListSection component + inline ConcentrationWarningDialog</name>
  <files>src/renderer/src/components/sheet/SpellListSection.tsx</files>
  <read_first>
    - src/renderer/src/components/sheet/ResourcesSection.tsx (FULL — isSpellcaster guard, spellSlotMutation optimistic triple, SpellSlotPips usage, ordinal import, CharacterWithResources type import)
    - src/renderer/src/components/sheet/sheetHelpers.ts (ordinal export)
    - src/renderer/src/components/ui/collapsible.tsx and ui/dialog.tsx and ui/button.tsx
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S5 Spell List Section — collapsed/expanded states, level group headers, spell row, spell card, cast flow (optimistic), upcast picker, undo chip, §S5c Concentration Warning Dialog; copywriting contract; spacing/typography)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (SpellListSection.tsx section — castSpell optimistic mutation, collapsible row state, spellcaster guard)
  </read_first>
  <action>
    Create `src/renderer/src/components/sheet/SpellListSection.tsx` exporting `function SpellListSection({ character }: { character: CharacterWithResources })`. Per UI-SPEC §S5.
    Guard: render `null` when not a spellcaster — `Object.keys(character.resources.spellSlots).length === 0` (mirror ResourcesSection).
    Queries: `spellsQuery = trpc.spells.listByCharacter` (queryKey ['spells','listByCharacter',character.id]); `allSpellsQuery = trpc.spells.listAllSpells` (queryKey ['spells','listAllSpells'], staleTime Infinity — static content). Build a name→metadata map from allSpellsQuery for lookups. On first load, if `spellsQuery.data` is empty, call `trpc.spells.seedFromJson.mutate({ characterId: character.id, className: character.class, campaignId: character.campaignId })` once (guard with a ref or `enabled`/effect) then invalidate listByCharacter.
    Section: outer `Collapsible` (default open). Header `CollapsibleTrigger` per S5a: chevron + `<h2 className="text-xl font-semibold">Spells</h2>`, and a concentrating pill on the right when `character.resources.concentratingOn` is set: `<span className="text-xs font-semibold text-amber-400 bg-amber-950/40 px-2 py-1 rounded-full">Concentrating: {concentratingOn}</span>`.
    Body: group `spellsQuery.data` by `spellLevel`. For each level group (ascending), render the group header per S5b: `Cantrips` for 0, else `${ordinal(level)} Level`; for level>0 append `({slotsRemaining} slot{plural} remaining)` where slotsRemaining = `spellSlots[String(level)].max - spellSlots[String(level)].used` (or null if no slot record). Within a group, render each spell as a collapsible row (use a local `expandedSpell` state keyed by spell name). Collapsed row per S5b: chevron-right, name (`font-semibold`), `C` flag (amber) when metadata.concentration, `R` flag (sky) when metadata.ritual, cast-time-short on the right. Expanded card per S5b: metadata row (Cast/Range/Duration/Components + Concentration/Ritual labels), description paragraph, then the cast control:
    - level 0 (cantrip): single `<Button size="sm" variant="default" className="h-8" onClick={() => handleCastCantrip(name)}>Cast</Button>` (no slot deduction, D-29).
    - level>0: `Cast at:` label + one `<Button variant="outline" className="h-7 px-2 text-xs">` per available slot level (`availableSlotLevels` = slot levels ≥ the spell's level where slotsRemaining > 0, D-27); label `ordinal(level)`; onClick `handleCast(name, level, metadata.concentration)`.
    `handleCastCantrip(name)`: prepend `[Casting ${name}] ` to chat input (see Task 3 callback prop `onCastPrefix`); toast not required here.
    `handleCast(name, level, isConcentration)`: if `isConcentration && character.resources.concentratingOn && concentratingOn !== name` → open ConcentrationWarningDialog with `{ existingSpell: concentratingOn, newSpell: name, level }`, return (do not deduct yet). Otherwise proceed: `castSpellMutation.mutate({ spellName: name, slotLevel: level })` (optimistic slot deduction via the ResourcesSection spellSlotMutation pattern — onMutate increments spellSlots[level].used), if isConcentration call `trpc.spells.updateConcentration.mutate({ characterId, spellName: name })` and optimistically set concentratingOn, then prepend `[Casting ${name} — ${ordinal(level)} level slot] ` to chat input (onCastPrefix). Optionally start the 30s undo window (undo handled in Task 3 via prefix-removal detection — at minimum expose an `onCastForUndo` so Task 3 can wire it; if cross-component wiring is heavy, document the undo prefix format and let Task 3 own detection).
    ConcentrationWarningDialog (inline component or in-file): `Dialog` with `DialogContent className="max-w-[360px] border-amber-600/40"`, title "Drop Concentration?", body exact copy "You are concentrating on {existingSpell}. Casting {newSpell} will end your concentration on {existingSpell} and you will lose its effects.", dismiss `<Button variant="ghost">Keep {existingSpell}</Button>` (closes, no cast), confirm `<Button variant="default">Cast {newSpell}</Button>` (calls updateConcentration to the new spell + proceeds with castSpellMutation + onCastPrefix, then closes).
    Implement `castSpellMutation` with the optimistic onMutate/onError/onSettled triple from ResourcesSection.tsx spellSlotMutation (QUERY_KEY = ['characters','getByCampaignId',character.campaignId]).
    The component accepts an `onCastPrefix?: (prefix: string) => void` prop (wired in Task 3); when absent, fall back to a no-op (so the section still renders/casts even before wiring).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SpellListSection renders only for spellcasters; groups spells by level with slot counts; expandable cards show metadata + description from listAllSpells; Cast deducts optimistically (cantrips don't); upcast shows only available levels; concentration warning dialog gates a second concentration cast; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/sheet/SpellListSection.tsx` exports `SpellListSection` and returns null when `Object.keys(character.resources.spellSlots).length === 0`
    - spells are grouped by level with headers `Cantrips` / `${ordinal(n)} Level` and `(N slot(s) remaining)` for level>0
    - expanded spell card reads metadata (cast time/range/duration/components/description/concentration/ritual) from `trpc.spells.listAllSpells`
    - cantrip Cast calls no slot deduction; leveled Cast calls `trpc.spells.castSpell` with the chosen slotLevel and optimistically increments `spellSlots[level].used`
    - upcast buttons render only for slot levels ≥ the spell level with `slotsRemaining > 0`
    - a concentration warning Dialog with title "Drop Concentration?" opens when casting a concentration spell while `concentratingOn` is set to a different spell
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Mount SpellListSection in CharacterSheetTab + wire cast prefix to chat input</name>
  <files>src/renderer/src/components/CharacterSheetTab.tsx</files>
  <read_first>
    - src/renderer/src/components/CharacterSheetTab.tsx (FULL — section order, character query, render div)
    - src/renderer/src/components/sheet/SpellListSection.tsx (the component from Task 2 — props { character, onCastPrefix? })
    - src/renderer/src/components/ChatInputArea.tsx (the handleRoll prefix-prepend pattern from 05-04 — the chat input is uncontrolled via a ref in CampaignViewScreen; cast prefix must reach the same textarea)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S5 section order — SpellListSection after ResourcesSection, before CurrencySection; §S5b cast flow prefix format; undo chip)
  </read_first>
  <action>
    Per UI-SPEC §S5 section order. Import `SpellListSection` and render it in CharacterSheetTab's section list AFTER `<ResourcesSection character={character} />` and BEFORE `<CurrencySection character={character} />`.
    Cast-prefix wiring: the chat textarea lives in ChatInputArea (rendered by CampaignViewScreen, uncontrolled via ref). The cleanest seam without a cross-screen prop drill is a tiny shared mechanism: introduce a module-level event the chat input listens for. Implement the lightest option that fits the codebase:
    - Preferred: add an `onCastPrefix` prop chain — but CharacterSheetTab does not own the textarea. Instead, dispatch a `CustomEvent('campaign:chat-prefix', { detail: prefix })` on `window` from SpellListSection's cast handlers, and add a listener in ChatInputArea (05-04 already centralizes prefix prepend in `handleRoll`) that calls the same prefix-prepend logic. In this task, pass `onCastPrefix={(prefix) => window.dispatchEvent(new CustomEvent('campaign:chat-prefix', { detail: prefix }))}` to SpellListSection, AND add a `useEffect` in ChatInputArea (note this requires a 05-04-compatible follow-up — if ChatInputArea wiring is needed, document it as a one-line addition here and implement it: add a window 'campaign:chat-prefix' listener in ChatInputArea that calls handleRoll(detail), with handleRoll already stripping/prepending). Reuse handleRoll so cast prefixes and dice prefixes share the same prepend/replace behavior.
    Keep the loading/no-character branches of CharacterSheetTab unchanged. Add the level-up banner placeholder is NOT this plan (05-06 owns it) — only mount SpellListSection here.
    NOTE on file ownership: ChatInputArea was last modified in 05-04 (prior wave). Adding the 'campaign:chat-prefix' listener to ChatInputArea here is a Wave 3 edit to a Wave 2 file — that is allowed (later wave touching an earlier-wave file = sequential, no conflict). Add the listener; do not alter the dice button behavior.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SpellListSection renders in CharacterSheetTab between Resources and Currency; casting a spell prepends the casting prefix to the chat textarea via the shared prefix mechanism; CharacterSheetTab loading/no-character branches unchanged; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/CharacterSheetTab.tsx` renders `<SpellListSection character={character} ... />` after `<ResourcesSection>` and before `<CurrencySection>`
    - the cast handler dispatches (or otherwise delivers) a prefix string of the form `[Casting <Spell> — <ordinal> level slot] ` (leveled) or `[Casting <Spell>] ` (cantrip) to the chat input
    - ChatInputArea has a `window` `campaign:chat-prefix` listener that reuses its prefix-prepend logic (handleRoll)
    - the loading and no-character branches of CharacterSheetTab are unchanged
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (spells router) | castSpell/undoCast/seedFromJson/updateConcentration inputs cross from the untrusted renderer into spell-slot + concentration DB writes |
| spells.json content → renderer | Static bundled content; malformed JSON would break the spell card metadata lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | spells.ts tRPC inputs | mitigate | Every procedure validates input with Zod: characterId string, slotLevel `.int().min(0/1).max(9)`, spellName `.max(100)`, campaignId campaignIdSchema; updateSpellSlot clamps used to [0,max] in the repo |
| T-05-05-02 | Tampering | optimistic slot cache vs server | mitigate | castSpell optimistic onMutate snapshots prev and onError restores; onSettled invalidates so the server value wins — a tampered optimistic value cannot persist |
| T-05-05-03 | Information Disclosure | listAllSpells payload | accept | spells.json is bundled SRD content with no secrets; returning the full array to the renderer is intended (content lookup) |
| T-05-05-SC | Tampering | npm/shadcn installs | mitigate | No new npm packages; shadcn dialog/collapsible already present; spells.json authored in 05-01 |
</threat_model>

<verification>
- `npm run typecheck` green
- Manual (per VALIDATION.md): open Character Sheet for a spellcaster → Spells section appears grouped by level; expand a spell → metadata + description show; Cast a leveled spell → slot pip increments + prefix in chat input; Cast Bless then attempt Hold Person → concentration warning dialog (VALIDATION.md manual-only item).
</verification>

<success_criteria>
- spells tRPC router registered; spell list seeded from class content; metadata served from spells.json (CHAR-08)
- SpellListSection: grouped, expandable cards, cantrip + leveled cast, upcast picker, optimistic deduction, concentration warning (D-23 through D-29)
- Cast prefix reaches the chat input via the shared prefix mechanism
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-05-SUMMARY.md` when done
</output>
