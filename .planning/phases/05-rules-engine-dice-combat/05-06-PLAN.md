---
phase: 05-rules-engine-dice-combat
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-05"]
files_modified:
  - src/main/db/charactersRepo.ts
  - src/main/trpc/routers/characters.ts
  - src/renderer/src/components/LevelUpModal.tsx
  - src/renderer/src/components/sheet/LevelUpBanner.tsx
  - src/renderer/src/components/CharacterSheetTab.tsx
  - src/renderer/src/components/RestPickerDialog.tsx
  - src/renderer/src/components/ShortRestHitDiceModal.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [PROG-01, PROG-02]

must_haves:
  truths:
    - "When a character's XP crosses the next level threshold, an amber 'Level up available' banner appears at the top of the Character Sheet tab (PROG-01, D-30, S7a)"
    - "Clicking Level Up opens a modal showing the new level, an HP gain choice (roll the hit die or take average), and the new spell slot totals (D-31, S7b)"
    - "Confirming level-up increments level, applies HP gain, updates slots, and appends a [System: …] message to the story (D-32)"
    - "Subclass-granting levels show a deferred note instead of subclass choices (D-33)"
    - "A 'Rest' button in the campaign header opens a Short/Long rest picker; selecting sends a [Player requests a …] message to the AI (PROG-02, D-34)"
    - "When the AI grants a short rest (processRest tool, 05-02), the Short Rest Hit Die modal opens for the player to roll hit dice (D-36)"
    - "Long rest recovery is auto-applied by the processRest mutation (05-02); the player sees recovery chips (D-36)"
  artifacts:
    - path: "src/renderer/src/components/LevelUpModal.tsx"
      provides: "Level-up modal (HP choice + slot table + confirm)"
      min_lines: 120
    - path: "src/renderer/src/components/sheet/LevelUpBanner.tsx"
      provides: "Amber XP-threshold banner"
      min_lines: 20
    - path: "src/renderer/src/components/RestPickerDialog.tsx"
      provides: "Short/Long rest picker"
      min_lines: 40
    - path: "src/renderer/src/components/ShortRestHitDiceModal.tsx"
      provides: "Hit-dice roll modal for short rest"
      min_lines: 80
    - path: "src/main/trpc/routers/characters.ts"
      provides: "levelUp + recordSystemMessage procedures"
      contains: "levelUp"
  key_links:
    - from: "src/renderer/src/components/CharacterSheetTab.tsx"
      to: "src/renderer/src/components/sheet/LevelUpBanner.tsx + LevelUpModal.tsx"
      via: "XP threshold check renders banner; banner opens modal"
      pattern: "LevelUpBanner|LevelUpModal"
    - from: "src/renderer/src/components/LevelUpModal.tsx"
      to: "src/main/trpc/routers/characters.ts"
      via: "trpc.characters.levelUp mutation + recordSystemMessage"
      pattern: "trpc\\.characters\\.(levelUp|recordSystemMessage)"
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "src/renderer/src/components/RestPickerDialog.tsx + ShortRestHitDiceModal.tsx"
      via: "Rest button opens picker; processRest ai:mutations-applied opens hit-dice modal"
      pattern: "RestPickerDialog|ShortRestHitDiceModal"
---

<objective>
Wave 4 progression slice (PROG-01 + PROG-02): XP crossing a threshold surfaces a level-up banner and modal that advances the character (HP + slots, no subclass/feat per D-33), and a Rest button drives the short/long rest flow — short rest opens a hit-dice roll modal after the AI grants it, long rest recovery is auto-applied by the processRest mutation shipped in 05-02. The level-up system message uses the StoryScrollPanel `system` role branch added in 05-04.

Purpose: PROG-01 and PROG-02 are the two progression/rest success criteria. This depends on the dice wrapper (05-01), the processRest mutation + rest_taken event (05-02), the spells section (05-05, shared CharacterSheetTab file), the combat header buttons (05-03, shared CampaignViewScreen file), and the system-message render branch (05-04).
Output: charactersRepo.levelUp + characters router additions, LevelUpModal, LevelUpBanner, CharacterSheetTab banner wiring, RestPickerDialog, ShortRestHitDiceModal, CampaignViewScreen Rest button + modal mounting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md

<interfaces>
<!-- characters table (schema.ts): level (int, default 1), xp (int), class (text). characterResources: hpCurrent, hpMax, spellSlots (JSON), hitDiceCurrent (added 05-01, nullable), hitDiceTotal (added 05-01, nullable), deathSaveSuccesses, deathSaveFailures, concentratingOn. -->
<!-- charactersRepo (existing): getByCampaignId(campaignId), updateXp(id, delta), updateHp(id, delta), updateSpellSlot(id, slotLevel, delta), updateDeathSaves(id, succ, fail). NO levelUp method yet — add it. CharacterWithResources type exported from charactersRepo. -->
<!-- characters router (existing) procedures: create, getByCampaignId, updateXp, updateHp, updateCurrency, updateSpellSlot, toggleCondition. Add levelUp + recordSystemMessage. -->
<!-- messagesRepo.insert({ campaignId, role, content, sessionId? }) — used to persist the [System: ...] message with role 'system' (StoryScrollPanel renders role 'system' as of 05-04). -->
<!-- dice.ts (05-01): rollExpression('d8') → { result, breakdown } for HP roll + hit-dice rolls. -->
<!-- XP_THRESHOLDS + HIT_DIE_BY_CLASS constants: RESEARCH.md Spell Data section (XP_THRESHOLDS level→xp; HIT_DIE_BY_CLASS class→die). Embed as a renderer constant (no library). resources/spells-by-class.json has per-class/level slot tables for the new-slots display. resources/classes.json has hitDie + spellcastingAbility + spellcaster flag. -->
<!-- processRest (05-02 mutationPipeline): LONG auto-applies HP→max + all slots restored + hitDice→half + death saves cleared, writes rest_taken event, returns chips. SHORT resets pact slots + death saves and SIGNALS the renderer (hit-dice HP recovery is the renderer's job — this modal). 05-02's index.ts emits 'ai:mutations-applied' { campaignId, chips }; the rest_taken event payload includes { type }. -->
<!-- CampaignViewScreen: action bar ~223-294 (05-03 added Start/End Combat before Start Session — add Rest button next to it per UI-SPEC S1 order). useCombatStore for activeTab. -->
<!-- CharacterSheetTab: renders sections; SpellListSection added in 05-05 after ResourcesSection. The level-up banner mounts at the TOP of the scroll div, before SheetHeader (S7a). -->
<!-- conMod = Math.floor((character.constitution - 10) / 2). proficiencyBonus already on characters. -->
<!-- EndSessionModal.tsx + RestPickerDialog/ShortRestHitDiceModal share the shadcn Dialog shell + close-guard pattern. Stepper from sheet/Stepper.tsx for hit-dice count. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: charactersRepo.levelUp + characters router (levelUp + recordSystemMessage + hit-dice/HP recovery helper)</name>
  <files>src/main/db/charactersRepo.ts, src/main/trpc/routers/characters.ts</files>
  <behavior>
    - levelUp increments characters.level by 1, increases hpMax (and hpCurrent) by the provided hpGain, and applies the provided new spell-slot max map
    - levelUp clamps so level never exceeds 20
    - recordSystemMessage inserts a messages row with role 'system'
    - applyShortRestHp increases hpCurrent (clamped to hpMax) and decrements hitDiceCurrent by the dice spent
  </behavior>
  <read_first>
    - src/main/db/charactersRepo.ts (updateXp/updateHp/updateSpellSlot patterns; CharacterWithResources; transaction usage)
    - src/main/trpc/routers/characters.ts (existing procedure shapes + schemas usage)
    - src/main/db/messagesRepo.ts (insert signature for the system message)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (XP_THRESHOLDS, HIT_DIE_BY_CLASS; D-36 short-rest = renderer rolls hit dice, app applies HP + decrements hitDiceCurrent)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S7b level-up applies HP + slots only; §S8b short rest applies HP recovery + hit dice spent)
  </read_first>
  <action>
    charactersRepo.ts — add three synchronous methods (follow the existing transaction + sql clamp patterns):
    - `levelUp(characterId: string, hpGain: number, newSlotMax: Record<string, number>): void` — in a transaction: `UPDATE characters SET level = MIN(20, level + 1)`; `UPDATE character_resources SET hp_max = hp_max + hpGain, hp_current = hp_current + hpGain`; then for each `[level, max]` in newSlotMax, merge into the spellSlots JSON setting that level's `max` (preserve `used`, default used 0 for new levels) — read/parse/merge/stringify/write like updateSpellSlot. Bound hpGain to a sane range in the caller (router Zod).
    - `applyShortRestHp(characterId: string, hpRecovered: number, diceSpent: number): void` — `UPDATE character_resources SET hp_current = MIN(hp_max, hp_current + hpRecovered), hit_dice_current = MAX(0, hit_dice_current - diceSpent)` (D-36).
    - `setConcentration` may already have been added in 05-05; if not present and needed, leave to 05-05. Do not duplicate.
    characters router — add procedures:
    - `levelUp`: `.input(z.object({ characterId: z.string(), hpGain: z.number().int().min(1).max(50), newSlotMax: z.record(z.string(), z.number().int().min(0).max(9)) })).mutation(({input}) => { charactersRepo.levelUp(input.characterId, input.hpGain, input.newSlotMax); return { leveled: true } })`.
    - `recordSystemMessage`: `.input(z.object({ campaignId: campaignIdSchema, content: z.string().min(1).max(500), sessionId: z.string().optional() })).mutation(({input}) => { messagesRepo.insert({ campaignId: input.campaignId, role: 'system', content: input.content, sessionId: input.sessionId }); return { recorded: true } })`.
    - `applyShortRestHp`: `.input(z.object({ characterId: z.string(), hpRecovered: z.number().int().min(0).max(9999), diceSpent: z.number().int().min(0).max(20) })).mutation(({input}) => { charactersRepo.applyShortRestHp(input.characterId, input.hpRecovered, input.diceSpent); return { applied: true } })`.
    Add a unit test alongside charactersRepo (e.g. extend the existing charactersRepo test or create `src/main/db/charactersRepo.levelUp.test.ts`) using the in-memory test DB pattern: levelUp increments level + raises hpMax + sets the slot max; level caps at 20; applyShortRestHp raises hpCurrent (clamped) and lowers hitDiceCurrent.
  </action>
  <verify>
    <automated>npm test -- src/main/db/charactersRepo.levelUp.test.ts && npm run typecheck</automated>
  </verify>
  <done>charactersRepo.levelUp + applyShortRestHp added and tested; characters router exposes levelUp/recordSystemMessage/applyShortRestHp with Zod-bounded inputs; level caps at 20; typecheck passes</done>
  <acceptance_criteria>
    - `src/main/db/charactersRepo.ts` exports `levelUp` and `applyShortRestHp` on the charactersRepo object
    - `levelUp` increments level (capped at 20), adds hpGain to hp_max and hp_current, and sets the provided slot maxes (preserving used)
    - `src/main/trpc/routers/characters.ts` adds `levelUp`, `recordSystemMessage` (inserts role 'system'), and `applyShortRestHp` with Zod input schemas
    - the levelUp/applyShortRestHp unit test passes (`npm test -- src/main/db/charactersRepo.levelUp.test.ts` exits 0)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: LevelUpBanner + LevelUpModal + CharacterSheetTab banner wiring</name>
  <files>src/renderer/src/components/sheet/LevelUpBanner.tsx, src/renderer/src/components/LevelUpModal.tsx, src/renderer/src/components/CharacterSheetTab.tsx</files>
  <read_first>
    - src/renderer/src/components/CharacterSheetTab.tsx (FULL — section order with SpellListSection from 05-05; render div; character query)
    - src/renderer/src/components/EndSessionModal.tsx (Dialog shell + close guard + useMutation + loading spinner patterns)
    - src/renderer/src/lib/dice.ts (rollExpression for the HP roll)
    - src/main/trpc/routers/characters.ts (levelUp + recordSystemMessage from Task 1)
    - resources/spells-by-class.json (per-class/level slot tables for the new-slot display) and resources/classes.json (hitDie, spellcaster)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S7a banner exact className + copy; §S7b modal layout, HP gain radios, new spell slots table, subclass deferred note, footer, system event copy; copywriting contract)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (CharacterSheetTab level-up banner + XP threshold check; LevelUpModal analog EndSessionModal)
  </read_first>
  <action>
    Per UI-SPEC §S7. Embed the XP_THRESHOLDS and HIT_DIE_BY_CLASS constants from RESEARCH.md in a small shared module or inline in CharacterSheetTab.
    LevelUpBanner.tsx — export `function LevelUpBanner({ nextLevel, onLevelUp }: { nextLevel: number; onLevelUp: () => void })` rendering the amber banner per S7a exact className `bg-amber-950/30 border border-amber-900/40 rounded-lg px-4 py-3 flex items-center gap-3` with `role="status" aria-live="polite"`, `<Star className="h-4 w-4 text-amber-400 shrink-0" />`, text "Level up available — reach Level {nextLevel}!", and `<Button size="sm" variant="default" className="h-7 shrink-0" onClick={onLevelUp}>Level Up</Button>`.
    LevelUpModal.tsx — export `function LevelUpModal({ open, onClose, character }: {...})` per S7b. Dialog `max-w-[480px] w-full max-h-[85vh] overflow-hidden flex flex-col`, title "Level Up". Body: large new level number (`text-[28px] font-semibold`), "You are now Level {nextLevel}!", "Congratulations, {name}.". HP Gain section with two radios: "Roll d{hitDie}" (hitDie from HIT_DIE_BY_CLASS[character.class.toLowerCase()]) with an inline Roll button (`rollExpression('d'+hitDie)` → show result) and "Take average {hitDieAverage}" (hitDieAverage = floor(hitDie/2)+1). Show "HP gained: +{hpGainTotal} ({base} + CON modifier {conMod})" where hpGainTotal = chosen base + conMod (conMod from character.constitution, min total 1). New Spell Slots section: compute the level-{nextLevel} slot maxes from spells-by-class.json for the character's class and render the table (`{ordinal(level)}: {max}`); skip for non-spellcasters. Subclass note: show the deferred italic note "Subclass selection will be available in a future update." only at subclass-granting levels (a simple per-class lookup; if unknown, omit — D-33 says level still advances). Footer: "Not Now" (ghost, closes — banner remains) and "Confirm Level Up" (default/gold, disabled until an HP method chosen and, if Roll, the roll resolved). On confirm: `levelUpMutation.mutate({ characterId, hpGain: hpGainTotal, newSlotMax })`; onSuccess invalidate `['characters','getByCampaignId',campaignId]`, call `trpc.characters.recordSystemMessage.mutate({ campaignId, content: `[System: ${name} reached Level ${nextLevel}!]` })`, invalidate `['ai','getMessages',campaignId]`, and onClose. Loading label "Leveling Up…".
    CharacterSheetTab.tsx — compute `nextLevel = character.level + 1` and `isLevelUpAvailable = nextLevel <= 20 && character.xp >= (XP_THRESHOLDS[nextLevel] ?? Infinity)`. Add `showLevelUpModal` state. Render `<LevelUpBanner nextLevel={nextLevel} onLevelUp={() => setShowLevelUpModal(true)} />` at the TOP of the scroll div (before SheetHeader) when isLevelUpAvailable. Render `<LevelUpModal open={showLevelUpModal} onClose={() => setShowLevelUpModal(false)} character={character} />`. Keep SpellListSection (05-05) and all other sections in place.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>LevelUpBanner shows when XP crosses the threshold; LevelUpModal offers HP roll/average + new slot table + subclass deferred note; confirm calls levelUp + records the [System:...] message; CharacterSheetTab wires both; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/sheet/LevelUpBanner.tsx` exports `LevelUpBanner` with banner text `Level up available — reach Level {nextLevel}!`
    - `src/renderer/src/components/LevelUpModal.tsx` exports `LevelUpModal`, offers a "Roll d{hitDie}" (uses `rollExpression`) and "Take average {N}" HP choice, shows the new spell slot totals, and disables Confirm until HP is chosen
    - confirm calls `trpc.characters.levelUp` and `trpc.characters.recordSystemMessage` with content `[System: {name} reached Level {nextLevel}!]`
    - `src/renderer/src/components/CharacterSheetTab.tsx` renders `<LevelUpBanner>` at the top of the scroll div when `character.xp >= XP_THRESHOLDS[character.level + 1]` and mounts `<LevelUpModal>`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RestPickerDialog + ShortRestHitDiceModal + CampaignViewScreen Rest button + processRest hook</name>
  <files>src/renderer/src/components/RestPickerDialog.tsx, src/renderer/src/components/ShortRestHitDiceModal.tsx, src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (FULL — action bar with Start/End Combat from 05-03; useCombatStore; useAiStream send; the ai:finish / window.aiStream listeners; campaign + character queries)
    - src/renderer/src/components/EndSessionModal.tsx (Dialog shell + close guard)
    - src/renderer/src/components/sheet/Stepper.tsx (hit-dice count stepper)
    - src/renderer/src/lib/dice.ts (rollExpression for hit-dice rolls)
    - src/main/trpc/routers/characters.ts (applyShortRestHp from Task 1) + characters.getByCampaignId for hitDice/CON
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S8a Rest picker, §S8b Short Rest Hit Die modal — exact layout, copy, per-die result format, footer; §S1 Rest button spec; copywriting contract)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (D-35 no recovery if no processRest; D-36 short rest = player rolls hit dice; long rest auto-applied by 05-02)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (RestPickerDialog + ShortRestHitDiceModal analog EndSessionModal)
  </read_first>
  <action>
    Per UI-SPEC §S8 + §S1.
    RestPickerDialog.tsx — export `function RestPickerDialog({ open, onClose, onSelectRest }: { open; onClose; onSelectRest: (type: 'short'|'long') => void })`. Dialog `max-w-[400px]`, title "Take a Rest". Two big option buttons per S8a (Short Rest with Moon icon + body "Roll hit dice to regain HP. Recharges certain class abilities.", Long Rest with body "Regain all HP, spell slots, and half your hit dice. Requires ~8 hours."), the muted note "The DM will narrate whether the rest is possible.", and a Cancel ghost button. Selecting calls `onSelectRest(type)` then closes.
    ShortRestHitDiceModal.tsx — export `function ShortRestHitDiceModal({ open, onClose, character }: {...})`. Dialog `max-w-[440px]`, title "Short Rest — Hit Dice". Show "You have {hitDiceCurrent} of {hitDiceTotal} hit dice remaining. Roll hit dice to recover HP." (hitDiceCurrent/Total from character.resources; if null, fall back to character.level for total and treat current as level). A `Stepper` for `diceToRoll` (min 1, max hitDiceCurrent), a "Roll Hit Dice" button that calls `rollExpression('d'+hitDie)` `diceToRoll` times (hitDie from HIT_DIE_BY_CLASS[class]), per-die result rows ("d{N} → {roll} +CON ({conMod}) = {total} HP"), a running total (sum of max(0, roll+conMod)). Footer: "Skip" (ghost, closes, no changes) and "Done (+{N} HP)" (default, disabled until at least one roll) → `trpc.characters.applyShortRestHp.mutate({ characterId, hpRecovered: total, diceSpent: diceToRoll })`, onSuccess invalidate `['characters','getByCampaignId',campaignId]`, close.
    CampaignViewScreen.tsx — add `showRestPicker` and `showShortRest` state. In the action bar, add the Rest button per S1 (`<Button variant="outline" size="sm" className="gap-2" onClick={() => setShowRestPicker(true)}>` + `<Moon className="h-4 w-4" />` "Rest", tooltip "Take a short or long rest") next to the Combat button. Mount `<RestPickerDialog open={showRestPicker} onClose={() => setShowRestPicker(false)} onSelectRest={handleRest} />` and `<ShortRestHitDiceModal open={showShortRest} onClose={() => setShowShortRest(false)} character={characterQuery.data} />` (guard character present).
    `handleRest(type)`: send the context message to the AI via the existing `aiStream.send` (or `window.aiStream.sendMessage`) with content `[Player requests a ${type === 'short' ? 'short' : 'long'} rest]` (exact copy per copywriting contract), close the picker. Do NOT pre-apply recovery (D-35 — only the AI's processRest grants it; long rest recovery is applied by 05-02's mutation pipeline).
    processRest → short-rest modal hook: add a `window.aiStream` / IPC listener for the 05-02 `ai:mutations-applied` event. When the applied chips/event indicate a SHORT rest was processed (05-02's processRest short path signals the renderer; the simplest contract: 05-02 emits the rest_taken in chips or a dedicated `ai:rest-processed` { type } — if 05-02 only emits ai:mutations-applied with chips, detect a short-rest chip/marker), open the ShortRestHitDiceModal (`setShowShortRest(true)`). If 05-02 does not yet emit a distinguishable short-rest signal, add a minimal `ai:rest-processed` emission note: this plan listens for `window.aiStream.onRestProcessed?.((meta) => { if (meta.type === 'short') setShowShortRest(true) })` if available; otherwise fall back to opening the modal when a rest_taken short event is present in the latest campaign_events (query campaignEvents). Document whichever path the executor wires. Long rest needs no modal — recovery + chips are automatic (05-02 + 05-07 toast).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Rest button opens the picker; selecting sends the [Player requests a …] message; short rest grant opens the hit-dice modal which rolls dice and applies HP + spends hit dice; long rest needs no modal; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/RestPickerDialog.tsx` exports `RestPickerDialog` with Short Rest / Long Rest options and a Cancel button
    - `src/renderer/src/components/ShortRestHitDiceModal.tsx` exports `ShortRestHitDiceModal`, rolls `diceToRoll` × d{hitDie} via `rollExpression`, shows per-die results + total, and Done calls `trpc.characters.applyShortRestHp`
    - `src/renderer/src/screens/CampaignViewScreen.tsx` renders a "Rest" button (Moon icon) that opens RestPickerDialog and mounts ShortRestHitDiceModal
    - selecting a rest sends a chat message exactly `[Player requests a short rest]` or `[Player requests a long rest]`
    - the short-rest hit-dice modal opens in response to a processRest short signal from the main process (the wired path is documented in the SUMMARY)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (characters router) | levelUp/recordSystemMessage/applyShortRestHp inputs cross from the untrusted renderer into level/HP/slot DB writes |
| AI processRest → renderer rest modal | The decision to open the short-rest modal is driven by an AI tool call processed in main (05-02), signalled to the renderer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | characters.levelUp / applyShortRestHp inputs | mitigate | Zod bounds: hpGain `.int().min(1).max(50)`, newSlotMax `record(string, int.min(0).max(9))`, hpRecovered/diceSpent bounded; level capped at 20 in the repo; HP/hitDice clamped via SQL MIN/MAX |
| T-05-06-02 | Tampering | recordSystemMessage content | mitigate | content `.string().min(1).max(500)`; stored as a plain message; StoryScrollPanel renders system messages as text (no HTML, no dangerouslySetInnerHTML) |
| T-05-06-03 | Elevation of Privilege | renderer applying rest recovery without AI grant | mitigate | The renderer never applies LONG rest recovery (D-35 — only 05-02 processRest does); short-rest HP is applied only after the AI's processRest signal opens the modal and the player rolls |
| T-05-06-SC | Tampering | npm/shadcn installs | mitigate | No new npm packages; shadcn dialog already present; dice library installed in 05-01 |
</threat_model>

<verification>
- `npm test -- src/main/db/charactersRepo.levelUp.test.ts` green; `npm run typecheck` green
- Manual (per VALIDATION.md): award XP to cross 300 → banner appears → open modal → roll/average HP → confirm → level increments + [System:...] line in story. Rest → Short → AI grants → hit-dice modal rolls + applies HP. (processRest live-LLM firing is a VALIDATION.md manual-only item.)
</verification>

<success_criteria>
- XP-threshold banner + level-up modal advance the character (HP + slots, subclass deferred per D-33) and emit the [System:…] message (PROG-01, D-30–D-33)
- Rest picker + short-rest hit-dice modal + AI-gated recovery (PROG-02, D-34–D-40); long rest auto-applied by 05-02
- charactersRepo.levelUp/applyShortRestHp unit-tested
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-06-SUMMARY.md` when done
</output>
