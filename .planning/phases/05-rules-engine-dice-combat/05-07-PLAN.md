---
phase: 05-rules-engine-dice-combat
plan: 07
type: execute
wave: 5
depends_on: ["05-02", "05-03", "05-06"]
files_modified:
  - src/renderer/src/components/MutationChipStack.tsx
  - src/main/trpc/routers/campaigns.ts
  - src/renderer/src/components/AiSettingsModal.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [PROG-04, STATE-05]

must_haves:
  truths:
    - "Mutation chips slide in at the top of the right panel when the AI applies mutations, showing one chip per change (−6 HP, +150 XP, Poisoned applied, 3rd level slot used, +50 GP, …) and fade after ~4s, max 4 visible (D-07, S6)"
    - "The chip stack is driven by the 'ai:mutations-applied' IPC event emitted by 05-02 (chips payload)"
    - "When the AI applies mutations, TanStack Query caches for combat and character data are invalidated so the UI reflects the new state (integration)"
    - "A permadeath toggle in AI Settings persists campaigns.permadeathMode (PROG-04)"
    - "Currency-change chips render for STATE-05 currency mutations from the AI (updateCurrency)"
  artifacts:
    - path: "src/renderer/src/components/MutationChipStack.tsx"
      provides: "Toast/chip notification stack"
      min_lines: 60
    - path: "src/main/trpc/routers/campaigns.ts"
      provides: "setPermadeath (or extended updateAiConfig) procedure"
      contains: "permadeath"
    - path: "src/renderer/src/components/AiSettingsModal.tsx"
      provides: "Permadeath toggle UI"
      contains: "permadeath"
    - path: "src/renderer/src/screens/CampaignViewScreen.tsx"
      provides: "MutationChipStack mounted + ai:mutations-applied cache invalidation"
      contains: "MutationChipStack"
  key_links:
    - from: "src/renderer/src/components/MutationChipStack.tsx"
      to: "window 'ai:mutations-applied' IPC event"
      via: "useEffect listener pushes chips from event.chips payload"
      pattern: "ai:mutations-applied|mutations-applied"
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx"
      to: "TanStack Query caches (combat + characters)"
      via: "ai:mutations-applied handler invalidates queryKeys"
      pattern: "invalidateQueries"
    - from: "src/renderer/src/components/AiSettingsModal.tsx"
      to: "src/main/trpc/routers/campaigns.ts"
      via: "permadeath toggle persists via setPermadeath/updateAiConfig"
      pattern: "trpc\\.campaigns\\."
---

<objective>
Wave 5 integration slice (PROG-04 + STATE-05 UI + cross-cutting glue): mutation chips give the player live feedback whenever the AI changes state (the visible payoff of the 05-02 mutation pipeline — HP/XP/condition/slot/currency chips), the `ai:mutations-applied` event invalidates the combat + character query caches so every prior wave's UI updates without a manual refresh, and a per-campaign permadeath toggle satisfies PROG-04. STATE-05's currency mutation already runs server-side (05-02 updateCurrency); this plan surfaces its chip and ensures the character sheet refetches.

Purpose: This is the final wiring that makes the whole phase feel live. Without the chip stack + cache invalidation, the AI's mutations are invisible until a manual refetch. PROG-04 (permadeath config) is the last unstarted requirement.
Output: MutationChipStack.tsx, campaigns setPermadeath procedure, AiSettingsModal permadeath toggle, CampaignViewScreen MutationChipStack mount + ai:mutations-applied cache invalidation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rules-engine-dice-combat/05-CONTEXT.md
@.planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md
@.planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md
@.planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md

<interfaces>
<!-- 05-02 index.ts emits IPC: safeSend('ai:mutations-applied', { campaignId, chips }) where chips is the MutationChip[] returned by applyMutationBatch. Each chip from 05-02 has a label (e.g. '−6 HP', '+150 XP', 'Poisoned applied', '3rd level slot used', '+50 GP', 'Combat ended') and a type-ish discriminator the renderer maps to an icon. Confirm the chip shape in 05-02's mutationPipeline.ts MutationChip type. -->
<!-- The renderer accesses IPC via window.aiStream (see useAiStream.ts) — onFinish/onToken are registered there. Add an onMutationsApplied listener following the same window.aiStream.onFinish registration + removeAllListeners cleanup pattern. If the preload bridge does not yet expose ai:mutations-applied, add the bridge method (preload + window typing) the same way ai:finish is exposed. -->
<!-- StoryScrollPanel L1 warning bar (lines 251-265) is the analog for the positioned chip container (PATTERNS.md MutationChipStack analog). -->
<!-- campaigns table: permadeathMode (boolean, added 05-01, default false). campaigns router: create, get, getCoverDataUrl, updateAiConfig. AiSettingsModal already edits strictness via updateAiConfig and invalidates ['campaigns','get',campaignId]. -->
<!-- shadcn checkbox already present (ui/checkbox.tsx). UI-SPEC S6 allows custom positioned container (chosen over sonner per RESEARCH.md). -->
<!-- CampaignViewScreen right panel: the Tabs block (~352-435). MutationChipStack mounts inside the right panel, between TabsList and the TabsContent (UI-SPEC S6 anchor). queryClient available. Combat query key ['combat','listActive',id]; character query key ['characters','getByCampaignId',id]. -->
<!-- UI-SPEC S6 chip className: bg-card border border-border rounded-full px-3 py-1 text-sm text-foreground shadow-md flex items-center gap-2 animate-in slide-in-from-top-2 duration-200 self-start; container: absolute top-0 left-0 right-0 z-50 px-3 py-2 flex flex-col gap-2 pointer-events-none; role="status" aria-live="polite". Icon map (S6 table): HP damage ArrowDown red, HP heal ArrowUp green, XP Star amber, condition applied AlertCircle amber, condition removed CheckCircle green, slot Zap sky, slot restored RotateCcw muted, currency Coins amber, combatant added UserPlus muted, combat ended Shield muted. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MutationChipStack component</name>
  <files>src/renderer/src/components/MutationChipStack.tsx</files>
  <read_first>
    - src/renderer/src/components/StoryScrollPanel.tsx (lines 251-265 — positioned warning-bar analog; aria-live container)
    - src/renderer/src/hooks/useAiStream.ts (window.aiStream listener registration + cleanup pattern — how ai:finish/ai:token are subscribed and torn down)
    - src/main/ai/mutationPipeline.ts (the MutationChip shape returned by applyMutationBatch — label + type discriminator emitted on ai:mutations-applied)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S6 Toast/Chip system — container + chip className, icon map table, stack behavior max 4 + 4000ms fade, copywriting chip labels)
    - .planning/phases/05-rules-engine-dice-combat/05-PATTERNS.md (MutationChipStack.tsx section — useState<MutationChip[]> + per-chip setTimeout)
  </read_first>
  <action>
    Create `src/renderer/src/components/MutationChipStack.tsx` exporting `function MutationChipStack()`. Per UI-SPEC §S6.
    Local state `const [chips, setChips] = useState<DisplayChip[]>([])` where `DisplayChip = { id: string; label: string; type: string }` (type matches 05-02's chip discriminator: hp-damage/hp-heal/xp/condition-applied/condition-removed/slot-used/slot-restored/currency-gain/currency-loss/combatant-added/combat-ended — align to whatever 05-02 emits; if 05-02 emits only a label + a coarse `type`, derive the icon from `type`).
    On mount, subscribe to the IPC event: register a handler via `window.aiStream.onMutationsApplied?.((payload: { campaignId: string; chips: Array<{ label: string; type: string }> }) => { for each incoming chip push { id: crypto.randomUUID(), ...chip } and schedule setTimeout(() => removeChip(id), 4000) })`. If the bridge method does not exist yet, add `onMutationsApplied` to the preload bridge + window typing mirroring `onFinish` (subscribe to 'ai:mutations-applied'), and tear it down on cleanup with the same removeAllListeners approach useAiStream uses. Enforce max 4 visible: when pushing, keep only the last 4 (FIFO — drop oldest).
    Render the positioned container per S6: `<div className="absolute top-0 left-0 right-0 z-50 px-3 py-2 flex flex-col gap-2 pointer-events-none" aria-label="Notifications">`. Map chips to `<div role="status" aria-live="polite" className="bg-card border border-border rounded-full px-3 py-1 text-sm text-foreground shadow-md flex items-center gap-2 animate-in slide-in-from-top-2 duration-200 self-start">{iconFor(chip.type)}<span>{chip.label}</span></div>`. `iconFor(type)` returns the lucide icon per the S6 table (ArrowDown/ArrowUp/Star/AlertCircle/CheckCircle/Zap/RotateCcw/Coins/UserPlus/Shield, each `h-3.5 w-3.5` with the table's color class). `removeChip(id)` filters it out (optionally apply `animate-out fade-out-0 duration-300` before removal via a removing flag — minimum: filter on timeout).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>MutationChipStack subscribes to ai:mutations-applied, pushes one chip per payload chip with the right icon, caps at 4 visible, auto-removes after 4s; positioned absolute pointer-events-none container; typecheck passes</done>
  <acceptance_criteria>
    - `src/renderer/src/components/MutationChipStack.tsx` exports `MutationChipStack` and subscribes to the `ai:mutations-applied` IPC event (via window.aiStream bridge), tearing down on unmount
    - each incoming chip renders as a pill with an icon mapped from its type per UI-SPEC §S6 (e.g. ArrowDown red for HP damage, Star amber for XP, Coins amber for currency)
    - chips auto-remove after ~4000ms and at most 4 are visible at once
    - the container uses `absolute ... z-50 ... pointer-events-none` and chips are `role="status" aria-live="polite"`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: campaigns setPermadeath procedure + AiSettingsModal permadeath toggle (PROG-04)</name>
  <files>src/main/trpc/routers/campaigns.ts, src/renderer/src/components/AiSettingsModal.tsx</files>
  <read_first>
    - src/main/trpc/routers/campaigns.ts (updateAiConfig procedure — the pattern to extend or sit beside; campaignsRepo update usage; schemas import)
    - src/main/db/campaignsRepo.ts (update methods — find the setter that writes campaign columns; add a permadeath setter if none fits)
    - src/renderer/src/components/AiSettingsModal.tsx (FULL — strictness field rendering + updateAiConfig mutation + pre-fill useEffect + invalidate ['campaigns','get',campaignId])
    - src/renderer/src/components/ui/checkbox.tsx (shadcn Checkbox)
    - .planning/phases/05-rules-engine-dice-combat/05-RESEARCH.md (Open Question 2 — PROG-04: permadeath_mode column + a toggle in AiSettingsModal is the low-cost satisfying approach)
  </read_first>
  <action>
    campaigns router — add a `setPermadeath` procedure (or extend `updateAiConfig` to accept an optional `permadeathMode`): `.input(z.object({ campaignId: campaignIdSchema, permadeathMode: z.boolean() })).mutation(({input}) => { campaignsRepo.setPermadeath(input.campaignId, input.permadeathMode); return { updated: true } })`. If campaignsRepo has no permadeath setter, add `setPermadeath(id: string, value: boolean): void` updating `campaigns.permadeathMode` (synchronous Drizzle update). Prefer extending updateAiConfig only if its existing input shape makes a clean optional addition; otherwise a dedicated setPermadeath keeps the diff small.
    AiSettingsModal — add a permadeath toggle below the strictness section. State `const [permadeath, setPermadeath] = useState(false)`; pre-fill from `campaign.permadeathMode` in the existing pre-fill useEffect. Render a labeled `<Checkbox checked={permadeath} onCheckedChange={(v) => setPermadeath(!!v)} />` with a `<Label>` "Permadeath mode" and a short muted description ("When enabled, a character that dies cannot be revived."). On Save (the modal's existing save path), also fire `trpc.campaigns.setPermadeath.mutate({ campaignId, permadeathMode: permadeath })` (or include permadeathMode in the updateAiConfig payload) and keep the existing `['campaigns','get',campaignId]` invalidation. Do not alter the provider/personality/strictness behavior.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>campaigns router persists permadeathMode; AiSettingsModal shows a permadeath checkbox pre-filled from the campaign and saved on Save; typecheck passes</done>
  <acceptance_criteria>
    - `src/main/trpc/routers/campaigns.ts` exposes a procedure that writes `campaigns.permadeathMode` (setPermadeath or updateAiConfig extension), Zod-validated (campaignId uuid + boolean)
    - `src/main/db/campaignsRepo.ts` has a setter that updates the permadeathMode column
    - `src/renderer/src/components/AiSettingsModal.tsx` renders a "Permadeath mode" Checkbox pre-filled from `campaign.permadeathMode` and persists it on Save
    - `npm run typecheck` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CampaignViewScreen — mount MutationChipStack + ai:mutations-applied cache invalidation</name>
  <files>src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (FULL — right-panel Tabs ~352-435 from 05-03/05-06; existing window.aiStream.onFinish useEffect ~136-144; queryClient; id; useCombatStore)
    - src/renderer/src/components/MutationChipStack.tsx (the component from Task 1)
    - src/renderer/src/hooks/useAiStream.ts (ai:mutations-applied subscription/cleanup pattern; whether the bridge method exists)
    - .planning/phases/05-rules-engine-dice-combat/05-UI-SPEC.md (§S6 anchor — MutationChipStack between TabsList and TabsContent in the right panel)
  </read_first>
  <action>
    Per UI-SPEC §S6 anchor. Mount `<MutationChipStack />` inside the right-panel Tabs region so it anchors at the top of the panel below the tab strip: wrap per S6 — place a `<div className="relative">` (or position the existing right-panel flex column relatively) and render `<MutationChipStack />` immediately after `<TabsList>` / before the first `<TabsContent>`. Import MutationChipStack.
    Add an `ai:mutations-applied` listener that invalidates the relevant TanStack Query caches so all prior-wave UIs (combat tracker, character sheet, spell slots, currency) refetch after the AI mutates state. In a `useEffect` (mirroring the existing window.aiStream.onFinish registration at ~136-144): register a handler `(payload: { campaignId: string }) => { if (payload.campaignId !== id) return; queryClient.invalidateQueries({ queryKey: ['combat','listActive', id] }); queryClient.invalidateQueries({ queryKey: ['characters','getByCampaignId', id] }); queryClient.invalidateQueries({ queryKey: ['ai','getMessages', id] }) }`. Subscribe via `window.aiStream.onMutationsApplied(handler)` (the bridge method added in Task 1) and rely on useAiStream's removeAllListeners cleanup OR add an explicit cleanup. The MutationChipStack (Task 1) owns the chip display; this useEffect owns cache invalidation — both listen to the same event, which is fine.
    Do NOT alter the existing Start/End Combat (05-03), Rest (05-06), session, or delete logic.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>MutationChipStack is mounted at the top of the right panel; an ai:mutations-applied handler invalidates combat + character + messages query caches for the active campaign; existing logic untouched; typecheck + build pass</done>
  <acceptance_criteria>
    - `src/renderer/src/screens/CampaignViewScreen.tsx` imports and renders `<MutationChipStack />` inside the right-panel Tabs region (between TabsList and TabsContent)
    - a `useEffect` subscribes to `ai:mutations-applied` and, when `payload.campaignId === id`, invalidates `['combat','listActive',id]`, `['characters','getByCampaignId',id]`, and `['ai','getMessages',id]`
    - existing combat/rest/session/delete handlers are unchanged
    - `npm run typecheck` exits 0 and `npm run build` succeeds (renderer bundles MutationChipStack + dice library; if the rpg-dice-roller ESM import fails, add `optimizeDeps.include: ['rpg-dice-roller']` to the renderer block of electron.vite.config.ts per RESEARCH.md Pitfall 6)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (ai:mutations-applied) | The chips payload crosses from main into the renderer for display; it is informational only (no DB write triggered by the renderer from it) |
| renderer → main (campaigns setPermadeath) | The permadeath boolean crosses from the untrusted renderer into a campaign-config DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Tampering | campaigns setPermadeath input | mitigate | Zod: campaignId campaignIdSchema (uuid) + permadeathMode `z.boolean()`; campaignsRepo writes a single boolean column |
| T-05-07-02 | Spoofing | renderer acting on forged ai:mutations-applied | mitigate | The handler only invalidates query caches and renders display chips — it performs no DB writes; the authoritative state comes from the subsequent tRPC refetch, so a forged event at worst triggers a harmless refetch |
| T-05-07-03 | Information Disclosure | chip labels | accept | Chip labels are gameplay deltas (HP/XP/currency/conditions) with no secrets; intended for the player |
| T-05-07-SC | Tampering | npm/shadcn installs | mitigate | No new npm packages; custom MutationChipStack container chosen over sonner (RESEARCH.md); shadcn checkbox already present |
</threat_model>

<verification>
- `npm run typecheck` green; `npm run build` succeeds (final renderer bundle including dice library)
- Manual (per VALIDATION.md): AI deals damage → "−6 HP" chip slides in and the combat/character HP updates without manual refresh; AI awards XP/currency → chips + sheet update; toggle Permadeath in AI Settings, reopen → state persists.
</verification>

<success_criteria>
- MutationChipStack renders AI-mutation chips (HP/XP/condition/slot/currency, STATE-05 currency included) driven by ai:mutations-applied (D-07)
- ai:mutations-applied invalidates combat + character + messages caches so the whole phase's UI is live
- Permadeath toggle persists campaigns.permadeathMode (PROG-04)
- Final build green
</success_criteria>

<output>
Create `.planning/phases/05-rules-engine-dice-combat/05-07-SUMMARY.md` when done
</output>
