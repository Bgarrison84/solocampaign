---
context: phase
phase: 06-quests-npcs-world-state
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-30T19:02:25.480Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Per-button TooltipProvider | CampaignViewScreen wrapped each action-bar button individually in TooltipProvider but ChatInputArea's Tooltip was outside all of them → blank-screen crash on campaign load | advisory | Always hoist TooltipProvider to the screen root (or App.tsx) rather than wrapping individual buttons — this is the standard shadcn pattern |

<current_state>
Phase 06 (quests-npcs-world-state) is at the human-verify checkpoint in plan 06-07.
Plans 06-01 through 06-06 are fully executed, merged, and committed.
Plan 06-07 automated portion is done (tsc exits 0, all 7 pitfalls verified).
The TooltipProvider crash fix is committed (e37d05b).

Two UAT items remain from the human-verify checkpoint:
- **Item 2 (NPCs)**: Pipeline code is correct; AI likely just didn't introduce NPCs in the test session. Try a longer session or explicitly enter a tavern/town.
- **Item 4 (Inspiration)**: `awardInspiration` writes to DB correctly, but `CharacterSheetTab` has NO renderer for `hasInspiration` — user can't see it. Code fix needed.
</current_state>

<completed_work>
- Plan 06-01: Migration 0006, quests/npcs/factions tables, 3 repos, 15 tests ✓
- Plan 06-02: 8 AI tool schemas, ALL_TOOLS (20), mutation pipeline, 3 tRPC routers ✓
- Plan 06-03: formatWorldStateSummary() in ContextBuilder, 33 tests ✓
- Plan 06-04: QuestsTab (read-only, status badges, empty state) ✓
- Plan 06-05: NpcTrackerTab + collapsible Factions + MutationChipStack Phase 6 chips ✓
- Plan 06-06: CampaignViewScreen 6th tab + WorldStateBar + cache invalidation ✓
- Plan 06-07 Task 1: Integration sweep — typecheck seam fixed, all 7 pitfalls verified ✓
- TooltipProvider crash fix: hoisted to screen root, removed 4 individual providers ✓
- Human UAT: Items 1 (Quests), 3 (WorldStateBar), 5 (Live refresh) all passed ✓
</completed_work>

<remaining_work>
**Immediate next task:** Add `hasInspiration` display to `CharacterSheetTab`

Files to read first:
- `src/renderer/src/components/sheet/SheetHeader.tsx` — likely best place to show inspiration (top of sheet)
- OR `src/renderer/src/components/sheet/CombatStatsSection.tsx` — also reasonable

The `character` object passed to these sub-components comes from `trpc.characters.getByCampaignId` which returns `character.resources.hasInspiration` (boolean).

Implementation: Add a small indicator — e.g. a Star icon with "Inspired" label or a Badge — visible only when `character.resources.hasInspiration === true`. Style to match the existing sheet aesthetic (muted when false, amber/gold when true).

Cache invalidation for this field already fires: `cacheInvalidationHandler` invalidates `['characters','getByCampaignId',id]` after every AI mutation batch.

**After the display fix:**
1. Re-verify item 4 with user (ask AI to award inspiration)
2. Re-verify item 2 by playing a longer session that introduces characters
3. On user approval: mark plan 06-07 complete in ROADMAP/STATE
4. Run `/gsd:code-review 06`
5. Spawn verifier to create VERIFICATION.md
6. Phase 06 closes
</remaining_work>

<decisions_made>
- TooltipProvider hoisted to CampaignViewScreen root (not per-button, not App.tsx) — smallest change that fixes all Tooltip consumers in the screen
- tsc exits 0 after 06-07 agent fixed `onMutationsApplied` payload type (`MutationsAppliedPayload` named type in aiStream.d.ts) and `'args' in tc` narrowing in llmProvider.ts
- Pre-existing better-sqlite3 ABI mismatch (12 files, 94 tests) treated as baseline noise — same count before and after Phase 6, documented in deferred-items.md
</decisions_made>

<blockers>
- **better-sqlite3 ABI mismatch** (pre-existing, not Phase 6): `node_modules/better-sqlite3` compiled for Electron ABI 145 but vitest uses Node ABI 137. Run `npm run rebuild:sqlite` to fix for standalone test runs. Does NOT affect the running app (Electron uses ABI 145 correctly).
- **Inspiration display missing**: Code fix needed before user can verify item 4.
</blockers>

## Required Reading (in order)
1. `.planning/phases/06-quests-npcs-world-state/06-07-SUMMARY.md` — what the integration sweep found and fixed
2. `.planning/phases/06-quests-npcs-world-state/06-07-PLAN.md` — the human-verify checkpoint criteria
3. `.planning/phases/06-quests-npcs-world-state/deferred-items.md` — known pre-existing issues (ABI mismatch etc.)

## Infrastructure State
- App: `npm run dev` to start; connects to configured AI provider
- Git: on `master` branch, working tree has pre-existing uncommitted shadcn UI files (checkbox.tsx, textarea.tsx, tooltip.tsx, tsconfig.json) — these are NOT Phase 6 work, do not commit them
- Tests: `npm test` — 12 files failing is baseline (ABI issue), not a regression

<context>
Phase 6 is essentially done — all the hard work is merged. The only gap is a missing UI widget (inspiration indicator) and re-verification. The inspiration pipeline (awardInspiration → characterResources.hasInspiration = true → cache invalidation) is all wired correctly; the CharacterSheetTab sub-components just never had a render path for it. It's a small additive change to SheetHeader.tsx or CombatStatsSection.tsx.

For NPCs: the pipeline, repo, tRPC router, and UI component are all correct. If the AI introduces an NPC during a session, it will appear in the NPC Tracker tab. The user may need to play a slightly longer session or explicitly go somewhere that would trigger an NPC introduction.
</context>

<next_action>
Start with: Read `src/renderer/src/components/sheet/SheetHeader.tsx` to understand the component structure, then add a hasInspiration indicator. The `character` prop already includes `resources.hasInspiration` — no new queries needed.
</next_action>
