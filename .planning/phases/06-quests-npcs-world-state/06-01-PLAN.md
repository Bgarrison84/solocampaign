---
phase: 06-quests-npcs-world-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db/schema.ts
  - resources/migrations/0006_phase6_world_state.sql
  - src/main/db/questsRepo.ts
  - src/main/db/npcsRepo.ts
  - src/main/db/factionsRepo.ts
  - src/main/db/questsRepo.test.ts
  - src/main/db/npcsRepo.test.ts
  - src/main/db/factionsRepo.test.ts
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, WORLD-03]
must_haves:
  truths:
    - "A quest row can be created and have its status updated in SQLite"
    - "An NPC row can be created and patched (description/relationship/factionName) in SQLite"
    - "A faction can be upserted by (campaignId, name) — create-or-update tier"
    - "World-state columns exist and are nullable on the campaigns table"
  artifacts:
    - path: "resources/migrations/0006_phase6_world_state.sql"
      provides: "quests + npcs + factions tables, world-state columns on campaigns"
      contains: "CREATE TABLE `quests`"
    - path: "src/main/db/questsRepo.ts"
      provides: "quest CRUD repo"
      exports: ["questsRepo"]
    - path: "src/main/db/npcsRepo.ts"
      provides: "npc CRUD repo"
      exports: ["npcsRepo"]
    - path: "src/main/db/factionsRepo.ts"
      provides: "faction upsert repo"
      exports: ["factionsRepo"]
  key_links:
    - from: "src/main/db/factionsRepo.ts"
      to: "factions table unique(campaign_id, name)"
      via: "onConflictDoUpdate target"
      pattern: "onConflictDoUpdate"
---

<objective>
Lay the Phase 6 data foundation: migration 0006, three new Drizzle tables (quests, npcs, factions) plus nullable world-state columns on campaigns, three repos (questsRepo, npcsRepo, factionsRepo), and RED test stubs that downstream waves turn GREEN.

Purpose: Every Phase 6 mutation, tRPC query, and context-injection feature reads/writes these tables. No UI or AI wiring can exist until the schema and repos are in place. This is the clean Wave 0 dependency boundary identified in RESEARCH.md.
Output: Migration SQL file, extended schema.ts, three repo files, three repo test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-quests-npcs-world-state/06-RESEARCH.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md

<interfaces>
Existing patterns this plan replicates (read the files in read_first for full detail):

- All tables use `text('id').primaryKey()` with `randomUUID()` (node:crypto). No integer PKs.
- `campaigns` table currently ends at line 24 with `permadeathMode`. World-state columns append here.
- Repos are plain object literals (`export const xRepo = { ... }`), synchronous, using `getDb()` from './index', Drizzle `.run()` / `.get()` / `.all()`. See sessionsRepo.ts.
- Faction upsert uses Drizzle `.insert().onConflictDoUpdate({ target: [factions.campaignId, factions.name], set: { tier } })`.
- Existing migrations live in `resources/migrations/` numbered 0000–0005. Next is 0006. `migrate()` runs them at startup.
- Test harness: vitest + in-memory SQLite with `migrate()` using MIGRATIONS_FOLDER — copy the harness from src/main/ai/mutationPipeline.test.ts lines 29-39.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0006 SQL + schema.ts table definitions</name>
  <files>resources/migrations/0006_phase6_world_state.sql, src/main/db/schema.ts</files>
  <read_first>
    - src/main/db/schema.ts (current campaigns table lines 4-24; existing sqliteTable patterns for sessions/combatants; the `unique()` import usage)
    - resources/migrations/0005_sessions_unique_session_number.sql (migration file format + the unique-index syntax to mirror)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Code Examples" (verbatim schema for quests, npcs, factions; world-state ALTER TABLE block)
  </read_first>
  <behavior>
    - quests table accepts a row with id, campaignId, name, description, status='Active', createdAt
    - npcs table accepts a row with id, campaignId, name, description, relationship='Unknown', factionName nullable, createdAt
    - factions table rejects a second row with the same (campaignId, name) pair — UNIQUE constraint enforced
    - campaigns table accepts NULL for all four world-state columns (no NOT NULL)
  </behavior>
  <action>
    Write `resources/migrations/0006_phase6_world_state.sql` containing: CREATE TABLE `quests` (id text PK, campaign_id text NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE, name text NOT NULL, description text NOT NULL DEFAULT '', status text NOT NULL DEFAULT 'Active', created_at integer NOT NULL DEFAULT (unixepoch() * 1000)); CREATE TABLE `npcs` (same id/campaign_id/created_at columns plus name text NOT NULL, description text NOT NULL DEFAULT '', relationship text NOT NULL DEFAULT 'Unknown', faction_name text nullable); CREATE TABLE `factions` (id text PK, campaign_id text NOT NULL REFERENCES campaigns(id) ON DELETE CASCADE, name text NOT NULL, tier text NOT NULL DEFAULT 'Neutral', created_at integer NOT NULL DEFAULT (unixepoch() * 1000)); CREATE UNIQUE INDEX `factions_campaign_name_unique` ON `factions` (`campaign_id`,`name`); and four ALTER TABLE `campaigns` ADD statements: `world_time_of_day` text, `world_day_number` integer, `world_season` text, `world_location_path` text (all nullable). Mirror the SQL formatting style of 0005.

    In `src/main/db/schema.ts`: add the four world-state columns to the `campaigns` sqliteTable definition (after permadeathMode, before the closing `})`): `worldTimeOfDay: text('world_time_of_day')`, `worldDayNumber: integer('world_day_number')`, `worldSeason: text('world_season')`, `worldLocationPath: text('world_location_path')`. Then add three new `sqliteTable` definitions: `quests`, `npcs`, `factions` exactly per the RESEARCH § Code Examples blocks. The factions table uses the third `(table) => ({ ... })` arg with `unique('factions_campaign_name_unique').on(table.campaignId, table.name)` — import `unique` from 'drizzle-orm/sqlite-core' if not already imported. Export inferred row types: `export type Quest = typeof quests.$inferSelect`, `export type Npc = typeof npcs.$inferSelect`, `export type Faction = typeof factions.$inferSelect` following the existing `export type Session = ...` pattern.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
    - resources/migrations/0006_phase6_world_state.sql contains the exact strings `CREATE TABLE \`quests\``, `CREATE TABLE \`npcs\``, `CREATE TABLE \`factions\``, and `CREATE UNIQUE INDEX \`factions_campaign_name_unique\``
    - 0006 SQL contains `ALTER TABLE \`campaigns\` ADD \`world_time_of_day\``, `world_day_number`, `world_season`, `world_location_path`
    - src/main/db/schema.ts exports `quests`, `npcs`, `factions` sqliteTable objects and types `Quest`, `Npc`, `Faction`
    - schema.ts campaigns table includes `worldTimeOfDay`, `worldDayNumber`, `worldSeason`, `worldLocationPath` columns
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
  </acceptance_criteria>
  <done>Migration 0006 exists with three tables + unique index + four campaigns columns; schema.ts defines all three tables and the world-state columns; TypeScript compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: questsRepo, npcsRepo, factionsRepo + RED test stubs</name>
  <files>src/main/db/questsRepo.ts, src/main/db/npcsRepo.ts, src/main/db/factionsRepo.ts, src/main/db/questsRepo.test.ts, src/main/db/npcsRepo.test.ts, src/main/db/factionsRepo.test.ts</files>
  <read_first>
    - src/main/db/sessionsRepo.ts (canonical repo shape: object literal, getDb(), randomUUID, .run()/.get()/.all(), create-then-select-back pattern)
    - src/main/ai/mutationPipeline.test.ts lines 1-45 (the exact in-memory SQLite + migrate() test harness to copy: MIGRATIONS_FOLDER, beforeEach setup, how a campaign row is seeded)
    - src/main/db/schema.ts (the quests/npcs/factions tables + Quest/Npc/Faction types created in Task 1)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Code Examples" (factionsRepo upsert with onConflictDoUpdate)
    - .planning/phases/06-quests-npcs-world-state/06-VALIDATION.md (per-task verification map: questsRepo rejects empty name; npcsRepo patch; factionsRepo upserts on conflict)
  </read_first>
  <behavior>
    - questsRepo.create({campaignId, name, description}) returns a Quest with status 'Active' and a non-empty id
    - questsRepo.updateStatus(questId, 'Completed') changes that row's status; questsRepo.list(campaignId) returns it
    - questsRepo.list returns quests ordered by createdAt ASC (chronological — D-02)
    - npcsRepo.create returns an Npc; npcsRepo.patch(npcId, {relationship:'Friendly'}) updates only provided fields, leaving description unchanged
    - npcsRepo.list returns NPCs ordered by createdAt ASC (D-06)
    - factionsRepo.upsert called twice with same (campaignId, name) but different tier results in ONE row with the latest tier (no duplicate)
    - factionsRepo.list returns factions for a campaign
  </behavior>
  <action>
    Create `questsRepo.ts` exporting `questsRepo` object with: `create(input: { campaignId, name, description })` → randomUUID id, status defaults 'Active', insert then select-back, return Quest; `list(campaignId): Quest[]` ordered `asc(quests.createdAt)`; `updateStatus(questId: string, status: string)` → Drizzle update set status where id eq questId, `.run()`.

    Create `npcsRepo.ts` exporting `npcsRepo` with: `create(input: { campaignId, name, description, relationship, factionName? })` → insert then select-back; `list(campaignId): Npc[]` ordered `asc(npcs.createdAt)`; `patch(npcId: string, fields: { description?, relationship?, factionName? })` → build a partial set object from only the defined fields and run a single Drizzle update where id eq npcId (skip update if no fields provided).

    Create `factionsRepo.ts` exporting `factionsRepo` with: `upsert(input: { campaignId, name, tier }): Faction` using `db.insert(factions).values({ id: randomUUID(), campaignId, name, tier }).onConflictDoUpdate({ target: [factions.campaignId, factions.name], set: { tier } }).run()` then select-back the row by (campaignId, name) using `and(eq(...), eq(...))` and return it (throw if null); `list(campaignId): Faction[]` ordered `asc(factions.createdAt)`.

    Create the three `.test.ts` files using the in-memory SQLite + migrate() harness copied from mutationPipeline.test.ts. Each test file seeds a campaign row, then asserts the behaviors listed above. These are real assertions (not placeholder stubs) — they exercise the repo methods directly and must PASS once the repos are implemented.
  </action>
  <verify>
    <automated>npx vitest run src/main/db/questsRepo.test.ts src/main/db/npcsRepo.test.ts src/main/db/factionsRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/db/questsRepo.ts` exports `questsRepo` with `create`, `list`, `updateStatus`
    - `src/main/db/npcsRepo.ts` exports `npcsRepo` with `create`, `list`, `patch`
    - `src/main/db/factionsRepo.ts` exports `factionsRepo` with `upsert`, `list`
    - factionsRepo.ts source contains `onConflictDoUpdate`
    - `npx vitest run src/main/db/questsRepo.test.ts src/main/db/npcsRepo.test.ts src/main/db/factionsRepo.test.ts` exits 0
    - factionsRepo.test.ts asserts that two upserts with the same (campaignId, name) yield exactly one row
  </acceptance_criteria>
  <done>Three repos exist following the sessionsRepo pattern; three test files pass; faction upsert proven idempotent on (campaignId, name).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI tool args → repo write | AI-generated strings reach DB inserts via downstream mutationPipeline (Wave 1); repos must accept already-validated args |
| migration → existing campaigns rows | ALTER TABLE on a populated table must not break existing rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | factions upsert | mitigate | UNIQUE(campaign_id, name) index prevents duplicate faction rows from repeated updateFaction calls (Pitfall 5) |
| T-06-01-02 | Tampering | campaigns ALTER TABLE | mitigate | All four world-state columns are nullable with no default-breaking constraints; existing rows get NULL |
| T-06-01-03 | Denial of Service | quests/npcs unbounded growth | accept | Repo layer does not cap row counts; UI render caps and context-injection caps (Wave 2/3) are the control point; single-user local DB |
| T-06-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in Phase 6 (RESEARCH § Package Legitimacy Audit confirms zero installs) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.node.json` exits 0
- `npx vitest run src/main/db/questsRepo.test.ts src/main/db/npcsRepo.test.ts src/main/db/factionsRepo.test.ts` exits 0
- Migration 0006 file present in resources/migrations/
</verification>

<success_criteria>
- Migration 0006 creates quests, npcs, factions tables + factions unique index + four nullable campaigns world-state columns
- schema.ts defines all three tables, the world-state columns, and exports Quest/Npc/Faction types
- questsRepo, npcsRepo, factionsRepo implemented and unit-tested
- All repo tests green
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-01-SUMMARY.md` when done
</output>
