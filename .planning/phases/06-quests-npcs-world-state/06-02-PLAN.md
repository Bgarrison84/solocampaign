---
phase: 06-quests-npcs-world-state
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/main/ai/toolSchemas.ts
  - src/main/ai/mutationPipeline.ts
  - src/main/index.ts
  - src/main/db/campaignsRepo.ts
  - src/main/trpc/routers/quests.ts
  - src/main/trpc/routers/npcs.ts
  - src/main/trpc/routers/worldState.ts
  - src/main/trpc/router.ts
  - src/main/ai/toolSchemas.test.ts
  - src/main/ai/mutationPipeline.test.ts
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, WORLD-03, PARTY-03]
must_haves:
  truths:
    - "The AI can call any of 8 Phase 6 tools and the mutation pipeline applies it to the DB"
    - "Phase 5 tools still work — ALL_TOOLS includes both Phase 5 and Phase 6 tools"
    - "streamText receives ALL_TOOLS so the AI is offered the Phase 6 tools"
    - "addQuest creates a quest with status 'Active'; awardInspiration flips hasInspiration true"
    - "updateFaction upserts a faction; updateWorldTime/updateLocation write to campaigns world columns"
    - "Renderer can query quests.list, npcs.list, factions.list, worldState.get via tRPC"
  artifacts:
    - path: "src/main/ai/toolSchemas.ts"
      provides: "8 Phase 6 Zod schemas + PHASE6_TOOLS + ALL_TOOLS"
      exports: ["PHASE6_TOOLS", "ALL_TOOLS", "addQuestSchema", "awardInspirationSchema"]
    - path: "src/main/trpc/routers/quests.ts"
      provides: "quests.list procedure"
      exports: ["questsRouter"]
    - path: "src/main/trpc/routers/npcs.ts"
      provides: "npcs.list procedure"
      exports: ["npcsRouter"]
    - path: "src/main/trpc/routers/worldState.ts"
      provides: "worldState.get + factions.list procedures"
      exports: ["worldStateRouter"]
  key_links:
    - from: "src/main/index.ts"
      to: "ALL_TOOLS"
      via: "streamText tools option"
      pattern: "tools: ALL_TOOLS"
    - from: "src/main/trpc/router.ts"
      to: "questsRouter, npcsRouter, worldStateRouter"
      via: "router registration"
      pattern: "quests: questsRouter"
    - from: "src/main/ai/mutationPipeline.ts"
      to: "questsRepo / npcsRepo / factionsRepo / campaignsRepo"
      via: "8 new switch cases"
      pattern: "case 'addQuest'"
---

<objective>
Wire the 8 Phase 6 AI tools end-to-end through the main process: Zod schemas + tool() registrations + PHASE6_TOOLS + ALL_TOOLS in toolSchemas.ts; 8 new switch cases in mutationPipeline.ts; the MutationChip type extension; world-state write methods on campaignsRepo; three tRPC routers (quests, npcs, worldState) registered in router.ts; and the critical streamText swap from PHASE5_TOOLS to ALL_TOOLS in index.ts.

Purpose: This is the AI mutation contract extension. Without it, the AI is never offered the Phase 6 tools (Pitfall 2) and the renderer has nothing to query. Every Phase 6 requirement's write path passes through this plan.
Output: Extended toolSchemas.ts + mutationPipeline.ts + campaignsRepo.ts + index.ts, three new router files, updated router.ts, extended tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-quests-npcs-world-state/06-RESEARCH.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md
@.planning/phases/06-quests-npcs-world-state/06-01-SUMMARY.md

<interfaces>
From src/main/ai/toolSchemas.ts (Phase 5 — extend, do not replace):
```
export const PHASE5_TOOLS = { updateHp, applyCondition, ... showDiceRoll } as const satisfies ToolSet
// each tool: export const xTool = tool({ description, inputSchema: xSchema }) — NO execute property
```

From src/main/ai/mutationPipeline.ts:
```
export interface MutationChip { id: string; label: string; type: 'hp'|'xp'|'condition'|'slot'|'currency'|'combat'|'rest' }
function applyOneTool(toolName, args, campaignId, sessionId, acc): void   // switch on toolName
function chipId(): string
function logEvent(campaignId, sessionId, eventType, payload): void
function resolvePlayerCharacterId(campaignId): string | undefined
// case pattern: safeParse → if(!r.success) log.warn+return → repo call → acc.chips.push → logEvent → return
```

From src/main/index.ts (around line 368):
```
import { PHASE5_TOOLS } from './ai/toolSchemas'
// ...
}, { abortSignal: ..., tools: PHASE5_TOOLS, onToolCallsFinish: async (toolCalls) => { ... applyMutationBatch(...) } })
```

From src/main/trpc/router.ts — flat router map; add quests/npcs/worldState keys.
From src/main/trpc/routers/combat.ts — router pattern: `t.router({ list: t.procedure.input(z.object({ campaignId: campaignIdSchema })).query(({input}) => repo.list(input.campaignId)) })`. `campaignIdSchema` imported from '../schemas'.
From src/main/db/schema.ts — characterResources has hasInspiration boolean; campaigns has worldTimeOfDay/worldDayNumber/worldSeason/worldLocationPath (added in 06-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 8 Phase 6 Zod schemas + PHASE6_TOOLS + ALL_TOOLS</name>
  <files>src/main/ai/toolSchemas.ts, src/main/ai/toolSchemas.test.ts</files>
  <read_first>
    - src/main/ai/toolSchemas.ts (full Phase 5 file — schema style, tool() registration style, PHASE5_TOOLS export, ToolSet typing, the "NO execute" rule)
    - src/main/ai/toolSchemas.test.ts (existing test structure; how PHASE5_TOOLS key count is asserted)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Pattern 2" (verbatim 8 schemas + PHASE6_TOOLS + ALL_TOOLS)
    - .planning/phases/06-quests-npcs-world-state/06-CONTEXT.md D-14 (exact tool argument shapes), D-16 (which tools chip vs silent)
  </read_first>
  <behavior>
    - addQuestSchema accepts { name (1-200 chars), description (max 500) }; rejects empty name
    - updateQuestStatusSchema accepts { questId, status in ['Active','Completed','Failed'] }; rejects an invalid status
    - addNpcSchema accepts { name (1-100), description (max 300), relationship in 4-enum, factionName? }
    - updateNpcSchema accepts { npcId, description?, relationship?, factionName? } (all optional patches)
    - updateFactionSchema accepts { factionName (1-100), tier in 5-enum }
    - updateWorldTimeSchema accepts { timeOfDay in 4-enum, dayNumber int 1-99999, season in 4-enum }; rejects dayNumber 0
    - updateLocationSchema accepts { path: string[] } with 1-10 segments, each max 100 chars
    - awardInspirationSchema accepts { characterId: string }
    - PHASE6_TOOLS has exactly 8 keys; ALL_TOOLS has 20 keys (12 Phase 5 + 8 Phase 6)
    - No tool registration has an execute property
  </behavior>
  <action>
    Append to `src/main/ai/toolSchemas.ts` (do NOT modify PHASE5_TOOLS): the 8 Zod schemas exactly per RESEARCH § Pattern 2 — `addQuestSchema`, `updateQuestStatusSchema`, `addNpcSchema`, `updateNpcSchema`, `updateFactionSchema`, `updateWorldTimeSchema`, `updateLocationSchema`, `awardInspirationSchema` (string `.min(1).max(N)` bounds, `z.enum([...])` for status/relationship/tier/timeOfDay/season, `z.array(z.string().max(100)).min(1).max(10)` for path, `z.number().int().min(1).max(99999)` for dayNumber). Per RESEARCH, `questId`/`npcId`/`characterId` are `z.string()` (not `.uuid()` — repos store randomUUID text but the AI may echo IDs and a strict uuid guard would reject otherwise-valid references; safeParse failure is silent so loosening to z.string() avoids silent drops — match the Phase 5 convention where characterId is `z.string()`). Then 8 `tool({ description, inputSchema })` registrations with NO execute property; descriptions per D-14 semantics (e.g. addQuest "Add a new quest to the quest log as it emerges in the story", awardInspiration "Award Inspiration to the player's character for exceptional roleplay"). Export `PHASE6_TOOLS` mapping the 8 tool names to their tool objects `as const satisfies ToolSet`. Export `ALL_TOOLS = { ...PHASE5_TOOLS, ...PHASE6_TOOLS } as const satisfies ToolSet`.

    Extend `src/main/ai/toolSchemas.test.ts`: add a describe block asserting each of the 8 schemas accepts a valid example and rejects one invalid case (empty quest name, dayNumber 0, bad status enum), and assert `Object.keys(PHASE6_TOOLS).length === 8` and `Object.keys(ALL_TOOLS).length === 20`, and assert no value in ALL_TOOLS has an `execute` property.
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/toolSchemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - toolSchemas.ts exports `addQuestSchema`, `updateQuestStatusSchema`, `addNpcSchema`, `updateNpcSchema`, `updateFactionSchema`, `updateWorldTimeSchema`, `updateLocationSchema`, `awardInspirationSchema`, `PHASE6_TOOLS`, `ALL_TOOLS`
    - toolSchemas.ts still exports `PHASE5_TOOLS` unchanged (12 entries)
    - toolSchemas.test.ts asserts `Object.keys(PHASE6_TOOLS).length === 8` and `Object.keys(ALL_TOOLS).length === 20`
    - `npx vitest run src/main/ai/toolSchemas.test.ts` exits 0
  </acceptance_criteria>
  <done>8 Phase 6 schemas + tools exported; ALL_TOOLS combines both phases (20 tools); tests green; no execute property anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: campaignsRepo world-state writes + 8 mutationPipeline cases + MutationChip extension + ALL_TOOLS in streamText</name>
  <files>src/main/db/campaignsRepo.ts, src/main/ai/mutationPipeline.ts, src/main/index.ts, src/main/ai/mutationPipeline.test.ts</files>
  <read_first>
    - src/main/ai/mutationPipeline.ts (full file — applyOneTool switch, MutationChip type at line 40-44, chipId/logEvent/resolvePlayerCharacterId helpers, the awardXp/addCombatant cases as templates, the synchronous-transaction WR-07 constraint)
    - src/main/db/campaignsRepo.ts (current methods + getDb usage — add world-state write methods here per RESEARCH Open Question 1)
    - src/main/index.ts lines 360-390 (the streamText call: `tools: PHASE5_TOOLS` and the onToolCallsFinish callback + the import line 14)
    - src/main/ai/mutationPipeline.test.ts (test harness + how an existing case like awardXp is asserted)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Pattern 3" (verbatim addQuest/updateQuestStatus/awardInspiration cases), § "Pitfall 2" (ALL_TOOLS), § "Pitfall 7" (awardInspiration characterId fallback)
    - .planning/phases/06-quests-npcs-world-state/06-CONTEXT.md D-16 (chip vs silent matrix), D-05 (quest failure silent)
  </read_first>
  <behavior>
    - applyMutationBatch with [{addQuest, {name:'X', description:'Y'}}] creates one quest row (status Active) and pushes a chip of type 'quest'
    - applyMutationBatch with updateQuestStatus status 'Completed' pushes a 'quest_complete' chip; status 'Failed' pushes NO chip (D-05)
    - applyMutationBatch with addNpc creates an npc row and pushes an 'npc' chip; updateNpc patches and pushes NO chip (D-10)
    - applyMutationBatch with updateFaction upserts a faction and pushes NO chip (D-16)
    - applyMutationBatch with updateWorldTime writes worldTimeOfDay/worldDayNumber/worldSeason to the campaigns row, NO chip
    - applyMutationBatch with updateLocation writes worldLocationPath as JSON array text to campaigns, NO chip
    - applyMutationBatch with awardInspiration flips characterResources.hasInspiration to true and pushes an 'inspiration' chip; if characterId does not match, falls back to resolvePlayerCharacterId(campaignId)
  </behavior>
  <action>
    In `src/main/db/campaignsRepo.ts` add two methods: `updateWorldTime(campaignId, { timeOfDay, dayNumber, season })` → Drizzle update set worldTimeOfDay/worldDayNumber/worldSeason where id eq campaignId, `.run()`; `updateLocation(campaignId, path: string[])` → update set worldLocationPath = `JSON.stringify(path)` where id eq campaignId, `.run()`. Also add `getWorldState(campaignId)` returning `{ worldTimeOfDay, worldDayNumber, worldSeason, worldLocationPath }` selected from campaigns (used by Wave 2 contextBuilder and the worldState router).

    In `src/main/ai/mutationPipeline.ts`: extend the `MutationChip` `type` union to add `'quest' | 'quest_complete' | 'npc' | 'inspiration'`. Import the 8 new schemas + questsRepo, npcsRepo, factionsRepo, campaignsRepo. Add 8 `case` blocks in `applyOneTool` following the exact safeParse→guard→repo→chip→logEvent pattern (RESEARCH § Pattern 3 is verbatim for addQuest/updateQuestStatus/awardInspiration). Chip rules per D-16: addQuest → chip type 'quest' label `Quest: ${name}`; updateQuestStatus → chip 'quest_complete' label 'Quest complete!' ONLY when status==='Completed' (Failed/Active silent); addNpc → chip 'npc' label `${name} encountered`; updateNpc → no chip; updateFaction → call factionsRepo.upsert({campaignId, name: factionName, tier}), no chip; updateWorldTime → campaignsRepo.updateWorldTime(campaignId, ...), no chip; updateLocation → campaignsRepo.updateLocation(campaignId, path), no chip; awardInspiration → resolve charId = `r.data.characterId` then verify it has a characterResources row; if not, fall back to `resolvePlayerCharacterId(campaignId)` (Pitfall 7), update characterResources.hasInspiration=true, chip 'inspiration' label 'Inspiration awarded!'. Every case calls logEvent with an event type (quest_added, quest_status_changed, npc_added, npc_updated, faction_updated, world_time_updated, location_updated, inspiration_awarded). All callees remain synchronous (WR-07).

    In `src/main/index.ts`: change the import on line 14 from `PHASE5_TOOLS` to `ALL_TOOLS` and change `tools: PHASE5_TOOLS` (line ~368) to `tools: ALL_TOOLS`. Grep the whole src/main/ tree first to confirm no other PHASE5_TOOLS reference remains in production code (test files may keep it).

    Extend `src/main/ai/mutationPipeline.test.ts` with assertions for each of the 8 behaviors listed above using the existing harness (seed a campaign + a character with a characterResources row for the inspiration test).
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/mutationPipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/ai/mutationPipeline.ts MutationChip type union includes `'quest'`, `'quest_complete'`, `'npc'`, `'inspiration'`
    - mutationPipeline.ts applyOneTool contains `case 'addQuest'`, `case 'updateQuestStatus'`, `case 'addNpc'`, `case 'updateNpc'`, `case 'updateFaction'`, `case 'updateWorldTime'`, `case 'updateLocation'`, `case 'awardInspiration'`
    - src/main/index.ts contains `tools: ALL_TOOLS` and does NOT contain `tools: PHASE5_TOOLS`
    - `grep -rn "tools: PHASE5_TOOLS" src/main/` returns no production matches
    - campaignsRepo.ts exports `updateWorldTime`, `updateLocation`, `getWorldState`
    - `npx vitest run src/main/ai/mutationPipeline.test.ts` exits 0
  </acceptance_criteria>
  <done>8 pipeline cases apply correctly with the right chip behavior; campaignsRepo writes world state; streamText receives ALL_TOOLS (Pitfall 2 closed); tests green.</done>
</task>

<task type="auto">
  <name>Task 3: quests / npcs / worldState tRPC routers + register in router.ts</name>
  <files>src/main/trpc/routers/quests.ts, src/main/trpc/routers/npcs.ts, src/main/trpc/routers/worldState.ts, src/main/trpc/router.ts</files>
  <read_first>
    - src/main/trpc/routers/combat.ts (router pattern: t.router, t.procedure, campaignIdSchema, .query/.mutation, repo delegation)
    - src/main/trpc/router.ts (the flat router map to register into)
    - src/main/trpc/schemas.ts (campaignIdSchema definition)
    - src/main/db/questsRepo.ts, npcsRepo.ts, factionsRepo.ts, campaignsRepo.ts (the repo methods the routers delegate to)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Pattern 1" (verbatim router + registration), § "Pitfall 1" (must register in router.ts)
  </read_first>
  <action>
    Create `src/main/trpc/routers/quests.ts` exporting `questsRouter = t.router({ list: t.procedure.input(z.object({ campaignId: campaignIdSchema })).query(({input}) => questsRepo.list(input.campaignId)) })`. Create `src/main/trpc/routers/npcs.ts` exporting `npcsRouter` with a `list` procedure delegating to `npcsRepo.list`. Create `src/main/trpc/routers/worldState.ts` exporting `worldStateRouter = t.router({ get: t.procedure.input(z.object({ campaignId: campaignIdSchema })).query(({input}) => campaignsRepo.getWorldState(input.campaignId)), factions: t.procedure.input(z.object({ campaignId: campaignIdSchema })).query(({input}) => factionsRepo.list(input.campaignId)) })`. Note: the UI-SPEC's NpcTrackerTab queries `trpc.factions.list` — to keep that key working, ALSO add a `factionsRouter` (either as its own file or inline) exporting a `list` procedure, and register it as `factions:` in router.ts so `trpc.factions.list.query({ campaignId })` resolves. In `src/main/trpc/router.ts`: import questsRouter, npcsRouter, worldStateRouter, factionsRouter and add keys `quests: questsRouter, npcs: npcsRouter, worldState: worldStateRouter, factions: factionsRouter` to the router map (Pitfall 1).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json</automated>
  </verify>
  <acceptance_criteria>
    - router.ts imports and registers `quests`, `npcs`, `worldState`, `factions` keys
    - quests.ts exports `questsRouter` with a `list` query
    - npcs.ts exports `npcsRouter` with a `list` query
    - worldState.ts exports `worldStateRouter` with `get` and a factions list path; a `factions.list` query resolves via the registered factions router
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
  </acceptance_criteria>
  <done>Four router registrations live in router.ts; renderer can call trpc.quests.list, trpc.npcs.list, trpc.factions.list, trpc.worldState.get; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI tool args → mutationPipeline | Untrusted AI-generated strings/numbers cross into DB writes |
| renderer → tRPC routers | Renderer input (campaignId) crosses into main-process queries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering / DoS | 8 tool schemas | mitigate | Zod .max() on name(200)/description(500)/path segments(100, max 10) bounds oversized AI text (RESEARCH security table) |
| T-06-02-02 | Spoofing | updateQuestStatus / updateNpc fake UUID | mitigate | Drizzle UPDATE silently updates 0 rows on a non-matching id; pipeline continues; logEvent records attempt; no crash |
| T-06-02-03 | Tampering | awardInspiration wrong characterId | mitigate | Fallback to resolvePlayerCharacterId(campaignId) when supplied id has no characterResources row (Pitfall 7) |
| T-06-02-04 | Tampering | updateLocation JSON storage | mitigate | path validated as z.array(z.string().max(100)); stored via JSON.stringify; consumers guard JSON.parse with try/catch (Pitfall 4) |
| T-06-02-05 | Input Validation | tRPC router inputs | mitigate | Every procedure validates campaignId with campaignIdSchema (V5 ASVS) |
| T-06-02-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run src/main/ai/toolSchemas.test.ts src/main/ai/mutationPipeline.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.node.json` exits 0
- `grep -rn "tools: PHASE5_TOOLS" src/main/` returns no production matches (only ALL_TOOLS)
</verification>

<success_criteria>
- 8 Phase 6 tools defined, combined into ALL_TOOLS, and passed to streamText (Pitfall 2 closed)
- 8 mutationPipeline cases apply with correct chip/silent behavior per D-16
- campaignsRepo writes world time + location
- quests/npcs/worldState/factions tRPC routers registered (Pitfall 1 closed)
- All tests green
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-02-SUMMARY.md` when done
</output>
