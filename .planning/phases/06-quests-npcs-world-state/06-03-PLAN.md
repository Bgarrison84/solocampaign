---
phase: 06-quests-npcs-world-state
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/main/ai/contextBuilder.ts
  - src/main/ai/contextBuilder.test.ts
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, WORLD-03, PARTY-03]
must_haves:
  truths:
    - "The AI system prompt includes a world-state summary block: active quests with IDs, NPCs with IDs+relationship, factions with tier, current time, current location"
    - "The toolDescriptionsBlock describes all 8 Phase 6 tools so JSON-tail (non-tool-calling) providers can drive them"
    - "The player characterId is included in the world-state summary so awardInspiration targets correctly"
    - "The NPC list in the summary is capped (20) to avoid token bloat"
  artifacts:
    - path: "src/main/ai/contextBuilder.ts"
      provides: "formatWorldStateSummary() + extended toolDescriptionsBlock"
      contains: "formatWorldStateSummary"
  key_links:
    - from: "src/main/ai/contextBuilder.ts buildContext()"
      to: "formatWorldStateSummary(campaignId)"
      via: "system prompt assembly after toolDescriptionsBlock"
      pattern: "formatWorldStateSummary"
---

<objective>
Extend ContextBuilder so the AI is aware of current world state and the Phase 6 tools. Add `formatWorldStateSummary()` (active quests with IDs, NPCs with IDs + relationship, factions with tier, time, location, and the player characterId), inject it into the system prompt immediately after the tool descriptions block, and extend `toolDescriptionsBlock` with natural-language descriptions of all 8 Phase 6 tools for JSON-tail fallback providers.

Purpose: Without world-state injection the AI cannot reference the quest/NPC IDs it needs for updateQuestStatus/updateNpc, and without the extended toolDescriptionsBlock, local LLMs (which use JSON-tail fallback, not native tool calls) can never populate quests/NPCs (Pitfall 6). This closes the read-back loop (D-18).
Output: Extended contextBuilder.ts + contextBuilder.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-quests-npcs-world-state/06-RESEARCH.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md
@.planning/phases/06-quests-npcs-world-state/06-02-SUMMARY.md

<interfaces>
From src/main/ai/contextBuilder.ts (the assembly to extend):
```
const toolDescriptionsBlock = `Game mechanics you control ... - Use \`updateHp\` ... - Use \`processRest\` ...
If your provider does not support native tool calls, append a single fenced block ...`.trim()

export function buildContext(args): BuiltContext {
  // ... character = charactersRepo.getByCampaignId(campaignId)
  const systemPrompt =
    [preamble, strictnessDirective, personality, characterSummaryBlock].filter(...).join('\n\n')
    + '\n\n' + toolDescriptionsBlock     // ← inject world-state AFTER this
    + referenceDocBlock + l3Block + l2Block + sessionContextBlock
}
```

Repos available (from 06-01 / 06-02):
- questsRepo.list(campaignId): Quest[]  (Quest has id, name, status)
- npcsRepo.list(campaignId): Npc[]       (Npc has id, name, relationship)
- factionsRepo.list(campaignId): Faction[] (Faction has name, tier)
- campaignsRepo.getWorldState(campaignId): { worldTimeOfDay, worldDayNumber, worldSeason, worldLocationPath }
- charactersRepo.getByCampaignId(campaignId): character with .id  (already imported in contextBuilder)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: formatWorldStateSummary() + toolDescriptionsBlock extension + system prompt injection</name>
  <files>src/main/ai/contextBuilder.ts, src/main/ai/contextBuilder.test.ts</files>
  <read_first>
    - src/main/ai/contextBuilder.ts (full file — toolDescriptionsBlock constant lines 154-171, buildContext assembly lines 277-288, formatCharacterSummary as the function-style template, the charactersRepo import)
    - src/main/ai/contextBuilder.test.ts (existing test structure + harness)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Pattern 4" (verbatim formatWorldStateSummary + injection point), § "Pitfall 6" (toolDescriptionsBlock additions), § "Pitfall 7" (include characterId)
    - .planning/phases/06-quests-npcs-world-state/06-CONTEXT.md D-18 (what to inject), § specifics (the compact summary block format)
  </read_first>
  <behavior>
    - formatWorldStateSummary returns '' when there are no quests, no NPCs, no factions, and no world time/location set
    - When a quest with status 'Active' exists, the summary contains a line with that quest's id and name; non-active quests are excluded from the active-quests list
    - When NPCs exist, the summary lists each with [ID: <id>] <name> (<relationship>), capped at 20 entries
    - When factions exist, the summary lists each faction name and tier
    - When world time is set, the summary contains a "Time:" line; when location is set, a "Location:" line with segments joined by ' > '
    - The summary includes the player characterId explicitly (a "Player character ID:" line) so awardInspiration can target it (Pitfall 7)
    - Newlines inside location segments are stripped before rendering (security — RESEARCH threat table)
    - buildContext's systemPrompt contains the world-state summary text, positioned after toolDescriptionsBlock and before referenceDocBlock
    - toolDescriptionsBlock contains the literal substrings `addQuest`, `updateQuestStatus`, `addNpc`, `updateNpc`, `updateFaction`, `updateWorldTime`, `updateLocation`, `awardInspiration`
  </behavior>
  <action>
    In `src/main/ai/contextBuilder.ts`: add a module-level function `formatWorldStateSummary(campaignId: string): string` per RESEARCH § Pattern 4 — read questsRepo.list, npcsRepo.list, factionsRepo.list, campaignsRepo.getWorldState; build a `lines: string[]` starting with 'Current world state:'. Add a 'Player character ID:' line from `charactersRepo.getByCampaignId(campaignId)?.id` (Pitfall 7) so the AI knows the awardInspiration target. Add Time line (`- Time: ${timeOfDay}, Day ${dayNumber}, ${season}`) only if worldTimeOfDay or worldDayNumber present. Add Location line by `JSON.parse(worldLocationPath)` guarded in try/catch (fallback []) joined with ' > '; strip `\n`/`\r` from each segment before join (security). Add Active quests (filter status==='Active') each `  * [ID: ${q.id}] ${q.name}`. Add Known NPCs `n.slice(0,20)` each `  * [ID: ${n.id}] ${n.name} (${n.relationship})`. Add Factions each `  * ${f.name}: ${f.tier}`. Return `lines.length > 1 ? lines.join('\n') : ''`. Import questsRepo, npcsRepo, factionsRepo, campaignsRepo at the top.

    Extend the `toolDescriptionsBlock` constant: append 8 bullet lines before the JSON-tail fallback paragraph, one per Phase 6 tool, in the existing `- Use \`toolName\` to ...` style (e.g. ``- Use `addQuest` to add a quest to the log when one emerges. Use `updateQuestStatus` to mark it Completed or Failed.``; ``- Use `addNpc` when the player meets someone new; `updateNpc` to update a known NPC by id.``; ``- Use `updateFaction` to set the player's standing with a faction.``; ``- Use `updateWorldTime` and `updateLocation` as time passes and the party moves.``; ``- Use `awardInspiration` with the player's character id for exceptional roleplay.``). Also extend the JSON-tail example comment to mention these are valid `toolName` values.

    In `buildContext()`: after computing `const worldStateSummary = formatWorldStateSummary(campaignId)`, change the systemPrompt assembly to insert `+ (worldStateSummary ? '\n\n' + worldStateSummary : '')` immediately after `'\n\n' + toolDescriptionsBlock` and before `referenceDocBlock` (RESEARCH § Pattern 4 / Assumption A5). Export `formatWorldStateSummary` in the test-export block at the bottom of the file.

    Extend `src/main/ai/contextBuilder.test.ts`: add tests for the behaviors above (empty → '', active quest included with id, non-active excluded, NPC cap at 20, factions listed, time/location lines, characterId line, location newline stripping) and assert buildContext's systemPrompt contains the summary after the tool block. Use the existing harness (seed campaign + character + quests/npcs/factions rows).
  </action>
  <verify>
    <automated>npx vitest run src/main/ai/contextBuilder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - contextBuilder.ts defines and exports `formatWorldStateSummary`
    - toolDescriptionsBlock contains all 8 substrings: addQuest, updateQuestStatus, addNpc, updateNpc, updateFaction, updateWorldTime, updateLocation, awardInspiration
    - buildContext systemPrompt assembly inserts the world-state summary after toolDescriptionsBlock and before referenceDocBlock
    - contextBuilder.test.ts asserts: empty state returns '', active quest id appears, NPC list caps at 20, player characterId line present, location segments newline-stripped
    - `npx vitest run src/main/ai/contextBuilder.test.ts` exits 0
  </acceptance_criteria>
  <done>formatWorldStateSummary injected into the system prompt with quests/NPCs/factions/time/location/characterId; toolDescriptionsBlock describes all 8 Phase 6 tools (Pitfall 6 closed); tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored world state → system prompt | DB-stored AI-generated location/quest/NPC text is rendered back into the system prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | location path in system prompt | mitigate | Strip `\n`/`\r` from each location segment in formatWorldStateSummary so injected newlines cannot forge new system-prompt directives (RESEARCH threat table) |
| T-06-03-02 | DoS | unbounded NPC list in prompt | mitigate | NPC list capped at 20 entries (Pitfall token-bloat / Assumption A4) |
| T-06-03-03 | Tampering | malformed worldLocationPath JSON | mitigate | JSON.parse guarded with try/catch and [] fallback (Pitfall 4) |
| T-06-03-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run src/main/ai/contextBuilder.test.ts` exits 0
- `npx tsc --noEmit -p tsconfig.node.json` exits 0
</verification>

<success_criteria>
- formatWorldStateSummary emits quests (with IDs), NPCs (with IDs + relationship, capped 20), factions (with tier), time, location, and the player characterId
- World-state summary injected into the system prompt after the tool block
- toolDescriptionsBlock describes all 8 Phase 6 tools for JSON-tail providers
- Location segments are newline-stripped
- Tests green
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-03-SUMMARY.md` when done
</output>
