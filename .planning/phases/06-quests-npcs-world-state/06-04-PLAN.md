---
phase: 06-quests-npcs-world-state
plan: 04
type: execute
wave: 4
depends_on: ["06-02"]
files_modified:
  - src/renderer/src/components/QuestsTab.tsx
autonomous: true
requirements: [STATE-01]
must_haves:
  truths:
    - "QuestsTab renders a chronological list of quests with name + status badge + one-line description"
    - "Quest status badges color by status: Active green, Completed muted, Failed red"
    - "An empty QuestsTab shows the 'No quests yet' empty state with a ScrollText icon"
    - "QuestsTab is read-only — no player edit controls"
  artifacts:
    - path: "src/renderer/src/components/QuestsTab.tsx"
      provides: "AI-populated quest log tab component"
      exports: ["QuestsTab"]
  key_links:
    - from: "src/renderer/src/components/QuestsTab.tsx"
      to: "trpc.quests.list"
      via: "useQuery"
      pattern: "quests.*list"
---

<objective>
Build the new QuestsTab component (the 6th right-panel tab): a read-only, chronological list of AI-populated quests, each rendered as name + status badge + one-line description, with an empty state. Pure TanStack Query consumer of `trpc.quests.list`.

Purpose: This is the player-facing surface of STATE-01 (quest log). It is independent of NpcTrackerTab (no shared files), so it runs in parallel with 06-05. CampaignViewScreen wiring (06-06) mounts it.
Output: src/renderer/src/components/QuestsTab.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md
@.planning/phases/06-quests-npcs-world-state/06-02-SUMMARY.md

<interfaces>
From src/renderer/src/lib/trpc (proxy client): `trpc.quests.list.query({ campaignId })` returns Quest[] where Quest = { id, campaignId, name, description, status, createdAt }.

Reusable renderer primitives (read them before importing):
- src/renderer/src/components/ui/badge.tsx — Badge with `variant="outline"`
- src/renderer/src/components/ui/scroll-area.tsx — ScrollArea
- src/renderer/src/lib/utils — `cn()` classname helper
- @tanstack/react-query — useQuery
- lucide-react — ScrollText icon

TanStack Query pattern (from CombatTrackerTab / 06-UI-SPEC §S2):
```
const questsQuery = useQuery({ queryKey: ['quests','list',campaignId], queryFn: () => trpc.quests.list.query({ campaignId }), enabled: !!campaignId })
const quests = questsQuery.data ?? []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuestsTab component (rows, status badges, empty state)</name>
  <files>src/renderer/src/components/QuestsTab.tsx</files>
  <read_first>
    - src/renderer/src/components/CombatTrackerTab.tsx (the canonical "full tab" component: useQuery wiring, ScrollArea usage, row layout, empty-state convention, how campaignId prop is typed)
    - src/renderer/src/components/ui/badge.tsx (Badge variant API)
    - src/renderer/src/components/ui/scroll-area.tsx (ScrollArea API)
    - src/renderer/src/lib/trpc.ts (how the proxy client is imported in renderer components)
    - .planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md § "S2. QuestsTab" + § "S2a. Empty State" (verbatim JSX, className strings, questStatusColor helper, copy)
  </read_first>
  <action>
    Create `src/renderer/src/components/QuestsTab.tsx` exporting a `QuestsTab({ campaignId }: { campaignId: string })` component. Wire the `questsQuery` useQuery exactly per the UI-SPEC §S2 snippet (queryKey `['quests','list',campaignId]`). Container: `<div className="h-full flex flex-col overflow-hidden">` wrapping a `<ScrollArea className="flex-1">` with an inner `<div className="flex flex-col gap-2 p-3">`. When `quests.length === 0` render the empty state per UI-SPEC §S2a verbatim (centered column, `<ScrollText className="h-8 w-8 text-muted-foreground/40 mb-2" />`, heading "No quests yet", body "Quests will appear here as the story unfolds and the DM introduces them."). Otherwise `quests.map` to a `QuestRow` rendering the UI-SPEC §S2 QuestRow JSX: `bg-card border border-border rounded-lg px-3 py-2 flex items-start gap-3`, name as `text-sm font-semibold text-foreground truncate`, a `<Badge variant="outline" className={cn('text-xs font-semibold shrink-0', questStatusColor(quest.status))}>{quest.status}</Badge>`, and the description as `<p className="text-xs text-muted-foreground leading-[1.4]">`. Add the `questStatusColor(status)` helper verbatim from UI-SPEC §S2 (Active → 'text-green-400 border-green-400', Completed → 'text-muted-foreground border-border', Failed → 'text-red-400 border-red-400', default → muted). No player-edit controls — fully read-only (D-04). No loading spinner (TanStack returns [] before data).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/QuestsTab.tsx exports `QuestsTab` taking `{ campaignId: string }`
    - QuestsTab.tsx contains useQuery with queryKey `['quests', 'list', campaignId]` calling `trpc.quests.list.query`
    - QuestsTab.tsx defines `questStatusColor` mapping Active→green, Completed→muted, Failed→red
    - QuestsTab.tsx contains the empty-state strings "No quests yet" and "Quests will appear here as the story unfolds and the DM introduces them."
    - QuestsTab.tsx imports `ScrollText` from lucide-react and `Badge` from ui/badge
    - QuestsTab.tsx contains no `<button>`, no input, and no mutation call (read-only)
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>QuestsTab renders a chronological read-only quest list with status-colored badges and an empty state; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-generated quest text → React render | quest.name / quest.description originate from AI tool calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering (XSS) | QuestRow name/description | mitigate | Rendered as React text nodes (`{quest.name}`, `{quest.description}`); no dangerouslySetInnerHTML; static Tailwind classNames (RESEARCH threat table) |
| T-06-04-02 | DoS | very long quest list | accept | Single-user local campaign; ScrollArea handles arbitrary length; no pagination needed at this scope |
| T-06-04-SC | Tampering | npm installs | mitigate | No new packages — all primitives (Badge, ScrollArea, lucide ScrollText) already present (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.web.json` exits 0
- No dangerouslySetInnerHTML in QuestsTab.tsx
</verification>

<success_criteria>
- QuestsTab is a read-only, chronological quest list with status-colored badges
- Empty state matches the UI-SPEC copy and icon
- Component consumes trpc.quests.list via TanStack Query
- TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-04-SUMMARY.md` when done
</output>
