---
phase: 06-quests-npcs-world-state
plan: 05
type: execute
wave: 4
depends_on: ["06-02"]
files_modified:
  - src/renderer/src/components/NpcTrackerTab.tsx
  - src/renderer/src/components/MutationChipStack.tsx
autonomous: true
requirements: [STATE-02, STATE-03, PARTY-03]
must_haves:
  truths:
    - "NpcTrackerTab renders a chronological NPC list (name + relationship tag + one-line description)"
    - "A collapsible Factions section renders below the NPC list with faction name + tier badge"
    - "NPC relationship tags and faction tier badges color by their semantic palette"
    - "An empty NpcTrackerTab (no NPCs and no factions) shows the 'No NPCs yet' empty state"
    - "MutationChipStack renders quest/quest_complete/npc/inspiration chip types with the right icons and colors"
  artifacts:
    - path: "src/renderer/src/components/NpcTrackerTab.tsx"
      provides: "AI-populated NPC tracker + collapsible Factions section"
      exports: ["NpcTrackerTab"]
    - path: "src/renderer/src/components/MutationChipStack.tsx"
      provides: "extended ChipType + iconFor for Phase 6 chips"
      contains: "inspiration"
  key_links:
    - from: "src/renderer/src/components/NpcTrackerTab.tsx"
      to: "trpc.npcs.list + trpc.factions.list"
      via: "useQuery"
      pattern: "npcs.*list"
    - from: "src/renderer/src/components/MutationChipStack.tsx"
      to: "ChipType union"
      via: "iconFor switch"
      pattern: "case 'inspiration'"
---

<objective>
Build NpcTrackerTab (replacing the placeholder): a chronological NPC list with relationship tags plus a collapsible Factions section, and extend MutationChipStack with the 4 new Phase 6 chip types (quest, quest_complete, npc, inspiration). Both are independent of QuestsTab (06-04), so this plan runs in parallel.

Purpose: NpcTrackerTab is the player-facing surface of STATE-02 (NPC tracker) and STATE-03 (factions). The MutationChipStack extension surfaces quest_added / quest_complete / npc_added / inspiration_awarded notifications (PARTY-03 chip + STATE-01/02 chips) emitted by the Wave 2 pipeline. These two files share no surface with QuestsTab and are grouped because the chip extension and the NPC tab are both "the visible reaction to Phase 6 mutations."
Output: src/renderer/src/components/NpcTrackerTab.tsx (new) + extended MutationChipStack.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md
@.planning/phases/06-quests-npcs-world-state/06-02-SUMMARY.md

<interfaces>
From renderer trpc proxy client:
- `trpc.npcs.list.query({ campaignId })` → Npc[] = { id, campaignId, name, description, relationship, factionName, createdAt }
- `trpc.factions.list.query({ campaignId })` → Faction[] = { id, campaignId, name, tier, createdAt }

Primitives to read before importing:
- src/renderer/src/components/ui/collapsible.tsx — Collapsible/CollapsibleTrigger/CollapsibleContent (already used by CombatTrackerTab)
- src/renderer/src/components/ui/scroll-area.tsx, ui/badge.tsx, src/renderer/src/lib/utils (cn)
- lucide-react — Users, ChevronDown, UserPlus, Star, ScrollText, Check

From src/renderer/src/components/MutationChipStack.tsx (extend):
```
type ChipType = 'hp' | 'xp' | 'condition' | 'slot' | 'currency' | 'combat' | 'rest'
function iconFor(type: ChipType) { switch(type){ case 'hp': return <...>; ... } }
// UserPlus is already imported (used by the 'combat' chip)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NpcTrackerTab — NPC list + collapsible Factions section + empty state</name>
  <files>src/renderer/src/components/NpcTrackerTab.tsx</files>
  <read_first>
    - src/renderer/src/components/CombatTrackerTab.tsx (full tab pattern + how Collapsible is imported and used; row layout cadence; campaignId prop typing)
    - src/renderer/src/components/ui/collapsible.tsx (Collapsible API + defaultOpen)
    - src/renderer/src/components/ui/badge.tsx, ui/scroll-area.tsx
    - .planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md § "S3. NpcTrackerTab" + § "S3a Empty State" + § "S3b Factions Section" (verbatim JSX, className, npcRelationshipColor + factionTierColor helpers, copy)
    - .planning/phases/06-quests-npcs-world-state/06-CONTEXT.md D-06, D-07, D-08 (chronological order, row content, factions section placement)
  </read_first>
  <action>
    Create `src/renderer/src/components/NpcTrackerTab.tsx` exporting `NpcTrackerTab({ campaignId }: { campaignId: string })`. Wire two useQueries per UI-SPEC §S3: `npcsQuery` (queryKey `['npcs','list',campaignId]` → `trpc.npcs.list.query`) and `factionsQuery` (queryKey `['factions','list',campaignId]` → `trpc.factions.list.query`); derive `npcs = npcsQuery.data ?? []`, `factions = factionsQuery.data ?? []`. Container: `<div className="h-full flex flex-col overflow-hidden">` > `<ScrollArea className="flex-1">` > `<div className="flex flex-col gap-2 p-3">`. When BOTH `npcs.length === 0 && factions.length === 0` render the §S3a empty state (`<Users className="h-8 w-8 text-muted-foreground/40 mb-2" />`, "No NPCs yet", "Characters you meet will be tracked here as the DM introduces them."). Otherwise: `npcs.map` to an NpcRow per §S3 (`bg-card border border-border rounded-lg px-3 py-2 flex items-start gap-3`, name `text-sm font-semibold text-foreground truncate`, relationship `<Badge variant="outline" className={cn('text-xs font-semibold shrink-0', npcRelationshipColor(npc.relationship))}>`, description `<p className="text-xs text-muted-foreground leading-[1.4]">`); then, when `factions.length > 0`, render the §S3b `<Collapsible className="mt-4" defaultOpen={true}>` with a `CollapsibleTrigger` ("Factions" label + `({factions.length})` count + ChevronDown `h-3.5 w-3.5`) and a `CollapsibleContent` mapping factions to rows (`px-3 py-1.5 flex items-center gap-3`, name `text-sm text-foreground flex-1 truncate`, tier `<Badge variant="outline" className={cn('text-xs font-semibold shrink-0', factionTierColor(f.tier))}>`). Add the `npcRelationshipColor` and `factionTierColor` helpers verbatim from the UI-SPEC (factionTierColor: Hostile red / Unfriendly orange / Neutral muted / Friendly green / Allied sky). Read-only — no edit controls (D-09).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <acceptance_criteria>
    - NpcTrackerTab.tsx exports `NpcTrackerTab` taking `{ campaignId: string }`
    - Contains useQuery with queryKeys `['npcs','list',campaignId]` and `['factions','list',campaignId]`
    - Defines `npcRelationshipColor` (Friendly→green, Hostile→red, Neutral/Unknown→muted) and `factionTierColor` (Hostile→red, Unfriendly→orange, Neutral→muted, Friendly→green, Allied→sky)
    - Renders a `<Collapsible defaultOpen={true}>` Factions section only when `factions.length > 0`
    - Contains empty-state strings "No NPCs yet" and "Characters you meet will be tracked here as the DM introduces them."
    - Contains no mutation call / no edit input (read-only)
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>NpcTrackerTab renders chronological NPCs with relationship tags + a collapsible Factions section with tier badges + an empty state; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: MutationChipStack — 4 Phase 6 chip types + icons</name>
  <files>src/renderer/src/components/MutationChipStack.tsx</files>
  <read_first>
    - src/renderer/src/components/MutationChipStack.tsx (full file — the ChipType union, the iconFor switch, the existing lucide imports incl. UserPlus, the chip label rendering)
    - .planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md § "S4. MutationChipStack" (verbatim ChipType extension + iconFor cases + the note that UserPlus/Star may already be imported)
    - .planning/phases/06-quests-npcs-world-state/06-CONTEXT.md D-16 (chip color/icon mapping)
  </read_first>
  <action>
    In `src/renderer/src/components/MutationChipStack.tsx`: extend the `ChipType` union to add `'quest' | 'quest_complete' | 'npc' | 'inspiration'`. Add icon imports `ScrollText` and `Check` from lucide-react ONLY IF not already imported; do NOT re-import `UserPlus` or `Star` if they are already present (UI-SPEC note — UserPlus is used by the 'combat' chip). Add 4 cases to the `iconFor` switch per UI-SPEC §S4: `case 'quest': return <ScrollText className={\`${cls} text-green-400\`} />`; `case 'quest_complete': return <Check className={\`${cls} text-muted-foreground\`} />`; `case 'npc': return <UserPlus className={\`${cls} text-muted-foreground\`} />`; `case 'inspiration': return <Star className={\`${cls} text-amber-400\`} />`. Leave all other chip behavior (4-chip max, 4000ms auto-dismiss, FIFO, slide-in animation, role/aria-live) unchanged. The chip type strings must exactly match the ones the Wave 2 pipeline emits (`'quest'`, `'quest_complete'`, `'npc'`, `'inspiration'`).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <acceptance_criteria>
    - MutationChipStack.tsx ChipType union includes `'quest'`, `'quest_complete'`, `'npc'`, `'inspiration'`
    - iconFor switch contains `case 'quest'` (ScrollText, green), `case 'quest_complete'` (Check, muted), `case 'npc'` (UserPlus, muted), `case 'inspiration'` (Star, amber)
    - `ScrollText` and `Check` are imported from lucide-react; no duplicate UserPlus/Star import
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>MutationChipStack handles all 4 Phase 6 chip types with the correct icon + color; chip type strings match the pipeline output; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-generated NPC/faction text → React render | npc.name/description, faction.name originate from AI tool calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering (XSS) | NpcRow / faction row text | mitigate | All AI text rendered as React text nodes; no dangerouslySetInnerHTML; static classNames (RESEARCH threat table) |
| T-06-05-02 | DoS | hundreds of factions from repeated updateFaction | mitigate | Collapsible Factions section + ScrollArea contain growth; repo unique constraint caps distinct factions per name (RESEARCH threat table) |
| T-06-05-03 | Spoofing | chip type mismatch with pipeline | mitigate | Chip type string literals matched exactly to pipeline emit values (Wave 2) |
| T-06-05-SC | Tampering | npm installs | mitigate | No new packages — all primitives already present (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.web.json` exits 0
- No dangerouslySetInnerHTML in NpcTrackerTab.tsx
- Chip type strings 'quest'/'quest_complete'/'npc'/'inspiration' match pipeline emit values
</verification>

<success_criteria>
- NpcTrackerTab is a read-only chronological NPC list with relationship tags + collapsible Factions section
- Empty state matches UI-SPEC copy and icon
- MutationChipStack renders all 4 Phase 6 chip types correctly
- TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-05-SUMMARY.md` when done
</output>
