---
phase: 06-quests-npcs-world-state
plan: 06
type: execute
wave: 5
depends_on: ["06-04", "06-05"]
files_modified:
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, WORLD-03, PARTY-03]
must_haves:
  truths:
    - "A 'Quests' tab appears as the 6th tab and mounts QuestsTab"
    - "The NPC Tracker tab mounts NpcTrackerTab (placeholder replaced)"
    - "A WorldStateBar renders below the action bar showing calendar + location breadcrumb when world state exists"
    - "After AI mutations, quests/npcs/factions/world-state/inspiration UI refreshes (cache invalidation)"
  artifacts:
    - path: "src/renderer/src/screens/CampaignViewScreen.tsx"
      provides: "6th tab + NpcTrackerTab mount + WorldStateBar + Phase 6 cache invalidation"
      contains: "QuestsTab"
  key_links:
    - from: "src/renderer/src/screens/CampaignViewScreen.tsx cacheInvalidationHandler"
      to: "quests.list / npcs.list / factions.list / campaigns.get query keys"
      via: "queryClient.invalidateQueries"
      pattern: "quests.*list"
    - from: "CampaignViewScreen WorldStateBar"
      to: "campaignQuery.data world-state columns"
      via: "conditional render"
      pattern: "worldTimeOfDay"
---

<objective>
Integrate every Phase 6 surface into CampaignViewScreen: add the 6th "Quests" tab trigger + content mounting QuestsTab, replace the NPC Tracker placeholder with NpcTrackerTab, render the WorldStateBar (calendar + location breadcrumb) below the action bar, and extend the cacheInvalidationHandler so AI mutations refresh quests/NPCs/factions/world-state/inspiration.

Purpose: This is the final wiring wave. It depends on both UI components (06-04 QuestsTab, 06-05 NpcTrackerTab + chips) and closes Pitfall 3 (missing cache invalidation). All six Phase 6 success criteria become observable in the running app after this plan.
Output: Extended src/renderer/src/screens/CampaignViewScreen.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md
@.planning/phases/06-quests-npcs-world-state/06-CONTEXT.md
@.planning/phases/06-quests-npcs-world-state/06-04-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-05-SUMMARY.md

<interfaces>
From src/renderer/src/screens/CampaignViewScreen.tsx (current structure — verified line refs):
- line 6: `import { Tabs, TabsList, TabsTrigger, TabsContent } from '../components/ui/tabs'`
- line ~242: `cacheInvalidationHandler` invalidates `['combat','listActive',id]`, `['characters','getByCampaignId',id]`, `['ai','getMessages',id]`
- line 452: closing `</div>` of the action bar
- line 454: `<PanelGroup direction="horizontal" ...>` opens
- lines 526-531: existing `<TabsTrigger value="npc-tracker">NPC Tracker</TabsTrigger>` (className unchanged)
- lines 538-543: `<TabsTrigger value="inventory">` (Quests trigger goes AFTER inventory)
- lines 569-576: NPC Tracker placeholder `<TabsContent value="npc-tracker">...</TabsContent>` to replace
- `campaignQuery` = `useQuery(['campaigns','get',id] → trpc.campaigns.get.query)`; its `.data` now includes worldTimeOfDay/worldDayNumber/worldSeason/worldLocationPath (added in 06-01, returned by campaigns.get)

New components to import:
- `import { QuestsTab } from '../components/QuestsTab'`
- `import { NpcTrackerTab } from '../components/NpcTrackerTab'`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorldStateBar + 6th Quests tab + NpcTrackerTab mount + cache invalidation</name>
  <files>src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignViewScreen.tsx (full file — imports at top, cacheInvalidationHandler ~line 242, action bar closing line 452, PanelGroup line 454, the 5 existing TabsTrigger/TabsContent blocks lines 514-589, how campaignQuery.data is referenced)
    - src/renderer/src/components/QuestsTab.tsx (the exported QuestsTab signature from 06-04)
    - src/renderer/src/components/NpcTrackerTab.tsx (the exported NpcTrackerTab signature from 06-05)
    - .planning/phases/06-quests-npcs-world-state/06-UI-SPEC.md § "S1 WorldStateBar" (verbatim JSX + timeOfDayIcon + formatLocationPath helpers), § "S2 tab trigger/content", § "S3 NpcTrackerTab mount", § "S5 CampaignViewScreen wiring" (exact cache invalidation keys)
  </read_first>
  <action>
    Edit `src/renderer/src/screens/CampaignViewScreen.tsx`:

    1. Add imports `QuestsTab` and `NpcTrackerTab` from '../components/QuestsTab' and '../components/NpcTrackerTab'.

    2. Add two helper functions (module scope, top of file) per UI-SPEC §S1: `timeOfDayIcon(timeOfDay: string): string` (Morning ☀️, Afternoon 🌤, Evening 🌙, Night 🌑, default 🕐) and `formatLocationPath(jsonPath: string | null): string` (try/catch JSON.parse → join(' > '), fallback to the raw string).

    3. Insert the WorldStateBar between the action bar's closing `</div>` (line 452) and `<PanelGroup>` (line 454), using the verbatim §S1 JSX guarded by `(campaign.worldTimeOfDay || campaign.worldLocationPath)` where `campaign = campaignQuery.data`. The bar: `<div className="flex items-center gap-4 px-4 py-2 border-b border-border bg-background text-xs text-muted-foreground shrink-0">` with a left calendar block (aria-hidden emoji via timeOfDayIcon + `{timeOfDay}{ • Day N}{ • season}`), a `<span className="flex-1" />` spacer, and a right breadcrumb block (`📍` aria-hidden + a `truncate max-w-[280px]` span with `title` and `aria-label="Current location: ..."` = formatLocationPath). Guard each side on its own field so partial state renders correctly (calendar-only or breadcrumb-only). Render nothing when both fields are null.

    4. Add the 6th tab trigger AFTER the inventory `<TabsTrigger>` (line 543): `<TabsTrigger value="quests" className="...identical className string as the other 5 triggers...">Quests</TabsTrigger>` (copy the exact className from a sibling trigger — UI-SPEC §S2 confirms no deviation). The trailing "Change Cover Image" `div` with `ml-auto` stays last so Quests sits before it.

    5. Replace the NPC Tracker placeholder `<TabsContent value="npc-tracker">` body (lines 569-576) with `<TabsContent value="npc-tracker" className="flex-1 overflow-hidden p-0"><NpcTrackerTab campaignId={id} /></TabsContent>`.

    6. Add `<TabsContent value="quests" className="flex-1 overflow-hidden p-0"><QuestsTab campaignId={id} /></TabsContent>` after the inventory TabsContent.

    7. Extend the `cacheInvalidationHandler` (line ~242) with the §S5 keys: `queryClient.invalidateQueries({ queryKey: ['quests','list',id] })`, `['npcs','list',id]`, `['factions','list',id]`, `['campaigns','get',id]` (world-state refresh — the key Phase 6 addition, Pitfall 3), and confirm `['characters','getByCampaignId',id]` is already invalidated (for hasInspiration). Do not remove existing invalidations.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json</automated>
  </verify>
  <acceptance_criteria>
    - CampaignViewScreen.tsx imports `QuestsTab` and `NpcTrackerTab`
    - Defines `timeOfDayIcon` and `formatLocationPath` helpers
    - Contains a `<TabsTrigger value="quests">` and `<TabsContent value="quests">` mounting `<QuestsTab campaignId={id} />`
    - The `value="npc-tracker"` TabsContent mounts `<NpcTrackerTab campaignId={id} />` (placeholder text "NPCs you meet will be tracked here once the AI starts populating them in Phase 6." is gone)
    - Renders a WorldStateBar guarded by `campaign.worldTimeOfDay || campaign.worldLocationPath` between the action bar and PanelGroup
    - cacheInvalidationHandler invalidates query keys `['quests','list',id]`, `['npcs','list',id]`, `['factions','list',id]`, `['campaigns','get',id]`
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
  </acceptance_criteria>
  <done>Quests tab is the 6th tab, NpcTrackerTab replaces the placeholder, WorldStateBar shows calendar + breadcrumb conditionally, and AI mutations invalidate all Phase 6 query keys (Pitfall 3 closed); TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI-generated world-state text → WorldStateBar render | worldLocationPath / time fields originate from AI tool calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering (XSS) | WorldStateBar breadcrumb | mitigate | formatLocationPath returns a plain string rendered as a React text node; no dangerouslySetInnerHTML; time-of-day emoji is a static glyph (RESEARCH threat table) |
| T-06-06-02 | Tampering | malformed worldLocationPath JSON | mitigate | formatLocationPath guards JSON.parse with try/catch and falls back to the raw string (Pitfall 4) |
| T-06-06-03 | DoS | campaigns.get invalidation frequency | accept | campaigns.get is a fast single-row SQLite read; invalidating it per mutation batch is negligible (RESEARCH Open Question 2) |
| T-06-06-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.web.json` exits 0
- `npm run test -- --run` (full suite) green
- No dangerouslySetInnerHTML in CampaignViewScreen.tsx WorldStateBar
</verification>

<success_criteria>
- 6th "Quests" tab mounts QuestsTab; NPC Tracker tab mounts NpcTrackerTab
- WorldStateBar shows calendar + location breadcrumb conditionally with correct partial-state behavior
- cacheInvalidationHandler refreshes quests/npcs/factions/world-state/inspiration after AI mutations
- TypeScript compiles; full suite green
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-06-SUMMARY.md` when done
</output>
