---
phase: 06-quests-npcs-world-state
plan: 07
type: execute
wave: 6
depends_on: ["06-03", "06-06"]
files_modified: []
autonomous: false
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, WORLD-03, PARTY-03]
must_haves:
  truths:
    - "Full test suite is green across all Phase 6 changes"
    - "No PHASE5_TOOLS reference remains in production main-process code (only ALL_TOOLS)"
    - "The end-to-end Phase 6 flow is human-verified in the running app"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 6: run the full suite, sweep the diffs from plans 01-06 for integration bugs (the well-known Phase 6 pitfalls), apply fixes, then a single blocking human-verify checkpoint for the UI/AI behaviors that cannot be automated.

Purpose: Plans 01-06 each verified their own slice; this plan verifies the seams between them (the ALL_TOOLS wiring, cache invalidation, chip-type string matching, world-state column flow through campaigns.get) and confirms the live experience. Standard end-of-phase pattern.
Output: Any bug-fix edits the review surfaces + a human-verified sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-quests-npcs-world-state/06-RESEARCH.md
@.planning/phases/06-quests-npcs-world-state/06-VALIDATION.md
@.planning/phases/06-quests-npcs-world-state/06-01-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-02-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-03-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-04-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-05-SUMMARY.md
@.planning/phases/06-quests-npcs-world-state/06-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite run + integration bug sweep</name>
  <files>(determined by review — likely src/main/index.ts, src/main/ai/mutationPipeline.ts, src/renderer/src/screens/CampaignViewScreen.tsx)</files>
  <read_first>
    - All six 06-0N-SUMMARY.md files (what each plan actually built + any deviations or follow-ups it flagged)
    - .planning/phases/06-quests-npcs-world-state/06-RESEARCH.md § "Common Pitfalls" (the 7 pitfalls — use as the review checklist)
    - src/main/index.ts (confirm `tools: ALL_TOOLS`, not PHASE5_TOOLS)
    - src/main/ai/mutationPipeline.ts (confirm chip type strings emitted match the renderer ChipType union)
    - src/renderer/src/screens/CampaignViewScreen.tsx (confirm all Phase 6 cache keys invalidated)
  </read_first>
  <action>
    Run `npm run test -- --run` and `npx tsc --noEmit -p tsconfig.node.json` and `npx tsc --noEmit -p tsconfig.web.json`. Then review the diffs from plans 01-06 against the 7 pitfalls in RESEARCH § Common Pitfalls: (1) all four routers registered in router.ts; (2) `tools: ALL_TOOLS` in index.ts with zero remaining `PHASE5_TOOLS` production references — run `grep -rn "PHASE5_TOOLS" src/main/ --include=*.ts` and confirm only toolSchemas.ts (the export) and test files reference it; (3) cacheInvalidationHandler invalidates quests.list, npcs.list, factions.list, campaigns.get; (4) worldLocationPath JSON.parse is guarded everywhere it is read; (5) factions UNIQUE(campaign_id, name) present and onConflictDoUpdate targets it; (6) toolDescriptionsBlock contains all 8 Phase 6 tool names; (7) awardInspiration has the resolvePlayerCharacterId fallback. Also verify the chip type string literals emitted by mutationPipeline ('quest','quest_complete','npc','inspiration') exactly match the renderer MutationChipStack ChipType union. Fix any mismatch found. If everything is correct, record "no fixes required" in the summary.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- --run` exits 0
    - `npx tsc --noEmit -p tsconfig.node.json` exits 0
    - `npx tsc --noEmit -p tsconfig.web.json` exits 0
    - `grep -rn "tools: PHASE5_TOOLS" src/main/` returns no matches
    - Chip type strings in mutationPipeline.ts match the MutationChipStack ChipType union (verified by reading both)
  </acceptance_criteria>
  <done>Full suite + both tsc projects green; all 7 pitfalls verified closed; chip type strings reconciled across main/renderer; fixes (if any) applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of Phase 6</name>
  <action>Pause for the human to run the app and verify the live Phase 6 flow described below. This is a verification-only checkpoint; no code is written. Issues raised become gap-closure tasks.</action>
  <what-built>
    Phase 6 end-to-end: 8 AI tools (addQuest/updateQuestStatus/addNpc/updateNpc/updateFaction/updateWorldTime/updateLocation/awardInspiration), the Quests tab, the NPC Tracker tab with Factions section, the WorldStateBar (calendar + location breadcrumb), and four new mutation chips — all wired through the Phase 5 mutation pipeline and ContextBuilder.
  </what-built>
  <how-to-verify>
    Run the app (`npm run dev`), open or create a campaign with a configured AI provider and an active session, then drive a short narration that introduces a quest, an NPC, a faction, advances time, moves location, and rewards roleplay. Confirm each (from 06-VALIDATION.md § Manual-Only Verifications):
    1. The "Quests" tab is the 6th tab (after Journal/Inventory) and lists the new quest with an Active badge; completing it in narration flips the badge and fires a "Quest complete!" chip.
    2. The NPC Tracker tab shows the new NPC (name + relationship tag + description); the collapsible Factions section shows the faction with its tier badge; an `npc_added` chip fired.
    3. The WorldStateBar appears below the action bar showing e.g. "🌙 Evening • Day 14 • Autumn" on the left and "📍 Forest > Ancient Ruins > ..." on the right, updating as the AI narrates time/location changes.
    4. An amber "Inspiration awarded!" chip appears when the AI awards inspiration, and the Character Sheet inspiration indicator flips on.
    5. After each AI turn, the tabs refresh without a manual reload (cache invalidation works).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (which become gap-closure tasks)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live AI session → full Phase 6 stack | End-to-end exercise of all AI-driven world-state mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Tampering | cross-plan integration seams | mitigate | Pitfall checklist review of all six plan diffs before sign-off |
| T-06-07-02 | Spoofing | chip-type string drift main↔renderer | mitigate | Explicit reconciliation of emitted chip strings vs ChipType union |
| T-06-07-SC | Tampering | npm installs | mitigate | No new packages introduced across the phase (RESEARCH § Package Legitimacy Audit) |
</threat_model>

<verification>
- `npm run test -- --run` exits 0
- Both tsc projects exit 0
- Human verifier approves the end-to-end flow
</verification>

<success_criteria>
- Full suite green; both TypeScript projects compile
- All 7 RESEARCH pitfalls verified closed
- Human-verified: quests, NPCs, factions, world time/location, and inspiration all populate and refresh live
</success_criteria>

<output>
Create `.planning/phases/06-quests-npcs-world-state/06-07-SUMMARY.md` when done
</output>
