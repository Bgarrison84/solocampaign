---
phase: 07-content-depth-advanced-character
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main/db/schema.ts
  - resources/migrations/0007_phase7_content_depth.sql
  - src/main/characters/calculations.ts
  - src/main/characters/calculations.test.ts
  - src/main/db/characterFeatsRepo.ts
  - src/main/db/customFeatsRepo.ts
  - src/main/db/campaignReferenceDocsRepo.ts
  - src/main/db/characterFeatsRepo.test.ts
  - src/main/db/customFeatsRepo.test.ts
  - src/main/db/campaignReferenceDocsRepo.test.ts
  - src/main/services/pdfExtractor.ts
  - src/main/services/pdfExtractor.test.ts
  - src/main/services/fixtures/test.pdf
autonomous: true
requirements: [CHAR-02, CHAR-04, CHAR-05, PARTY-01, PARTY-02, PROG-03, WORLD-01, RULES-03, RULES-04, STATE-06]
must_haves:
  truths:
    - "Migration 0007 applies cleanly on a fresh DB and integrity_check passes"
    - "calcPointBuyCost returns RAW point-buy costs for scores 8-15"
    - "calcMulticlassSpellSlots returns correct slot table for combined caster levels"
    - "unpdf extracts non-empty text from a real PDF in the main process"
    - "character_feats, custom_feats, campaign_reference_docs tables exist with FK cascades"
  artifacts:
    - path: "resources/migrations/0007_phase7_content_depth.sql"
      provides: "Phase 7 schema migration"
      contains: "DROP INDEX IF EXISTS characters_campaign_id_unique"
    - path: "src/main/characters/calculations.ts"
      provides: "Point buy + multiclass spell slot pure functions"
      contains: "calcMulticlassSpellSlots"
    - path: "src/main/services/pdfExtractor.ts"
      provides: "unpdf text extraction wrapper"
      exports: ["extractTextFromFile", "readTextFile"]
    - path: "src/main/db/characterFeatsRepo.ts"
      provides: "character_feats CRUD"
  key_links:
    - from: "src/main/db/schema.ts"
      to: "resources/migrations/0007_phase7_content_depth.sql"
      via: "schema definitions match migration DDL; unique() removed from characters"
      pattern: "characters_campaign_id_unique"
---

<objective>
Lay the Phase 7 foundation: install unpdf, author migration 0007 (6 new campaign columns, 3 new character columns, 3 new tables, drop the unique index), update schema.ts to match, add pure calculation functions (point buy + multiclass spell slots), create three new repos, build the PDF/text extraction service, and write/extend all Wave 0 test stubs.

Purpose: Every other Phase 7 plan depends on this schema + these repos + these pure functions. Nothing downstream can proceed until migration 0007 lands and the calculation/repo/service contracts exist.
Output: Migration 0007, extended schema.ts, calculations.ts, 3 repos, pdfExtractor.ts, and green Wave 0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-VALIDATION.md

<interfaces>
<!-- Existing contracts the executor needs — extracted from codebase / research. Use directly. -->

Migration 0007 DDL (RESEARCH.md lines 349-404). Order matters: custom_feats BEFORE character_feats (FK).

New campaigns columns: party_size INTEGER NOT NULL DEFAULT 1; world_setup_mode TEXT;
world_brief TEXT; world_document TEXT; encumbrance_enabled INTEGER NOT NULL DEFAULT 0; homebrew_content TEXT.

New characters columns: classes TEXT; is_companion INTEGER NOT NULL DEFAULT 0; negative_traits TEXT.

DROP INDEX IF EXISTS characters_campaign_id_unique.

Drizzle schema patterns (07-PATTERNS.md "Schema Table Definition Pattern"):
- boolean column: integer('col', { mode: 'boolean' }).notNull().default(false)
- JSON/nullable text: text('col')  (parsed at application layer)
- FK: text('campaign_id').notNull().references(() => campaigns.id, { onDelete: 'cascade' })
- createdAt: integer('created_at', { mode: 'timestamp_ms' }).notNull().default(sql`(unixepoch() * 1000)`)
- CRITICAL: remove `uniqueCampaign: unique().on(table.campaignId)` from characters table def (schema.ts ~lines 139-143)

Repo pattern (07-PATTERNS.md "Repository Pattern"): analog src/main/db/questsRepo.ts / npcsRepo.ts.
- imports: { and, asc, eq } from 'drizzle-orm'; randomUUID from 'node:crypto'; log from 'electron-log/main'; getDb from './index'; table + type from './schema'

Point buy cost table (RESEARCH.md lines 286-298): {8:0,9:1,10:2,11:3,12:4,13:5,14:7,15:9}. Budget 27.
calcPointBuyBudget = 27 + sum(presetFlaw.points) + min(nonEmptyFreeFormCount, 2) * 2

Multiclass (RESEARCH.md lines 243-280): FULL_CASTERS=[bard,cleric,druid,sorcerer,wizard] add full level;
HALF_CASTERS=[paladin,ranger] add floor(level/2); third casters (Fighter+Eldritch Knight, Rogue+Arcane Trickster)
add floor(level/3); Warlock EXCLUDED; non-casters add 0. Then look up combined caster level in MULTICLASS_SPELL_SLOTS table (full 20-row table in RESEARCH.md lines 258-276).

Proficiency past level 20 (RESEARCH.md line 339): Math.ceil(level/4) + 1.

unpdf usage (07-PATTERNS.md lines 240-256):
  import { getDocumentProxy, extractText } from 'unpdf'
  const pdf = await getDocumentProxy(new Uint8Array(buffer)); const { text } = await extractText(pdf, { mergePages: true })

campaignReferenceDocsRepo.create caps content at 50,000 chars (Pitfall 7): content.substring(0, 50_000).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install unpdf, author migration 0007, update schema.ts</name>
  <files>package.json, resources/migrations/0007_phase7_content_depth.sql, src/main/db/schema.ts</files>
  <read_first>
    - src/main/db/schema.ts (current schema — see campaigns + characters table defs; locate the `uniqueCampaign: unique().on(table.campaignId)` line on characters)
    - resources/migrations/0006_phase6_world_state.sql (migration format: `--> statement-breakpoint` separator, FK cascade syntax)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Database Schema Migration 0007 — exact DDL lines 349-404)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ Schema Table Definition Pattern; § migration analog lines 80-110)
  </read_first>
  <action>
    Install `unpdf@1.6.2` as a production dependency (`npm install unpdf@1.6.2`). Do NOT install @napi-rs/canvas (optional peer for image extraction only — not needed for text).

    Hand-write `resources/migrations/0007_phase7_content_depth.sql` using the `--> statement-breakpoint` separator (exact string from 0006). Statement order: (1) CREATE TABLE custom_feats (id TEXT PK, campaign_id TEXT NOT NULL FK→campaigns ON DELETE cascade, name TEXT NOT NULL, description TEXT NOT NULL DEFAULT '', created_at integer DEFAULT (unixepoch()*1000) NOT NULL); (2) CREATE TABLE character_feats (id TEXT PK, character_id TEXT NOT NULL FK→characters ON DELETE cascade, feat_name TEXT NOT NULL, feat_source TEXT NOT NULL DEFAULT 'srd', custom_feat_id TEXT FK→custom_feats ON DELETE SET NULL, created_at ...); (3) CREATE TABLE campaign_reference_docs (id TEXT PK, campaign_id TEXT NOT NULL FK→campaigns ON DELETE cascade, filename TEXT NOT NULL, content TEXT NOT NULL DEFAULT '', created_at ...); (4) six ALTER TABLE campaigns ADD COLUMN statements: party_size INTEGER NOT NULL DEFAULT 1, world_setup_mode TEXT, world_brief TEXT, world_document TEXT, encumbrance_enabled INTEGER NOT NULL DEFAULT 0, homebrew_content TEXT; (5) three ALTER TABLE characters ADD COLUMN statements: classes TEXT, is_companion INTEGER NOT NULL DEFAULT 0, negative_traits TEXT; (6) DROP INDEX IF EXISTS characters_campaign_id_unique. custom_feats MUST precede character_feats.

    Update `src/main/db/schema.ts`: add the 6 campaign columns and 3 character columns matching the migration; add three new table definitions (customFeats, characterFeats, campaignReferenceDocs) following the established sqliteTable pattern with `$inferSelect`/`$inferInsert` type exports (export types `CustomFeat`, `CharacterFeat`, `CampaignReferenceDoc` and their `New*` variants). characterFeats.customFeatId uses `.references(() => customFeats.id, { onDelete: 'set null' })` so customFeats must be declared before characterFeats. CRITICAL (Pitfall 1): remove the `uniqueCampaign: unique().on(table.campaignId)` constraint from the characters table definition so `drizzle-kit generate` will not re-add the index. Use `integer('classes' as never)`-style only where types demand; classes/negativeTraits are nullable `text(...)` (JSON parsed at app layer), isCompanion is `integer('is_companion', { mode: 'boolean' }).notNull().default(false)`, encumbranceEnabled likewise, partySize is `integer('party_size').notNull().default(1)`.

    Do NOT run drizzle-kit generate to author this migration (it is hand-written per the schema_push_requirement). The migrate() auto-runs at app startup (Phase 1 pattern).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -v "node_modules" | head -20; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"unpdf"` under dependencies at version `1.6.2`
    - resources/migrations/0007_phase7_content_depth.sql exists and contains `DROP INDEX IF EXISTS characters_campaign_id_unique`
    - 0007 migration file contains `CREATE TABLE` for `custom_feats` appearing BEFORE `character_feats` (custom_feats line number < character_feats line number)
    - 0007 migration contains `ALTER TABLE campaigns ADD` for all of: party_size, world_setup_mode, world_brief, world_document, encumbrance_enabled, homebrew_content
    - 0007 migration contains `ALTER TABLE characters ADD` for: classes, is_companion, negative_traits
    - src/main/db/schema.ts does NOT contain `uniqueCampaign` or `unique().on(table.campaignId)` for the characters table
    - src/main/db/schema.ts exports types `CustomFeat`, `CharacterFeat`, `CampaignReferenceDoc`
    - `npx tsc --noEmit -p tsconfig.node.json` reports no new errors in src/main/db/schema.ts
  </acceptance_criteria>
  <done>Migration 0007 authored with correct FK ordering and index drop; schema.ts matches it and has the unique constraint removed; unpdf installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add point-buy + multiclass calculation functions with tests</name>
  <files>src/main/characters/calculations.ts, src/main/characters/calculations.test.ts</files>
  <behavior>
    - calcPointBuyCost({all scores 8}) returns 0; ({all 15}) returns 54; ({str:14,dex:13,con:12,int:10,wis:8,cha:8}) returns 7+5+4+2+0+0 = 18
    - calcPointBuyBudget([], []) returns 27; with one preset flaw worth +2 returns 29; with 2 non-empty free-form flaws returns 27 + 4 = 31; free-form count capped at 2 (3 entries still only +4)
    - calcMulticlassCasterLevel([{className:'Wizard',level:5},{className:'Fighter',level:3}]) returns 5 (Fighter base non-caster); ([{className:'Paladin',level:6}]) returns 3 (half caster floor(6/2)); Warlock contributes 0
    - calcMulticlassSpellSlots for combined caster level 8 returns {1:4,2:3,3:3,4:2}; for caster level 0 returns empty/no slots
    - proficiency bonus helper for level 21 returns 7 via Math.ceil(21/4)+1
  </behavior>
  <read_first>
    - src/main/characters/calculations.ts (existing functions: buildSpellSlots, existing exports, ClassEntry/AbilityName types if present)
    - src/main/characters/calculations.test.ts (existing test structure, describe/it style, imports)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Multiclass Spell Slot Calculation lines 243-280 full table; § Point Buy Math lines 286-298; § Epic Boons proficiency line 339)
  </read_first>
  <action>
    Add to `src/main/characters/calculations.ts`: (1) `POINT_BUY_COST_TABLE` constant `{8:0,9:1,10:2,11:3,12:4,13:5,14:7,15:9}`; (2) `calcPointBuyCost(scores: Record<string, number>): number` summing POINT_BUY_COST_TABLE[score] ?? 0 across values; (3) `calcPointBuyBudget(presetFlawPoints: number[], freeFormFlaws: string[]): number` returning `27 + sum(presetFlawPoints) + Math.min(freeFormFlaws.filter(f => f.trim()).length, 2) * 2`; (4) `MULTICLASS_SPELL_SLOTS: Record<number, Record<number, number>>` with all 20 rows from RESEARCH.md table (caster level → {slotLevel: count}); (5) `calcMulticlassCasterLevel(classes: ClassEntry[]): number` per the FULL/HALF/third-caster/Warlock-excluded rules — third caster check is case-insensitive substring match on the entry's subclass against 'eldritch knight' (Fighter) and 'arcane trickster' (Rogue), accept the ClassEntry shape `{className: string; level: number; subclass?: string}`; (6) `calcMulticlassSpellSlots(classes: ClassEntry[]): Record<number, number>` that computes caster level then returns the matching MULTICLASS_SPELL_SLOTS row (or {} for caster level 0); (7) extend or add a `proficiencyBonusForLevel(level: number): number` returning `Math.ceil(level / 4) + 1` if not already present. Export all new functions and the ClassEntry type if it does not already exist.

    Write the tests in calculations.test.ts covering every case in <behavior> above. Use the existing describe/it/expect style from the file.
  </action>
  <verify>
    <automated>npm run test -- src/main/characters/calculations.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - calculations.ts contains `export function calcPointBuyCost` and `export function calcMulticlassSpellSlots` and `export function calcMulticlassCasterLevel`
    - calculations.ts contains `MULTICLASS_SPELL_SLOTS` with a key `20` mapping to `{1:4,2:3,3:3,4:3,5:3,6:2,7:1,8:1,9:1}`
    - `npm run test -- src/main/characters/calculations.test.ts` exits 0
    - test file asserts calcMulticlassCasterLevel for a Paladin-6 returns 3 (half caster)
    - test file asserts calcMulticlassSpellSlots for Fighter 5 / Wizard 3 (caster level 5) returns the level-5 row {1:4,2:3,3:2}
  </acceptance_criteria>
  <done>Point buy and multiclass pure functions implemented and fully tested; all calculation tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Create 3 repos + pdfExtractor service + Wave 0 test stubs</name>
  <files>src/main/db/characterFeatsRepo.ts, src/main/db/customFeatsRepo.ts, src/main/db/campaignReferenceDocsRepo.ts, src/main/services/pdfExtractor.ts, src/main/db/characterFeatsRepo.test.ts, src/main/db/customFeatsRepo.test.ts, src/main/db/campaignReferenceDocsRepo.test.ts, src/main/services/pdfExtractor.test.ts, src/main/services/fixtures/test.pdf</files>
  <read_first>
    - src/main/db/questsRepo.ts (repo analog: create/list pattern, imports, throw-on-missing)
    - src/main/db/npcsRepo.ts (campaign-scoped repo with patch/delete + the `and(eq,eq)` guard)
    - src/main/db/questsRepo.test.ts (repo test setup: how the test DB is initialized, beforeEach migration, fixture campaign/character creation)
    - src/main/db/contentLoader.ts (main-process file-read + log.error+throw error handling pattern)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ characterFeatsRepo / customFeatsRepo / campaignReferenceDocsRepo / pdfExtractor pattern blocks lines 114-258)
  </read_first>
  <action>
    Create `src/main/db/characterFeatsRepo.ts` exporting `characterFeatsRepo` with: `add(input: { characterId: string; featName: string; featSource: 'srd' | 'custom' | 'epic_boon'; customFeatId?: string }): CharacterFeat`, `listByCharacter(characterId: string): CharacterFeat[]` (ordered by createdAt asc), `remove(featId: string, characterId: string): void` (guarded by `and(eq(characterFeats.id, featId), eq(characterFeats.characterId, characterId))`).

    Create `src/main/db/customFeatsRepo.ts` exporting `customFeatsRepo` with: `create(input: { campaignId: string; name: string; description: string }): CustomFeat`, `listByCampaign(campaignId: string): CustomFeat[]`, `delete(id: string, campaignId: string): void`.

    Create `src/main/db/campaignReferenceDocsRepo.ts` exporting `campaignReferenceDocsRepo` with: `create(input: { campaignId: string; filename: string; content: string }): CampaignReferenceDoc` — CRITICAL: cap content with `input.content.substring(0, 50_000)` before insert (Pitfall 7); `list(campaignId: string): CampaignReferenceDoc[]`; `delete(id: string, campaignId: string): void`.

    Create `src/main/services/pdfExtractor.ts` (create the `src/main/services/` directory if absent) exporting async `extractTextFromFile(filePath: string): Promise<string>` (reads file via fs/promises, runs unpdf getDocumentProxy + extractText with mergePages:true) and `readTextFile(filePath: string): Promise<string>` (fs.readFile utf-8). Use `log.error` + rethrow on failure, mirroring contentLoader's pattern.

    Add a small real test PDF fixture at `src/main/services/fixtures/test.pdf` (a minimal valid single-page PDF containing the literal text token used in the assertion — generate it with a tiny script or write the raw PDF bytes; it must contain extractable text).

    Write test stubs: characterFeatsRepo.test.ts (add then listByCharacter returns the feat; remove deletes it), customFeatsRepo.test.ts (create + listByCampaign + delete), campaignReferenceDocsRepo.test.ts (create caps content at 50,000 chars — insert a 60,000-char string and assert stored length is 50,000), pdfExtractor.test.ts (extractTextFromFile on the fixture returns a non-empty string containing the known token). Reuse the test-DB setup pattern from questsRepo.test.ts (beforeEach applies migrations to an in-memory or temp DB and seeds a campaign + character).
  </action>
  <verify>
    <automated>npm run test -- src/main/db/characterFeatsRepo.test.ts src/main/db/customFeatsRepo.test.ts src/main/db/campaignReferenceDocsRepo.test.ts src/main/services/pdfExtractor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - characterFeatsRepo.ts contains `export const characterFeatsRepo` with `add`, `listByCharacter`, `remove`
    - campaignReferenceDocsRepo.ts create contains `substring(0, 50_000)` or `substring(0, 50000)`
    - pdfExtractor.ts exports `extractTextFromFile` and `readTextFile` and imports from `'unpdf'`
    - src/main/services/fixtures/test.pdf exists and is a valid PDF (non-zero size)
    - `npm run test -- src/main/db/campaignReferenceDocsRepo.test.ts` asserts a 60,000-char input is stored at length 50,000
    - `npm run test -- src/main/services/pdfExtractor.test.ts` passes (extractTextFromFile returns non-empty text) — if unpdf fails to load in the main/test process, record the failure in the SUMMARY as the Wave 0 PDF risk-gate trigger and fall back per RESEARCH risk gate
    - all four test files exit 0
  </acceptance_criteria>
  <done>Three repos and the pdfExtractor service exist with passing test stubs; the PDF risk gate is resolved (unpdf works) or its failure is recorded for the fallback decision.</done>
</task>

<task type="auto">
  <name>Task 4 [BLOCKING]: Apply migration 0007 and verify DB integrity</name>
  <files>src/main/db/schema.ts</files>
  <read_first>
    - src/main/db/index.ts (or wherever applyMigrations / migrate() + integrity_check is wired — the Phase 1 auto-migrate-at-startup pattern)
    - resources/migrations/0007_phase7_content_depth.sql (the file authored in Task 1)
    - resources/migrations/meta (drizzle journal — confirm whether the hand-written migration needs a journal entry)
  </read_first>
  <action>
    This is the mandatory [BLOCKING] schema-push step. Migration 0007 is hand-written (do NOT run `drizzle-kit push`). Ensure migration 0007 is registered in the drizzle migration journal (`resources/migrations/meta/_journal.json`) so the app's startup `migrate()` picks it up — add the journal entry for `0007_phase7_content_depth` following the existing entry format for 0006. Then run a clean migration against a fresh database to confirm all statements apply in order without error and `PRAGMA integrity_check` returns `ok`. If a `drizzle-kit generate` cross-check is run to validate schema↔migration parity, it MUST NOT produce a new migration that recreates `characters_campaign_id_unique` (if it does, the unique() removal in Task 1 was incomplete — fix schema.ts).
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -25; echo "fullsuite-exit"</automated>
  </verify>
  <acceptance_criteria>
    - resources/migrations/meta/_journal.json contains an entry referencing `0007_phase7_content_depth`
    - A fresh-DB migration run applies 0007 with no SQL error (confirmed via the repo tests in Task 3 which apply all migrations including 0007 in their beforeEach)
    - `drizzle-kit generate` (if run) does NOT emit a new migration containing `CREATE UNIQUE INDEX characters_campaign_id_unique`
    - `npm run test` full suite exits 0 (no regressions from schema changes)
  </acceptance_criteria>
  <done>Migration 0007 is journaled, applies cleanly on a fresh DB, integrity_check passes, and no unique-index regression is generated. Phase 7 schema is live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→tRPC (future plans) | File paths and feat/doc inputs cross here; validated in later plans |
| filesystem→pdfExtractor | Imported PDF/text files read in main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Denial of Service | campaignReferenceDocsRepo.create | mitigate | Cap content at 50,000 chars (`substring(0, 50_000)`) before insert (Pitfall 7) |
| T-07-01-02 | Tampering | pdfExtractor file read | accept | filePath validation lives in the campaignDocs tRPC router (plan 07-03); this service trusts the validated path |
| T-07-01-SC | Tampering | npm install unpdf@1.6.2 | mitigate | RESEARCH Package Legitimacy Audit marks unpdf [OK]/Approved; no human gate required |
</threat_model>

<verification>
- `npm run test` full suite green (includes new calculation, repo, and pdfExtractor tests)
- Migration 0007 applies on fresh DB; integrity_check ok
- schema.ts has no `uniqueCampaign` on characters
</verification>

<success_criteria>
- unpdf@1.6.2 installed
- Migration 0007 authored, journaled, applies cleanly with correct FK ordering and index drop
- schema.ts matches migration (3 tables, 9 new columns, unique removed)
- calcPointBuyCost, calcPointBuyBudget, calcMulticlassCasterLevel, calcMulticlassSpellSlots implemented + tested
- characterFeatsRepo, customFeatsRepo, campaignReferenceDocsRepo created + tested
- pdfExtractor.ts created + tested (PDF risk gate resolved)
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-01-SUMMARY.md` when done. Record the PDF risk-gate outcome (unpdf works in-process: yes/no) explicitly.
</output>
