---
phase: 07-content-depth-advanced-character
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - src/main/trpc/routers/library.ts
  - src/main/trpc/routers/feats.ts
  - src/main/trpc/routers/campaignDocs.ts
  - src/main/trpc/router.ts
  - src/main/ai/toolSchemas.ts
  - src/main/ai/mutationPipeline.ts
  - src/main/ai/contextBuilder.ts
  - src/main/db/charactersRepo.ts
  - src/main/db/campaignsRepo.ts
  - src/main/ai/toolSchemas.test.ts
  - src/main/ai/mutationPipeline.test.ts
  - src/main/ai/contextBuilder.test.ts
  - src/main/db/charactersRepo.test.ts
autonomous: true
requirements: [CHAR-04, CHAR-05, PARTY-01, PARTY-02, WORLD-01, RULES-01, RULES-02, RULES-03, RULES-04, STATE-06]
must_haves:
  truths:
    - "library tRPC router returns feats/magicItems/rules/monsters lists"
    - "feats tRPC router persists and lists character feats and custom feats"
    - "campaignDocs tRPC router imports a PDF/text file and lists/deletes docs"
    - "addCompanion AI tool creates an isCompanion=true character; removeCompanion deletes it"
    - "contextBuilder injects a World Overview block when worldBrief/worldDocument is set"
    - "character summary includes feats, multiclass breakdown, companions, negative traits, encumbrance status"
    - "charactersRepo.create enforces partySize at the application layer"
  artifacts:
    - path: "src/main/trpc/routers/library.ts"
      provides: "SRD reference queries"
    - path: "src/main/trpc/routers/feats.ts"
      provides: "Feat picker + custom feat CRUD"
    - path: "src/main/trpc/routers/campaignDocs.ts"
      provides: "PDF/text import + list + delete"
    - path: "src/main/ai/toolSchemas.ts"
      provides: "addCompanionSchema + removeCompanionSchema"
      contains: "addCompanionSchema"
  key_links:
    - from: "src/main/ai/mutationPipeline.ts"
      to: "src/main/db/charactersRepo.ts"
      via: "addCompanion case calls createCompanion"
      pattern: "addCompanion"
    - from: "src/main/ai/contextBuilder.ts"
      to: "campaign.worldBrief"
      via: "World Overview injection block"
      pattern: "World Overview"
---

<objective>
Build the Phase 7 AI + data layer: three new tRPC routers (library, feats, campaignDocs), the addCompanion/removeCompanion tool schemas + mutation pipeline cases, the contextBuilder World Overview injection + extended character summary, partySize enforcement in charactersRepo, and a generateWorldBrief procedure on the campaigns router.

Purpose: This is the backend contract every Phase 7 UI plan consumes. The library router feeds the LibraryScreen; feats router feeds the feat pickers; campaignDocs feeds homebrew/world import; the tools let the AI manage companions; contextBuilder makes the AI aware of all new character/world state.
Output: 3 routers registered, 2 tools wired, contextBuilder extended, partySize enforced, generateWorldBrief available.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-01-SUMMARY.md

<interfaces>
<!-- Contracts from 07-01 + existing code. -->
From 07-01: characterFeatsRepo {add, listByCharacter, remove}; customFeatsRepo {create, listByCampaign, delete};
campaignReferenceDocsRepo {create(caps 50k), list, delete}; pdfExtractor {extractTextFromFile, readTextFile}.
schema.ts now has CustomFeat / CharacterFeat / CampaignReferenceDoc types + new columns.

Router registration (src/main/trpc/router.ts): add keys library, feats, campaignDocs to t.router({...}).

Static content cache pattern (07-PATTERNS.md "Static Content Cache Pattern"): per-file module-level
`let _x = null` + getResourcesPath() (app.isPackaged ? process.resourcesPath : path.join(__dirname,'../../resources')).

feats router procedures (07-PATTERNS.md lines 331-339): listSrd, listEpicBoons, listByCharacter({characterId}),
add({characterId, featName, featSource enum, customFeatId?}), remove({featId, characterId}),
listCustomByCampaign({campaignId}), createCustom({campaignId, name max100, description max2000}), deleteCustom({id, campaignId}).

campaignDocs router (07-RESEARCH.md lines 786-814): list({campaignId}), import({campaignId, filePath}) —
ext .pdf → extractTextFromFile else readTextFile, substring(0,50_000), basename(filePath); delete({docId}).
SECURITY: validate filePath is absolute and contains no '..' before reading (threat T-07-03-01).

Tool schemas (07-PATTERNS.md lines 599-609):
  addCompanionSchema = z.object({ name: z.string().min(1).max(100), type: z.enum(['Familiar','Animal Companion','Summoned Creature']), hpMax: z.number().int().positive().max(9999), ac: z.number().int().min(1).max(30) })
  removeCompanionSchema = z.object({ companionId: z.string() })
Register via tool({ description, inputSchema }) — NO execute property. Add both to PHASE7_TOOLS and ALL_TOOLS.

mutationPipeline (07-PATTERNS.md lines 612-629): add 'companion' to MutationChip.type union; addCompanion case
calls charactersRepo.createCompanion({campaignId,name,type,hpMax,ac}) + pushes chip; removeCompanion case deletes.

charactersRepo: add createCompanion({campaignId,name,type,hpMax,ac}) → inserts character row with isCompanion=true,
class/background/abilityScores null, and a characterResources row with hpMax/hpCurrent/ac. Add partySize enforcement
in create(): count non-companion characters for campaign, throw if count >= campaign.partySize (PARTY-01).

contextBuilder (07-RESEARCH.md lines 408-451; 07-PATTERNS.md lines 631-657):
- World Overview block between character summary and toolDescriptionsBlock. worldDocument → "World Reference Document:\n"+substring(0,16000); else worldBrief → "World Overview:\n"+brief; else omit. Apply stripNewlines per line.
- formatCharacterSummary extensions: Classes line (multiclass, omit if classes null), Feats line, Negative Traits line, Companions line, Encumbrance line (only when encumbranceEnabled).

campaigns router: add generateWorldBrief({campaignId}) mutation — non-streaming generateText (llmProvider) producing
500-800 word brief; save to campaign.worldBrief via campaignsRepo. Same generateText path as recap (Phase 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: library + feats + campaignDocs tRPC routers, registered</name>
  <files>src/main/trpc/routers/library.ts, src/main/trpc/routers/feats.ts, src/main/trpc/routers/campaignDocs.ts, src/main/trpc/router.ts</files>
  <read_first>
    - src/main/trpc/routers/spells.ts (static content cache + getResourcesPath pattern lines 49-67)
    - src/main/trpc/routers/quests.ts (Zod-validated procedure pattern, campaignIdSchema import)
    - src/main/trpc/router.ts (registration — current keys, t.router shape)
    - src/main/trpc/schemas.ts (campaignIdSchema if it lives here)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ library.ts / feats.ts / Static Content Cache / tRPC Procedure Pattern)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Campaign Reference Docs tRPC Procedure lines 783-814; § Security Domain path-traversal mitigation)
  </read_first>
  <action>
    Create `src/main/trpc/routers/library.ts` exporting `libraryRouter` with sub-routers feats/magicItems/rules/monsters, each exposing `.list` query that returns the full JSON array via a per-file module-level cache + getResourcesPath() (mirror spells.ts). Define typed FeatEntry/MagicItemEntry/RuleEntry/MonsterEntry interfaces matching 07-02 field shapes. Search filtering is client-side, so each `.list` returns the full array.

    Create `src/main/trpc/routers/feats.ts` exporting `featsRouter` with: `listSrd` (no input → getFeats()), `listEpicBoons` (no input → getEpicBoons()), `listByCharacter({characterId})` → characterFeatsRepo.listByCharacter, `add` mutation (Zod: characterId string, featName string, featSource z.enum(['srd','custom','epic_boon']), customFeatId optional) → characterFeatsRepo.add, `remove` mutation ({featId, characterId}) → characterFeatsRepo.remove, `listCustomByCampaign({campaignId: campaignIdSchema})` → customFeatsRepo.listByCampaign, `createCustom` mutation ({campaignId, name: z.string().max(100), description: z.string().max(2000)}) → customFeatsRepo.create, `deleteCustom` mutation ({id, campaignId}) → customFeatsRepo.delete. Reuse the library cache for getFeats/getEpicBoons (import from library.ts or share a small content-cache helper).

    Create `src/main/trpc/routers/campaignDocs.ts` exporting `campaignDocsRouter` with: `list({campaignId})` → campaignReferenceDocsRepo.list, `import` mutation ({campaignId, filePath}) — SECURITY: reject if `filePath` is not absolute (path.isAbsolute) or contains `..` (throw a tRPC error); choose extractTextFromFile for `.pdf` else readTextFile; substring(0,50_000); basename for filename; then campaignReferenceDocsRepo.create; `delete` mutation ({docId, campaignId}) → campaignReferenceDocsRepo.delete.

    Register all three in `src/main/trpc/router.ts`: add `library: libraryRouter`, `feats: featsRouter`, `campaignDocs: campaignDocsRouter` to the t.router object (keep keys alphabetized to match existing style).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.node.json 2>&1 | grep -v node_modules | head -20; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - src/main/trpc/router.ts contains `library:`, `feats:`, and `campaignDocs:` keys
    - library.ts exposes `.feats.list`, `.magicItems.list`, `.rules.list`, `.monsters.list`
    - feats.ts contains a procedure `createCustom` with `z.string().max(100)` for name and `z.string().max(2000)` for description
    - campaignDocs.ts import procedure contains `path.isAbsolute` and rejects paths containing `..`
    - campaignDocs.ts import contains `substring(0, 50_000)` or `50000`
    - `npx tsc --noEmit -p tsconfig.node.json` reports no new errors in the three router files
  </acceptance_criteria>
  <done>Three routers created, security-validated, and registered in the root router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: addCompanion/removeCompanion tools + mutationPipeline + charactersRepo partySize enforcement</name>
  <files>src/main/ai/toolSchemas.ts, src/main/ai/mutationPipeline.ts, src/main/db/charactersRepo.ts, src/main/ai/toolSchemas.test.ts, src/main/ai/mutationPipeline.test.ts, src/main/db/charactersRepo.test.ts</files>
  <behavior>
    - addCompanionSchema rejects empty name, ac > 30, hpMax <= 0
    - mutationPipeline.apply with an addCompanion tool call creates a character row with isCompanion=true and a 'companion' chip
    - mutationPipeline.apply with removeCompanion deletes the companion character
    - charactersRepo.create throws when the campaign already has partySize non-companion characters
    - charactersRepo.create allows companions to exceed partySize (companions don't count toward the limit)
  </behavior>
  <read_first>
    - src/main/ai/toolSchemas.ts (existing schemas + tool() registration + PHASE6_TOOLS/ALL_TOOLS arrays, lines 24-113)
    - src/main/ai/mutationPipeline.ts (applyOneTool switch, MutationChip.type union, existing case pattern lines 155-165)
    - src/main/db/charactersRepo.ts (existing create, how characterResources row is created alongside character, campaign lookup)
    - src/main/ai/mutationPipeline.test.ts (existing pipeline test setup)
    - src/main/db/charactersRepo.test.ts (existing create test setup)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ Zod Tool Schema / Mutation Pipeline Case patterns)
  </read_first>
  <action>
    In `toolSchemas.ts`: add `addCompanionSchema` (name min1 max100, type enum Familiar/Animal Companion/Summoned Creature, hpMax int positive max9999, ac int min1 max30) and `removeCompanionSchema` ({companionId: z.string()}). Register `addCompanionTool` and `removeCompanionTool` via `tool({ description, inputSchema })` (no execute). Add both tools to a `PHASE7_TOOLS` collection and include them in `ALL_TOOLS` alongside Phase 5/6 tools.

    In `mutationPipeline.ts`: add `'companion'` to the `MutationChip.type` union. Add an `addCompanion` case to the applyOneTool switch: safeParse against addCompanionSchema, on failure log.warn + break; on success call `charactersRepo.createCompanion({ campaignId, name, type, hpMax, ac })` and push a chip `{ id, label: \`${name} joined\`, type: 'companion' }`. Add a `removeCompanion` case: safeParse removeCompanionSchema, then delete the companion (charactersRepo delete-by-id scoped to campaign), push a chip.

    In `charactersRepo.ts`: add `createCompanion({ campaignId, name, type, hpMax, ac })` that inserts a characters row with isCompanion=true (name set, class/background/race/abilityScores null where the schema allows) and a paired characterResources row with hpMax/hpCurrent=hpMax/ac/type-stored-appropriately (store the companion type in an existing suitable column or the negativeTraits/notes path — choose a column that exists; if none fits, store type in the character `class` field is NOT acceptable — instead store in a dedicated field: reuse `subclass` or add nothing schema-breaking; prefer storing type in a JSON or existing text column documented in the SUMMARY). Add partySize enforcement to the existing `create()`: before insert, look up campaign.partySize, count existing non-companion characters for the campaign, and throw an Error if count >= partySize (PARTY-01). Companions bypass this check.

    Write/extend tests per <behavior>: toolSchemas.test.ts (schema validation cases), mutationPipeline.test.ts (addCompanion creates isCompanion row + chip; removeCompanion deletes), charactersRepo.test.ts (partySize throw + companion bypass + isCompanion creation).
  </action>
  <verify>
    <automated>npm run test -- src/main/ai/toolSchemas.test.ts src/main/ai/mutationPipeline.test.ts src/main/db/charactersRepo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - toolSchemas.ts contains `addCompanionSchema` and `removeCompanionSchema` and `PHASE7_TOOLS`
    - toolSchemas.ts ALL_TOOLS includes the companion tools (grep for addCompanionTool in the ALL_TOOLS aggregation)
    - mutationPipeline.ts MutationChip type union includes `'companion'`
    - mutationPipeline.ts contains a `case 'addCompanion'` and `case 'removeCompanion'`
    - charactersRepo.ts contains `createCompanion` and partySize enforcement (a throw referencing partySize) in create
    - `npm run test -- src/main/db/charactersRepo.test.ts` asserts create throws when partySize is reached and a companion is allowed past it
    - all three test files exit 0
  </acceptance_criteria>
  <done>Companion tools wired through schema → pipeline → repo; partySize enforced; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: contextBuilder World Overview + extended character summary + generateWorldBrief</name>
  <files>src/main/ai/contextBuilder.ts, src/main/db/campaignsRepo.ts, src/main/trpc/routers/campaigns.ts, src/main/ai/contextBuilder.test.ts</files>
  <behavior>
    - buildContext includes "World Overview:" + brief text when campaign.worldBrief is set and worldDocument is null
    - buildContext includes "World Reference Document:" when campaign.worldDocument is set (truncated to 16,000 chars)
    - buildContext omits the World Overview block entirely when both are null
    - formatCharacterSummary includes a "Classes:" line for a multiclass character and omits it when classes is null
    - formatCharacterSummary includes "Encumbrance:" only when campaign.encumbranceEnabled is true
    - formatCharacterSummary includes feats list, negative traits, and companions when present
  </behavior>
  <read_first>
    - src/main/ai/contextBuilder.ts (formatCharacterSummary lines 78-148; injection order; stripNewlines helper usage lines 157-159; where worldStateSummary + referenceDocBlock are assembled)
    - src/main/ai/contextBuilder.test.ts (existing buildContext / formatCharacterSummary tests, fixture shapes)
    - src/main/db/campaignsRepo.ts (existing update methods — world write pattern for worldBrief)
    - src/main/trpc/routers/campaigns.ts (existing mutations; updateAiConfig pattern; how generateText/llmProvider is invoked for recaps)
    - src/main/ai/llmProvider.ts (generateText signature)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ ContextBuilder Injection Order lines 408-451)
  </read_first>
  <action>
    In `contextBuilder.ts`: build a World Overview block positioned between the character summary and the toolDescriptionsBlock. Logic: `campaign.worldDocument ? "World Reference Document:\n" + campaign.worldDocument.substring(0,16000) : campaign.worldBrief ? "World Overview:\n" + campaign.worldBrief : null` — apply the existing stripNewlines treatment per line and only include the block when non-null. Extend `formatCharacterSummary` to push additional lines at the end of the lines[] array: a `Classes: Fighter 5 / Wizard 3 — Total Level 8` line built from character.classes (omit when classes is null — use the normalization helper `character.classes ?? [{className: character.class, level: character.level}]` only for display when multiclass, but DO NOT emit the line for single-class), a `Feats: <names>` line from the character's feats (passed in or queried), a `Negative Traits: <flaw summaries>` line from negativeTraits JSON, a `Companions: <name (type, HP cur/max)>` line from isCompanion characters in the campaign, and an `Encumbrance: Enabled (carrying X/Y lbs)` line only when campaign.encumbranceEnabled is true. Wire whatever new data (feats, companions) the summary needs through buildContext's existing data-loading (extend the campaign/character fetch).

    In `campaignsRepo.ts`: add an `updateWorldBrief(campaignId, worldBrief)` write (or extend an existing world-write method) and ensure worldBrief/worldDocument/encumbranceEnabled/homebrew_content are selectable on the campaign object used by contextBuilder.

    In `campaigns.ts` router: add `generateWorldBrief` mutation ({campaignId}) — load the campaign + its AI config, call llmProvider.generateText with a prompt that produces a 500-800 word world brief (setting name, tone, factions, main conflict, key locations, Session 1 hook), save the result via campaignsRepo.updateWorldBrief, and return the brief text. Mirror the non-streaming generateText path used by the Phase 4 recap generator. On failure, surface the error (the modal handles non-blocking fallback per UI-SPEC).

    Extend contextBuilder.test.ts per <behavior>.
  </action>
  <verify>
    <automated>npm run test -- src/main/ai/contextBuilder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - contextBuilder.ts contains the literal string `World Overview:` and `World Reference Document:`
    - contextBuilder.ts truncates worldDocument with `substring(0, 16000)` or `16_000`
    - contextBuilder.test.ts asserts the World Overview block is present when worldBrief is set and absent when both world fields are null
    - contextBuilder.test.ts asserts `Encumbrance:` appears only when encumbranceEnabled is true
    - campaigns.ts router contains a `generateWorldBrief` procedure that calls generateText and saves via campaignsRepo
    - `npm run test -- src/main/ai/contextBuilder.test.ts` exits 0
  </acceptance_criteria>
  <done>World Overview injection + extended character summary implemented and tested; generateWorldBrief tRPC available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→tRPC | filePath, feat names, custom feat text, companion args cross here (untrusted) |
| AI tool output→mutationPipeline | AI-proposed companion mutations (untrusted) |
| imported doc text→system prompt | extracted PDF/text injected into AI context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | campaignDocs.import filePath | mitigate | Reject non-absolute paths and any path containing `..`; only OS file-dialog-provided absolute paths accepted |
| T-07-03-02 | Tampering | custom feat name/description injection | mitigate | Zod max lengths (name ≤100, desc ≤2000); stripNewlines before injecting into character summary |
| T-07-03-03 | Tampering | addCompanion tool-call abuse | mitigate | addCompanionSchema Zod-validates all fields in mutationPipeline before any DB write |
| T-07-03-04 | Tampering | party-size enforcement bypass | mitigate | Application-level count check in charactersRepo.create (DB no longer has the unique index) |
| T-07-03-05 | Denial of Service | oversized world/reference doc injection | mitigate | 50,000-char storage cap + 16,000-char injection truncation |
</threat_model>

<verification>
- `npm run test` for toolSchemas, mutationPipeline, charactersRepo, contextBuilder all green
- tsc clean for the three new routers
- router.ts registers library, feats, campaignDocs
</verification>

<success_criteria>
- library / feats / campaignDocs routers created, registered, security-validated
- addCompanion / removeCompanion tools wired schema → pipeline → repo
- partySize enforced at the application layer
- contextBuilder World Overview + extended character summary implemented + tested
- generateWorldBrief tRPC mutation available
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md` when done. Document where companion `type` is stored on the characters row.
</output>
