---
phase: 07-content-depth-advanced-character
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/renderer/src/components/wizard/StepAbilityScores.tsx
  - src/renderer/src/components/ui/radio-group.tsx
  - src/renderer/src/components/ui/separator.tsx
  - src/renderer/src/lib/pointBuy.ts
autonomous: false
requirements: [CHAR-02, CHAR-03]
must_haves:
  truths:
    - "User can switch ability score method between Standard Array, 4d6 Roll, Point Buy, and Manual"
    - "Point Buy shows a live 'X / Y pts' budget that updates on score and flaw changes"
    - "Selecting negative-trait flaws increases the available point budget"
    - "Per-stat reroll button rerolls one stat, keeps the better result, then the reroll button is gone (disappears, per D-06)"
  artifacts:
    - path: "src/renderer/src/components/wizard/StepAbilityScores.tsx"
      provides: "Method selector + point buy + negative traits + per-stat reroll"
      contains: "Point Buy"
  key_links:
    - from: "src/renderer/src/components/wizard/StepAbilityScores.tsx"
      to: "src/renderer/src/lib/pointBuy.ts"
      via: "budget + cost computation imported"
      pattern: "calcPointBuy"
---

<objective>
Extend the character-creation ability score wizard step with a four-method selector (Standard Array, 4d6 Roll, Point Buy, Manual), a Point Buy tab with a live budget counter, a Negative Traits panel (10 preset flaws + 2 free-form fields) that adds points, and a per-stat reroll button for the 4d6 method.

Purpose: Delivers CHAR-02 (all ability score methods) and CHAR-03 (negative-trait point buy) end-to-end in the wizard. This is a self-contained renderer slice depending only on the Wave 0 schema (negativeTraits column).
Output: Extended StepAbilityScores.tsx + radio-group/separator shadcn components + a pointBuy helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md

<interfaces>
<!-- UI contract from 07-UI-SPEC.md §StepAbilityScores; math from RESEARCH. -->
POINT_BUY_COST: {8:0,9:1,10:2,11:3,12:4,13:5,14:7,15:9}. Base budget 27.
Budget = 27 + sum(presetFlaw.points) + min(nonEmptyFreeForm, 2) * 2.

10 preset flaws (RESEARCH.md lines 304-316): frail(+2 −2 maxHP/level), clumsy(+2 −2 AC), dim(+1 −2 passive Per/Inv),
weak(+2 −2 STR checks/saves), unlucky(+3 disadv one save type), slow(+2 −10ft speed), sickly(+1 disadv disease/poison saves),
timid(+1 −2 initiative), fumbling(+2 1/session disadv one attack), cursed(+2 nat-1 complication).

negativeTraits saved JSON shape: {presetFlaws: string[] (ids), freeFormFlaws: [string, string]}.
Persisted to characters.negativeTraits (Wave 0 column) on wizard advance.

WizardState (existing): StepAbilityScores props {wizardState, onChange}; add `negativeTraits` field to WizardState
and (if not present) `abilityScoreMethod`.

UI tokens (07-UI-SPEC §Color/§StepAbilityScores): budget counter font-mono; muted under budget, text-accent-gold at exact,
text-destructive over budget. Reroll green flash bg-green-950/40 text-green-400 600ms then default.
Method tabs use shadcn Tabs (h-9). Negative traits checkboxes use existing checkbox className="accent-amber-500".
radio-group + separator must be added via shadcn CLI before use.

Reroll button behavior (D-06 — locked decision): "Reroll button disappears once used." Implementation: after a stat's reroll
fires, the reroll control is removed — conditionally render the button to `null` once that stat's `used` flag is set (the
control is gone entirely, not disabled/greyed). A small muted "✓ used" indicator MAY replace the spot the button occupied;
the button control itself MUST be unmounted (the disappear), and only the used-state indicator remains. Do not leave a
disabled/greyed reroll button in place.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn radio-group + separator; create pointBuy helper</name>
  <files>src/renderer/src/components/ui/radio-group.tsx, src/renderer/src/components/ui/separator.tsx, src/renderer/src/lib/pointBuy.ts</files>
  <read_first>
    - src/renderer/src/components/ui/checkbox.tsx (existing shadcn component file shape/imports for this project)
    - components.json (shadcn config — alias paths)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Point Buy Math + preset flaws list lines 286-317)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Component Inventory — radio-group/separator needed)
  </read_first>
  <action>
    Add the shadcn `radio-group` and `separator` components (via `npx shadcn@latest add radio-group separator`, or by creating the component files following the exact structure of the existing `checkbox.tsx` if the CLI is unavailable in this environment). They must live at `src/renderer/src/components/ui/radio-group.tsx` and `separator.tsx` and export the standard shadcn API (RadioGroup, RadioGroupItem; Separator).

    Create `src/renderer/src/lib/pointBuy.ts` with: `POINT_BUY_COST` constant `{8:0,9:1,10:2,11:3,12:4,13:5,14:7,15:9}`; `PRESET_FLAWS` array of the 10 flaws `{id, name, penalty, points}` from RESEARCH.md lines 304-316; `calcPointBuyCost(scores)`; `calcPointBuyBudget(selectedPresetIds, freeFormFlaws)` returning `27 + sum(points of selected presets) + min(nonEmptyFreeForm, 2)*2`. This is the renderer-side mirror of the main-process calculations (renderer cannot import main code), so duplicate the small pure functions here.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "pointBuy|radio-group|separator" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/src/components/ui/radio-group.tsx exports `RadioGroup` and `RadioGroupItem`
    - src/renderer/src/components/ui/separator.tsx exports `Separator`
    - src/renderer/src/lib/pointBuy.ts contains `PRESET_FLAWS` with exactly 10 entries each having `id`, `name`, `penalty`, `points`
    - pointBuy.ts contains `calcPointBuyCost` and `calcPointBuyBudget`
    - tsc reports no errors in pointBuy.ts/radio-group.tsx/separator.tsx
  </acceptance_criteria>
  <done>radio-group + separator installed; pointBuy helper with 10 flaws + budget math created.</done>
</task>

<task type="auto">
  <name>Task 2: Method selector + Point Buy tab + Negative Traits panel + per-stat reroll</name>
  <files>src/renderer/src/components/wizard/StepAbilityScores.tsx</files>
  <read_first>
    - src/renderer/src/components/wizard/StepAbilityScores.tsx (current two-column layout, roll4d6DropLowest function, standard-array sidebar, manual override inputs, isStepXValid export, WizardState shape usage)
    - src/renderer/src/lib/pointBuy.ts (the helper from Task 1)
    - src/renderer/src/components/ui/tabs.tsx (Tabs/TabsList/TabsTrigger/TabsContent)
    - src/renderer/src/components/ui/checkbox.tsx + ui/textarea.tsx (flaw inputs)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ StepAbilityScores full spec lines 112-152; § Interaction Contracts; § Copywriting)
  </read_first>
  <action>
    Extend `StepAbilityScores.tsx` per UI-SPEC. Add a four-tab method selector (`Tabs`, h-9 triggers) at the top: "Standard Array" | "4d6 Roll" | "Point Buy" | "Manual". Track the active method in local `useState<AbilityScoreMethod>`; switching method, if any score is already assigned, shows the inline "Switching method will clear your current scores." confirmation with "Yes, switch" / "Keep current" (h-7 text-xs); on confirm, reset all six scores to null.

    Point Buy tab: single-column table of 6 ability rows, each `[ABBREV w-10][− Stepper md][score font-mono w-8][+ Stepper md][modifier w-12]`, score range 8–15 (decrement disabled at 8, increment at 15). Budget bar above rows: `"X / Y pts"` font-mono where X = calcPointBuyCost(scores), Y = calcPointBuyBudget(selectedPresets, freeForm); muted under budget, text-accent-gold at exact, text-destructive over (advance blocked). Below a Separator, the Negative Traits panel: heading "Negative Traits" + subtitle; Preset subsection = checkbox list of the 10 PRESET_FLAWS each row `[Checkbox][name bold][penalty muted][+N pts badge]`; Free-Form subsection = two Textarea rows={2}, each showing a `+2 pts` badge when non-empty.

    4d6 Roll tab: keep the existing roll-all + standard-array sidebar + assignment dropdowns. Add a per-stat `🎲` reroll button (h-6 w-6 ghost, aria-label="Reroll [ABILITY]") to the right of each stat after roll-all. On click: compute a fresh roll4d6DropLowest, set the stat to max(old, new), apply a 600ms green flash (bg-green-950/40 text-green-400 transition-colors duration-600). Then, per D-06 ("Reroll button disappears once used"), set that stat's `used` flag and CONDITIONALLY RENDER THE REROLL BUTTON TO `null` for that stat — the button control is fully unmounted (it disappears), not disabled/greyed. A muted "✓ used" span MAY occupy the now-empty slot as the used-state indicator, but the reroll button itself must be gone. Once-per-stat only; "Roll Stats" re-roll-all does NOT reset the `used` state.

    Manual tab: keep existing per-stat override inputs unchanged (rename the trigger label to "Manual").

    Persist `abilityScoreMethod` and `negativeTraits` ({presetFlaws: selected ids, freeFormFlaws: [s1, s2]}) into WizardState via onChange so the wizard saves them to characters.negativeTraits. Update the step's validity export so Point Buy is valid only when spent ≤ budget and all six scores assigned.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "StepAbilityScores" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - StepAbilityScores.tsx contains tab triggers with labels "Point Buy" and "Manual"
    - StepAbilityScores.tsx imports `calcPointBuyBudget` (or calcPointBuyCost) from `../../lib/pointBuy`
    - StepAbilityScores.tsx renders the 10 PRESET_FLAWS as checkboxes (maps over PRESET_FLAWS)
    - StepAbilityScores.tsx conditionally renders the per-stat reroll button to `null` once that stat's `used` flag is set (button unmounts — the D-06 "disappears" behavior), with a per-stat reroll handler that uses `Math.max`
    - The budget counter applies `text-destructive` when spent > budget (over-budget class present in source)
    - onChange persists `negativeTraits` into WizardState
    - tsc reports no errors in StepAbilityScores.tsx
  </acceptance_criteria>
  <done>Method selector, Point Buy + budget, Negative Traits panel, and per-stat reroll (button disappears once used, D-06) implemented per UI-SPEC; CHAR-02/CHAR-03 deliverable in the wizard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Extended ability score wizard step: 4-method selector, Point Buy with live budge</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>Extended ability score wizard step: 4-method selector, Point Buy with live budget + negative traits, and per-stat 4d6 reroll.</what-built>
  <how-to-verify>
    1. Run the app in dev (`npm run dev`).
    2. Start creating a character; reach the Ability Scores step.
    3. Verify four method tabs appear: Standard Array, 4d6 Roll, Point Buy, Manual.
    4. Select Point Buy: confirm a "X / 27 pts" counter; raise a score and watch spent increase; check a preset flaw (e.g. Frail) and confirm the budget rises by its points.
    5. Confirm over-spending turns the counter red and blocks advancing.
    6. Type into a free-form flaw field and confirm a "+2 pts" badge appears and budget rises.
    7. Switch to 4d6 Roll, click Roll Stats, then click a per-stat 🎲 reroll: confirm a brief green highlight, the value keeps the better roll, and the reroll button disappears (a muted "✓ used" remains, no clickable reroll button).
    8. Switch methods with scores assigned: confirm the clear-scores confirmation appears.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard input→characters.negativeTraits | free-form flaw text persisted + later injected into AI context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering | free-form flaw text | mitigate | Injection-time stripNewlines on negativeTraits handled in contextBuilder (07-03); wizard length-bounds the two fields |
</threat_model>

<verification>
- tsc clean for StepAbilityScores + pointBuy + new ui components
- Human-verify checkpoint approves the point buy + reroll UX (reroll button disappears once used)
</verification>

<success_criteria>
- Four-method selector with clear-on-switch confirmation
- Point Buy budget counter reactive to scores + flaws; over-budget blocks advance
- 10 preset flaws + 2 free-form fields adding points
- Per-stat 4d6 reroll keeping the better result, once per stat; reroll button disappears once used (D-06)
- negativeTraits persisted to WizardState
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
