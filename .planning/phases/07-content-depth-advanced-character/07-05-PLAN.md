---
phase: 07-content-depth-advanced-character
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - src/renderer/src/components/LevelUpModal.tsx
  - src/renderer/src/components/FeatPicker.tsx
  - src/main/db/charactersRepo.ts
  - src/main/trpc/routers/characters.ts
autonomous: false
requirements: [CHAR-04, CHAR-05, PROG-03]
must_haves:
  truths:
    - "At level-up the player can continue the primary class or add/level a multiclass with no ability prerequisites"
    - "At ASI levels the player chooses an ability score improvement or a feat"
    - "At the class subclass level the player chooses a subclass from classes.json"
    - "Above level 20 the player picks an Epic Boon instead of ASI/feat"
    - "Characters can level past 20 — no hard level-20 XP cap blocks the level-up flow (D-24)"
    - "Existing single-class characters level up exactly as before"
  artifacts:
    - path: "src/renderer/src/components/LevelUpModal.tsx"
      provides: "Multiclass + ASI/feat + subclass + Epic Boon level-up flow"
      contains: "isMulticlass"
    - path: "src/renderer/src/components/FeatPicker.tsx"
      provides: "Searchable SRD feat list + custom feat inline editor"
    - path: "src/main/db/charactersRepo.ts"
      provides: "Uncapped levelUp accepting optional classes/subclass"
      contains: "levelUp"
  key_links:
    - from: "src/renderer/src/components/LevelUpModal.tsx"
      to: "trpc.feats"
      via: "feat add + epic boon persistence"
      pattern: "feats\\.(add|listSrd|listEpicBoons)"
    - from: "src/renderer/src/components/FeatPicker.tsx"
      to: "trpc.feats.createCustom"
      via: "inline custom feat creation"
      pattern: "createCustom"
    - from: "src/renderer/src/components/LevelUpModal.tsx"
      to: "trpc.characters.levelUp"
      via: "multiclass classes + subclass persisted through extended levelUp"
      pattern: "characters\\.levelUp"
---

<objective>
Extend the Phase 5 LevelUpModal with four new branches: a multiclass choice (continue primary class or add/level a secondary class), an ASI-or-Feat choice at ASI levels, a subclass picker at the class subclass level, and an Epic Boon picker above level 20. Remove any hard level-20 XP cap so characters can level past 20 (D-24). Build a reusable FeatPicker (search + SRD list + inline custom feat editor) used here and in the creation wizard.

Purpose: Delivers CHAR-04 (free multiclassing), CHAR-05 (feats), PROG-03 (Epic Boons), and the D-24 uncapped progression through the level-up flow. All new branches gate behind guards so single-class characters are unchanged (Pitfall 5).
Output: Extended LevelUpModal.tsx + new FeatPicker.tsx + uncapped/extended charactersRepo.levelUp + characters.levelUp tRPC procedure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md

<interfaces>
<!-- From 07-03 (tRPC) + 07-02 (content) + existing LevelUpModal. -->
trpc.feats.listSrd() → FeatEntry[] {id,name,description,prerequisites}
trpc.feats.listEpicBoons() → {id,name,description}[]
trpc.feats.add({characterId, featName, featSource:'srd'|'custom'|'epic_boon', customFeatId?})
trpc.feats.listCustomByCampaign({campaignId}); trpc.feats.createCustom({campaignId, name, description})
trpc.library / classes content: subclasses come from classes.json (loaded via content router; subclasses[] + subclassLevel per class).

LevelUpModal guard (07-PATTERNS.md lines 792-818 / 07-RESEARCH.md lines 819-838):
  const isMulticlass = character.classes != null && character.classes.length > 1
  const totalLevel = isMulticlass ? sum(classes.level) : character.level
  const nextLevel = totalLevel + 1   // replaces character.level + 1
  const isEpicBoonLevel = nextLevel > 20
  const ASI_LEVELS = [4,8,12,16,19]; const isASILevel = ASI_LEVELS.includes(nextLevel)
  const subclassLevels = isMulticlass ? [] : (SUBCLASS_LEVELS[classKey] ?? []); isSubclassLevel = subclassLevels.includes(nextLevel)
All existing single-class HP/slot paths gate behind !isMulticlass — DO NOT refactor them (Pitfall 5).

classes JSON column shape: [{className, level}]. Persisting a multiclass pick writes to characters.classes.

Persistence contract (PRESCRIBED — do not improvise):
  Extend the existing `charactersRepo.levelUp` to accept an optional second argument:
    charactersRepo.levelUp(id: string, opts?: { classes?: ClassEntry[]; subclass?: string })
  where `ClassEntry = { className: string; level: number }` (the classes JSON column element shape).
  Extend the matching tRPC procedure `characters.levelUp` input to accept optional fields:
    { id: string, classes?: ClassEntry[], subclass?: string }  (both optional, Zod-validated).
  When `classes` is provided, it replaces characters.classes; when `subclass` is provided, it writes characters.subclass.
  When neither is provided, levelUp behaves exactly as it does today (single-class regression-safe).
  Do NOT create a separate new mutation — extend levelUp / characters.levelUp in place.

Level cap (D-24): XP thresholds and level-up checks are UNCAPPED — characters can level past 20. Any hard
  level-20 limit (e.g. a capped XP_THRESHOLDS table, a `Math.min(level, 20)`, or a `level >= 20` early-return guard)
  must be located and removed (see Task 0). The LevelUpModal `isEpicBoonLevel = nextLevel > 20` guard handles the UI
  branch, but the underlying XP→level progression must be unblocked so the modal can even open at level 21+.

UI-SPEC §LevelUpModal: max-h-[90vh]; multiclass radio (Continue / Add multiclass + Select of 12 classes);
ASI/Feat radio cards; Feat Picker (search Input h-8, ScrollArea h-200, selected row bg-secondary border-l-2 border-accent-gold,
"Create custom feat" below Separator → inline Name + Description + Save); Subclass picker Select from classes.json subclasses
(required to confirm); Epic Boon picker ScrollArea h-240 (required to confirm, no search). Confirm disabled until all required chosen.
radio-group + separator components: may already exist from 07-04; if so, do not recreate — import them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Remove hard level-20 XP cap; extend levelUp to accept classes/subclass (D-24)</name>
  <files>src/renderer/src/components/LevelUpModal.tsx, src/main/db/charactersRepo.ts, src/main/trpc/routers/characters.ts</files>
  <read_first>
    - src/renderer/src/components/LevelUpModal.tsx (the `XP_THRESHOLDS` constant / level-derivation logic and the `character.level + 1` usage; any `>= 20` or `Math.min(..., 20)` guard from Phase 5)
    - src/main/db/charactersRepo.ts (the existing `levelUp` function signature + body — how it computes the new level and writes the row; any XP-threshold or level cap there)
    - src/main/trpc/routers/characters.ts (the existing `characters.levelUp` procedure + its Zod input schema)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Epic Boons / Level Cap; A1 proficiency-bonus-past-20 note line 912)
    - .planning/phases/07-content-depth-advanced-character/07-CONTEXT.md (D-24 locked decision)
  </read_first>
  <action>
    D-24 (level cap removed): Audit Phase 5 for any hard level-20 cap and remove it so characters can level past 20.
    - In `LevelUpModal.tsx`: if the `XP_THRESHOLDS` table (or the function that maps XP→level) stops at level 20, extend it so the next-level computation continues past 20 (per RESEARCH A1: proficiency bonus follows `Math.ceil(level/4)+1`; extend the XP curve past 20 using the existing per-level delta, or remove any clamp so `nextLevel` is never capped at 20). Remove any `Math.min(level, 20)` / `level >= 20 ? ... : ...` ceiling on the displayed/computed level.
    - In `charactersRepo.levelUp`: remove any `if (level >= 20) return`/`Math.min(level, 20)` guard so the write proceeds at level 21+.
    Then extend the persistence contract (PRESCRIBED in <interfaces>): change the signature to
    `charactersRepo.levelUp(id, opts?: { classes?: ClassEntry[]; subclass?: string })` where `ClassEntry = { className: string; level: number }`. When `classes` is provided, write it to characters.classes; when `subclass` is provided, write characters.subclass; when neither is provided, preserve the exact existing single-class behavior. Extend the `characters.levelUp` tRPC Zod input to `{ id: string, classes?: ClassEntry[], subclass?: string }` (both optional) and thread the new fields through to the repo. Do NOT create a separate new mutation.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "LevelUpModal|charactersRepo|characters" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - No hard level-20 cap remains in LevelUpModal.tsx or charactersRepo.ts (no `Math.min(level, 20)` and no `level >= 20` early-return guard that blocks leveling)
    - charactersRepo.levelUp accepts an optional `{ classes?, subclass? }` argument and writes characters.classes / characters.subclass when provided
    - characters.levelUp tRPC input Zod schema includes optional `classes` and `subclass` fields
    - With no opts provided, levelUp behaves identically to the Phase 5 single-class path
    - tsc reports no errors in the touched files
  </acceptance_criteria>
  <done>Level-20 cap removed (D-24); levelUp + characters.levelUp extended to accept optional classes/subclass; single-class path regression-safe.</done>
</task>

<task type="auto">
  <name>Task 1: Build reusable FeatPicker component</name>
  <files>src/renderer/src/components/FeatPicker.tsx, src/renderer/src/components/ui/radio-group.tsx, src/renderer/src/components/ui/separator.tsx</files>
  <read_first>
    - src/renderer/src/components/ui/scroll-area.tsx, ui/input.tsx, ui/textarea.tsx, ui/button.tsx (primitives)
    - src/renderer/src/components/LevelUpModal.tsx (existing mutation pattern with useMutation + queryClient.invalidateQueries lines ~144-166)
    - src/renderer/src/lib/trpc.ts (trpc client usage)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Feat Picker spec lines 171-176; § Copywriting feat picker labels)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ TanStack Query + tRPC mutation pattern)
  </read_first>
  <action>
    Create `src/renderer/src/components/FeatPicker.tsx` exporting a `FeatPicker` component with props `{ campaignId: string; selectedFeatName: string | null; onSelect: (sel: { featName: string; featSource: 'srd' | 'custom'; customFeatId?: string }) => void }`. Layout per UI-SPEC: a search `Input` (h-8, placeholder "Search feats…") filtering the in-memory list client-side; a `ScrollArea h-[200px]` listing SRD feats (from `trpc.feats.listSrd` query) and campaign custom feats (from `trpc.feats.listCustomByCampaign`), each row showing name (14px semibold) + prerequisites (12px muted, single line); the selected row gets `bg-secondary border-l-2 border-accent-gold`. Below a `Separator`, a "Create custom feat" row that opens an inline form (Name `Input h-9` + Description `Textarea rows={3}`) with a "Save Custom Feat" button (size sm, variant outline) that calls `trpc.feats.createCustom`; on success the new feat appears at the top of the list pre-selected. List has `role="listbox"`, rows `role="option"` + `aria-selected`. Keyboard: arrow keys navigate, Enter selects.

    If `radio-group.tsx` / `separator.tsx` do not yet exist (07-04 not merged), add them via shadcn following checkbox.tsx structure; if they exist, import them and do not recreate.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "FeatPicker" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - FeatPicker.tsx exports `FeatPicker`
    - FeatPicker.tsx queries `trpc.feats.listSrd` and `trpc.feats.listCustomByCampaign`
    - FeatPicker.tsx contains a "Create custom feat" affordance calling `trpc.feats.createCustom`
    - FeatPicker.tsx applies `role="listbox"` and `role="option"` for accessibility
    - search input filters the list client-side (a filter using toLowerCase on name)
    - tsc reports no errors in FeatPicker.tsx
  </acceptance_criteria>
  <done>Reusable FeatPicker with SRD search, custom feats, and inline custom-feat editor.</done>
</task>

<task type="auto">
  <name>Task 2: Extend LevelUpModal with multiclass, ASI/feat, subclass, Epic Boon branches</name>
  <files>src/renderer/src/components/LevelUpModal.tsx</files>
  <read_first>
    - src/renderer/src/components/LevelUpModal.tsx (FULL — current derived values lines 104-138, SUBCLASS_LEVELS map, HIT_DIE_BY_CLASS, XP_THRESHOLDS, footer confirmDisabled pattern, the deferred-subclass-note paragraph, the existing single-class HP/slots path)
    - src/renderer/src/components/FeatPicker.tsx (from Task 1)
    - src/renderer/src/components/ui/select.tsx (subclass + class + ability dropdowns)
    - src/renderer/src/components/ui/radio-group.tsx, ui/separator.tsx, ui/scroll-area.tsx
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ LevelUpModal Extension Guard lines 819-838; § Subclass Levels lines 459-473)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ LevelUpModal full spec lines 152-191; § Interaction Contracts level-up guard)
  </read_first>
  <action>
    Extend `LevelUpModal.tsx` adding the guard block after the existing derived declarations: `isMulticlass`, `totalLevel`, `nextLevel = totalLevel + 1` (replace the existing `character.level + 1`), `isEpicBoonLevel = nextLevel > 20`, `ASI_LEVELS = [4,8,12,16,19]`, `isASILevel`, and the subclass-level check (subclassLevels empty when multiclass). Expand max-h to `max-h-[90vh]`. (Task 0 has already removed any hard level-20 cap so `nextLevel` can exceed 20 — do not reintroduce a clamp.)

    Multiclass branch (top of body, before HP section): two radio options — "Continue as [primary class] (Level N)" (default) and "Add / level multiclass" with a `Select` of the 12 SRD class names. Skip this entire section for single-class characters (no `classes` or length ≤ 1) — the existing single-class path is unchanged and gated behind `!isMulticlass` where it reads class-specific hit die / slots.

    ASI/Feat choice (only when isASILevel and not isEpicBoonLevel): a Separator + heading "Ability Score Improvement or Feat" + two radio cards. ASI card reveals "+2 to one" (Select of 6 abilities) or "+1 to two" (two Selects). Feat card mounts `<FeatPicker campaignId={...} selectedFeatName onSelect />`.

    Subclass picker (only when isSubclassLevel): replace the Phase 5 deferred-note paragraph with a heading "Choose Your Subclass" + a `Select` populated from the character's class `subclasses` array (loaded via content/classes query) + the selected subclass description (3-line clamp + Show more). Required before confirm.

    Epic Boon picker (only when isEpicBoonLevel — replaces ASI/feat section): heading "Epic Boon" + subtitle "Choose one boon for reaching Level N." + a `ScrollArea h-[240px]` list from `trpc.feats.listEpicBoons` (selected row bg-secondary border-l-2 border-accent-gold). Required before confirm.

    On Confirm Level Up: persist the chosen path through the extended `trpc.characters.levelUp` (PRESCRIBED contract from <interfaces> / Task 0): a multiclass pick passes the updated `classes: ClassEntry[]` array; a subclass pick passes `subclass: string`. Feat persistence uses `trpc.feats.add({featSource:'srd'|'custom'})`; Epic Boon uses `trpc.feats.add({featSource:'epic_boon'})`. Extend the existing `confirmDisabled` boolean: disabled until HP chosen AND (subclass chosen if subclass level) AND (feat chosen if Feat selected) AND (Epic Boon chosen if 21+). Keep "Not Now" ghost.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "LevelUpModal" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - LevelUpModal.tsx contains `const isMulticlass` and `const isEpicBoonLevel` and `ASI_LEVELS`
    - LevelUpModal.tsx mounts `<FeatPicker` when the Feat option is selected
    - LevelUpModal.tsx contains the heading text "Epic Boon" and "Choose Your Subclass"
    - LevelUpModal.tsx calls `trpc.feats.add` with `feat_source`/`featSource` value `'epic_boon'` for boons
    - LevelUpModal.tsx persists multiclass `classes` / `subclass` via `trpc.characters.levelUp` (the extended procedure), not a hand-rolled mutation
    - The deferred-subclass note paragraph from Phase 5 is removed (no "future update" placeholder text remains)
    - single-class HP/slot code remains gated behind `!isMulticlass` (existing path not deleted)
    - tsc reports no errors in LevelUpModal.tsx
  </acceptance_criteria>
  <done>LevelUpModal handles multiclass, ASI/feat, subclass, and Epic Boon; persists via extended characters.levelUp; single-class regression-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Extended LevelUpModal: multiclass choice, ASI/Feat picker, subclass picker, Epic</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>Extended LevelUpModal: multiclass choice, ASI/Feat picker, subclass picker, Epic Boon picker; reusable FeatPicker; level-20 cap removed (D-24).</what-built>
  <how-to-verify>
    1. Run the app (`npm run dev`) with an existing single-class character.
    2. Level up a Fighter to level 3: verify a "Choose Your Subclass" picker appears with options.
    3. Level a character to level 4: verify the "Ability Score Improvement or Feat" choice; pick Feat and confirm the FeatPicker shows a searchable SRD list; type to filter; select a feat.
    4. In the FeatPicker, click "Create custom feat", enter a name + description, save, and confirm it appears pre-selected.
    5. Trigger a level-up and choose "Add / level multiclass": confirm a class dropdown with all 12 classes; confirm the existing single-class HP flow still works when NOT multiclassing.
    6. Set a character to level 20 and trigger level up: verify the Epic Boon picker replaces ASI/feat and lists boons; confirm cannot be confirmed until a boon is selected.
    7. Set a character to level 20 with enough XP for level 21 and confirm the level-up actually proceeds past 20 (D-24 — no cap blocks it), reaching level 21 with another Epic Boon offered.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| custom feat input→trpc.feats.createCustom | name/description persisted + AI-injected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | custom feat name/description | mitigate | Length bounds enforced server-side by feats.createCustom Zod (name ≤100, desc ≤2000) in 07-03; stripNewlines at injection |
| T-07-05-02 | Tampering | multiclass/feat persistence | mitigate | All writes go through Zod-validated tRPC mutations (extended characters.levelUp + feats.add); no direct DB access from renderer |
</threat_model>

<verification>
- tsc clean for LevelUpModal + FeatPicker + charactersRepo + characters router
- No hard level-20 cap remains (D-24)
- Human-verify checkpoint approves all four level-up branches + past-20 leveling + single-class regression
</verification>

<success_criteria>
- Level-20 cap removed; characters can level past 20 (D-24)
- Multiclass, ASI/feat, subclass, Epic Boon branches all functional and gated
- Multiclass/subclass persisted via extended charactersRepo.levelUp / characters.levelUp
- Reusable FeatPicker with search + custom feat editor
- Single-class characters level up unchanged (Pitfall 5)
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-05-SUMMARY.md` when done. Note how the multiclass pick + subclass are persisted (the extended characters.levelUp) and confirm the level-20 cap removal.
</output>
</content>
</invoke>
