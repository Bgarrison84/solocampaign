---
phase: 07-content-depth-advanced-character
plan: 06
type: execute
wave: 4
depends_on: ["07-01", "07-03", "07-04", "07-05"]
files_modified:
  - src/renderer/src/components/CreateCharacterWizard.tsx
autonomous: false
requirements: [CHAR-05, PARTY-01]
must_haves:
  truths:
    - "Character creation includes an optional Starting Feat step using the shared FeatPicker"
    - "A selected starting feat is persisted to the created character"
    - "The negative traits chosen in the ability step are saved to the character"
    - "The wizard can be re-triggered to add additional party members until partySize is filled"
  artifacts:
    - path: "src/renderer/src/components/CreateCharacterWizard.tsx"
      provides: "Starting Feat step + re-triggerable party member flow"
      contains: "FeatPicker"
  key_links:
    - from: "src/renderer/src/components/CreateCharacterWizard.tsx"
      to: "trpc.feats.add"
      via: "starting feat persisted after character creation"
      pattern: "feats\\.add"
---

<objective>
Extend the character creation wizard with an optional Starting Feat step (reusing FeatPicker), persist the ability-step negative traits to the character, and make the wizard re-triggerable to add additional party members. After the character is created, the starting feat is written via trpc.feats.add.

Purpose: Delivers the creation-side of CHAR-05 (starting feats) and the multi-character creation flow for PARTY-01.
Output: Extended CreateCharacterWizard.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-04-SUMMARY.md
@.planning/phases/07-content-depth-advanced-character/07-05-SUMMARY.md

<interfaces>
<!-- From 07-04 (negativeTraits in WizardState), 07-05 (FeatPicker), 07-03 (feats.add + partySize enforcement). -->
FeatPicker props: { campaignId, selectedFeatName, onSelect(sel: {featName, featSource:'srd'|'custom', customFeatId?}) }
trpc.feats.add({characterId, featName, featSource, customFeatId?})
WizardState now carries negativeTraits + abilityScoreMethod (07-04) — persisted to characters.negativeTraits on create.
charactersRepo.create enforces partySize (07-03) — throws if full; the wizard surfaces that error.

UI-SPEC: Starting Feat is an optional step OR a section in the review step (Copywriting CTA "Add Starting Feat").
Re-trigger: the "+ Add Character" affordance lives in CharacterSheetTab (built in 07-08); this plan only ensures the wizard
accepts an existing campaign + can run standalone for an additional member (no campaign-creation coupling) and that on
finish it returns control so the caller can refresh the party list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add optional Starting Feat step + persist negative traits + feat on create</name>
  <files>src/renderer/src/components/CreateCharacterWizard.tsx</files>
  <read_first>
    - src/renderer/src/components/CreateCharacterWizard.tsx (FULL — current steps, WizardState, the final create mutation, how the character is saved, review step layout)
    - src/renderer/src/components/FeatPicker.tsx (props contract from 07-05)
    - src/renderer/src/components/wizard/StepAbilityScores.tsx (WizardState negativeTraits field from 07-04)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Copywriting "Add Starting Feat"; Feat Picker reuse)
  </read_first>
  <action>
    Add an optional "Starting Feat" step (or a clearly labeled section in the review step, whichever fits the existing wizard's step model with least disruption — prefer a dedicated step appended before the review step) that mounts `<FeatPicker campaignId={campaignId} selectedFeatName={...} onSelect={...} />` and stores the chosen feat in WizardState (`startingFeat: { featName, featSource, customFeatId? } | null`). The step is skippable (a "Skip" / "No starting feat" path) — a starting feat is optional.

    Ensure the create flow persists `negativeTraits` (from the ability step, 07-04) to `characters.negativeTraits` as part of the existing character-create mutation payload (add the field to the create input if not already plumbed). After the character is successfully created and its id is known, if a starting feat was chosen, call `trpc.feats.add({ characterId, featName, featSource, customFeatId })`.

    Make the wizard reusable for additional party members: it must accept the campaignId it is creating into and not assume it is the first/only character. On finish, invalidate the campaign's characters query and close/return so the caller (CharacterSheetTab in 07-08) can refresh the switcher. If `charactersRepo.create` throws because partySize is full, surface a non-blocking inline error ("This campaign's party is full.").
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "CreateCharacterWizard" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - CreateCharacterWizard.tsx mounts `<FeatPicker`
    - CreateCharacterWizard.tsx stores a `startingFeat` in WizardState and the step is skippable
    - The create flow includes `negativeTraits` in the character-create payload
    - After create, `trpc.feats.add` is called when a starting feat is selected
    - The wizard invalidates the characters list query on finish
    - tsc reports no errors in CreateCharacterWizard.tsx
  </acceptance_criteria>
  <done>Optional Starting Feat step added; negative traits + starting feat persisted; wizard reusable for party members.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Character wizard Starting Feat step + negative-trait persistence + re-triggerabl</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>Character wizard Starting Feat step + negative-trait persistence + re-triggerable party-member creation.</what-built>
  <how-to-verify>
    1. Run the app; create a character and reach the new Starting Feat step.
    2. Confirm the FeatPicker appears, that the step is skippable, and that selecting a feat then finishing creates the character with that feat (check the character sheet / AI summary).
    3. Create a character via Point Buy with a couple of negative traits; finish; confirm the traits are saved (visible in the AI character context or the sheet).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wizard input→character create | feat name + negative traits persisted, later AI-injected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | starting feat persistence | mitigate | feats.add Zod-validated server-side (07-03); custom feats bounded by createCustom |
| T-07-06-02 | Tampering | party-size bypass on add member | mitigate | charactersRepo.create enforces partySize (07-03); wizard surfaces the throw |
</threat_model>

<verification>
- tsc clean for CreateCharacterWizard
- Human-verify checkpoint approves starting feat + negative trait persistence
  (the "+ Add Character" re-trigger flow is verified in 07-08's checkpoint, where that affordance is built)
</verification>

<success_criteria>
- Optional, skippable Starting Feat step using shared FeatPicker
- negativeTraits + starting feat persisted to the created character
- Wizard re-triggerable for additional party members; partySize-full handled
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-06-SUMMARY.md` when done.
</output>
</content>
</invoke>
