---
phase: 07-content-depth-advanced-character
plan: 07
type: execute
wave: 3
depends_on: ["07-01", "07-03", "07-04"]
files_modified:
  - src/renderer/src/components/CreateCampaignModal.tsx
  - src/renderer/src/components/wizard/StepWorldSetup.tsx
autonomous: false
requirements: [PARTY-01, WORLD-01, STATE-06]
must_haves:
  truths:
    - "Campaign creation lets the user set party size (Solo/2/3/4) and toggle encumbrance"
    - "Campaign creation has a World Setup step with AI / Write Brief / Import Document modes"
    - "AI Generates mode produces a world brief on submit via generateWorldBrief"
    - "Write a Brief saves the textarea to worldBrief; Import Document saves extracted text to worldDocument"
  artifacts:
    - path: "src/renderer/src/components/wizard/StepWorldSetup.tsx"
      provides: "World setup radio cards + conditional content"
      contains: "worldSetupMode"
    - path: "src/renderer/src/components/CreateCampaignModal.tsx"
      provides: "Party size + encumbrance + 4-step wizard + world brief generation"
      contains: "party"
  key_links:
    - from: "src/renderer/src/components/CreateCampaignModal.tsx"
      to: "trpc.campaigns.generateWorldBrief"
      via: "AI-generate submit path"
      pattern: "generateWorldBrief"
---

<objective>
Extend campaign creation to a 4-step wizard: Step 0 (Campaign) gains a Party Size radio (Solo/Small/Party/Full) and an Encumbrance toggle; a new Step 1 (World Setup) offers three modes (AI Generates / Write a Brief / Import a Document); existing AI Provider and DM Style shift to steps 2 and 3. AI-generate fires generateWorldBrief on submit with a spinner.

Purpose: Delivers PARTY-01 (party size selection), WORLD-01 (three world-setup modes), and STATE-06 (encumbrance toggle) at campaign creation.
Output: Extended CreateCampaignModal.tsx + new StepWorldSetup.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md

<interfaces>
<!-- From 07-03 (generateWorldBrief + campaign columns) + existing wizard. -->
trpc.campaigns.create — extend payload with partySize (int), encumbranceEnabled (bool), worldSetupMode ('ai'|'brief'|'import'),
worldBrief (string|null), worldDocument (string|null). (Confirm the campaigns.create input schema is extended in 07-03 or extend here.)
trpc.campaigns.generateWorldBrief({campaignId}) → returns generated brief; saves to campaign.worldBrief.
File import for 'import' mode: a tRPC/IPC call that opens dialog.showOpenDialog (.pdf/.txt/.md) and returns extracted text
(reuse pdfExtractor via a campaignDocs-style import or a dedicated world-doc import procedure). Save the text to worldDocument.

UI-SPEC §CreateCampaignModal: STEP_LABELS = ['Campaign','World Setup','AI Provider','DM Style']; WizardProgress totalSteps={4}.
Party Size 2×2 RadioGroup cards (Solo=User icon default; Small/Party/Full=Users). Encumbrance Checkbox below.
World Setup: 3 stacked RadioGroup cards (Sparkles/PenLine/FileText), 'ai' default; 'brief' reveals Textarea rows=6;
'import' reveals Choose file button + filename + >12,000-char truncation warning (Pitfall 4).
AI-generate submit: "Create Campaign" → spinner "Generating your world…" + "This may take up to 30 seconds…";
on success navigate to campaign; on failure campaign still created with null brief + non-blocking error chip.
radio-group: may already exist from 07-04; import, do not recreate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepWorldSetup component</name>
  <files>src/renderer/src/components/wizard/StepWorldSetup.tsx, src/renderer/src/components/ui/radio-group.tsx</files>
  <read_first>
    - src/renderer/src/components/wizard/StepAbilityScores.tsx (wizard step props {wizardState, onChange} + isStepValid export pattern)
    - src/renderer/src/components/ReferenceDocSelect.tsx (existing dialog.showOpenDialog file-pick IPC pattern)
    - src/renderer/src/components/ui/textarea.tsx, ui/button.tsx, ui/radio-group.tsx
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Step 1 World Setup lines 214-228; § Copywriting world setup labels/placeholder)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ StepWorldSetup pattern lines 384-423)
  </read_first>
  <action>
    Create `src/renderer/src/components/wizard/StepWorldSetup.tsx` exporting `StepWorldSetup` ({wizardState, onChange}) and `isStepWorldSetupValid(state): boolean`. Three stacked `RadioGroup` cards mapped to `worldSetupMode` 'ai' | 'brief' | 'import' (default 'ai'), each card `bg-secondary rounded-lg p-4 border border-border`, selected `border-accent-gold`, with lucide icons Sparkles/PenLine/FileText, title (font-semibold) + subtitle (text-xs muted) per the Copywriting labels ("AI Generates", "Write a Brief", "Import a Document"). Conditional content below the selected card: 'brief' → a `Textarea rows={6}` (placeholder from Copywriting) bound to wizardState.worldBrief; 'import' → a "Choose file…" `Button` (variant outline, sm) that triggers the OS file picker (`.pdf`,`.txt`,`.md`) via the existing dialog IPC pattern, displays the chosen filename, stores the extracted text into wizardState.worldDocument, and shows the amber truncation warning when the text length > 12,000 chars (TriangleAlert icon, copy from §Copywriting "file too large warning"); 'ai' → static helper text, no input. Validity: always valid for 'ai'; 'brief' valid (empty allowed — AI can still run); 'import' valid once a file is chosen OR allow proceeding without (document optional). Keep it permissive — no hard blocks.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "StepWorldSetup" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - StepWorldSetup.tsx exports `StepWorldSetup` and `isStepWorldSetupValid`
    - Three RadioGroup cards mapped to 'ai' | 'brief' | 'import' with 'ai' default
    - 'brief' reveals a Textarea bound to worldBrief; 'import' reveals a file picker storing worldDocument
    - The >12,000-char truncation warning is rendered conditionally
    - tsc reports no errors in StepWorldSetup.tsx
  </acceptance_criteria>
  <done>StepWorldSetup component with three modes + conditional content + large-doc warning.</done>
</task>

<task type="auto">
  <name>Task 2: Extend CreateCampaignModal to 4 steps with party size, encumbrance, world brief generation</name>
  <files>src/renderer/src/components/CreateCampaignModal.tsx</files>
  <read_first>
    - src/renderer/src/components/CreateCampaignModal.tsx (FULL — current 3-step structure, STEP_LABELS, WizardProgress totalSteps, handleSubmit, state reset on open lines 87-102, the create mutation)
    - src/renderer/src/components/wizard/StepWorldSetup.tsx (from Task 1)
    - src/renderer/src/components/ui/radio-group.tsx, ui/checkbox.tsx
    - src/renderer/src/components/WizardProgress.tsx (totalSteps prop)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ CreateCampaignModal lines 193-228; § World brief generation spinner; § Copywriting)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ Wizard Step Extension Pattern lines 705-735)
  </read_first>
  <action>
    Change `STEP_LABELS` to `['Campaign','World Setup','AI Provider','DM Style']` and `WizardProgress totalSteps={4}`. In Step 0 (Campaign), below the name field, add: a Party Size `RadioGroup` 2×2 grid (Solo=1 default / Small Party=2 / Party=3 / Full Party=4) with User/Users lucide icons, storing `partySize` in modal state; and an Encumbrance `Checkbox` + `Label` ("Enable encumbrance tracking…") storing `encumbranceEnabled`. Insert the new `StepWorldSetup` as Step 1 (passing the wizard state + onChange); shift AI Provider to Step 2 and DM Style to Step 3. Update step navigation handlers and the `completedUpTo` progression accordingly, and extend the open-reset effect to reset partySize=1, encumbranceEnabled=false, worldSetupMode='ai', worldBrief='', worldDocument=null.

    Extend the create payload to send partySize, encumbranceEnabled, worldSetupMode, worldBrief (for 'brief' mode), worldDocument (for 'import' mode). On final submit: create the campaign first; if worldSetupMode === 'ai', then call `trpc.campaigns.generateWorldBrief({campaignId})` while showing the submit button as a spinner labeled "Generating your world…" with the "This may take up to 30 seconds…" helper text (Pitfall 3). On generateWorldBrief success, navigate to the campaign. On failure, still navigate (campaign exists with null brief) and surface a non-blocking error chip ("World brief generation failed. You can write a brief manually from the campaign settings later.").
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "CreateCampaignModal" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - CreateCampaignModal.tsx STEP_LABELS equals `['Campaign','World Setup','AI Provider','DM Style']`
    - WizardProgress is rendered with `totalSteps={4}`
    - Step 0 renders a Party Size RadioGroup (4 options) and an encumbrance Checkbox
    - Step 1 mounts `<StepWorldSetup`
    - handleSubmit calls `trpc.campaigns.generateWorldBrief` only when worldSetupMode is 'ai'
    - The create payload includes partySize, encumbranceEnabled, worldSetupMode
    - tsc reports no errors in CreateCampaignModal.tsx
  </acceptance_criteria>
  <done>4-step campaign wizard with party size, encumbrance, world setup, and AI brief generation on submit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 4-step campaign creation: party size + encumbrance + world setup modes + AI worl</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>4-step campaign creation: party size + encumbrance + world setup modes + AI world-brief generation.</what-built>
  <how-to-verify>
    1. Run the app; click New Campaign.
    2. Step 0: confirm Party Size cards (Solo default) + an encumbrance checkbox.
    3. Step 1 (World Setup): confirm three cards; select "Write a Brief" and type text; select "Import a Document" and pick a .txt/.pdf and confirm filename + (for a large file) the truncation warning.
    4. Select "AI Generates" and complete the wizard with a configured provider: confirm the submit button shows "Generating your world…" spinner, then the campaign opens.
    5. Start a session and inspect the AI context (or behavior) to confirm the world brief is present.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported world doc→worldDocument→system prompt | extracted file text injected into AI context |
| file picker→pdfExtractor | user-chosen file read in main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | world document file import | mitigate | filePath validated absolute + no `..` in the import procedure (07-03); content capped on storage + truncated on injection |
| T-07-07-02 | Denial of Service | oversized world document | mitigate | >12,000-char UI warning + 16,000-char injection truncation in contextBuilder |
</threat_model>

<verification>
- tsc clean for CreateCampaignModal + StepWorldSetup
- Human-verify checkpoint approves the 4-step flow + AI brief generation
</verification>

<success_criteria>
- Party size + encumbrance in Step 0
- World Setup step with 3 working modes
- generateWorldBrief fires only for AI mode with spinner + non-blocking failure
- create payload carries all new campaign fields
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-07-SUMMARY.md` when done.
</output>
