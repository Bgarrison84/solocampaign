---
phase: 07-content-depth-advanced-character
plan: 08
type: execute
wave: 5
depends_on: ["07-01", "07-03", "07-05", "07-06"]
files_modified:
  - src/renderer/src/screens/CampaignViewScreen.tsx
  - src/renderer/src/stores/campaignViewStore.ts
  - src/renderer/src/components/CompanionsSection.tsx
  - src/renderer/src/components/sheet/SheetHeader.tsx
  - src/renderer/src/components/CharacterSheetTab.tsx
autonomous: false
requirements: [PARTY-01, PARTY-02, CHAR-04, STATE-06]
must_haves:
  truths:
    - "In a party campaign the Character Sheet tab shows a switcher chip per party member"
    - "Clicking a chip switches the displayed character sheet via activeCharacterId"
    - "A '+ Add Character' chip appears until partySize is filled and opens the wizard"
    - "Companions appear in a collapsible Companions section with HP stepper, AC, type badge, and remove"
    - "Multiclass characters show a Fighter 5 / Wizard 3 — Level 8 breakdown in the sheet header"
    - "When encumbrance is enabled the Inventory tab shows carried weight and threshold badges"
  artifacts:
    - path: "src/renderer/src/components/CompanionsSection.tsx"
      provides: "Collapsible companions section with add/remove/HP"
    - path: "src/renderer/src/stores/campaignViewStore.ts"
      provides: "activeCharacterId state"
      contains: "activeCharacterId"
  key_links:
    - from: "src/renderer/src/components/CharacterSheetTab.tsx"
      to: "campaignViewStore.activeCharacterId"
      via: "switcher chips drive the displayed sheet"
      pattern: "activeCharacterId"
---

<objective>
Build the party-mode Character Sheet view: character switcher chips driven by a new `activeCharacterId` Zustand field (reset on campaign change per Pitfall 6), a "+ Add Character" chip that re-triggers the creation wizard until partySize is filled, a collapsible CompanionsSection (HP stepper, AC, type badge, conditions, add/remove), a multiclass breakdown in SheetHeader, and the encumbrance display in the Inventory tab.

Purpose: Delivers the party/companion UI for PARTY-01 + PARTY-02, the multiclass display for CHAR-04, and the encumbrance display for STATE-06.
Output: switcher chips + CompanionsSection.tsx + SheetHeader multiclass + Inventory encumbrance + campaignViewStore.activeCharacterId.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md
@.planning/phases/07-content-depth-advanced-character/07-06-SUMMARY.md

<interfaces>
<!-- From 07-03 (companion data, type storage), 07-06 (wizard re-trigger), existing sheet/store. -->
charactersRepo: companions are characters with isCompanion=true; companion `type` stored per 07-03-SUMMARY (read it).
trpc.characters.list({campaignId}) returns all characters; split party members (isCompanion=false) vs companions (isCompanion=true).
Companion HP updates via the existing characters/resources HP mutation (same as character HP stepper).
Companion removal: trpc — either characters delete or via the same path mutationPipeline.removeCompanion uses; use a characters
tRPC delete scoped to campaign (add if missing).
Pitfall 6: activeCharacterId MUST reset to null when the active campaign changes.

UI-SPEC §CharacterSheetTab: switcher row (party mode only, hidden when partySize===1) flex flex-wrap gap-2;
active chip bg-accent-gold/20 border-accent-gold; "+ Add Character" dashed chip when characters.length < partySize.
role="tablist"/role="tab"/aria-selected on chips. CompanionsSection below switcher (shown if any companions, even solo).
§Companions: collapsible, ChevronDown/Right + "Companions (N)"; rows with name, type badge (color by type), Stepper sm HP,
AC text, condition pills, ghost X remove (inline confirm "Remove [name]?"). Add Companion button → Dialog max-w-360
(Name/Type Select/HP Max/AC; validation name+HP+AC).
§SheetHeader multiclass: replace subtitle with race line + "Fighter 5 / Wizard 3 — Level 8" when classes.length>1, null-safe fallback.
§Inventory encumbrance: banner "Carrying Capacity" + "X / Y lbs" font-mono; Encumbered badge at 5×STR (amber), Heavily at 10×STR (red).
type badge colors: Familiar purple, Animal Companion green, Summoned Creature blue (ConditionBadge pill pattern).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: activeCharacterId store + character switcher chips + Add Character chip</name>
  <files>src/renderer/src/stores/campaignViewStore.ts, src/renderer/src/components/CharacterSheetTab.tsx, src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/stores/campaignViewStore.ts (current Zustand state shape, set/reset patterns)
    - src/renderer/src/components/CharacterSheetTab.tsx (current sheet orchestration — how the single character is fetched + rendered)
    - src/renderer/src/screens/CampaignViewScreen.tsx (campaign load, where campaign change happens, how CharacterSheetTab is mounted, how the wizard is opened)
    - src/renderer/src/components/CreateCharacterWizard.tsx (re-trigger contract from 07-06)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Character switcher lines 231-236; § Interaction Contracts character switcher; § Accessibility chips)
  </read_first>
  <action>
    Add `activeCharacterId: string | null` to `campaignViewStore` with a setter and ensure it resets to `null` whenever the active campaign changes (Pitfall 6) — wire the reset where CampaignViewScreen loads/switches a campaign.

    In `CharacterSheetTab.tsx`: query `trpc.characters.list({campaignId})`, split into party members (isCompanion=false) and companions (isCompanion=true). When `campaign.partySize > 1`, render a switcher chip row at the top (flex flex-wrap gap-2, role="tablist"): one chip per party member (role="tab", aria-selected), active chip uses accent-gold styling and is driven by `activeCharacterId` (default to the first member when null). Clicking a chip sets activeCharacterId. When `partyMembers.length < campaign.partySize`, render a dashed "+ Add Character" chip that opens `CreateCharacterWizard` for this campaign; on wizard finish, invalidate the characters query so the new member appears. The displayed sheet below renders for the active character. In solo mode (partySize===1) hide the switcher entirely and render the single character as before.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "campaignViewStore|CharacterSheetTab|CampaignViewScreen" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - campaignViewStore.ts contains `activeCharacterId`
    - CampaignViewScreen.tsx resets activeCharacterId to null on campaign change
    - CharacterSheetTab.tsx renders the switcher only when partySize > 1 (a `partySize > 1` guard)
    - CharacterSheetTab.tsx chips use role="tablist"/role="tab"/aria-selected
    - A dashed "+ Add Character" chip renders when partyMembers.length < partySize and opens the wizard
    - tsc reports no errors in the three files
  </acceptance_criteria>
  <done>Party switcher chips + Add Character chip driven by activeCharacterId, reset on campaign change.</done>
</task>

<task type="auto">
  <name>Task 2: CompanionsSection + SheetHeader multiclass + Inventory encumbrance</name>
  <files>src/renderer/src/components/CompanionsSection.tsx, src/renderer/src/components/sheet/SheetHeader.tsx, src/renderer/src/components/CharacterSheetTab.tsx</files>
  <read_first>
    - src/renderer/src/components/QuestsTab.tsx (item-row + collapsible list analog, lines 48-68)
    - src/renderer/src/components/ui/collapsible.tsx, ui/dialog.tsx, ui/select.tsx, ui/input.tsx
    - src/renderer/src/components/sheet/Stepper.tsx (HP stepper sizes)
    - src/renderer/src/components/sheet/ConditionBadge.tsx (pill pattern for type/encumbrance badges)
    - src/renderer/src/components/sheet/SheetHeader.tsx (current race/class/level subtitle lines 71-74)
    - the Inventory tab component (find via grep for the inventory weight field / character_items rendering)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ Companions collapsible lines 238-251; § SheetHeader multiclass lines 254-259; § Inventory Tab Encumbrance Display lines 313-323; § Copywriting companions/encumbrance)
    - .planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md (where companion type is stored)
  </read_first>
  <action>
    Create `src/renderer/src/components/CompanionsSection.tsx` with props `{ companions: CharacterWithResources[]; onUpdateHp: (companionId, delta) => void; onRemove: (companionId) => void; onAdd: () => void }`. Outer `Collapsible` (ChevronDown/Right + "Companions (N)", 40px toggle row). Each companion row: name (semibold), type badge colored by type (Familiar purple / Animal Companion green / Summoned Creature blue, ConditionBadge pill style), `Stepper size="sm"` HP "N / N", "AC: N" muted, condition pills, a ghost `X` remove button (text-muted-foreground hover:text-destructive) with an inline "Remove [name]?" confirm ("Remove" destructive / "Keep" ghost). An "Add Companion" outline button at the bottom opens a `Dialog` (max-w-360) with Name `Input`, Type `Select` (3 options), HP Max `Input type=number min=1`, AC `Input type=number min=1 max=30`, footer Cancel/Add Companion, validating name + HP≥1 + AC 1–30 (error copy "Name, HP, and AC are required."). Wire HP via the existing characters HP mutation and removal via a campaign-scoped characters delete (add the tRPC procedure if missing); the manual Add Companion uses charactersRepo.createCompanion (expose a characters.addCompanion tRPC mutation if one is not already available from 07-03).

    Mount `CompanionsSection` in `CharacterSheetTab.tsx` directly below the switcher row (render whenever any companions exist, even in solo mode); pass the companions list + handlers. Include the empty-state copy from §Copywriting when there are none but the section is intentionally shown.

    Extend `SheetHeader.tsx`: when `character.classes` is non-null and length > 1, replace the single subtitle with a race line plus a multiclass breakdown `Fighter 5 / Wizard 3 — Level 8` (built from classes.map + total). Null-safe: single-class characters render exactly as before.

    In the Inventory tab: when `campaign.encumbranceEnabled`, render a top banner "Carrying Capacity" + "X / Y lbs" (font-mono, X = sum of item weights, Y = 15× STR) plus a status badge — Encumbered (amber) at X ≥ 5× STR, Heavily Encumbered (red) at X ≥ 10× STR, no badge below 5× STR. Use the existing `weight` field on character_items.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "CompanionsSection|SheetHeader|Inventory" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - CompanionsSection.tsx exists, uses `Collapsible`, renders an HP `Stepper`, a type badge, and a remove button with inline confirm
    - CompanionsSection.tsx Add Companion dialog validates name + HP ≥ 1 + AC 1–30
    - SheetHeader.tsx contains a multiclass branch building `— Level` from `character.classes` with a null-safe single-class fallback
    - The Inventory tab renders "Carrying Capacity" + "X / Y lbs" and Encumbered/Heavily Encumbered badges gated on encumbranceEnabled and 5×/10× STR thresholds
    - CharacterSheetTab.tsx mounts `<CompanionsSection`
    - tsc reports no errors in the touched files
  </acceptance_criteria>
  <done>Companions section, multiclass sheet header, and encumbrance inventory display implemented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Party switcher chips, Add Character flow, Companions section, multiclass header,</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>Party switcher chips, Add Character flow, Companions section, multiclass header, encumbrance inventory display.</what-built>
  <how-to-verify>
    1. Create a campaign with party size 3; add 2 characters; open Character Sheet — confirm switcher chips and that clicking a chip swaps the sheet.
    2. Confirm a "+ Add Character" chip appears (since 2 < 3) and opens the wizard; after adding the 3rd, the chip disappears.
    3. Trigger an addCompanion AI tool (or use Add Companion manually): confirm the companion appears in the Companions collapsible with HP stepper, type badge, AC; adjust HP; remove with the inline confirm.
    4. Multiclass a character (level up into a second class): confirm the sheet header shows "ClassA N / ClassB M — Level X".
    5. Enable encumbrance on a campaign, add items past 5× STR: confirm the "Encumbered" badge; past 10× STR confirm "Heavily Encumbered".
    6. Switch to a different campaign and back: confirm the correct character is shown (no stale activeCharacterId).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manual companion form→characters | name/HP/AC persisted (untrusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | manual Add Companion input | mitigate | tRPC addCompanion mutation Zod-validates name/type/hpMax/ac server-side (same schema as the AI tool) |
| T-07-08-02 | Spoofing | activeCharacterId cross-campaign | mitigate | Reset to null on campaign change (Pitfall 6); queries scoped by campaignId |
</threat_model>

<verification>
- tsc clean for all touched renderer files
- Human-verify checkpoint approves party + companion + multiclass + encumbrance UI
</verification>

<success_criteria>
- Switcher chips + Add Character chip driven by activeCharacterId (reset on campaign change)
- CompanionsSection with add/remove/HP/type/conditions
- Multiclass breakdown in SheetHeader (null-safe)
- Encumbrance display with 5×/10× STR badges
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-08-SUMMARY.md` when done.
</output>
