---
phase: 07-content-depth-advanced-character
plan: 09
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/renderer/src/screens/LibraryScreen.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/screens/CampaignListScreen.tsx
  - src/renderer/src/screens/CampaignViewScreen.tsx
autonomous: false
requirements: [RULES-01, RULES-02]
must_haves:
  truths:
    - "A /library route renders a browsable SRD reference with Spells, Magic Items, Rules, and Monsters sections"
    - "Each section has a search filter and a detail panel"
    - "The library is reachable from the campaign list and from the campaign header"
    - "A back button returns to the prior screen"
  artifacts:
    - path: "src/renderer/src/screens/LibraryScreen.tsx"
      provides: "SRD reference browser (/library route)"
      contains: "library"
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "/library"
      via: "react-router Route"
      pattern: "/library"
    - from: "src/renderer/src/screens/LibraryScreen.tsx"
      to: "trpc.library"
      via: "section list queries"
      pattern: "library\\.(feats|magicItems|rules|monsters)"
---

<objective>
Create the standalone SRD reference browser at `/library`: a three-panel screen (section pills sidebar + searchable list + detail panel) covering Spells, Magic Items, Rules, and Monsters, wired into a react-router route and reachable from both the campaign list header and the in-campaign header.

Purpose: Delivers RULES-01 (browsable SRD reference) and RULES-02 (searchable magic items database).
Output: LibraryScreen.tsx + route + two navigation entry points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md

<interfaces>
<!-- From 07-03 (library router) + 07-02 (content) + existing routing. -->
trpc.library.feats.list / magicItems.list / rules.list / monsters.list → full arrays (client-side search).
Spells: existing trpc.spells (or content) source — reuse the existing spells query already in the app.
Existing routes (src/renderer/src/App.tsx): <Route path="/" .../> and <Route path="/campaign/:id" .../> inside <Routes>.
Navigation: useNavigate() from react-router-dom (pattern in CampaignViewScreen).

UI-SPEC §LibraryScreen lines 278-301: three-panel (sidebar 200px pills + search; center ScrollArea list; right 320px detail).
Section pills role="tablist"/role="tab"/aria-selected. Magic item rarity badge colors; monster stat block 2-col mono table.
Detail uses react-markdown for rules/monster markdown content. Back button "← Back" → navigate(-1).
Empty states: "Select an entry to view details." / "No results for \"{query}\"".
Navigation entry points: "Rules" link in CampaignListScreen header (navigate('/library')); "📖" button in CampaignViewScreen header.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LibraryScreen with four sections + search + detail</name>
  <files>src/renderer/src/screens/LibraryScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/CampaignListScreen.tsx (screen structure, useQuery + trpc pattern, error/loading states)
    - src/renderer/src/components/QuestsTab.tsx (ScrollArea list rows lines 33-68)
    - src/renderer/src/components/ui/scroll-area.tsx, ui/input.tsx, ui/tabs.tsx, ui/badge.tsx
    - src/renderer/src/components/sheet/ConditionBadge.tsx (rarity badge pill pattern)
    - existing spells query usage (grep for trpc.spells or content.spells to reuse the Spells data source)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ LibraryScreen full spec lines 278-301; § Copywriting library labels; § Accessibility library pills)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ LibraryScreen pattern lines 343-381)
  </read_first>
  <action>
    Create `src/renderer/src/screens/LibraryScreen.tsx`. Three-panel `h-full flex flex-row bg-background` layout. Left sidebar (`w-[200px] flex-shrink-0 border-r border-border`): a "← Back" ghost button + "SRD Library" label, then four section pills (Spells / Magic Items / Rules / Monsters) as a `role="tablist"` with `role="tab"`/`aria-selected` (active pill `bg-secondary text-foreground font-semibold`), then a search `Input` (h-9) that filters the active section. Center panel (`flex-1 min-w-0`, `ScrollArea h-full`): list rows per the active section — Spells (name + level/school), Magic Items (name + rarity badge colored by rarity: Common muted, Uncommon green, Rare blue, Very Rare purple, Legendary amber/gold), Rules (title + category), Monsters (name + type/CR); selected row `bg-secondary`; empty-search state "No results for \"{query}\"". Right detail panel (`w-[320px] border-l border-border p-4 overflow-y-auto`): "Select an entry to view details." when none selected; otherwise name (text-xl semibold) + content (14px body), using `react-markdown` for rules/monster markdown; magic items show an attunement badge; monsters show a compact 2-column mono stat block (STR/DEX/CON/INT/WIS/CHA + AC/HP/Speed/CR). Section data: Spells from the existing spells query; Magic Items/Rules/Monsters from `trpc.library.*.list`. Search is local state filtering the in-memory list (no per-keystroke IPC).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "LibraryScreen" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - LibraryScreen.tsx exists and queries `trpc.library.magicItems.list`, `trpc.library.rules.list`, `trpc.library.monsters.list`
    - The four section pills use role="tablist"/role="tab"/aria-selected
    - A search input filters the active section client-side (toLowerCase filter)
    - The detail panel renders the "Select an entry to view details." empty state and a per-item detail view
    - Magic item rows render a rarity badge
    - tsc reports no errors in LibraryScreen.tsx
  </acceptance_criteria>
  <done>LibraryScreen with four searchable sections + detail panel built.</done>
</task>

<task type="auto">
  <name>Task 2: Register /library route + navigation entry points</name>
  <files>src/renderer/src/App.tsx, src/renderer/src/screens/CampaignListScreen.tsx, src/renderer/src/screens/CampaignViewScreen.tsx</files>
  <read_first>
    - src/renderer/src/App.tsx (the <Routes> block lines 12-15)
    - src/renderer/src/screens/CampaignListScreen.tsx (header area where a "Rules" link fits)
    - src/renderer/src/screens/CampaignViewScreen.tsx (campaign header bar where a "📖" button fits; existing useNavigate usage)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ SRD Library Navigation interaction contract lines 408-412)
  </read_first>
  <action>
    In `App.tsx`: add `<Route path="/library" element={<LibraryScreen />} />` inside the existing `<Routes>` and import LibraryScreen.

    In `CampaignListScreen.tsx`: add a "Rules" link/button in the header that calls `navigate('/library')` (useNavigate from react-router-dom).

    In `CampaignViewScreen.tsx`: add a "📖" icon button in the campaign header bar that calls `navigate('/library')`. The LibraryScreen back button uses `navigate(-1)` so it returns to whichever screen launched it.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep -iE "App.tsx|CampaignListScreen|CampaignViewScreen" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `path="/library"` and imports LibraryScreen
    - CampaignListScreen.tsx contains a navigate('/library') call
    - CampaignViewScreen.tsx contains a navigate('/library') call (📖 button)
    - tsc reports no errors in the three files
  </acceptance_criteria>
  <done>/library route registered and reachable from campaign list and campaign header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: SRD reference browser at /library reachable from the campaign list and the campa</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>SRD reference browser at /library reachable from the campaign list and the campaign header.</what-built>
  <how-to-verify>
    1. Run the app; from the campaign list, click "Rules" → confirm the /library screen opens.
    2. Confirm four section pills (Spells / Magic Items / Rules / Monsters); click each.
    3. Type in the search box and confirm the list filters; clear it.
    4. Click a spell, a magic item (confirm rarity badge + attunement), a rule (confirm prose renders), and a monster (confirm the stat block).
    5. Click "← Back" and confirm it returns to the campaign list.
    6. Open a campaign, click the "📖" header button, confirm the library opens and Back returns to the campaign.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled SRD content→renderer | read-only static content, no mutation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Information Disclosure | library content rendering | accept | Static bundled SRD content only; no user data or secrets surfaced |
</threat_model>

<verification>
- tsc clean for LibraryScreen + routing files
- Human-verify checkpoint approves the library browser + both entry points
</verification>

<success_criteria>
- /library route with four searchable sections + detail panel
- Reachable from campaign list ("Rules") and campaign header ("📖")
- Back button returns to the launching screen
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-09-SUMMARY.md` when done.
</output>
