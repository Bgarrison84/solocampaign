---
phase: 07-content-depth-advanced-character
plan: 10
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - src/renderer/src/components/AiSettingsModal.tsx
  - src/renderer/src/components/ReferenceDocSelect.tsx
  - src/main/ai/referenceDocLoader.ts
autonomous: false
requirements: [RULES-03, RULES-04]
must_haves:
  truths:
    - "The settings modal has a Homebrew tab with a textarea + word count that persists homebrew_content"
    - "The user can import a .txt/.md/.pdf rules document; it is stored and listed with a remove option"
    - "Imported reference docs and homebrew reach the AI via the existing reference-docs context path"
    - "ReferenceDocSelect shows campaign-imported docs alongside bundled docs"
  artifacts:
    - path: "src/renderer/src/components/AiSettingsModal.tsx"
      provides: "Homebrew tab + imported docs list"
      contains: "Homebrew"
    - path: "src/renderer/src/components/ReferenceDocSelect.tsx"
      provides: "Merged bundled + imported reference docs"
      contains: "campaignId"
  key_links:
    - from: "src/renderer/src/components/AiSettingsModal.tsx"
      to: "trpc.campaignDocs.import"
      via: "rules document import"
      pattern: "campaignDocs\\.(import|list|delete)"
    - from: "src/main/ai/referenceDocLoader.ts"
      to: "campaign_reference_docs"
      via: "imported docs + homebrew included in reference-doc context"
      pattern: "campaign_reference_docs|homebrew"
---

<objective>
Add a Homebrew tab to the settings modal (textarea + word count + file import, persisting `homebrew_content`), extend ReferenceDocSelect to merge campaign-imported docs with bundled docs, and extend the reference-doc loader so homebrew text + imported documents reach the AI via the existing Phase 3 reference-docs context path.

Purpose: Delivers RULES-03 (homebrew editor + file import) and RULES-04 (PDF/text rules import as AI reference).
Output: AiSettingsModal Homebrew tab + ReferenceDocSelect merge + referenceDocLoader extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md
@.planning/phases/07-content-depth-advanced-character/07-PATTERNS.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md

<interfaces>
<!-- From 07-03 (campaignDocs router + homebrew_content column) + existing Phase 3 reference docs. -->
trpc.campaignDocs.list({campaignId}) → CampaignReferenceDoc[] {id, filename, content, createdAt}
trpc.campaignDocs.import({campaignId, filePath}) → created doc; trpc.campaignDocs.delete({docId, campaignId})
homebrew_content TEXT column on campaigns (Wave 0); save via campaigns update mutation (extend campaigns router if needed).
Existing Phase 3: reference_docs JSON column (list of enabled identifiers) + referenceDocLoader in main + ReferenceDocSelect.

UI-SPEC §AiSettingsModal Homebrew tab lines 261-275: Tabs wrapper (AI Settings | Homebrew); Homebrew tab =
Textarea rows=12 + "N words" count + "Import file…" (.txt/.md, appends to textarea); below, an "Imported Rules Documents"
section listing campaign_reference_docs with × remove, + "Import PDF or text file…" button (.pdf/.txt/.md) with "Importing…" spinner;
empty state "No imported documents."
UI-SPEC §ReferenceDocSelect (PATTERNS lines 820-846): add `campaignId?` prop; second query trpc.campaignDocs.list;
merge bundled + imported into one checkbox list; imported rows show a "user" badge; selection stored in reference_docs JSON
(identifiers = bundled relative paths OR campaign_reference_docs.id UUIDs).

referenceDocLoader (07-RESEARCH.md D-37 / open question 3): extend so enabled identifiers that are UUIDs resolve to
campaign_reference_docs rows (content) and homebrew_content is appended as a reference-doc entry. Relative-path identifiers
continue loading bundled files from disk. Inject under the existing referenceDocBlock slot (lower priority than World Overview).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homebrew tab in AiSettingsModal + imported docs list</name>
  <files>src/renderer/src/components/AiSettingsModal.tsx</files>
  <read_first>
    - src/renderer/src/components/AiSettingsModal.tsx (FULL — current content layout lines 157-215, how it saves campaign AI config, the gear-modal open/close)
    - src/renderer/src/components/ui/tabs.tsx, ui/textarea.tsx, ui/button.tsx
    - src/renderer/src/components/ReferenceDocSelect.tsx (existing dialog.showOpenDialog file-pick IPC pattern to reuse for import)
    - .planning/phases/07-content-depth-advanced-character/07-UI-SPEC.md (§ AiSettingsModal Homebrew tab lines 261-275; § Copywriting homebrew placeholder, imported docs empty state, import CTA)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ AiSettingsModal Tab Extension Pattern lines 765-790)
  </read_first>
  <action>
    Wrap the existing modal content in a `Tabs` with triggers "AI Settings" (existing content) and "Homebrew" (new). The Homebrew tab: a "Homebrew Content" label + subtitle, a `Textarea rows={12}` (placeholder from Copywriting) bound to the campaign's `homebrew_content` with a "N words" count below; an "Import file…" outline button (.txt/.md) that appends the chosen file's text to the textarea (blank-line separator); a Save action that persists homebrew_content via a campaigns update mutation (extend the campaigns router if a homebrew write does not exist). Below a Separator, an "Imported Rules Documents" section: list `trpc.campaignDocs.list({campaignId})` rows (filename + ghost × remove calling `trpc.campaignDocs.delete`), an "Import PDF or text file…" button (.pdf/.txt/.md) that opens the OS picker and calls `trpc.campaignDocs.import({campaignId, filePath})` with an "Importing…" spinner state and the error copy "Could not read that file…" on failure, and the empty state "No imported documents." when the list is empty. Invalidate the campaignDocs list query after import/delete.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "AiSettingsModal" | head; echo "tsc-exit"</automated>
  </verify>
  <acceptance_criteria>
    - AiSettingsModal.tsx wraps content in `Tabs` with a "Homebrew" trigger
    - The Homebrew tab renders a Textarea bound to homebrew_content with a word count
    - The imported-docs section calls `trpc.campaignDocs.list`, `trpc.campaignDocs.import`, and `trpc.campaignDocs.delete`
    - The empty state "No imported documents." is present
    - tsc reports no errors in AiSettingsModal.tsx
  </acceptance_criteria>
  <done>Homebrew tab with textarea + word count + file import + imported-docs list.</done>
</task>

<task type="auto">
  <name>Task 2: Merge imported docs into ReferenceDocSelect + extend referenceDocLoader</name>
  <files>src/renderer/src/components/ReferenceDocSelect.tsx, src/main/ai/referenceDocLoader.ts</files>
  <read_first>
    - src/renderer/src/components/ReferenceDocSelect.tsx (FULL — current listReferenceDocs query, checkbox rendering, onChange(selected) contract, large-doc tooltip lines 48-167)
    - src/main/ai/referenceDocLoader.ts (how reference_docs identifiers resolve to disk files; the readReferenceDocs function)
    - src/main/ai/referenceDocLoader.test.ts (existing loader test setup)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ ReferenceDocSelect Refactor lines 502-508; § Open Question 3 mixed-array strategy lines 933-936; § D-37 referenceDoc injection)
    - .planning/phases/07-content-depth-advanced-character/07-PATTERNS.md (§ ReferenceDocSelect Extension Pattern lines 820-846)
  </read_first>
  <action>
    Extend `ReferenceDocSelect.tsx`: add an optional `campaignId?: string` prop. When provided, add a second query `trpc.campaignDocs.list({campaignId})` (enabled only when campaignId is set) and merge its rows into the rendered checkbox list with a discriminator `source: 'bundled' | 'imported'`; imported rows show a "user" badge instead of the large-doc warning icon. The selection (`onChange(selected: string[])`) stores bundled relative paths OR `campaign_reference_docs.id` UUIDs in the same `reference_docs` JSON array (mixed-array approach per Open Question 3). Existing campaigns with only bundled paths continue to work.

    Extend `src/main/ai/referenceDocLoader.ts`: when resolving the enabled reference_docs identifiers, recognize identifiers that are UUIDs (campaign_reference_docs.id) and load their `content` from the table; relative-path identifiers continue loading bundled files from disk (unchanged). Additionally, append the campaign's `homebrew_content` (when non-empty) as a reference-doc entry. All of this feeds the existing referenceDocBlock slot in contextBuilder (lower priority than the World Overview block). Apply the same content sanitization (stripNewlines / cap) already used for reference docs. Extend referenceDocLoader.test.ts to cover UUID resolution + homebrew inclusion.
  </action>
  <verify>
    <automated>npm run test -- src/main/ai/referenceDocLoader.test.ts; npx tsc --noEmit -p tsconfig.web.json 2>&1 | grep -v node_modules | grep "ReferenceDocSelect" | head; echo "done"</automated>
  </verify>
  <acceptance_criteria>
    - ReferenceDocSelect.tsx has a `campaignId?` prop and a second `trpc.campaignDocs.list` query gated on it
    - Imported rows render a "user" badge and are selectable in the same checkbox list
    - referenceDocLoader.ts resolves UUID identifiers to campaign_reference_docs content and includes homebrew_content
    - `npm run test -- src/main/ai/referenceDocLoader.test.ts` exits 0 and covers UUID + homebrew cases
    - tsc reports no errors in ReferenceDocSelect.tsx
  </acceptance_criteria>
  <done>Imported docs merged into ReferenceDocSelect; loader resolves imported docs + homebrew into AI context.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Homebrew tab + rules document import + merged reference-doc selection reaching t</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>Homebrew tab + rules document import + merged reference-doc selection reaching the AI.</what-built>
  <how-to-verify>
    1. Run the app; open a campaign's gear/settings modal; switch to the Homebrew tab.
    2. Type homebrew content, save, reload the app, reopen — confirm the text persisted.
    3. Use "Import PDF or text file…" to import a .txt and a .pdf; confirm each appears in the imported docs list; remove one with ×.
    4. In the campaign's reference doc selector, confirm the imported docs appear alongside bundled docs with a "user" badge and can be toggled.
    5. Enable an imported doc + homebrew, start a session, and confirm (via AI behavior or context inspection) the AI references the imported content.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| homebrew text + imported doc→system prompt | user-authored/extracted text injected into AI context |
| file picker→pdfExtractor | user-chosen file read in main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Tampering | imported doc filePath | mitigate | campaignDocs.import validates absolute path + rejects `..` (07-03) |
| T-07-10-02 | Denial of Service | oversized homebrew/imported doc | mitigate | 50,000-char storage cap (07-03); loader caps + stripNewlines at injection |
| T-07-10-03 | Tampering | homebrew prompt injection | mitigate | Injected as literal reference text (not instructions); same sanitization as Phase 3 reference docs |
</threat_model>

<verification>
- referenceDocLoader test green (UUID + homebrew)
- tsc clean for AiSettingsModal + ReferenceDocSelect
- Human-verify checkpoint approves homebrew + import + AI reach
</verification>

<success_criteria>
- Homebrew tab persists homebrew_content with word count + file import
- Rules document import (.pdf/.txt/.md) stored + listed + removable
- ReferenceDocSelect merges bundled + imported docs
- referenceDocLoader resolves imported docs + homebrew into the AI context
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-10-SUMMARY.md` when done.
</output>
