---
phase: 07-content-depth-advanced-character
plan: 11
type: execute
wave: 6
depends_on: ["07-03", "07-05", "07-06", "07-07", "07-08", "07-09", "07-10"]
files_modified:
  - src/main/ai/contextBuilder.ts
  - src/main/ai/contextBuilder.test.ts
autonomous: false
requirements: [PARTY-01, PARTY-02, CHAR-04, PROG-03, WORLD-01, RULES-04]
must_haves:
  truths:
    - "In party mode the AI can award Inspiration to any party member (all member IDs injected)"
    - "The full test suite is green"
    - "An end-to-end party flow works: create party campaign, add members, multiclass, earn an Epic Boon"
    - "PDF import extracts text in a dev build"
  artifacts:
    - path: "src/main/ai/contextBuilder.ts"
      provides: "Party Members ID injection for awardInspiration party mode"
      contains: "Party Members"
  key_links:
    - from: "src/main/ai/contextBuilder.ts"
      to: "awardInspiration"
      via: "all non-companion party member IDs injected so AI can target any of them"
      pattern: "Party Members"
---

<objective>
Close the Phase 7 integration gaps: inject all party-member character IDs into the AI context so the Phase 6 `awardInspiration` tool works in party mode (Open Question 1 / Phase 6 D-08), run the full test suite to green, and verify the end-to-end party + multiclass + Epic Boon + PDF-import flow.

Purpose: Ties together the parallel Phase 7 subsystems and resolves the one cross-phase integration item (party-mode Inspiration) before phase verification.
Output: contextBuilder Party Members injection + green suite + e2e human-verify.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-content-depth-advanced-character/07-RESEARCH.md
@.planning/phases/07-content-depth-advanced-character/07-VALIDATION.md
@.planning/phases/07-content-depth-advanced-character/07-03-SUMMARY.md

<interfaces>
<!-- From 07-03 (contextBuilder) + Phase 6 awardInspiration. -->
Phase 6 awardInspiration({characterId: string}) already accepts any character ID (no schema change — RESEARCH A4).
Gap: in party mode the AI must know ALL non-companion party member IDs to target any of them.
RESEARCH Open Question 1 recommendation: inject a "Party Members" section listing all non-companion character IDs
(and names) in the world-state/character context so the AI can call awardInspiration with the right ID.

Full suite: npm run test (07-VALIDATION.md). PDF import dev verification: run dev build, import a real PDF.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject Party Members IDs for party-mode awardInspiration</name>
  <files>src/main/ai/contextBuilder.ts, src/main/ai/contextBuilder.test.ts</files>
  <behavior>
    - For a partySize>1 campaign with 3 non-companion characters, buildContext includes a "Party Members" section listing each member's name + id
    - Companions (isCompanion=true) are excluded from the Party Members list
    - For a solo campaign (partySize===1) the Party Members section is omitted (single character context unchanged)
  </behavior>
  <read_first>
    - src/main/ai/contextBuilder.ts (the world-state/character summary assembly + the existing single-character injection; where the campaign's character list is available)
    - src/main/ai/contextBuilder.test.ts (existing fixtures, party/companion fixture shape)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Open Questions 1 lines 922-926; A4 line 915)
  </read_first>
  <action>
    Extend `contextBuilder.ts` so that when `campaign.partySize > 1`, a "Party Members" section is injected listing each non-companion character's name and id (so the AI can target `awardInspiration({characterId})` at any member). Exclude isCompanion characters. Place it alongside the world-state/character context (do not change the awardInspiration tool schema — RESEARCH A4 confirms no schema change is needed). When partySize === 1, omit the section so solo context is unchanged. Extend contextBuilder.test.ts per <behavior>.
  </action>
  <verify>
    <automated>npm run test -- src/main/ai/contextBuilder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - contextBuilder.ts emits a "Party Members" section only when partySize > 1
    - The section lists non-companion members and excludes companions
    - contextBuilder.test.ts asserts the section is present for a 3-member party and absent for solo
    - `npm run test -- src/main/ai/contextBuilder.test.ts` exits 0
  </acceptance_criteria>
  <done>Party-mode Inspiration enabled via Party Members ID injection; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite green + pitfall sweep</name>
  <files>src/main/ai/contextBuilder.ts</files>
  <read_first>
    - .planning/phases/07-content-depth-advanced-character/07-VALIDATION.md (Per-Task Verification Map + Wave 0 requirements)
    - .planning/phases/07-content-depth-advanced-character/07-RESEARCH.md (§ Common Pitfalls 1-8 — confirm each is addressed)
  </read_first>
  <action>
    Run the full `npm run test` suite and resolve any failures introduced across Phase 7. Do a pitfall sweep against RESEARCH Common Pitfalls 1-8: (1) confirm `drizzle-kit generate` does not re-add the unique index; (2) confirm `character.classes` consumers are null-safe; (3) world-brief generation is non-blocking; (4) world-document >12,000-char warning + 16,000 truncation present; (5) LevelUpModal single-class path gated behind `!isMulticlass`; (6) activeCharacterId resets on campaign change; (7) campaign_reference_docs content capped at 50,000; (8) content JSON loads in dev. Fix any gaps found. If any code change is needed it should be minimal and localized; document the sweep results in the SUMMARY.
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -20; echo "fullsuite-exit"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` full suite exits 0
    - SUMMARY documents the pitfall sweep result for each of pitfalls 1-8 (addressed / N/A)
    - No `CREATE UNIQUE INDEX characters_campaign_id_unique` appears in any migration
  </acceptance_criteria>
  <done>Full suite green; all 8 research pitfalls confirmed addressed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end Phase 7: party campaign + companions + multiclass + Epic Boon + world</name>
  <action>Human verification checkpoint — see how-to-verify steps. Claude has completed all automated work in prior tasks; this task pauses for the user to confirm the built behavior before the phase proceeds.</action>
  <what-built>End-to-end Phase 7: party campaign + companions + multiclass + Epic Boon + world import + library + homebrew.</what-built>
  <how-to-verify>
    1. Run a dev build. Create a campaign with party size 3, encumbrance on, and World Setup = Import a Document — import a real PDF and confirm extracted text (RULES-04 / WORLD-01).
    2. Add 3 characters (one via Point Buy with negative traits, one with a starting feat).
    3. Multiclass one character (level into a second class) and confirm the sheet header breakdown and multiclass spell slots behave.
    4. Trigger an AI addCompanion (play a turn that summons a familiar) and confirm it appears in the Companions section; adjust its HP.
    5. Set a character to level 20 and level up; confirm the Epic Boon picker and that the boon is recorded (PROG-03).
    6. Play a turn that earns Inspiration and confirm the AI can award it to a specific party member (PARTY-03 party mode).
    7. Open the SRD Library (📖) and browse all four sections. Open the Homebrew tab, add content + import a rules doc.
    8. Confirm encumbrance badges appear at the thresholds in the Inventory tab.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| party member IDs→system prompt | character IDs/names injected (non-secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Information Disclosure | Party Members ID injection | accept | Character IDs/names are non-sensitive campaign data already in the renderer |
| T-07-11-SC | Tampering | full-suite + pitfall sweep gate | mitigate | Phase cannot pass verification until npm run test is green and pitfalls 1-8 confirmed |
</threat_model>

<verification>
- `npm run test` full suite green
- contextBuilder Party Members test green
- e2e human-verify checkpoint approves the full Phase 7 flow including PDF import
</verification>

<success_criteria>
- Party-mode awardInspiration enabled (all member IDs injected)
- Full test suite green; 8 pitfalls confirmed addressed
- End-to-end party + multiclass + Epic Boon + world import + library + homebrew verified
</success_criteria>

<output>
Create `.planning/phases/07-content-depth-advanced-character/07-11-SUMMARY.md` when done. Record the pitfall-sweep table and the PDF-import dev verification result.
</output>
