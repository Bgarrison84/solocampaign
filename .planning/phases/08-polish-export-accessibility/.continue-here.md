---
context: phase
phase: 08-polish-export-accessibility
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-06-02
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Runtime bug — campaigns list fails on launch — must be fixed before merging 08-07 worktree or completing the accessibility checkpoint.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Parallel agents creating the same file | Both 08-04 and 08-06 agents independently created `src/renderer/src/components/ui/alert.tsx` since neither's `files_modified` frontmatter listed it. Caused an add/add merge conflict. | advisory | Before spawning parallel agents that both touch "utility" components (alert, badge, etc.), check that the file is listed in only one plan's `files_modified`. If both need it, have the earlier-wave plan create it, or pre-create it on master before spawning. |
| npm install side-effect bleeds to main checkout | The 08-05 worktree agent ran `npm install --legacy-peer-deps` to install @react-pdf/renderer. This wrote to the main checkout's `package-lock.json` (shared node_modules path), causing the next merge to fail with "local changes would be overwritten". | advisory | Before merging worktrees after a wave where any agent installed packages, run `git stash push -- package.json package-lock.json` first, then merge worktrees, then drop the stash. |

<current_state>
Phase 8 execution paused mid-wave 4 (the final wave, 1 plan: 08-07).

08-07 code is fully committed in a live worktree:
- Worktree path: C:\Users\psyco\Claude CLI\SoloCampaign\.claude\worktrees\agent-abd4b95db09044580
- Branch: worktree-agent-abd4b95db09044580
- Commits: 64c7303 (StoryScrollPanel aria-live), 8afbe01 (aria-label audit), 1aec42b (SUMMARY.md)

The user attempted to run the app to complete the accessibility human-verify checkpoint (Task 3 of 08-07) and encountered a runtime error: "Couldn't load your campaigns. Restart SoloCampaign and try again." This is the CampaignListScreen's `campaignsQuery.isError` branch, meaning the `campaigns.list` tRPC call is throwing.

The bug was not diagnosed before context ran out. TypeCheck passes, so it is a runtime issue.
</current_state>

<completed_work>

Plans completed this session (all merged to master):
- 08-01 Foundation (appPrefs store, IPC bridge, /settings route, gear icon) — MERGED
- 08-02 Appearance settings (font scale, high contrast, FOUC prevention) — MERGED
- 08-03 Campaign JSON export/import (DIST-01, 15-table, UUID remap) — MERGED
- 08-04 Starter templates (DIST-03, CreateCampaignModal pre-fill) — MERGED
- 08-05 Character sheet PDF (DIST-02, react-pdf, pdfService) — MERGED
- 08-06 Data folder migration (DIST-04, WAL-safe sqlite.backup) — MERGED

Plan 08-07 (accessibility pass):
- Task 1: StoryScrollPanel off-screen aria-live region ✓ (commit 64c7303, worktree)
- Task 2: aria-label audit on icon-only buttons ✓ (commit 8afbe01, worktree)
- Task 3: Human-verify checkpoint ⏸ BLOCKED — runtime bug prevents app launch
</completed_work>

<remaining_work>

1. **Fix runtime bug**: "Couldn't load your campaigns" — diagnose from electron-log, fix root cause, commit to master
2. **Merge 08-07 worktree**: After bug fix, merge `worktree-agent-abd4b95db09044580` into master using the standard cleanup flow (restore STATE.md/ROADMAP.md backups, remove worktree, delete branch)
3. **Human-verify checkpoint**: User must launch app and verify keyboard nav + screen reader + icon labels (full checklist is in the conversation above this pause, or in the 08-07 plan file)
4. **Update ROADMAP.md**: Mark 08-07 complete: `- [x] 08-07-PLAN.md — Accessibility pass...`
5. **Run code review**: `/gsd:code-review 8`
6. **Run verifier**: spawn gsd-verifier for phase 8 goal verification, create VERIFICATION.md
7. **Mark phase complete** in STATE.md and ROADMAP.md
</remaining_work>

<decisions_made>

- Two electron-store instances for 'appPrefs' (main/index.ts + tRPC router) — intentional per 08-01 plan design; main/index.ts needs it before BrowserWindow for IPC pre-mount bridge.
- alert.tsx conflict resolved by keeping 08-04 version (trailing comma style) — content identical between 08-04 and 08-06 agents.
- @radix-ui/react-dropdown-menu and @react-pdf/renderer installed via `npm install --legacy-peer-deps` (pre-existing peer dep conflict from tRPC/react-query).
- Wave 2 parallel agents (08-03, 08-05): merged cleanly — no file overlap. Package stash technique used to allow merge.
- Wave 3 parallel agents (08-04, 08-06): merged with one alert.tsx conflict — resolved manually.
</decisions_made>

<blockers>
- **Runtime bug (BLOCKING)**: `campaigns.list` tRPC call fails at runtime. TypeCheck passes so it's not a compile error. Most likely candidates:
  1. `characters.ts` statically imports `pdfService.ts`; pdfService imports `@react-pdf/renderer` as a type. If the ESM-only react-pdf package causes a module resolution error in the Electron main process bundle, the entire tRPC router initialization could fail, making ALL procedures (including campaigns.list) throw.
  2. Check: `%AppData%\SoloCampaign\logs\main.log` for the actual Electron main process error.
  3. Alternative: open Electron DevTools → Console for renderer-side error, or check Main Process DevTools if available.
- **08-07 worktree pending merge (BLOCKED on bug fix)**: Do not merge until the campaigns list bug is fixed.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-polish-export-accessibility/08-07-PLAN.md` — The plan for what 08-07 built and what the human-verify checkpoint requires
2. `.planning/phases/08-polish-export-accessibility/08-07-SUMMARY.md` (in worktree, or after merge) — What was actually built
3. `src/main/services/pdfService.ts` — The dynamic import pattern; check if it's compatible with how Electron bundles the main process
4. `src/main/trpc/routers/characters.ts` — Lines 37-38: static import of pdfService — verify this doesn't cause module load failure

## Infrastructure State
- **Active worktree**: `C:\Users\psyco\Claude CLI\SoloCampaign\.claude\worktrees\agent-abd4b95db09044580` on branch `worktree-agent-abd4b95db09044580` (08-07 accessibility code — DO NOT DELETE until merged)
- **Unrelated active worktree**: `C:\Users\psyco\AppData\Local\Temp\sv-06-reviewfix-eUi4tZ` on `gsd-reviewfix/06-1361` — from a prior review-fix session, unrelated to phase 8
- **Pre-existing uncommitted files** (in main checkout, NOT part of phase 8 work, do NOT commit):
  - `.planning/config.json` (modified)
  - `src/renderer/src/components/ui/checkbox.tsx`, `textarea.tsx`, `tooltip.tsx` (modified)
  - `tsconfig.json` (modified)
  - Various deleted `.continue-here.md` files from phases 04 and 06

<context>
Phase 8 is essentially done — 6/7 plans fully merged and all features built. Plan 08-07 has its code committed in a worktree but couldn't be merged because the user encountered a runtime bug when testing the app for the accessibility checkpoint. The bug causes the campaigns list to fail to load. The exact cause was not determined before context ran out (TypeCheck passes, so it's a runtime/bundling issue, most likely related to the ESM-only @react-pdf/renderer being imported — directly or indirectly — in the Electron main process bundle synchronously instead of dynamically).

The accessibility code itself (StoryScrollPanel aria-live region + aria-label audit) looks correct and should pass verification once the app launches.
</context>

<next_action>
Start with: Open `%AppData%\SoloCampaign\logs\main.log` (or run `npm run dev` and watch the terminal for main process errors) to find the exact runtime error. Most likely it will say something about `@react-pdf/renderer` or a module initialization failure. Fix the root cause, commit to master, then merge the 08-07 worktree and complete the accessibility checkpoint.
</next_action>
