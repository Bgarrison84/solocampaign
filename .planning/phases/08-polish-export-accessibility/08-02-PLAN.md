---
phase: 08-polish-export-accessibility
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - src/renderer/src/styles/globals.css
  - src/renderer/src/main.tsx
  - src/renderer/src/screens/SettingsScreen.tsx
autonomous: true
requirements: [A11Y-01]
must_haves:
  truths:
    - "User can pick Small/Normal/Large text size in settings and the whole app rescales immediately"
    - "User can toggle high contrast and the dark palette boosts contrast immediately"
    - "Both font size and high contrast persist across app restarts (applied before first paint)"
  artifacts:
    - path: "src/renderer/src/styles/globals.css"
      provides: ".high-contrast token overrides + --font-scale html rule + .sr-only utility"
      contains: ".high-contrast"
    - path: "src/renderer/src/main.tsx"
      provides: "Pre-mount appPrefs application (font scale + high contrast class)"
      contains: "getInitialPrefs"
    - path: "src/renderer/src/screens/SettingsScreen.tsx"
      provides: "Appearance section UI (font segmented control + high contrast switch)"
      contains: "Text Size"
  key_links:
    - from: "src/renderer/src/main.tsx"
      to: "window.appPrefsSync.getInitialPrefs"
      via: "await before ReactDOM.createRoot"
      pattern: "getInitialPrefs\\(\\)"
    - from: "src/renderer/src/screens/SettingsScreen.tsx"
      to: "appPrefs.setFontSize / setHighContrast"
      via: "tRPC mutations on change + immediate DOM update"
      pattern: "setFontSize|setHighContrast"
---

<objective>
Implement the Appearance settings: a 3-button font-size segmented control (Small/Normal/Large), a high-contrast Switch, the `--font-scale` CSS rule, the `.high-contrast` OKLCH override block, the `.sr-only` utility, and the pre-mount application of saved prefs in `main.tsx` to prevent FOUC.

Purpose: Delivers A11Y-01 end-to-end — a user changes text size or contrast in settings, sees it apply instantly, and it persists across launches.
Output: globals.css theme additions, main.tsx pre-mount logic, completed SettingsScreen Appearance section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-export-accessibility/08-CONTEXT.md
@.planning/phases/08-polish-export-accessibility/08-RESEARCH.md
@.planning/phases/08-polish-export-accessibility/08-PATTERNS.md
@.planning/phases/08-polish-export-accessibility/08-UI-SPEC.md
@.planning/phases/08-polish-export-accessibility/08-01-SUMMARY.md

<interfaces>
Consumes from 08-01:
- window.appPrefsSync.getInitialPrefs(): Promise<{ fontSize: 'small'|'normal'|'large', highContrast: boolean, dataFolder: string|null }>
- trpc.appPrefs.setFontSize.mutate({ fontSize })
- trpc.appPrefs.setHighContrast.mutate({ highContrast })
- SettingsScreen scaffold with empty "Appearance" + "Data" sections

Font scale map (from 08-UI-SPEC.md): small → '0.875', normal → '1', large → '1.125'
Applied as: document.documentElement.style.setProperty('--font-scale', value)
High contrast: document.documentElement.classList.toggle('high-contrast', boolean)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: globals.css — --font-scale rule, .high-contrast overrides, .sr-only</name>
  <files>src/renderer/src/styles/globals.css</files>
  <read_first>
    - src/renderer/src/styles/globals.css (read the existing `:root` and `.dark` OKLCH token blocks and the `@layer base` block; the .high-contrast block must follow the same token-override structure and be placed AFTER the .dark block so it cascades on top)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "High Contrast Override Theme" (the exact OKLCH override values to use) and "appPrefs Store Contract" (the html font-size rule)
  </read_first>
  <action>
    In `@layer base`, add an `html { font-size: calc(1rem * var(--font-scale, 1)); }` rule so all rem-based Tailwind `text-*` classes scale with `--font-scale`. Add the `.sr-only` utility class (position absolute; width 1px; height 1px; padding 0; margin -1px; overflow hidden; clip rect(0,0,0,0); white-space nowrap; border-width 0) per UI-SPEC — do not rely on Tailwind v4 auto-generating it (Assumption A6 / Open Question 2).

    After the existing `.dark` block, add a `.high-contrast { ... }` block that overrides the OKLCH theme tokens with the exact values from 08-UI-SPEC.md "High Contrast Override Theme": `--foreground: oklch(0.98 0.002 260)`, `--card-foreground` / `--popover-foreground` / `--secondary-foreground` same; `--muted-foreground: oklch(0.80 0.008 260)`; `--primary: oklch(0.88 0.14 78)` + `--primary-foreground: oklch(0.10 0.004 260)`; `--border: oklch(0.50 0.015 260)` + `--input` same; `--destructive: oklch(0.72 0.22 27)` + `--destructive-foreground: oklch(0.10 0.004 260)`; `--card: oklch(0.12 0.003 260)`; `--secondary: oklch(0.20 0.005 260)`; `--muted: oklch(0.16 0.004 260)`; `--ring: oklch(0.88 0.14 78)`; `--accent-gold: oklch(0.90 0.20 75)`. Add a CSS comment noting these target WCAG AA and require webaim contrast-checker verification before ship (Assumption A3). The block keeps the dark fantasy palette — it does NOT switch to a light theme.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `font-size: calc(1rem * var(--font-scale, 1))`
    - globals.css contains a `.high-contrast {` block with `--muted-foreground: oklch(0.80 0.008 260)` and `--border: oklch(0.50 0.015 260)`
    - globals.css contains a `.sr-only` class with `clip: rect(0, 0, 0, 0)`
    - `.high-contrast` block appears AFTER the `.dark` block in the file (verify by reading)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The font-scale rule, .high-contrast override block, and .sr-only utility are present and the renderer builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: main.tsx pre-mount prefs application (FOUC prevention)</name>
  <files>src/renderer/src/main.tsx</files>
  <read_first>
    - src/renderer/src/main.tsx (current synchronous setup and the ReactDOM.createRoot(...).render(...) call with its provider tree — QueryClientProvider, HashRouter, App)
    - .planning/phases/08-polish-export-accessibility/08-RESEARCH.md Section 4 (pre-mount application pattern) and Pitfall 5 (FOUC) and Open Question 3 (show:false ready-to-show window mitigates FOUC)
    - .planning/phases/08-polish-export-accessibility/08-PATTERNS.md "src/renderer/src/main.tsx (modification)" (async IIFE wrapping pattern)
  </read_first>
  <action>
    Wrap the existing `ReactDOM.createRoot(...).render(...)` call in an async IIFE. Before createRoot, define `const FONT_SCALE_MAP: Record<string, string> = { small: '0.875', normal: '1', large: '1.125' }`. Inside the IIFE, `try { const prefs = await window.appPrefsSync?.getInitialPrefs(); if (prefs) { document.documentElement.style.setProperty('--font-scale', FONT_SCALE_MAP[prefs.fontSize] ?? '1'); if (prefs.highContrast) document.documentElement.classList.add('high-contrast') } } catch { /* non-fatal: defaults apply */ }`. Then call the existing createRoot/render with its provider tree unchanged. Add a TypeScript declaration for `window.appPrefsSync` (either a `declare global` augmentation in this file or a `(window as any)` cast consistent with how window.aiStream is typed in the renderer) so typecheck passes. Keep the provider tree (QueryClientProvider, HashRouter, App, StrictMode) exactly as it currently is.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - main.tsx contains `getInitialPrefs()` and `setProperty('--font-scale'`
    - main.tsx contains `classList.add('high-contrast')`
    - The createRoot/render call executes inside the async IIFE after the prefs read
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Saved font size and high contrast are applied to documentElement before React mounts, with a try/catch fallback to defaults.</done>
</task>

<task type="auto">
  <name>Task 3: SettingsScreen Appearance section (font picker + high contrast toggle)</name>
  <files>src/renderer/src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/SettingsScreen.tsx (the scaffold created in 08-01 — fill the Appearance section placeholder)
    - src/renderer/src/components/ui/button.tsx (Button variants: 'default' for active, 'outline' for inactive)
    - src/renderer/src/components/ui/switch.tsx (Switch component API for high contrast toggle)
    - src/renderer/src/components/ui/label.tsx (Label htmlFor pairing)
    - src/renderer/src/components/CampaignCard.tsx (useMutation + trpc + queryClient.invalidateQueries pattern ~lines 26-36)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "Interaction Contracts" (font segmented control = 3 Buttons, active=default/inactive=outline; high contrast = Switch + Label + description) and "Copywriting Contract" Settings section (exact labels: "Text Size", "Small"/"Normal"/"Large", "High Contrast", description "Increases text and border contrast for low-vision use")
  </read_first>
  <action>
    Fill the "Appearance" section of SettingsScreen. Use `useQuery` on `trpc.appPrefs.get` (queryKey ['appPrefs']) to read current values for initial button/switch state. Build a font-size segmented control as THREE `<Button>` components in a row labeled "Small" / "Normal" / "Large" (not ToggleGroup — per UI-SPEC use 3 Buttons): the active one uses `variant="default"`, the others `variant="outline"`. Label the group "Text Size". On click, call `handleFontChange(value)` which: (1) sets `document.documentElement.style.setProperty('--font-scale', { small:'0.875', normal:'1', large:'1.125' }[value])` immediately, then (2) calls `setFontSizeMutation.mutate({ fontSize: value })`. Define `setFontSizeMutation = useMutation({ mutationFn: (fontSize) => trpc.appPrefs.setFontSize.mutate({ fontSize }), onSuccess: () => queryClient.invalidateQueries({ queryKey: ['appPrefs'] }) })`.

    Build the high-contrast row: a `<Switch>` with a `<Label htmlFor="high-contrast">High Contrast</Label>` and the description text "Increases text and border contrast for low-vision use", laid out label-left / switch-right with space-between, full-width row. Add `aria-label="High contrast mode"` on the Switch. On change, call `handleHighContrastChange(checked)` which: (1) `document.documentElement.classList.toggle('high-contrast', checked)` immediately, then (2) `setHighContrastMutation.mutate({ highContrast: checked })`. Each interactive control row must be at least 44px tall (WCAG 2.5.5) per UI-SPEC spacing exceptions. Leave the "Data" section placeholder untouched (filled by 08-06).
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreen.tsx contains the strings "Text Size", "Small", "Normal", "Large", "High Contrast"
    - SettingsScreen.tsx contains `setFontSize` and `setHighContrast` tRPC mutation calls
    - SettingsScreen.tsx contains `setProperty('--font-scale'` and `classList.toggle('high-contrast'`
    - SettingsScreen.tsx contains `aria-label="High contrast mode"`
    - `npm run typecheck` exits 0 and `npm run test -- --run` (full suite) stays green
  </acceptance_criteria>
  <done>The Appearance section lets the user change font size and high contrast with immediate visual effect and persistence via tRPC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (tRPC setFontSize/setHighContrast) | Enum/boolean input validated by Zod in the 08-01 router |
| DOM manipulation (renderer) | CSS class/var changes on documentElement — same-origin, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | --font-scale value injected to CSS | mitigate | Value comes from a fixed FONT_SCALE_MAP keyed by an enum (small/normal/large); arbitrary strings cannot reach setProperty |
| T-08-05 | DoS | High contrast / font toggles spamming mutations | accept | Single-user desktop app; no rate concern; electron-store writes are local and cheap |
| T-08-SC | Tampering | npm install | accept | No new packages; Switch/Button/Label shadcn components already present (UI-SPEC confirms) |
</threat_model>

<verification>
- `npm run build` exits 0 (CSS compiles)
- `npm run typecheck` exits 0
- `npm run test -- --run` full suite green
- Manual: open /settings, switch font sizes (app text rescales live), toggle high contrast (palette shifts live), restart app and confirm both settings persisted with no FOUC
</verification>

<success_criteria>
- A11Y-01 complete: font scaling + high contrast both work, apply immediately, and persist across launches
</success_criteria>

<output>
Create `.planning/phases/08-polish-export-accessibility/08-02-SUMMARY.md` when done.
</output>
