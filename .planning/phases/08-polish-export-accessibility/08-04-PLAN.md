---
phase: 08-polish-export-accessibility
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/main/db/exportImport.ts
  - src/main/trpc/routers/campaigns.ts
  - src/renderer/src/components/CampaignCard.tsx
  - src/renderer/src/screens/CampaignListScreen.tsx
  - src/renderer/src/components/CreateCampaignModal.tsx
autonomous: true
requirements: [DIST-03]
must_haves:
  truths:
    - "User can export a campaign world as a starter template (world config only, no save state)"
    - "Importing a starter template opens the Create Campaign wizard pre-filled with the template fields"
    - "The importer reviews/edits all pre-filled fields and enters their own AI key before creating"
  artifacts:
    - path: "src/main/db/exportImport.ts"
      provides: "exportStarterTemplate returning D-15 fields with type starterTemplate"
      contains: "exportStarterTemplate"
    - path: "src/renderer/src/components/CreateCampaignModal.tsx"
      provides: "initialTemplate prop + pre-fill + pre-fill banner"
      contains: "initialTemplate"
  key_links:
    - from: "src/renderer/src/components/CampaignCard.tsx"
      to: "campaigns.exportTemplate"
      via: "Export as Starter Template menu item → tRPC mutate"
      pattern: "exportTemplate"
    - from: "src/renderer/src/screens/CampaignListScreen.tsx"
      to: "CreateCampaignModal initialTemplate"
      via: "import template branch opens modal with result.template"
      pattern: "initialTemplate"
---

<objective>
Complete starter templates (DIST-03). Add `exportStarterTemplate` to `exportImport.ts` (campaign-level world config only, `type: "starterTemplate"`), the `campaigns.exportTemplate` tRPC procedure, the "Export as Starter Template" menu item in CampaignCard, and the CreateCampaignModal `initialTemplate` pre-fill path (with banner) plus the CampaignListScreen template-import branch that opens the modal pre-filled.

Purpose: Players can share a world setup; importing one pre-fills the Create Campaign wizard. Finishes DIST-03 (export side + template-import UX) on top of the 08-03 import dispatcher.
Output: exportStarterTemplate, exportTemplate procedure, CampaignCard menu item wiring, CreateCampaignModal initialTemplate, CampaignListScreen template branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-export-accessibility/08-CONTEXT.md
@.planning/phases/08-polish-export-accessibility/08-RESEARCH.md
@.planning/phases/08-polish-export-accessibility/08-PATTERNS.md
@.planning/phases/08-polish-export-accessibility/08-UI-SPEC.md
@.planning/phases/08-polish-export-accessibility/08-03-SUMMARY.md

<interfaces>
StarterTemplatePayload (D-15, from 08-UI-SPEC.md "Starter Template JSON Structure"):
{ version:1, type:'starterTemplate', exportedAt:ISO, name, worldSetupMode, worldBrief, worldDocument, dmPersonality, strictness, partySize, encumbranceEnabled, homebrewContent }
NO characters/sessions/messages/quests/npcs/factions/save state.

Consumes from 08-03:
- exportImport.ts already has CampaignExportPayload/StarterTemplatePayload types and importCampaignOrTemplate (which already returns kind:'template' for starterTemplate — verify in 08-03-SUMMARY)
- CampaignCard.tsx already has the 3-dot menu with an "Export as Starter Template" placeholder/comment
- CampaignListScreen.tsx already has the importMutation with a template-branch comment

CreateCampaignModal current props (verify): { open, onClose }. Add optional `initialTemplate?: StarterTemplatePayload | null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: exportStarterTemplate + campaigns.exportTemplate procedure</name>
  <files>src/main/db/exportImport.ts, src/main/trpc/routers/campaigns.ts</files>
  <read_first>
    - src/main/db/exportImport.ts (the file from 08-03 — append exportStarterTemplate; reuse the StarterTemplatePayload type; confirm importCampaignOrTemplate already handles type 'starterTemplate')
    - src/main/db/schema.ts (confirm the campaigns column names: worldSetupMode, worldBrief, worldDocument, dmPersonality, strictness, partySize, encumbranceEnabled, homebrewContent — match exact casing)
    - src/main/trpc/routers/campaigns.ts (the `export` procedure added in 08-03 — mirror its save-dialog pattern for exportTemplate)
    - .planning/phases/08-polish-export-accessibility/08-RESEARCH.md "Starter Template Export" + .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "Starter Template JSON Structure"
  </read_first>
  <action>
    In `exportImport.ts`, add `export function exportStarterTemplate(campaignId: string): StarterTemplatePayload`. Get `const sqlite = getDb().$client`, SELECT the campaign row by id, and return `{ version: 1, type: 'starterTemplate', exportedAt: new Date().toISOString(), name: campaign.name, worldSetupMode: campaign.worldSetupMode, worldBrief: campaign.worldBrief, worldDocument: campaign.worldDocument, dmPersonality: campaign.dmPersonality, strictness: campaign.strictness, partySize: campaign.partySize, encumbranceEnabled: campaign.encumbranceEnabled, homebrewContent: campaign.homebrewContent }`. Include ONLY these D-15 fields — no characters/sessions/messages/save state. Wrap in try/catch with electron-log error logging.

    In `campaigns.ts`, add `exportTemplate` (mutation, input `z.object({ campaignId: campaignIdSchema })`): call `exportStarterTemplate(input.campaignId)`; `const safeName = payload.name.toLowerCase().replace(/\s+/g, '-')`; `dialog.showSaveDialog({ defaultPath: \`${safeName}-template.json\`, filters: [{ name: 'JSON', extensions: ['json'] }] })`. If `canceled || !filePath` return `{ canceled: true }`; `await writeFile(filePath, JSON.stringify(payload, null, 2), 'utf-8')`; return `{ canceled: false }`.
  </action>
  <verify>
    <automated>npm run test -- --run exportImport && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - exportImport.ts contains `export function exportStarterTemplate` returning `type: 'starterTemplate'` and the 9 D-15 fields
    - exportStarterTemplate does NOT select or return characters, sessions, messages, quests, npcs, or factions
    - campaigns.ts contains an `exportTemplate` mutation with `defaultPath` ending `-template.json`
    - `npm run test -- --run exportImport` and `npm run typecheck` exit 0
  </acceptance_criteria>
  <done>exportStarterTemplate produces a world-config-only template and campaigns.exportTemplate writes it via OS save dialog.</done>
</task>

<task type="auto">
  <name>Task 2: CreateCampaignModal initialTemplate pre-fill + banner</name>
  <files>src/renderer/src/components/CreateCampaignModal.tsx</files>
  <read_first>
    - src/renderer/src/components/CreateCampaignModal.tsx (READ FULLY — the CreateCampaignModalProps interface, all form state setters (name, worldSetupMode, worldBrief, worldDocument, dmPersonality, strictness, partySize, encumbranceEnabled, homebrewContent), and the existing reset-on-open useEffect)
    - src/renderer/src/components/ui/alert.tsx (Alert component for the pre-fill banner)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "Template Pre-fill Banner" (Alert variant default, copy "Pre-filled from template: {templateName}" + "Review and edit the settings below before creating your campaign.") and "Starter Template Import (CreateCampaignModal)" copy
    - .planning/phases/08-polish-export-accessibility/08-PATTERNS.md "CreateCampaignModal.tsx (modification)"
  </read_first>
  <action>
    Extend `CreateCampaignModalProps` with `initialTemplate?: StarterTemplatePayload | null` (import the type from the main exportImport types or define a matching renderer-side type; if a shared type is awkward across main/renderer, define a local `StarterTemplate` interface in the modal matching the D-15 shape). Add a `useEffect` keyed on `[open, initialTemplate]` that, when `open && initialTemplate`, pre-fills all wizard fields from the template: setName(initialTemplate.name ?? ''), setWorldSetupMode, setWorldBrief, setWorldDocument, setDmPersonality, setStrictness, setPartySize, setEncumbranceEnabled, setHomebrewContent. Every field stays fully editable after pre-fill (no disabling). The AI provider/key fields are NOT pre-filled (D-16 — importer enters their own key).

    Render an Alert banner at the top of the modal content (before the first form section) ONLY when `initialTemplate` is non-null: `variant="default"`, with a FileText or Star lucide icon, heading "Pre-filled from template: {initialTemplate.name}" and description "Review and edit the settings below before creating your campaign." The banner is not dismissible (stays until modal closes).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - CreateCampaignModal.tsx contains `initialTemplate?:` in the props interface
    - A useEffect pre-fills name, worldSetupMode, worldBrief, worldDocument, dmPersonality, strictness, partySize, encumbranceEnabled, homebrewContent from initialTemplate
    - CreateCampaignModal.tsx renders an Alert with text "Pre-filled from template:" only when initialTemplate is set
    - AI provider/key fields are NOT pre-filled from the template
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The Create Campaign modal accepts a template, pre-fills every D-15 field (editable), shows a pre-fill banner, and leaves the AI key blank.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Export Template menu item + template-import branch</name>
  <files>src/renderer/src/components/CampaignCard.tsx, src/renderer/src/screens/CampaignListScreen.tsx</files>
  <read_first>
    - src/renderer/src/components/CampaignCard.tsx (the 3-dot menu from 08-03 with the "Export as Starter Template" placeholder/comment — wire it now)
    - src/renderer/src/screens/CampaignListScreen.tsx (the importMutation from 08-03 with the template-branch comment; the CreateCampaignModal mount + open state)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "Campaign Export / Import" copy
  </read_first>
  <action>
    In `CampaignCard.tsx`: wire the "Export as Starter Template" DropdownMenuItem `onSelect` to `exportTemplateMutation.mutate()`, where `exportTemplateMutation = useMutation({ mutationFn: () => trpc.campaigns.exportTemplate.mutate({ campaignId: campaign.id }) })`. The item sits between "Export Campaign (JSON)" and the separator/Delete.

    In `CampaignListScreen.tsx`: add state `const [importedTemplate, setImportedTemplate] = useState<StarterTemplate | null>(null)`. In the existing importMutation `onSuccess`, complete the template branch: when `result?.kind === 'template'`, call `setImportedTemplate(result.template)` and open the CreateCampaignModal (set whatever open state the modal uses to true). Pass `initialTemplate={importedTemplate}` to the CreateCampaignModal mount, and clear `importedTemplate` to null in the modal's onClose handler so a later "New Campaign" click opens a blank wizard. Keep the campaign branch (invalidate ['campaigns','list']) unchanged.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CampaignCard.tsx contains `campaigns.exportTemplate` wired to the "Export as Starter Template" item
    - CampaignListScreen.tsx contains `setImportedTemplate(result.template)` in the template branch and passes `initialTemplate=` to CreateCampaignModal
    - importedTemplate is cleared on modal close
    - `npm run typecheck` and `npm run build` exit 0
  </acceptance_criteria>
  <done>Exporting a template writes a -template.json; importing one opens the pre-filled Create Campaign wizard with a banner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user template file → renderer modal state | Imported template fields pre-fill wizard inputs (no DB write at import) |
| renderer → main (exportTemplate tRPC) | campaignId (UUID) crosses; dialog returns trusted path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | template fields injected into wizard | mitigate | Template import performs NO DB write (D-16); fields land in editable React state and are validated by the existing CreateCampaign Zod schema only when the user confirms creation |
| T-08-12 | Information Disclosure | template export leaking save state | mitigate | exportStarterTemplate selects ONLY the 9 D-15 campaign-level fields — no characters/sessions/messages; no API key column exists in campaigns (D-02) |
| T-08-13 | Tampering | exportTemplate file path | mitigate | Path from OS showSaveDialog (trusted absolute); campaignId validated as UUID |
| T-08-SC | Tampering | npm install | accept | No new packages — DropdownMenu/Alert already installed in 08-03/prior phases |
</threat_model>

<verification>
- `npm run test -- --run exportImport` passes
- `npm run typecheck` and `npm run build` exit 0
- `npm run test -- --run` full suite green
- Manual: export a template, import it, confirm the Create Campaign wizard opens with name/world/DM personality/strictness/party size/encumbrance/homebrew pre-filled, the banner shown, and the AI key blank
</verification>

<success_criteria>
- DIST-03 complete: starter template export + import-to-pre-filled-wizard works end to end
</success_criteria>

<output>
Create `.planning/phases/08-polish-export-accessibility/08-04-SUMMARY.md` when done.
</output>
