---
phase: 08-polish-export-accessibility
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/main/services/pdfService.ts
  - src/main/services/CharacterSheetPdf.tsx
  - src/main/services/pdfService.test.ts
  - src/main/trpc/routers/characters.ts
  - src/renderer/src/components/CharacterSheetTab.tsx
autonomous: true
requirements: [DIST-02]
must_haves:
  truths:
    - "User can export the active character's sheet as a print-friendly PDF"
    - "Spellcasters get a 2-page PDF (sheet + spell list); martials get 1 page"
    - "PDF is white background, black text, two-column layout (dark theme stripped)"
  artifacts:
    - path: "src/main/services/pdfService.ts"
      provides: "generateCharacterPdf via dynamic import of @react-pdf/renderer"
      contains: "await import('@react-pdf/renderer')"
    - path: "src/main/services/CharacterSheetPdf.tsx"
      provides: "react-pdf Document component tree (no DOM)"
      contains: "@react-pdf/renderer"
    - path: "src/main/services/pdfService.test.ts"
      provides: "Tests: non-empty Buffer, spellcaster=2 pages, martial=1 page"
  key_links:
    - from: "src/main/trpc/routers/characters.ts"
      to: "generateCharacterPdf"
      via: "exportPdf procedure → renderToBuffer → save dialog"
      pattern: "generateCharacterPdf"
    - from: "src/renderer/src/components/CharacterSheetTab.tsx"
      to: "characters.exportPdf"
      via: "PDF button → tRPC mutate with activeCharacterId"
      pattern: "characters\\.exportPdf"
---

<objective>
Implement character sheet PDF export (DIST-02). Build `CharacterSheetPdf.tsx` (react-pdf primitives, two-column A4, conditional spell page), `pdfService.ts` (dynamic-import `renderToBuffer`), the `characters.exportPdf` tRPC procedure (build data → render → save dialog), and the PDF button in CharacterSheetTab for the active character.

Purpose: DIST-02 — a print-friendly PDF of the character sheet, generated entirely in the main process.
Output: pdfService.ts + CharacterSheetPdf.tsx + tests, characters.exportPdf procedure, CharacterSheetTab PDF button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-export-accessibility/08-CONTEXT.md
@.planning/phases/08-polish-export-accessibility/08-RESEARCH.md
@.planning/phases/08-polish-export-accessibility/08-PATTERNS.md
@.planning/phases/08-polish-export-accessibility/08-UI-SPEC.md
@.planning/phases/08-polish-export-accessibility/08-01-SUMMARY.md

<interfaces>
pdfService API (consumed by characters.exportPdf):
- generateCharacterPdf(data: CharacterPdfData): Promise<Buffer>
  - CRITICAL: const { renderToBuffer } = await import('@react-pdf/renderer')  (NOT top-level import — ESM-only v4, Landmine 1/Pitfall 2)
  - data.hasSpells: boolean controls page 2 (D-12)

CharacterPdfData: derived in the tRPC procedure from charactersRepo (character + character_resources + character_spells). Define this type in pdfService.ts and consume it in CharacterSheetPdf.tsx.

CharacterSheetTab consumes: activeCharacterId from campaignViewStore (D-14).

react-pdf primitives only (no DOM, no browser globals — Landmine 7): Document, Page, View, Text, StyleSheet from @react-pdf/renderer. Built-in Helvetica fonts (no Font.register — ASAR safety).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CharacterSheetPdf.tsx (react-pdf two-column document, conditional spell page)</name>
  <files>src/main/services/CharacterSheetPdf.tsx</files>
  <read_first>
    - src/main/db/charactersRepo.ts (the CharacterWithResources shape — ability scores, HP/AC, skills, saves, currency, conditions, spells — to know what data is available)
    - src/renderer/src/components/sheet/AbilityScoresSection.tsx (the data fields and how ability modifiers are presented — replicate the SAME computed values in PDF form, no browser APIs)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "PDF Layout Specifications" (Page 1 two-column layout, ability score boxes 36×50pt, skills, currency, Page 2 spell slots + spell table) and "PDF typography" + "PDF Color"
    - .planning/phases/08-polish-export-accessibility/08-RESEARCH.md Section 1 (two-column StyleSheet pattern, built-in fonts, conditional page) and Landmine 7 (no DOM imports)
  </read_first>
  <action>
    Create `src/main/services/CharacterSheetPdf.tsx`. Import ONLY from `@react-pdf/renderer` (Document, Page, View, Text, StyleSheet) — do NOT import anything from `src/renderer/src` and do NOT use window/document. Define the exported `CharacterPdfData` interface (or import it from pdfService.ts — pick one home for the type) covering: name, race, class/multiclass label, level, ability scores + modifiers, saving throws, skills (with proficiency flags), passive perception, hp current/max, ac, speed, initiative, proficiency bonus, death saves, conditions, currency (cp/sp/ep/gp/pp), equipment list, personality/ideals/bonds/flaws, spellSlots (by level current/max), spells (name/level/school/concentration/ritual), and `hasSpells: boolean`.

    Implement `export function CharacterSheetPdf({ data }: { data: CharacterPdfData })` returning a `<Document title={...}>`. Use a `StyleSheet.create` with `page: { backgroundColor: '#ffffff', padding: 40, fontFamily: 'Helvetica' }` and the PDF colors from UI-SPEC (#111 values, #333 headers, #666 labels). Page 1: header block (name 18pt Helvetica-Bold, "race class — Level N" 10pt #666), then a two-column row (`flexDirection: 'row', gap: 20, flex: 1`): LEFT = ability score boxes (3×2), saving throws, skills (with • filled / ◦ empty proficiency markers), passive perception; RIGHT = HP/AC/speed/initiative, proficiency bonus, death saves, conditions, currency. Bottom full-width section: equipment (compact, truncate at ~20 items) + personality/ideals/bonds/flaws. Page 2 (conditional on `data.hasSpells`): "{name} — Spell List" header, spell slots by level (filled/empty dots), then spells grouped by level in an 8pt table (Name/School/Concentration/Ritual columns per UI-SPEC). Use built-in Helvetica/Helvetica-Bold only — NO Font.register().
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - CharacterSheetPdf.tsx imports from `@react-pdf/renderer` and NOT from `src/renderer`
    - CharacterSheetPdf.tsx contains `backgroundColor: '#ffffff'` and a two-column `flexDirection: 'row'` body
    - The spell page is gated by `data.hasSpells`
    - No `Font.register` call and no `window`/`document` reference
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>CharacterSheetPdf renders a white two-column A4 sheet with a conditional spell page, using only react-pdf primitives and built-in fonts.</done>
</task>

<task type="auto">
  <name>Task 2: pdfService.ts (dynamic import renderToBuffer) + Wave 0 tests</name>
  <files>src/main/services/pdfService.ts, src/main/services/pdfService.test.ts</files>
  <read_first>
    - src/main/services/pdfExtractor.ts (THE dynamic-import pattern to copy: `const { ... } = await import('unpdf')`; electron-log/main import; try/catch error logging at lines ~27-35)
    - src/main/services/pdfExtractor.test.ts (test structure for a main-process service)
    - src/main/services/CharacterSheetPdf.tsx (the component + CharacterPdfData type from Task 1)
    - .planning/phases/08-polish-export-accessibility/08-RESEARCH.md Section 1 (renderToBuffer signature + dynamic import) and Landmine 1/Pitfall 2 (ESM-only) and Landmine 8 / Open Question 1 (React 19 reconciler — flag packaged-build verification)
    - .planning/phases/08-polish-export-accessibility/08-VALIDATION.md (pdfService test expectations: non-empty Buffer, 2 pages spellcaster, 1 page martial)
  </read_first>
  <action>
    Create `src/main/services/pdfService.ts`. Export `async function generateCharacterPdf(data: CharacterPdfData): Promise<Buffer>`. Inside: `const { renderToBuffer } = await import('@react-pdf/renderer')`; `const React = await import('react')`; `const { CharacterSheetPdf } = await import('./CharacterSheetPdf')`; `const element = React.createElement(CharacterSheetPdf, { data })`; `const buffer = await renderToBuffer(element)`; return buffer. Use dynamic imports for @react-pdf/renderer AND CharacterSheetPdf (the component pulls react-pdf transitively). Wrap in try/catch logging `[pdfService] generateCharacterPdf failed` via electron-log and rethrow. Re-export or import the `CharacterPdfData` type from CharacterSheetPdf.tsx (single source of truth).

    Create `src/main/services/pdfService.test.ts`. Tests (08-VALIDATION.md): (1) generateCharacterPdf with a martial payload (`hasSpells: false`) resolves to a non-empty Buffer (assert `buffer.length > 0`); (2) a spellcaster payload (`hasSpells: true` with sample spell slots/spells) produces a Buffer whose PDF page count is 2; (3) the martial payload produces a 1-page PDF. To count pages without a PDF parser dependency, count occurrences of the PDF page marker (e.g. `/Type /Page` excluding `/Pages`) in the buffer's latin1/utf string, OR assert spellcaster buffer length meaningfully exceeds martial — prefer the page-marker count for correctness. Add a comment: "Packaged-build smoke test required before ship — React 19 reconciler (Open Question 1) and yoga-layout __dirname (Assumption A2) only verifiable in electron-vite build, not dev/vitest."
  </action>
  <verify>
    <automated>npm run test -- --run pdfService</automated>
  </verify>
  <acceptance_criteria>
    - pdfService.ts contains `await import('@react-pdf/renderer')` and `renderToBuffer(`
    - pdfService.ts has NO top-level `import ... from '@react-pdf/renderer'`
    - pdfService.test.ts asserts non-empty Buffer, 2 pages for spellcaster, 1 page for martial
    - `npm run test -- --run pdfService` exits 0 with 3 passing tests
  </acceptance_criteria>
  <done>generateCharacterPdf returns a Buffer via dynamic import; tests confirm non-empty output and correct page counts.</done>
</task>

<task type="auto">
  <name>Task 3: characters.exportPdf procedure + CharacterSheetTab PDF button</name>
  <files>src/main/trpc/routers/characters.ts, src/renderer/src/components/CharacterSheetTab.tsx</files>
  <read_first>
    - src/main/trpc/routers/characters.ts (existing procedures, the characterIdSchema input validator, how charactersRepo is imported and which method returns a character with resources/spells; the dialog import)
    - src/main/db/charactersRepo.ts (the getter that returns character + resources + spells — confirm method name and shape to build CharacterPdfData)
    - src/renderer/src/components/CharacterSheetTab.tsx (READ the tab header row ~lines 47-61, how activeCharacterId is read from campaignViewStore, and the useMutation pattern)
    - .planning/phases/08-polish-export-accessibility/08-PATTERNS.md "characters.ts (modification)" + "CharacterSheetTab.tsx (modification)"
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "PDF Export Button (CharacterSheetTab)" (size sm, Download icon, "Export PDF" text, Loader2 spinner during generation, aria-label "Export character sheet as PDF")
  </read_first>
  <action>
    In `characters.ts`, add `exportPdf` (mutation, input `z.object({ characterId: characterIdSchema })`): read the character with resources + spells from charactersRepo (use the existing getter; if spells need a separate query, fetch character_spells/character_resources too). Build a `CharacterPdfData` object, computing modifiers/derived values as the sheet UI does (set `hasSpells = characterSpells.length > 0` per UI-SPEC). If the character is not found, throw `TRPCError NOT_FOUND`. Call `const buffer = await generateCharacterPdf(data)`. Derive `const safeName = (character.name ?? 'character').toLowerCase().replace(/\s+/g, '-')`; `dialog.showSaveDialog({ defaultPath: \`${safeName}-sheet.pdf\`, filters: [{ name: 'PDF', extensions: ['pdf'] }] })`. If `canceled || !filePath` return `{ canceled: true }`; `await writeFile(filePath, buffer)`; return `{ canceled: false }`.

    In `CharacterSheetTab.tsx`, add a PDF export Button in the tab header row (right-aligned), `variant="ghost" size="sm"`, containing a `<Download size={14} />` and "Export PDF" text, `aria-label="Export character sheet as PDF"`. Define `exportPdfMutation = useMutation({ mutationFn: ({ characterId }) => trpc.characters.exportPdf.mutate({ characterId }) })`. `onClick={() => exportPdfMutation.mutate({ characterId: activeCharacterId! })}`, `disabled={!activeCharacterId || exportPdfMutation.isPending}`. While `isPending`, swap the Download icon for `<Loader2 className="animate-spin" size={14} />` and change the text to "Generating...". On error, surface a non-blocking message (toast/inline) using the existing error display pattern in the tab.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - characters.ts contains an `exportPdf` mutation calling `generateCharacterPdf(` and `dialog.showSaveDialog` with `-sheet.pdf`
    - characters.ts sets `hasSpells` based on the character's spells
    - CharacterSheetTab.tsx contains `characters.exportPdf`, `aria-label="Export character sheet as PDF"`, and a Loader2 spinner during generation
    - The button uses activeCharacterId from campaignViewStore and is disabled when none is active
    - `npm run typecheck` exits 0 and full suite stays green
  </acceptance_criteria>
  <done>The CharacterSheetTab PDF button exports the active character's sheet via the main process to a user-chosen .pdf, with a loading state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (exportPdf tRPC) | characterId (UUID) crosses; dialog returns trusted path |
| main → filesystem (PDF save) | dialog-returned path written with the rendered Buffer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | Tampering | exportPdf characterId input | mitigate | characterIdSchema validates UUID at the procedure boundary; NOT_FOUND thrown for unknown ids |
| T-08-15 | Tampering | PDF save file path | mitigate | Path from OS showSaveDialog (trusted absolute) — no renderer-supplied path |
| T-08-16 | Information Disclosure | PDF content | accept | PDF contains only the user's own character data; no secrets/keys; generated locally, written to user-chosen path |
| T-08-SC | Tampering | `@react-pdf/renderer` (already installed) | accept | RESEARCH.md Package Legitimacy Audit: [OK] approved, 8-yr-old package, github.com/diegomura/react-pdf; no new install (already in package.json) |
</threat_model>

<verification>
- `npm run test -- --run pdfService` passes (3 tests)
- `npm run typecheck` exits 0
- `npm run test -- --run` full suite green
- Manual (REQUIRED before phase exit — Open Question 1 / Assumption A2): run a PACKAGED electron-vite build, export a martial character (1-page PDF) and a spellcaster (2-page PDF), confirm white background + readable two-column layout
</verification>

<success_criteria>
- DIST-02 complete: active-character PDF export, 2 pages for spellcasters, 1 for martials, print-friendly white layout
</success_criteria>

<output>
Create `.planning/phases/08-polish-export-accessibility/08-05-SUMMARY.md` when done.
</output>
