---
phase: 08-polish-export-accessibility
plan: 06
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - src/main/trpc/routers/appPrefs.ts
  - src/main/trpc/routers/appPrefs.test.ts
  - src/renderer/src/screens/SettingsScreen.tsx
autonomous: true
requirements: [DIST-04]
must_haves:
  truths:
    - "User can see the current data folder path in settings (with default/custom indicator)"
    - "User can pick a new data folder; the SQLite DB is copied with .backup() and integrity-checked"
    - "After a successful change a restart-required banner appears; the new path takes effect next launch"
  artifacts:
    - path: "src/main/trpc/routers/appPrefs.ts"
      provides: "changeDataFolder (.backup + integrity_check) + getCurrentDataFolder + pickDataFolder"
      contains: "changeDataFolder"
    - path: "src/main/trpc/routers/appPrefs.test.ts"
      provides: "Test: changeDataFolder uses sqlite.backup not fs.copyFile"
    - path: "src/renderer/src/screens/SettingsScreen.tsx"
      provides: "Data section UI: path display + Change Folder button + restart Alert"
      contains: "Campaign Data Folder"
  key_links:
    - from: "src/main/trpc/routers/appPrefs.ts"
      to: "sqlite.backup(newDbPath)"
      via: "WAL-safe copy (NOT fs.copyFile)"
      pattern: "\\.backup\\("
    - from: "src/renderer/src/screens/SettingsScreen.tsx"
      to: "appPrefs.changeDataFolder"
      via: "Change Folder button → pick dir → changeDataFolder → restart banner"
      pattern: "changeDataFolder"
---

<objective>
Implement data folder migration (DIST-04). Add `getCurrentDataFolder`, a folder picker, and `changeDataFolder` (WAL-safe `sqlite.backup()` + `PRAGMA integrity_check` + persist `appPrefs.dataFolder`) to the appPrefs router, then complete the SettingsScreen "Data" section (current path display, "Change Folder..." button, restart-required banner).

Purpose: DIST-04 — the user can move where campaign data lives, with corruption-safe copy and a restart-required flow.
Output: appPrefs migration procedures + test, completed SettingsScreen Data section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-polish-export-accessibility/08-CONTEXT.md
@.planning/phases/08-polish-export-accessibility/08-RESEARCH.md
@.planning/phases/08-polish-export-accessibility/08-PATTERNS.md
@.planning/phases/08-polish-export-accessibility/08-UI-SPEC.md
@.planning/phases/08-polish-export-accessibility/08-01-SUMMARY.md
@.planning/phases/08-polish-export-accessibility/08-02-SUMMARY.md

<interfaces>
Extends appPrefs router from 08-01 (reuse the exported appPrefsStore instance):
- getCurrentDataFolder (query) → { path: string, isCustom: boolean }
    path = appPrefsStore.get('dataFolder') ?? app.getPath('userData'); isCustom = dataFolder !== null
- pickDataFolder (mutation) → { canceled: boolean, folderPath?: string }  // dialog.showOpenDialog openDirectory
- changeDataFolder (mutation, input { folderPath: string }) → { success: true, pendingRestart: true }

CRITICAL (RESEARCH.md Section 3 / Landmine 2 / Pitfall 1): use `await sqlite.backup(newDbPath)` from getDb().$client — NOT fs.copyFile (WAL corruption). Then open the copy readonly, run PRAGMA integrity_check, close; on failure delete the copy and throw TRPCError; on success appPrefsStore.set('dataFolder', folderPath).

DB filename: 'solocampaign.db' (matches src/main/db/index.ts).
SettingsScreen "Data" section scaffold exists from 08-01; Appearance section completed in 08-02 (do not touch it).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: appPrefs migration procedures (.backup + integrity_check) + test</name>
  <files>src/main/trpc/routers/appPrefs.ts, src/main/trpc/routers/appPrefs.test.ts</files>
  <behavior>
    - changeDataFolder calls sqlite.backup(newDbPath) — NEVER fs.copyFile
    - changeDataFolder runs PRAGMA integrity_check on the new copy; if result !== 'ok' it deletes the copy and throws
    - changeDataFolder persists dataFolder only after a passing integrity check, returning { success:true, pendingRestart:true }
    - getCurrentDataFolder returns isCustom=false + userData path when dataFolder is null, isCustom=true + custom path when set
  </behavior>
  <read_first>
    - src/main/trpc/routers/appPrefs.ts (the router from 08-01 — append the three procedures; reuse the exported appPrefsStore; note current imports)
    - src/main/db/index.ts (getDb() and `.$client`; the 'solocampaign.db' filename and path.join(userData, ...) construction)
    - src/main/trpc/routers/appPrefs.test.ts (the test file from 08-01 — extend it; the electron-store mock already exists)
    - .planning/phases/08-polish-export-accessibility/08-RESEARCH.md Section 3 (changeDataFolder full pattern: backup → readonly open → integrity_check → unlink-on-fail → set) and Landmine 2 / Pitfall 1
    - .planning/phases/08-polish-export-accessibility/08-PATTERNS.md "appPrefs.ts (router)" + "TRPCError Pattern"
  </read_first>
  <action>
    In `appPrefs.ts`, add imports as needed: `path` (node), `unlink` from 'node:fs/promises', `TRPCError` from '@trpc/server', `Database` from 'better-sqlite3', `app` from 'electron', `getDb` from '../../db/index', `log` from 'electron-log'. Add three procedures to appPrefsRouter:

    `getCurrentDataFolder` (query): `const custom = appPrefsStore.get('dataFolder', null)`; return `{ path: custom ?? app.getPath('userData'), isCustom: custom !== null }`.

    `pickDataFolder` (mutation): `const { canceled, filePaths } = await dialog.showOpenDialog({ properties: ['openDirectory'] })`; if `canceled || !filePaths[0]` return `{ canceled: true }`; return `{ canceled: false, folderPath: filePaths[0] }`. (Import `dialog` from 'electron'.)

    `changeDataFolder` (mutation, input `z.object({ folderPath: z.string().min(1) })`): `const newDbPath = path.join(input.folderPath, 'solocampaign.db')`. Step 1: `const sqlite = getDb().$client; await sqlite.backup(newDbPath)` (CRITICAL — backup, never copyFile). Step 2: open `const newDb = new Database(newDbPath, { readonly: true })`; `const result = newDb.prepare('PRAGMA integrity_check').get() as { integrity_check: string }`; `newDb.close()`. Step 3: if `result.integrity_check !== 'ok'`: `await unlink(newDbPath).catch(() => {})` then throw `new TRPCError({ code: 'INTERNAL_SERVER_ERROR', message: 'Integrity check failed on the new database copy.' })`. Step 4: `appPrefsStore.set('dataFolder', input.folderPath)`. Return `{ success: true, pendingRestart: true }`. Wrap in try/catch logging `[appPrefs] changeDataFolder failed` and rethrow.

    In `appPrefs.test.ts`, add tests: mock `getDb` to return an object whose `$client` is a stub with a spied `backup` method; mock `better-sqlite3` Database so the readonly open returns a stubbed `prepare('PRAGMA integrity_check').get()`. Test (1): changeDataFolder calls `sqlite.backup` and does NOT call `fs.copyFile` (assert the spy called; assert fs.copyFile not imported/used); (2): when integrity_check returns `{ integrity_check: 'ok' }`, dataFolder is set and result is `{ success:true, pendingRestart:true }`; (3): when integrity_check returns a non-ok value, it throws and dataFolder is NOT set; (4): getCurrentDataFolder returns isCustom based on whether dataFolder is set.
  </action>
  <verify>
    <automated>npm run test -- --run appPrefs</automated>
  </verify>
  <acceptance_criteria>
    - appPrefs.ts contains `changeDataFolder`, `getCurrentDataFolder`, `pickDataFolder`
    - appPrefs.ts contains `sqlite.backup(` and `PRAGMA integrity_check` and does NOT contain `copyFile`
    - changeDataFolder deletes the copy and throws when integrity_check !== 'ok'
    - appPrefs.test.ts asserts backup is called (not copyFile) and that a failed integrity check does not persist dataFolder
    - `npm run test -- --run appPrefs` exits 0 with the new tests passing
  </acceptance_criteria>
  <done>The appPrefs router can pick a folder, WAL-safely back up the DB, integrity-check the copy, and persist the new path only on success.</done>
</task>

<task type="auto">
  <name>Task 2: SettingsScreen Data section (path display + Change Folder + restart banner)</name>
  <files>src/renderer/src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/renderer/src/screens/SettingsScreen.tsx (the "Data" section placeholder from 08-01; the Appearance section from 08-02 — do NOT modify Appearance; reuse the existing useQuery/useMutation + queryClient setup)
    - src/renderer/src/components/ui/alert.tsx (Alert for the restart banner)
    - src/renderer/src/components/ui/button.tsx (Button)
    - .planning/phases/08-polish-export-accessibility/08-UI-SPEC.md "Settings Screen — Data Folder" interaction contract + "Copywriting Contract" Settings ("Campaign Data Folder", description, "Change Folder...", "Restart Required" banner heading + body) + "Empty and Edge States" (default suffix, path truncation)
  </read_first>
  <action>
    Fill the "Data" section of SettingsScreen. Add `useQuery` on `trpc.appPrefs.getCurrentDataFolder` (queryKey ['appPrefs','dataFolder']). Render the label "Campaign Data Folder" and description "Location where your campaigns and save data are stored". Show the current path in a monospace element (`font-mono`, muted-foreground) with `text-overflow: ellipsis; overflow: hidden; white-space: nowrap` truncation; append " (default)" when `isCustom === false`. Add a "Change Folder..." Button (primary/accent variant per UI-SPEC), `aria-label`-friendly via its visible text, at least 44px tall.

    Add local state `const [pendingRestart, setPendingRestart] = useState(false)`. On "Change Folder..." click: call `pickDataFolderMutation.mutate()` → on success with a folderPath, call `changeDataFolderMutation.mutate({ folderPath })` → on success set `setPendingRestart(true)` and `queryClient.invalidateQueries({ queryKey: ['appPrefs','dataFolder'] })`. Define both mutations with `trpc.appPrefs.pickDataFolder.mutate()` and `trpc.appPrefs.changeDataFolder.mutate(...)`. On changeDataFolder error, surface the error message (integrity-check failure) inline or via toast. Render a shadcn `Alert variant="default"` (Info icon) with heading "Restart Required" and body "The data folder change takes effect after you restart SoloCampaign." shown ONLY when `pendingRestart` is true.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreen.tsx contains "Campaign Data Folder", "Change Folder...", and "Restart Required"
    - SettingsScreen.tsx calls `appPrefs.getCurrentDataFolder`, `appPrefs.pickDataFolder`, and `appPrefs.changeDataFolder`
    - The restart Alert renders only when pendingRestart is true
    - The path display appends " (default)" when isCustom is false
    - The Appearance section from 08-02 is unchanged
    - `npm run typecheck` exits 0 and full suite stays green
  </acceptance_criteria>
  <done>The Data section shows the current folder, lets the user pick + migrate to a new one (backup + integrity check), and shows a restart-required banner on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (changeDataFolder tRPC) | folderPath string crosses; originates from the OS folder picker |
| main → filesystem (.backup + new DB file) | A new DB copy is written to the chosen folder and integrity-checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-17 | Tampering/Integrity | DB copy to new folder | mitigate | Use `sqlite.backup()` (WAL-safe) NOT fs.copyFile (Pitfall 1); PRAGMA integrity_check validates the copy; corrupt copy is deleted and the change is rejected before persisting dataFolder |
| T-08-18 | Tampering | folderPath input | mitigate | folderPath comes from pickDataFolder's OS showOpenDialog (openDirectory) — trusted absolute path; changeDataFolder validates `z.string().min(1)`; path.join scopes the DB file under the chosen folder |
| T-08-19 | DoS | repeated migrations | accept | Single-user desktop; backup is local and bounded by DB size; no remote actor |
| T-08-SC | Tampering | npm install | accept | No new packages — better-sqlite3 .backup() and electron dialog are already available |
</threat_model>

<verification>
- `npm run test -- --run appPrefs` passes (backup-not-copyfile + integrity-check tests)
- `npm run typecheck` exits 0
- `npm run test -- --run` full suite green
- Manual: in /settings Data section, change the folder to an empty dir, confirm "Restart Required" banner appears, restart the app, confirm the DB opens from the new location with all campaigns intact
</verification>

<success_criteria>
- DIST-04 complete: data folder change via .backup() + integrity check, restart-required flow, new path used next launch (initDatabase custom-path wiring from 08-01)
</success_criteria>

<output>
Create `.planning/phases/08-polish-export-accessibility/08-06-SUMMARY.md` when done.
</output>
