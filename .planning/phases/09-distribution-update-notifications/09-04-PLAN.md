---
phase: 09-distribution-update-notifications
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified: []
autonomous: false
requirements: [DIST-05]

must_haves:
  truths:
    - "A public GitHub repository briston/solocampaign exists with the master branch pushed"
    - "The updateChecker GITHUB_RELEASES_API_URL slug matches the created repo's actual owner/name"
    - "A v0.1.0 tag is pushed, which triggers release.yml, producing a published GitHub Release with Windows .exe, macOS .zip, and Linux AppImage artifacts"
  artifacts: []
  key_links:
    - from: "git tag v0.1.0"
      to: "release.yml workflow run"
      via: "tag push triggers on: push: tags: ['v*']"
      pattern: "v0.1.0"
    - from: "src/main/services/updateChecker.ts"
      to: "created repo slug"
      via: "GITHUB_RELEASES_API_URL constant matches actual owner/name"
      pattern: "api.github.com/repos/"
---

<objective>
Create the public GitHub repository, push master, reconcile the update-check API URL with the real repo slug, and publish the first release `v0.1.0` by pushing a tag that triggers the release pipeline (D-06, D-03).

Purpose: This is the go-live step that turns the feature + pipeline into a real public distribution. It is last because it depends on both the update-notification feature (Plan 02) and the release workflow (Plan 03) being complete. It contains an unavoidable authentication checkpoint (GitHub auth) and a human-verify checkpoint (confirming the Release published with all three artifacts).
Output: a live public repo, a published v0.1.0 GitHub Release with three platform artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-distribution-update-notifications/09-CONTEXT.md
@.planning/phases/09-distribution-update-notifications/09-RESEARCH.md
@.planning/phases/09-distribution-update-notifications/09-01-SUMMARY.md
@.planning/phases/09-distribution-update-notifications/09-03-SUMMARY.md
@src/main/services/updateChecker.ts
@package.json

<interfaces>
<!-- Authoritative repo slug -->
package.json repository.url = "https://github.com/briston/solocampaign" → owner: briston, name: solocampaign.
updateChecker.ts already hardcodes GITHUB_RELEASES_API_URL = https://api.github.com/repos/briston/solocampaign/releases/latest.
Version is 0.1.0 (D-03); first tag is v0.1.0.

gh CLI is available locally and on GitHub-hosted runners (RESEARCH Environment Availability).
Repo-create one-liner candidate: gh repo create briston/solocampaign --public --source . --remote origin --push
(RESEARCH Assumption A6 — if gh syntax differs or auth fails, fall back to GitHub web UI + git remote add origin.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify repo slug consistency before creating the repo</name>
  <files></files>
  <read_first>
    - package.json (repository.url — authoritative owner/name)
    - src/main/services/updateChecker.ts (GITHUB_RELEASES_API_URL constant — must match the slug that will be created)
    - .planning/phases/09-distribution-update-notifications/09-RESEARCH.md (Open Question 1: repo slug authoritative source)
  </read_first>
  <action>
    Confirm the repo owner/name that Plan 01 baked into updateChecker.ts (briston/solocampaign) matches package.json repository.url. If they already match (expected), no change is needed and this task is a verification gate only. If the user intends a different GitHub account/repo name than `briston/solocampaign`, surface that at the checkpoint in Task 2 BEFORE creating the repo, and if it differs, update both updateChecker.ts GITHUB_RELEASES_API_URL and package.json repository.url to the chosen slug so the deployed app polls the correct releases endpoint. Do not create the repo in this task.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=require('./package.json');const url=p.repository.url;const m=url.match(/github.com[/:]([^/]+)\/([^/.]+)/);if(!m)throw new Error('cannot parse repo slug');const slug=m[1]+'/'+m[2];const uc=fs.readFileSync('src/main/services/updateChecker.ts','utf8');if(!uc.includes('api.github.com/repos/'+slug+'/releases/latest'))throw new Error('updateChecker slug mismatch vs package.json: '+slug);console.log('OK slug='+slug)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json repository.url owner/name matches the slug in updateChecker.ts GITHUB_RELEASES_API_URL
    - The node verify command prints `OK slug=briston/solocampaign` (or the user-chosen slug) and exits 0
  </acceptance_criteria>
  <done>The API URL constant and package.json agree on the repo slug; the slug is confirmed before repo creation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Authenticate gh CLI and confirm repo slug</name>
  <action>Confirm GitHub authentication and the target public repo slug before any repo is created. This is a human-only step: gh account auth and the choice of owner/name cannot be made by Claude.</action>
  <what-built>
    Plan 01 + 02 delivered the update-notification feature (updateChecker service, appPrefs procedures, shellBridge, UpdateBanner). Plan 03 delivered electron-builder.yml (.zip mac target) and release.yml (tag-triggered matrix build + aggregator publish). Task 1 confirmed the repo slug is consistent. Everything is ready to publish; the only remaining steps require GitHub account authentication, which Claude cannot perform on the user's behalf.
  </what-built>
  <decision>Create the public GitHub repo and authenticate gh CLI</decision>
  <context>
    D-06 requires creating the public repo `briston/solocampaign`, pushing master, and publishing the first release. Claude can run `gh` commands but needs the user's GitHub account to be authenticated (`gh auth login`) and needs confirmation that `briston/solocampaign` is the correct account/name. If the user wants a different owner or name, it must be decided here BEFORE the repo is created (changing it later means re-patching updateChecker.ts and re-releasing).
  </context>
  <how-to-verify>
    1. Run `gh auth status` — if not logged in, run `gh auth login` and complete the browser/device flow.
    2. Confirm the target repo slug: is `briston/solocampaign` correct, or should a different GitHub owner/name be used? (If different, the executor will update updateChecker.ts + package.json per Task 1 before creating the repo.)
    3. Confirm the repo should be PUBLIC (D-06).
  </how-to-verify>
  <resume-signal>Type "authenticated, slug confirmed: {owner}/{name}, public" or specify a different slug/visibility</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create repo, push master, push v0.1.0 tag to trigger release</name>
  <files></files>
  <read_first>
    - package.json (version 0.1.0 — the tag is v0.1.0, D-03)
    - .planning/phases/09-distribution-update-notifications/09-RESEARCH.md (Assumption A6: gh repo create one-liner + fallback)
    - .github/workflows/release.yml (the workflow the tag push will trigger — confirm it exists from Plan 03)
  </read_first>
  <action>
    After the checkpoint confirms authentication and the slug, create the public repo and push. If `git remote get-url origin` does not already point at the confirmed slug, run `gh repo create {owner}/{name} --public --source . --remote origin --push` (creates the repo, adds origin, pushes the current branch). If a remote already exists, instead `git push -u origin master`. Then confirm package.json version is exactly `0.1.0` (D-03 — do NOT bump to 1.0.0). Create and push the release tag: `git tag v0.1.0` then `git push origin v0.1.0`. This tag push triggers release.yml (on: push: tags: ['v*']). Do not manually create the GitHub Release — the workflow's publish job does that. Report the workflow run URL (`gh run list --workflow=release.yml` / `gh run watch`) so the next checkpoint can verify it.
  </action>
  <verify>
    <automated>git ls-remote --tags origin v0.1.0</automated>
  </verify>
  <acceptance_criteria>
    - A public repo at the confirmed slug exists (`gh repo view {owner}/{name}` succeeds)
    - The master branch is pushed (`git ls-remote --heads origin master` returns a ref)
    - package.json version is `0.1.0` (not bumped)
    - The tag `v0.1.0` exists on the remote (`git ls-remote --tags origin v0.1.0` returns a ref)
    - The release.yml workflow run for the v0.1.0 tag is started (visible via `gh run list --workflow=release.yml`)
  </acceptance_criteria>
  <done>Public repo created, master pushed, v0.1.0 tag pushed and triggering the release workflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify v0.1.0 Release published with three artifacts</name>
  <action>Confirm the release.yml run completed green and the GitHub Release v0.1.0 has Windows .exe, macOS .zip, and Linux AppImage attached with the security-bypass notes.</action>
  <what-built>
    The v0.1.0 tag push triggered release.yml. The matrix built Windows/macOS/Linux artifacts and the aggregator publish job created a single GitHub Release. This checkpoint confirms the release actually published with all three platform artifacts (the CI run can take several minutes; native rebuild + electron-builder packaging on three runners).
  </what-built>
  <how-to-verify>
    1. Watch the run: `gh run watch` (or open the Actions tab). Confirm all three build jobs and the publish job are green.
    2. Open the release: `gh release view v0.1.0 --web` (or visit https://github.com/{owner}/solocampaign/releases/tag/v0.1.0).
    3. Confirm THREE artifacts are attached: a Windows `.exe`, a macOS `.zip`, and a Linux `.AppImage`.
    4. Confirm the release notes show the Windows SmartScreen and macOS Gatekeeper ("right-click → Open" + `xattr -cr` fix) bypass instructions (D-09).
    5. If the run failed: capture the failing job log; common causes are macOS arch mismatch (Pitfall 1), FUSE missing on Linux (Pitfall — apt install step), or better-sqlite3 ASAR (Pitfall 3). Report which job failed so a gap-closure plan can target it.
  </how-to-verify>
  <resume-signal>Type "published, 3 artifacts present" or describe which job/artifact failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → GitHub (gh auth) | User credentials authenticate repo creation and push |
| tag push → CI publish | The v0.1.0 tag grants the workflow Release-write authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Spoofing | gh repo create with wrong account | mitigate | Blocking human-action checkpoint confirms the exact owner/name and authentication before the repo is created (slug mismatch would make the deployed updateChecker poll a non-existent repo) |
| T-09-15 | Information Disclosure | making the repo public | accept | D-06: the repo is intentionally public for distribution; verify no secrets are committed (electron-store/secretStorage keep API keys out of the repo — Phase 1 design) before push |
| T-09-16 | Tampering | published release artifacts unsigned | accept | D-01: unsigned for v1.0; integrity rests on GitHub Release provenance + documented bypass; signing deferred |
</threat_model>

<verification>
- `gh repo view {owner}/{name}` succeeds (repo is public)
- `git ls-remote --tags origin v0.1.0` returns a ref
- GitHub Release v0.1.0 published with .exe + .zip + .AppImage (human-verify checkpoint)
- Release notes contain SmartScreen + Gatekeeper bypass instructions
</verification>

<success_criteria>
- Public repo briston/solocampaign (or confirmed slug) live with master pushed
- v0.1.0 release published via CI with all three platform artifacts
- updateChecker API URL matches the live repo slug
- DIST-05 end-to-end: a future tag would surface the in-app update banner
</success_criteria>

<output>
Create `.planning/phases/09-distribution-update-notifications/09-04-SUMMARY.md` when done
</output>
